/**
 * 灵动ai 学生端品牌 —— 宿主侧。
 *
 * 这个包只贡献**浏览器里的呈现**（品牌插槽的占用者），宿主侧没有任何行为，
 * 留一个空的 apply 是为了让加载器有这一行、从而把 client 半边挂上去。
 * 官方同款包（@deepseek-ai/dsh-client-ui-brand-official）也是这么写的。
 *
 * ⚠️ 这个包只管品牌。学生端那三个功能开关（对话 / 写代码 / 做网页）在**另一个包**
 * `../feature-plugin` 里，别往这里并 —— 它曾经被并进来过，理由是「新包挂不上」，
 * 而那个理由是错的（见 `docs/operations/新对话交接-*.md`）。品牌是承重的：
 * 它挂了整个外观都没了，不该跟着别人一起出事。
 */

/** 宿主插件体：空的。 */
export function apply() {}
