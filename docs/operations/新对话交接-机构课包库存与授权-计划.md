# 新对话交接 · 机构课包库存与授权（002 那一套）—— 计划与数据契约

> 2026-09-17。用户给了 8 张线框图：**图1 教师工作台 / 图2 机构工作台 / 图3–图8 = 002 机构课包库存与学生授权**。
> 图1、图2、**002-01 库存列表、002-02 单课包详情** 已做完上线（`d6e432e`）；
> **还剩 002-03 学生授权中心 / 002-04 学生授权详情 / 002-06 采购·增购·开通记录**，本文是它们的施工图。
> ⚠️ 用户已定（2026-09-17 复述确认）：**取消授权只有平台端有权限，机构端没有** ——
> 图8 的取消授权按钮、那套 5 条取消资格校验、002-04B 里相关的展示**一并忽略**，
> `p55` 的 404 断言**保持不动**。这条已升级为**固定口径**，见下面 **一.4**；
> 施工图（二 / 三 / 四节）里与之冲突的条目**已删除**，照本文写不会再做出机构侧 revoke。
> 我已经把现有接口与表**逐行核对过**（下面的 file:line 都实读过），照这个写就不会对着不存在的字段编。

## 一、四个口径是用户定的（别自己改）

1. **授权状态那一层「先用现有口径近似」**：`student_course_grants` 只有 `granted_at/revoked_at`，
   **没有状态列**（schema.js:1189，且**没有任何 ALTER 给它加过列**）。所以
   「待激活 / 学习中」**不伪造**；只表达「有效（未撤销）/ 已取消（有 revoked_at）」。
2. **机构工作台保留现有课堂入口**（图2 原本写「不设置课堂创建/进入」，用户选了保留）。
3. **图3–图8 改造现有两页并合并**：`/series-overview`（机构课包库存与学生授权）吸收 `/grants`（学员许可）。
   会连带改 **p72 / p82 / p66** 三个守卫（**p55 不在其中**，见 4）。
4. **取消授权只有平台端有权限，机构端没有**（用户明确，别自己改）。
   机构端**不加**取消入口、**也不展示**「取消资格」那一层 —— 给机构看「取消资格」却不给取消，
   是误导。现状已经符合，**别去"修"它**：
   - `p55-course-quota-grant.mjs:90-91` 断言机构侧 revoke 返回 **404** → **保持**（曾建议改成
     "新路径受校验门控"，**作废**）；
   - `apps/server/src/routes/orgAdmin.js` 里没有 revoke 路由（里面的 `revoked_at` 只是**读出来显示
     「已取消」**，:1207 那处是**重新授权**时复位，不是撤销）；
   - `apps/org/src` 里没有取消按钮；
   - 平台侧才是权限所在：`POST /api/admin/course-grants/:id/revoke`（courses.js:624-650），
     另有 `POST /api/admin/course-series/:id/assignments/revoke`（:443）。
   → 因此**图8 的取消按钮与其 5 条校验、002-04B 的取消相关区块，一律不做**（见下二、四节的删除标记）。

## 二、六张图纸要什么

| 图纸 | 内容 |
|---|---|
| 002-01 | 库存列表：4 张卡（已开通课包 / 总人次 / 已授权 / 剩余）+ 筛选（课包名称 / 权益状态）+ 表（课包 / 当前版本 / 权益状态 / 总人次 / 已授权 / 剩余 / 开通时间 / 查看详情）+ 分页 |
| 002-02 | 单课包详情：头部课包卡（版本 / 适用对象 / 开通时间 / 权益有效）+ 两张记录入口按钮 + 3 张卡（总人次·只读 / 已授权 / 剩余）+ 已授权学生表 + **最近授权** + **最近库存变化（含 `53→52`）** |
| 002-06 | 采购 / 增购 / 开通记录：4 张卡（业务记录 / 初次开通 / 增购 / 平台调整）+ 筛选（课包/业务类型/时间区间/来源）+ 表（业务时间/课包/业务类型/人次数量/业务来源/经办/备注）+ 页面边界说明 + 分页 |
| 002-03 | 学生授权中心：4 张卡（学生总数 / 已有课包学生 / 暂无课包学生 / 本月新增授权）+ 筛选（学生/账号状态/授权情况）+ 表（学生/登录账号/账号状态/已授权课包数/最近授权时间/授权概览/查看授权） |
| 002-04 | 学生授权详情：学生卡（当前授权课包数 / 已产生正式学习记录数 / 待激活数）+ 当前课包授权表（课包/版本/授权时间/授权状态/正式学习记录/查看授权）+ 授权规则 4 条 + 本页负责 / 本页不包含。⚠️ 线框图表头里的「取消资格」列**去掉**（一.4） |
| 002-04B | 单授权详情（右侧抽屉）：授权对象 / 授权信息（时间·操作账号·来源·占用人次）+ 正式学习记录 + 页面边界 + 关闭。⚠️ **没有**「取消资格校验 5 条」、**没有**校验结论、**没有**「取消成功后的影响」、**没有**取消授权按钮（一.4） |

## 三、现有数据（已核对，直接用）

**能直接用**
- `GET /api/org/series-overview`（orgAdmin.js:1077-1130）每项：
  `seriesId, title, coverImageUrl, quotaTotal, quotaUsed, remaining, grantedCount, grantedStudents,
  pendingSessions, activeSessions, endedSessions, dissolvedSessions, teacherCount`；
  `totals{seriesCount, quotaTotal, quotaUsed, remaining, grantedStudents, pendingSessions, activeSessions}`。
  → 图3 的 4 张卡与「总人次/已授权/剩余」**齐了**，只缺**版本 / 开通时间 / 权益状态**。
- `GET /api/org/course-grants?seriesId=`（orgAdmin.js:1133-1148）每项：
  `id, studentId, studentName, studentLogin, seriesId, seriesTitle, grantedAt, revokedAt, revokeReason`。
- `POST /api/org/course-grants`（:1150-1193）body `{seriesId, studentIds[]}`，返回 `{granted, skipped, quotaTotal, quotaUsed}`。
- `course_assignments` 最终列：`id, series_id, org_id, status(ACTIVE|REVOKED), assigned_by, assigned_at, expires_at`
  **+ ALTER 加的 `quota_total`、`quota_used`**（schema.js:1184/1186）。→ 配额就在这两列。
- `license_purchase_batches`（schema.js:1206-1227）：`purchase_type(PURCHASE|LEGACY_OPENING_BALANCE), quantity,
  amount_minor, currency, purchase_status, status, order_no, contract_no, purchased_by, purchased_at` …
- `license_revenue_events`（:1229-1249）：`event_type(GRANT|REVERSAL), quantity(±1), actor_id, occurred_at` …
  写入方 `services/licenseLedger.js`（batch :87-111、GRANT :139-157、REVERSAL :159-192）。

**没有、必须新做**（下面带 ~~删除线~~ 的是**已作废**的旧提法，留在这儿只为防止有人再提一遍 —— **别照着做**）
1. **「最近库存变化」的 `53→52` 前后值：数据库里没有**（没有 `quota_before/quota_after` 列）。
   唯一的 before→after 是通用审计 `audit_logs.before_data/after_data`（schema.js:901），
   机构侧可经 `GET /api/org/audit-logs`（orgAdmin.js:307-319）读到。
   → 建议：**「最近授权」用 grants 真数据（时间/学生/±1），「最近库存变化」的增购与平台调整事件如实说明来自平台侧**，
   不要为了凑出箭头去解析审计日志（脆）。
2. **机构侧没有任何接口读 `license_purchase_batches` / `license_revenue_events`**（已确认：只有平台端在用）。
   → 图5 要**新开一个机构侧接口**读批次表，业务类型映射建议：
   `初次开通` = 该 assignment 的第一条批次；`增购` = 后续 PURCHASE；`平台调整` = `LEGACY_OPENING_BALANCE`。
   `purchased_by` → 经办；`order_no/contract_no` → 备注来源。
3. ~~**机构侧「取消授权」不存在，而且 p55 明确断言它必须 404** …需要**新增
   `POST /api/org/course-grants/:id/revoke` 并同步改 p55 的断言**。~~
   **【已作废 —— 2026-09-17 用户定：机构端没有取消权限】**
   机构侧「取消授权」**不新增**，`p55` 的 404 断言**保持**。见一.4。
4. **「正式学习记录 已产生/未产生」没有标志位**，但它有**现成口径**：
   完课判定用的就是「该学生这节课上有没有成功且非 mock 的 AI 调用」（`services/classroomSessions.js:164`）。
   这一条 **002-04 要用**（它不依赖取消权限，是学生学情的表达），照上面这个口径算即可。
   ~~图8 的 5 条校验也都能算：…→ 建议照课堂预检那样做成预检接口
   （`GET /api/org/course-grants/:id/precheck`）。~~
   **【已作废 —— 那 5 条校验只在服务「机构取消授权」时才需要，取消权限不在机构端，校验无用（一.4）】**

## 四、实现清单（建议顺序）

**服务端**（只剩 4 条。原来的第 5、6 条 = 机构侧 precheck 与 revoke，**按一.4 已删除，不要再补**）
1. `org/series-overview` 每项补 `version`（course_series.version）、`assignedAt`、`assignmentStatus`。
2. 新 `GET /api/org/license-batches` —— 图5 / 002-06 的采购/增购/开通记录（按业务类型映射，见三.2）。
3. 新 `GET /api/org/student-grants-summary` —— 图6 / 002-03 的聚合（已有课包学生/暂无课包学生/本月新增 + 每生授权概览）。
   ⚠️ 这一屏**不是纯前端**：按学生维度的聚合前端拼不出来，必须走这个接口。
4. 新 `GET /api/org/students/:studentId/course-grants` —— 图7 / 002-04 的单学生授权列表（带「正式学习记录」标志）。

**前端**（合并到 `/series-overview`，吸收 `/grants`）：002-01 / 002-02 / 002-06 / 002-03 / 002-04。
002-04B 只做剩下的部分，**不含取消相关区块**（一.4）。

**守卫**
- p55：**不动**。它断言的「机构侧撤销入口不存在（404）」就是现行口径；旧计划写着
  "改成断言新路径存在且受校验门控"，**作废** —— 照旧计划改会把口径推翻。
- p72：series-overview 的 items 键会变多（只加不减，应仍通过，但要跑）。
- p82 / p66：涉及 assignment 快照与 grant 门禁，改完必跑。
- 新页面要进真浏览器验收 → 扩展 `scripts/p111-classroom-ui-check.mjs`（它已有登录 + 造夹具 + 截图的能力）。
  ⚠️ 机构端那几屏**要先给 p111 补一个机构管理员登录** —— 它现在只有教师身份（`teacher-1`）。

## 五、已上线（本轮）

图1 教师工作台（`082c471` 起）与图2 机构工作台（`001-01`，含 `org/overview` 新增
`month{newStudents,newTeachers,grants,endedSessions}` 与 `attention{exhaustedSeries,restrictedAccounts}`）。
放图2 时**没有**照抄线框图那句「机构工作台不设置课堂入口」—— 用户明确选择保留现有入口。
