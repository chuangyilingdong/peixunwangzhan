# 交接 · 新对话（2026-09-23 深夜）

> **新会话先读这一份。** 这一天做完了：**服务器迁移（到 aicyld.com）→ OSS 接入（两阶段）→ RDS 阶段 0**，
> 并为 **RDS 阶段 1（数据访问异步化）** 打好了地基。下面按"现在是什么状态 → 还剩什么 → 下一条命令"写。

## 〇、当前状态（2026-09-23 实测，不是回忆）

```text
平台：**https://aicyld.com 已上线且健康** —— 五入口全 200，最近一次构建 38 通过 / 0 失败
      release 20260923T104406Z；数据 98 用户 / 494 作品 / 32 课堂
新机：8.134.80.184（阿里云广州，4核8G / 40G 盘 / Ubuntu 24.04）
      SSH：ssh -i ~/.ssh/ai_kids_platform_ecs_temp_ed25519 root@8.134.80.184
旧机：39.106.183.200 —— **已冻结，不要再启动、不要再写**（软件可回滚，数据回滚点已不成立）
      同 VPC 的跳板机 8.134.120.22（迁移时用过，key: ~/.ssh/aicyld-prod）
服务：learning-platform-production / nginx / certbot.timer / 每日备份 / 健康检查 全 active
      **dsh-host-broker 已主动下线**（用户口径：学生全用桌面客户端，服务器侧那套不需要了）
存储：**FILE_STORAGE=oss**（上传类走 OSS；广场媒体与安装包也走 OSS，见下）
RDS ：aild_admin 已建好 89 张表 + 11823 行 + **逐表比对 0 不一致**（阶段 0 完成）
仓库：分支 feature/vibecoding-ppt-quality-20260915，HEAD 9b431d1
      ⚠️ **领先远端 21 个提交，还没 push** —— 开工前先 push（或先确认要不要推）
└ 凭据：/etc/ai-kids-platform/production.env（600，含 OSS AK/SK 与 RDS 密码）
        ~/.my.cnf（600，MySQL 命令行用）—— **密码不进仓库/文档/聊天**
```

## 一、这一天做完的四件事

```text
① 服务器迁移（iicili.cyou → aicyld.com）  依据与执行记录：迁移-新服务器广州-20260923.md
   整站搬到广州新机、换域名、证书、dsh 上线又按用户口径下线、旧机冻结。
   过程中抓了 12 个坑（仓库里没有的资产、备案绑实例、客户端钉旧域名、DB 里 485 行旧域名…）。

② OSS 接入（两个阶段）                     依据与执行记录：OSS接入-步骤-20260923.md
   · 上传类（课件/生成产物，275 个）→ 私有 bucket + **签名 URL**（15 分钟）
   · 广场媒体 1.68G + 客户端安装包 373M → 也搬进 OSS，**共 2.06G 离开那 5 Mbps 出口**
   · 关键设计：**没有**给 bucket 开公开读（那会牵动私有课件），改成 nginx/应用 **302 到签名地址**；
     数据库地址一行没改、客户端一行没改
   · 实测两条 OSS 脾气：签名 URL **不能覆盖 content-type**（回 400）、`inline` 会被无视

③ RDS 阶段 0（建库 + 搬数据 + 证明搬对了） 记录：RDS迁移-阶段0-20260923.md
   89 张表 / 1167 列 / 11823 行 / 20 个"部分索引"的 MySQL 等价写法（业务约束一条没丢）；
   **逐表比对 0 不一致**（先取一致性快照、再比；两边的值归一到同类型；XML 里识别 NULL）。
   这一路踩了 5 个坑（类型按真实数据长度定、65535 单行上限、**线上 DDL 有处漏了逗号**、
   3072 索引键长、CONCAT 不能进函数索引要包 MD5）。

④ RDS 阶段 1 的地基（**这是下一步的主战场**） 方案：RDS迁移-阶段1-方案-20260923.md
   · 数据层加了**异步 API**（aq/arows/arow/aone/acount/atransaction）+ **"漏 await 当场炸"的检测器**
     ——已实测（临时库）并 dormant 上线（纯新增、不影响现有行为）
   · 静态进度尺/门禁：scripts/p137-async-db-await.mjs —— **起点 1203 处 / 82 个文件**
```

## 二、还剩什么（按优先级）

```text
1. **RDS 阶段 1 主体**：把 1203 处数据访问从同步改成异步、真正让应用用上 RDS。
   方案已定（见 RDS迁移-阶段1-方案-20260923.md），**关键结论**：
   · 改造**可以增量做**（我最初判断错了，实测更正：转换期异步层是"同步实现的外壳"、
     `await` 已 resolve 的 Promise 只进微任务、不让出事件循环 → 不会交错）
   · 建议先写 **codemod**：仓库里没有解析器，可在**服务器上（仓库外，不动 lockfile）**装一个，
     用 AST 自动加 await / 标 async / **顺着调用链传播** —— 这是把"以天计的人工改造"
     变成"几小时机械化 + 验证"的现实路径
   · 验证三件套：p137（静态）+ 那个检测器（运行时）+ **staging 真跑四条主流程**
   · 切换那一刻：mysql2 + **AsyncLocalStorage 记录事务连接**（那 ~70 处事务调用点就不用改）
2. **客户端重发**：不在我这边（用户说"他已经做了"）。文档：客户端-换域名改动清单-20260923.md
3. **用户侧小尾巴**：安全组里 `18201/18220` 那条规则可以撤（服务器侧学生环境已下线）
4. **可选**：给 RDS 加"每小时同步"（在阶段 1 完成前，让 RDS 那份数据保持新鲜）——
   用户尚未决定要不要
```

## 三、下一条命令（新会话直接从这里接）

```bash
# 0. 先把 21 个提交推到远端（开工前做）
cd "E:/学习平台正常" && git push origin feature/vibecoding-ppt-quality-20260915

# 1. 看当前改造进度（起点 1203）
node scripts/p137-async-db-await.mjs

# 2. 读方案
#    docs/operations/RDS迁移-阶段1-方案-20260923.md

# 3. 开工：写 codemod（在服务器上装解析器，仓库外）
ssh -i ~/.ssh/ai_kids_platform_ecs_temp_ed25519 root@8.134.80.184
#    → /opt/codemod/ 下装 acorn，写 12→ 转换器，分批跑 + p137 验证 + staging 真跑
```

## 四、这一天最重要的一条经验（值得先记住）

```text
**"看起来对"和"确实对"之间隔着一次实测。** 这一天里我自己栽的跟头几乎都是这个形状：
  · 把 IP 从截图里读错（180.184 vs 80.184）→ 花了两小时查一个根本不存在的问题
  · DDL 的类型选择"按有没有默认值拍脑袋"→ 4 个列会静默截断；改了之后"给长度加上限"
    → 同 4 个列还是截断（它们有 7 万～15 万字符）
  · 比对脚本"拿活库比"→ 误报搬丢了 62 行；"数字 vs 字符串" + "NULL vs 空串"→ 又误报 8 张表
  · 改了 OSS 代码**忘了重建**→ 275 个文件差点全下载不了（"302 存在"不等于"302 能用"）
  · 判断"必须一次性改完"→ 实测推翻了它
共同点：**都是靠"真跑一遍 / 逐列比一遍 / 跟着跳转走到底"抓出来的**，
而不是靠读代码想出来的。新会话请延续这个习惯：每个结论都要有一个量出来的依据。
```
