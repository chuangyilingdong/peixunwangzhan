# 第二十六轮 · 客户端自动更新后台配置（2026-09-20）

> 本轮只补“平台后台配置客户端更新”。客户端端的启动检查、下载校验和静默安装补丁在
> `E:\灵动ai客户端` 的 `deploy/desktop/client-patch/LingdongUpdater.ts`。

## 已做

- 后台新增“系统管理 → 客户端更新”：
  - 启用启动更新检查；
  - 强制更新；
  - 最低支持版本；
  - 更新说明；
  - 更新通道。
- 版本号、安装包名、大小、SHA256 只读展示，来源是发布脚本写入的
  `/srv/ai-kids-platform/downloads/manifest.json`，后台不得手填。
- 新增接口：
  - `GET /api/admin/client-update`
  - `PUT /api/admin/client-update`
- 权限归 `ADMIN_AUDIT`，写操作进审计。
- 发布脚本 `E:\灵动ai客户端\deploy\desktop\publish-client.sh` 已扩展 manifest 字段，
  并在写入后执行 `chown ai-kids-prod:ai-kids-prod manifest.json`。

## 尚未做

- 未部署到生产：当前域名正被阿里云 ICP 备案拦截，平台入口不可达。
- 未在生产后台实际点击保存验证；本机服务层守卫和后台前端构建已通过。
- 旧版客户端没有更新器，必须先人工发布/安装一次这一版；之后的版本才可在线更新。
- 当前安装包未签名。现方案依赖 HTTPS + manifest 内 SHA256 完整性校验；
  正式规模化前建议补 Windows 代码签名。

## 验证

```bash
node scripts/p120-client-update-config.mjs
pnpm run build
```

结果：守卫 `4/4`，平台全量前端构建通过。