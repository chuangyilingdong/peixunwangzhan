# 平台端 AI 能力与 CU 配置 —— 重梳理（2026-09-18）

> 用户原话：「现在平台端的 AI 能力与 CU 配置现在好好梳理下，很多无用的功能和逻辑很绕和重复的逻辑。
> 现在这个好乱好乱，我都不知道如何配了。必须做大量的减法。」
> 五条诉求：① 每个模型知道成本价（能 API 取就取、取不到让我们填）② 课程的算力上线预估在创建课包时填、
> 按成本价来 ③ 保留对外价格配置、仅用于统计 ④ 多渠道路由 ⑤ 每节课每个学生的实际算力消耗。
>
> **先说结论**：现在的配置面不是"功能多"，而是**同一件事有 3~6 套并行实现，其中大部分从不生效**。
> 量化基线（实测，不是感觉）：一个页面有 **4 个名字**、**6 个视图**、要人填 **≈130–320 个字段**
> （而真正拦住保存的只有 **4 项/渠道**）；**上游成本有 4 套机制**（填了合同单价，另两套就是白填）；
> **"学生的算力上限"有 6 套实现，只有 1 套真的会拦人**（而且它数的是**次数**不是钱）；
> **8 个后端端点前端从不调用**；1 个死组件；**5 张零业务引用的表**。

---

## 一、乱在哪：一张表看清"几套并行的同一件事"

| 概念 | 现在有几套 | 哪一套真在跑 | 其余是什么 |
|---|---|---|---|
| **上游成本价** | **4** | ③合同单价（`channel.upstreamUnitPrices` / `modelUnitPrices`）→ 落 `compute_attempts.upstream_cost_fen`，`cost_source=COMPUTED` | ①渠道级估算 `estimatedCostFen`、②逐模型估算 `modelCosts`（两者优先级低于③，**填了③就完全被忽略**）、④`platform_modality_settings.unit_cost`（**没有 UI 入口**，全仓只有它自己的读写） |
| **对外价格** | 1 | `compute_pricing`（`platform_settings` 的 JSON 列）→ `compute_attempts.sale_price_fen`，只进报表 | 与成本价**完全独立**：不同 API、不同面板、无加价率、不追溯改价。它**不扣任何人的钱** |
| **供应商** | **2** | `channels[]`（渠道列表） | 顶层 9 个字段（`provider/model/endpoint/displayName/note/websiteUrl/endpointMode/protocol/modelMappings`）= **永不触发的死路径**（UI 一个输入框都没有，后端还完整校验 + 落库） |
| **供应商账单** | **2** | 都不用 | CSV 手工导入（`supplier_billing_*` 4 张表）与账单 API 拉取（`provider_bill_*` 2 张表），**两套表两套路由一张页面上两个面板**。页面自己写着"我们上游是逐笔实扣，这里通常是空的 —— 不需要配"（`ModelCompute.jsx:48-50`） |
| **学生的算力上限** | **6** | 只有 `class_sessions.student_call_cap`（课堂×学生的**次数**上限）真的会 403 | ②课包 CU 额度（`student_course_cu_quotas/_ledger`：**`cu_limit` 全仓无人写** → 恒 UNLIMITED，两张表永远空）、③`platform_credit_quotas`（死表）、④`org_ai_budgets`（死表，且**建表写了两次**）、⑤`billing_packages.monthly_credits/bonus_credits`（积分时代遗留列）、⑥`per_student_budget_fen`（已降级为 write-only 兼容桩，`computePool.js:109-116` 恒返回 null） |
| **"记账"** | 3 | `usage_records`（事件流水，**钱的两列恒 0**：`creditUsage.js:31`） | `credit_entries`、`personal_credit_ledger`（均为死表） |

**"看着配了、其实不生效"的三个典型**（最坑人）：
1. **填了合同单价，另外两套成本估算就是白填** —— 取值链 `MOCK > REPORTED > COMPUTED > ESTIMATED > UNKNOWN`
   （`generationProvider.js:165-167`），而三者在 UI 上**挤在同一张渠道卡里**，用户不知道填哪个算数。
2. **备用渠道只在"没走算力网关"时才生效**（`generationProvider.js:126`）；
   且只有"明确的受理前拒绝"才切换（已输出/已提交/超时/网络歧义一律不切，`:198-200`）。
   另外网关模态白名单**只有 TEXT/IMAGE**（`computeGateway.js:24`）→ **视频/音乐永远直连、永远没有备用渠道**。
3. **`protocol` 字段**：UI 文案写"保留但暂不使用"（`BillingPanels.jsx:400`），
   但 `runtimeGateway.js:309-314` 还在按它拼路径（`ANTHROPIC` → `/messages`）。
   如果历史数据里存着 `ANTHROPIC`，用户**在界面上看不到也改不掉**。

---

## 二、你的五条诉求，逐条对现状

### ① 每个模型的成本价 —— 能自动取的比你想的多，但没有"价目表 API"

**现状**：4 套入口（见上），**全靠人工填**。仓库里没有任何"从上游拉价目表"的代码。

但要纠正一个前提：**上游确实没有价目表 API 可拉**（现有的两个"自动"都不是价目表）：
- 拉的是**模型清单**（`billingConfig.js:372-392`）：只有 id / 显示名 / 上下文长度，**不含任何价格字段**；
- 逐笔回传的是**实扣金额**（`REPORTED`，`openaiCompatibleProvider.js:108`）。

**所以更好的路是：用"实扣金额 ÷ 用量"反推实测单价**，而不是让你手填。
我们已经在逐笔收实扣金额了（`compute_attempts.upstream_cost_fen` + `usage_snapshot`），
按 (渠道, 模型, 模态) 滚动聚合就能得到"实测单价"，再与合同单价对照 —— 这也顺带解决
"实际成本与报价对不上"的对账问题（现在对账是另一套 CSV/API，见减法 #2）。

### ② 课程算力预估 —— 字段有、能填、官网还展示，**但从来没和实际消耗对比过**

- 字段：`course_series.estimated_credits_per_person`；填空在平台端课包编辑器
  （`CourseManagement.jsx:594/632`），官网课包详情还显示"预估消耗 **灵动值**/人"（`website/src/main.jsx:327`）。
- **全仓只有"写入 / 回显 / 官网展示"三处，没有任何代码把它和实际消耗做过对比** → 现在是一份死数据。
- 同名的 `per_student_budget_fen`（课时/课包各一列）也已成 write-only。
- 因此你要的"按成本价填"，缺的不是入口，而是**让这个数字有用**（对比、预警，见第五节的决策 3）。

### ③ 对外价格 —— 已经在做"仅统计"，问题只是它和成本价分居两处

`compute_pricing` 的语义边界写得很清楚（`computePool.js:33-46`）：**不扣学生、不是上游成本、
不作为课堂预算基准**，只进报表（`financialReporting.js:30/101/500`）。
要改的只是**把它和成本价放到同一个地方填**（一张价目表两列），而不是两个 API、两个面板。

### ④ 多渠道路由 —— 机制齐了，但有三条硬约束会让它"看着配了其实不生效"

选渠道的优先级（`aiGeneration.js:445-462`）：`modelRoutes[]` 精确命中 → `modalityChannels[模态]`
→ 旧单渠道配置（`channelId='default'`）。备用在 `modalityBackupChannels[模态]`
或 `modelRoutes[].backupChannelId/backupModel`。三条约束见第一节末。

### ⑤ 每节课每个学生的实际消耗 —— **算得出来，而且数据齐**

- **唯一正确口径**：`compute_attempts.sale_price_fen` 且 `status='SUCCESS'`
  （定义只有一处：`computePool.js:74-105`）；真实成本是同一张表的 `upstream_cost_fen`。
- **粒度数据齐备**：`compute_attempts` 的 `class_session_id / lesson_id / user_id` 三列都在
  （建表补列 `schema.js:1390-1391`），`GROUP BY class_session_id, user_id` 是现成可行的。
- **已经固化了一处但没人看**：结课时 `session_students.completed_cost_fen`
  （`classroomSessions.js:167` 写入）—— **三个前端 0 处渲染**。
- **缺口只有一个**：报表的分组维度只有 模态/渠道/模型/机构/学员，**没有 课包/课时 分组**
  （`financialReporting.js:439-445`），所以"课包 × 学生"的聚合目前没有接口。
- 顺带一个口径不一致要记住：平台端"调用账"**按每次尝试各计一行**，机构端**每次调用只取第一条成功尝试**
  （`orgAdmin.js:578-580`），`computePool.js:76-78` 明确警告过"两者用途不同，别互相对数"。

---

## 三、目标模型（提议：配置面从"20+ 屏长表单"压到 **3 处**）

**唯一的真相表 —— 价目表（键 = 渠道 × 模型）**，一行就是"这个渠道的这个模型多少钱"：

| 列 | 来源 | 说明 |
|---|---|---|
| 成本价 | **优先自动**：由实扣反推的实测单价回填（可一键采纳）；也可人工覆盖 | 按模态计价：TEXT 分/百万 token、IMAGE 分/张、VIDEO 分/秒、MUSIC 分/次 |
| 对外价 | 人工填（或按成本价 × 系数批量生成） | 只进统计，不扣钱 |
| 能力 | 沿用现有声明（比例/清晰度/时长/音频/曲风/生成模式） | 只在需要时展开 |
| 备注 | 合同号/供应商账户标识 | |

**渠道页只留 4 项必填**（名称 / 调用地址 / 可用模型 / 默认模型）+ 密钥，
价格与能力**全部移出渠道卡**，进价目表。这样：
- 同一模型不再在 4 个地方各填一次；
- "填了哪个算数"这个问题消失（只有一处）。

**课程侧**：创建课包/课时时填"算力预估"（默认按所选模型的成本价 × 预估用量算出来，可改），
并把"预估 vs 实际"的对比做进课包详情（这是你诉求 ② 真正想要的东西）。

**机构/学生侧**：不再有独立额度配置（见减法 #4），消耗只做**统计展示**。

---

## 四、减法清单

### A. 直接删（前端从不调用 / 零业务引用）

| # | 删什么 | 证据 |
|---|---|---|
| 1 | 顶层 9 个供应商字段 + `platformPerCallBudget/platformDailyBudget` | UI 无输入框，后端校验恒真（`providerContract.js:39`）；预算两字段 UI 永不提交 → 校验永过 |
| 2 | 死组件 `BillingUsagePanel`（73 行，含 14 字段筛选表单） | 全仓只有它自己的注释引用（`BillingPanels.jsx:444`） |
| 3 | 8 个零调用端点：`billing-config/alerts`、`org-overrides`（前端 0 调用）、`change-logs`、`compute-gateway/usage|channels|tokens`、`compute-pools/reconciliation`、`billing/usage-records|usage-overview|filter-options` | 报告逐一 grep 过；部分只剩守卫脚本在用 |
| 4 | 5 张零业务引用表：`platform_credit_quotas`、`org_ai_budgets`（含**重复的第二个建表语句** `schema.js:1077/1355`）、`personal_credit_ledger`、`credit_entries`、`user_credit_adjustments` | 全仓零引用 |
| 5 | 死列：`platform_modality_settings.unit_cost`、`platform_settings.modalities/billing_settings`、`usage_records.cost_fen/credits_charged`（三义混用、现行恒 0）、`org_billing_accounts` 全部列、`billing_packages` 的 5 个 allow_* + monthly_credits/bonus_credits、`course_lessons.cu_limit/course_series.cu_limit`（无人写） | 报告逐一 grep |
| 6 | 死函数/兼容桩：`computePool.js` 的 `seriesBudgetFen/poolUsedFen/computePoolStatus/computeBudgetedSeries/assertComputePoolBudget`、`aiGeneration.js:64 packageForUser`、`courseCuLedger.js:9 getCourseCuStatus` | 空壳或零调用 |
| 7 | 前端死 CSS 类：`channel-unit-prices`、`channel-capability-editor`、`capability-row`（都未定义）、`.capability-template`（无引用） | grep |

### B. 收敛（同一件事只留一套）

| # | 收敛什么 | 怎么收敛 |
|---|---|---|
| 8 | **上游成本 4 套 → 1 套** | 删 `estimatedCostFen` + `modelCosts`（UI 上各少 1~N 个字段/渠道），只留"价目表"；`unit_cost` 直接删 |
| 9 | **供应商账单 2 套 → 0 套**（或只读） | 我们上游逐笔实扣（`REPORTED`），账已在 `compute_attempts`。CSV 导入 + 账单 API + 7 张表 + 2 个面板整体下线（已做）；真要对账就用"实测单价 vs 合同单价"（第一节 ① 的方案），不需要两张账 |
| 10 | **"学生算力上限" 6 套 → 1 套** | 只留一套按**钱**的（基于成本价），删其余 5 套（含正在跑但数**次数**的 `student_call_cap`，或明确保留它作为"课堂节流"而不再称额度） |
| 11 | **对外价与成本价两个面板 → 一个** | 并进同一张价目表 |
| 12 | **页面 4 个名字 → 1 个**；2 个菜单项指向同一组件 → 1 个 | 菜单/分组头/页面标题/路径统一；`/billing`、`/compute` 两条僵尸重定向删掉 |
| 13 | **`protocol` 字段** | 要么在 UI 放回选择器、要么后端停止读取（现在"看不见但生效"最糟） |
| 14 | 重编辑器默认展开 → 收进折叠/二级页签 | 「上游合同单价」「模型能力」目前默认展开（各占数屏），应折叠或按模态分页签；全站已有 `.wizard-steps` 分步组件（机构端在用），配置页没用 |

### C. 保留并修好（不要删）

| # | 保留 | 要补什么 |
|---|---|---|
| 15 | 对外价格 | 与成本价并排；仍然只进统计（维持现行语义，别偷偷改成扣费） |
| 16 | 多渠道路由 + 备用 | ① 让备用在"走网关"时也能生效，或明确告知"网关上没有备用"；② 视频/音乐也要有备用（现在网关白名单只有 TEXT/IMAGE）；③ UI 上把"配了但不生效"的三种情形标出来 |
| 17 | 实扣金额（`REPORTED`） | 它是成本价的**自动来源**，别删 |
| 18 | `session_students.completed_cost_fen` | 已经算好了但没人看 → 在课堂详情里显示（这直接服务诉求 ⑤） |
| 19 | 报表分组 | 补 课包 / 课时 两个分组维度，才能"课包 × 学生"聚合出**机构总消耗** |

---

## 五、要你定的四件事（决定"减法"的边界）

1. **供应商账单那两条线（CSV 导入 + 账单 API 拉取）整体下线？** 6 张表 2 套路由 2 个面板，
   而我们上游是逐笔实扣、账已经在 `compute_attempts` 里，页面自己都写着"通常不需要配"。
2. **成本价的主要来源**：用"实扣反推的实测单价"（自动，可人工覆盖），还是坚持人工填合同单价？
3. **课程的算力预估要不要"咬人"**：现在填了没人看。改成 (a) 只展示 + 超预估预警，
   还是 (b) 真的拦截（超预估就不让继续调用）？（现在唯一会拦的是课堂**次数**上限）
4. **"学生算力上限"保留哪一套**：建议只留一套"按钱"的（基于成本价、按课包/按课堂），
   其余 5 套删掉；也可以选择"什么额度都不要，只统计"。

---

## 六、执行记录与顺序（按"先删干净、再收敛、最后补能力"）

### 已定口径（用户 2026-09-18 四条决定，都选了推荐项）

1. **成本价 = 实扣反推为主 + 人工可覆盖**（上游没有价目表 API，但逐笔实扣我们已经在收）。
2. **供应商账单两条线整体下线**（CSV 导入 + 账单 API 拉取）。
3. **课程算力预估 = 展示 + 超预估预警，不拦人**。
4. **"学生算力上限" 6 套收敛成 1 套按钱的**。

### Phase 1 已完成（纯删死代码，零行为变化）

- 删 `BillingUsagePanel`（`apps/admin`：73 行 + 一个 14 字段大筛选表单，**全仓零引用**）。
- 删 6 个死导出 + 它们全部调用点（`computePool.js`）：`seriesBudgetFen` / `poolUsedFen` /
  `computePoolStatus` / `computePoolReport` / `budgetedSeriesOverview` / `assertComputePoolBudget`
  —— 分别是恒 null / 恒 unlimited 的兼容桩与**从不抛错的空壳**，调用点分布在
  `runtimeGateway.js` / `aiGeneration.js` / `vibecoding.js` / `runtimeSearchGateway.js`（4 处预检）。
  这两处注释顺手更正了（原来还写着"调用前的 assert 已经拦过一次"——它从来没拦过）。
- 守卫 `p60` 的 ⑤ 段按【口径变更，不是测试漂移】重写：原来断言"四种模态被同一个闸门拦住"，
  而那个闸门本身已退役 → 改成**反向断言"退役的兼容桩不许加回来"**。

**这一步踩到的坑（值得记）**：删掉 `computePoolStatus` 时漏了**文件内部**的调用方
（`computePoolSummary` 就在用），因为我 grep 时特意排除了 `computePool.js` 自身 →
学生项目创建直接 500。**守卫 p13/p60 当场抓到**（报 `ReferenceError: computePoolStatus is not defined`）。
→ 规矩：**删一个导出，必须连本文件内的调用一起 grep**（别把定义文件排除掉）。

### Phase 2 / 3 / 7 已完成（2026-09-18 第二轮，同日）

**Phase 2 供应商账单整体下线**
- 删 2 个 service、2 个 route（含 `apps/server/src/index.js` 里的定时任务启动/停止、
  `routes/adminOrg.js` 里第二处注册）、**7 张表**的 CREATE 与配套索引（不写 DROP，老库数据保留）。
- 三账报表改**"两账"**：`apps/server/src/services/financialReporting.js` 528 → 361 行，
  "实际核销"的数据源从账单匹配换成 `compute_attempts.upstream_cost_fen`；
  **响应键名一个都没改**（前端零改动即兼容），成本未知时金额给 `null` 绝不按 0，
  "成本未知"另给笔数（新增只加不减的键 `costUnknownCallCount`）。
  实测三条接口全部 200（改前 `/summary` 是 500）。
- 删 `scripts/p86`、`scripts/p91` 两个守卫；p88/p9-r03 里相关断言随之下线并注明口径变更。

**Phase 3 成本机制 4 套 → 1 套**
- 界面：删掉渠道卡里的「上游估算成本」「逐模型估算成本」两个输入框（填了也被合同单价覆盖，纯干扰）。
- 后端：`generationProvider.js` 的取值链去掉 `ESTIMATED` 那一档 ——
  现在只有 **MOCK（本地模拟）/ REPORTED（上游逐笔实回扣）/ COMPUTED（价目表合同单价 × 用量）**，
  都没有就是 **UNKNOWN（绝不按 0 计）**；`billingConfig.js` 不再回显也不接受写入那两个字段
  （老库里的值保留，但已无读取方）。顺带删 `platform_modality_settings.unit_cost`（无 UI、无消费者的死列）。

**Phase 7 配置面收敛（"简单明了"）**
- 一个页面一个名字：菜单/分组头/标题/权限标签统一成 **「AI 能力与价格」**；
  删掉 `/billing → /compute`、`/compute → /compute/config` 两条僵尸重定向。
- 「渠道与价格」页压成**三块**：**① 渠道（怎么连上游）→ ② 价目表（每行 = 渠道 × 模型，
  成本价 ‖ 对外价 ‖ 毛利 ‖ 能力）→ ③ 路由与开关（4 模态主/备渠道 + 读图渠道 + 总开关）**，
  底部一个「保存全部配置」。
- 删掉「算力网关（new-api）」整块 UI（生产未启用）、「高级：供应商账单/匹配与核销」两个子视图
  （连同「高级」一级页签），用量页 4 视图 → 3 视图。
- 量化结果：**同一个模型的价从"4 处填"变成"1 行 2 列"**；**默认铺满屏幕的重编辑器 2 个 → 0 个**；
  上游成本机制 **4 套 → 1 套**；配置页从 5 面板 + 3 折叠块 → **3 块**；保存按钮 4 个 → 2 个。
  （控件总数 84 → 76 只降 10%，因为成本与能力是"搬家 + 折叠"而非删掉 —— 真正解决"不知道如何配"的是
  **同一个数只有一处可填、且填了就算数**。）

### 仍未做（下一轮）

| 项 | 说明 |
|---|---|
| **Phase 4 实扣反推"实测单价"** | 用户诉求①的核心：按 渠道×模型×模态 滚动聚合 `upstream_cost_fen ÷ 用量` 得出实测单价，回填价目表并显示与合同单价的偏差。需要一个聚合服务 + 价目表上一列"实测/偏差"。 |
| **Phase 5 课程算力预估** | 现在 `course_series.estimated_credits_per_person` 只是死数据（能填、官网展示、**从不与实际对比**）。要做：按成本价自动算出预估 + 课包/课时两个报表分组维度 + 超预估预警（不拦人）。 |
| **Phase 6 学生额度收敛** | 6 套 → 1 套按钱的。目前唯一会拦人的是 `class_sessions.student_call_cap`（数**次数**），收敛时会动到课堂流程，风险最高，建议单独一轮。 |
| 8 个零调用后端端点 | 其中 3 个仍有守卫脚本在用（p13/p57/p58/p60），要连守卫一起改。 |
| 3 张保留的死表 | `credit_entries` / `personal_credit_ledger` / `user_credit_adjustments` 被 p6 / p67 / p11 / p4-02 / p9-r04 用"存在性断言"钉着，要删得连那 5 个守卫一起改。 |
| 渠道上残留的 `estimatedCostFen` / `modelCosts` | 已无读取方（等价退役），但字段还在老库里；要彻底清得动存储结构。 |
| `protocol` 字段 | "看不见但生效"（UI 说明写"暂不使用"，后端仍按它拼路径）。要么放回选择器、要么后端停止读取。 |

### 本轮踩到的坑

1. **删导出时要连"同文件内部"的调用一起 grep**：删 `computePoolStatus` 时漏了同文件的
   `computePoolSummary`，学生项目创建直接 500（被 p13/p60 抓到）。我当时的 grep 特意排除了定义文件自身。
2. **删表之前要查"在用的报表"**：`financialReporting.js` 有 22 处在查供应商账单表，
   只删表会让 `/financial-reporting/summary` 变 500 —— 这条是动手前查出来才避免了带病上线。
3. **`/admin/billing` 这类旧路径改动会波及通知深链**：`reminderScheduler.js` 的「合同即将到期」
   通知 `targetUrl` 写死 `/admin/billing`，重定向一删老通知就掉到工作台；已改成 `/authorizations`
   （注意 targetUrl 写的是**应用内部路由**，`/admin` 是 basename 不进这个字段）。

### ✅ 那个依赖已经处理了（原「Phase 2 前必须处理的一个依赖」）

三账报表 `routes/admin/financialReporting.js` **依赖 `supplier_billing_matches` / `supplier_billing_lines`**
（9 + 10 处引用）来算"**实际核销**"。所以"供应商账单整体下线"**不能只删表**，否则那一列没有数据源。
建议做法（与用户决定一致、且更准）：**"三账"简化为"两账"——对外售价 / 上游实扣成本**，
把"实际核销"的数据源从账单匹配换成 `compute_attempts.upstream_cost_fen`
（我们上游本来就逐笔回实扣，那个数才是真正的核销额；而 CSV/账单 API 那套要等"按账期开票"的上游才用得上）。

---

*本文的全部事实来自 2026-09-18 的三路只读代码调查（渠道/价格、算力账、平台端界面），
关键处都给了 `文件:行号`，可以逐条复核。Phase 1 的删除已跑过 p13/p16/p18/p55/p57/p58/p60/p66/p72/p78/p82/p9-r03。*
