# 新对话交接（2026-09-15）

> ⚠️ 这是**时点交接**，不是口径源。领域口径、金额口径、代码地图**一律以 `docs/README.md` 为准**
> （它本会话新增了 §三 第 11、12 条）。本文件只回答「新开一个对话时先做什么、现在到哪儿了、什么还没定」。

## 一、开工第一件事

读 `docs/README.md`（唯一入口）。然后按需读，不要通读：

| 要做什么 | 读哪个 |
|---|---|
| 找代码 / 接口 | `docs/architecture/代码结构与路由.md` |
| 碰金额、对账、消耗口径 | `docs/operations/真实三账对照-20260914.md` |
| 碰上游实扣（Seedance/DeepSeek） | `docs/operations/上游实扣接入-20260915.md` |
| 碰课堂 / 课包 / 机构授权 | `docs/operations/全流程重梳理-20260913.md` |
| 发布 / 运维 / 系统级依赖 | `deploy/production/RUNBOOK.md`（有「服务器上的系统级依赖」一节） |
| 历史（只当历史看） | `docs/archive/` |

## 二、2026-09-15 实测核实（真查过，不是抄文档）

```text
仓库：branch main，HEAD dd1d9f3 = origin/main（本会话 20 个提交，全部已推送）
生产：release 20260915T054458Z / commit 6e22263
      服务 learning-platform-production：active、NRestarts=0、ExecMainStatus=0
      五个公网入口实测全 200：/、/admin/、/org/、/student/、/api/health
守卫：scripts/*.mjs 共 104 个（p92 消耗口径、p93 素材预览、p94 按产物提交 为本会话新增），全绿
Node：用 "C:\Program Files\nodejs\node"（v24）；默认 PATH 的 node 是 v16
系统依赖：服务器已装 libreoffice-impress/writer + fonts-noto-cjk（教学素材转换用）
```

注意：**本地 HEAD 通常比生产多一个纯文档提交**——不要为它再发一次。

## 三、本会话做完的（20 个提交，按主题）

**① 上游实扣接入（Seedance 直连）**
- 认清上游的回执信封：`data.usage` / 顶层 `usage` / `task.usage` 三处；
- **实测币种写法是 `¥` 不是文档示例的 `CNY`**（逐字比对会一条都读不到）；`USD`/`$`/`JPY` 仍不认；
- 金额是小数元，落库要四舍五入；REPORTED 也留 `upstreamCurrency`/`upstreamAmount` 证据；
- 图片、视频、音乐三个模态都真机验证过实扣（文本上游不给金额，只能折算）。

**② 文本渠道（以前一直没配价）**
- 上游价目表是「元 / 百万 token」，而字段单位原来是「分 / 千 token」→ **0.2 分填不进去**，
  所以文本渠道此前根本没法配价。已改成 **`inputFenPer1MTokens` / `outputFenPer1MTokens`（分/百万）**，
  deepseek-flash 高峰价 = 200 / 800，正好整数；旧的「1k」字段名不再被接受（p90 钉住）。
- **逐笔成本保留分以下小数（4 位），不按整数分取整**：DeepSeek 一次课堂对话约 0.2~0.4 分，
  取整会把每一笔都记成 0、整节课的文本成本凭空消失。
- 生产实测：文本那笔 `cost_source=COMPUTED`、`upstream_cost_fen=0.3678`。

**③ 金额口径统一 + 机构端「消耗」**
- 一句话口径：**一切以「分」计；配置里的单价必须整数分，逐笔折算出来的成本可以是小数分。**
- 机构端/学员端「消耗」= **对外售价合计**（`compute_attempts.sale_price_fen`，**只计成功尝试**）；
  平台自己的进货成本与毛利只在平台端「用量与成本」看。改前那些地方读的是被清零的
  `usage_records.cost_fen`，所以机构端「这节课消耗」**永远显示 ¥0.00**。
- 「用量与成本 → **机构与学员**」现在并排给出：学生消耗（对外售价）/ 我们的成本 / 差额。
- 后台金额显示：整数分 2 位、小数分 4 位（否则 0.37 分显示成 0.00，看着像没记账）。

**④ 后台「模型与算力」重排**
- 两条主线：**渠道与模型配置**（配好基本不动）+ **用量与成本**（调用账 / 机构与学员 / 三账与毛利 / 高级）。
- 接回了三个**死面板**（其中 `OrgStudentUsagePanel` 正是「机构下每个学员」的入口，以前没人渲染）。
- 「供应商账单 / 匹配与核销」收进「高级」——我们上游逐笔回实扣，这两屏本来就是空的。
- 移除了「接口协议」下拉：后端只保存、适配器从不使用，选了不生效属于静默误导。
- 模型清单从「半宽一列长龙」改成横跨整行 + 限高滚动。

**⑤ 教学素材在线预览（口径 A + LibreOffice）**
- 机构/老师端**只能看不能下载**：删掉下载按钮与其 blob 逻辑，改 preview 浮层；
- PPT/Word 由服务端转 PDF 再发（**原始 Office 文件不出服务器**）；视频/PDF 走 `inline`；
- 预览链接带**短时签名票据**（1 小时），每次预览写 `FILE_PREVIEW` 审计；
- 边界（写进代码/文档/界面）：**视频与 PDF 拦不住录屏截屏**，这是 web 物理限制。

**⑥ VibeCoding 按产物提交（用户口径：不提交整个作品，而是针对能展示的作品提交）**
- 唯一性从「一个对话一条」改成 **`(conversation_id, entry_file)`**；老库**重建表**迁移
  （在生成库副本上验证：行数/列集合/每行内容一字不差）；
- 工作台预览工具栏与文档预览各有一个「提交这份作品」按钮，针对**正在预览的那一份**；
- 真机端到端验证 12 项全过：AI 真的产出了多文件网页（index.html+script.js+style.css）与一份 pptx，
  两份分别提交、重复提交是覆盖（round+1）、非法产物名被拒、平台分别发布、官网公开接口可见。

## 四、金额四类口径速查（最容易混）

| 金额 | 含义 | 是不是真实钱 |
|---|---|---|
| 对外售价 | 公告价，逐笔快照（`sale_price_fen`） | 否，仅观测；**换成了机构/学员看到的「消耗」** |
| 上游计费 COMPUTED | 合同单价 × 用量证据自动折算 | 否，估值 |
| 上游实扣 REPORTED | 上游回执里的实际扣款（Seedance 逐笔给） | 是 |
| 上游账单 / 核销 | 供应商账单匹配（本平台上游**没有**账期账单接口） | 是（按账期开票的上游才用得上） |
| 机构购买实收 / 许可确认收入 | 收入侧 | 是 |

来源优先级 `REPORTED > COMPUTED > ESTIMATED > UNKNOWN`；缺用量或缺单价一律 UNKNOWN，**绝不按 0**。
文本渠道**没有**任何外部数字可印证（DeepSeek 无账单接口，余额只有 0.01 元精度、非逐笔更新），
只能靠按官方价目表填的合同单价 —— 打算改文本单价时记住这一点。

## 五、待你决定 / 待你操作（新对话里先问清这些）

1. **Cloudflare 要 Purge Cache**：`/vibe-preview.html` 被边缘缓存，源站已经把 CSP 放宽
   （`style-src` 加 `https:`、`connect-src` 加 `wss:`），但**线上响应头还是旧的** ——
   不 Purge，学生作品仍用不了 CDN 样式表/WebSocket。
2. **验证遗留**：生产里有 **2 条已发布的作品**（标题「按产物提交验证」，官网广场可见）+ 一个测试会话；
   另有一个 **VIBECODING 演示课堂被我留成 ACTIVE**（原来「待上课」，方便直接演示）。
   要不要清理/还原？
3. **生产口令硬编码**在 `deploy/production/seed-demo-teaching-data.mjs` 与 `docs/operations/交接说明.md`
   （早于本会话、已在远端历史）。用户说过「测试最后自己改」，真要处置只有**轮换**——删文件删不掉历史。
4. **仓库是公开的**（`chuangyilingdong/peixunwangzhan`）——上面那条口令就暴露在公网，优先级高。
5. `compute_attempts` 金额列在库里是 REAL：按新口径（小数分）这**不再是矛盾**，不必重建表。
6. `buildStudentDataExport()` 没有任何路由调用（死代码，其它导出都接了点）——接出来还是删掉？
7. `packages/shared` / `packages/canvas` 未声明自身 import 的依赖；约 114 个未使用 import；CSS 有压缩整块没法 diff。

## 六、VibeCoding 还缺的（评估过、用户已选做范围）

- 用户明确**不做**：开 `deepseek-v4-pro`（模型保持 flash 档）、版本历史/回滚。
- **没做**：③ 控制台报错回喂给模型（「跑不起来」时 AI 看不到真实错误，这是最值得做的一项）；
  ⑤ 把图片直接放进项目文件列表（注意：学生**已经能给对话上传图片附件**，生成页面可以引用）；
  ⑦ 只写 `script.js` 的作品不会运行，预览只显示源码。

## 七、本会话踩过的新坑（都写进代码注释或文档了）

- **跨文件系统 rename**：服务器 `/tmp` 是独立挂载的 tmpfs，临时目录写 `/tmp`、产物 rename 到上传目录
  → EXDEV 失败，而且被 `catch` 静默吞成「转换失败」。临时目录必须与产物**同盘**。
- **上传目录属主**：root 留下的目录会让服务（ai-kids-prod）EACCES；且 `mkdir` 若在 try 之外，
  会冒成 **500 内部错误**，而正确表现是干净的「无法预览」。
- **`sites-enabled/iicili.cyou` 不是 `sites-available` 的软链**（两份独立文件）→ 改 nginx 要改两份，
  否则 reload 成功但完全不生效。
- **重建表会把索引一起带走**：复合唯一索引必须**在重建之后**再建一次，否则唯一性静默失效
  （迁移验证脚本当场照出来了）。
- **守卫不能假绿**：p94 第一版没跑种子数据，插入验证被静默跳过 —— 看着「通过」，其实什么都没验。
- **内联 `node -e` 做含引号的替换又翻车两次**：改用脚本文件（`Edit` 工具或 `.tmp/*.mjs`）。
- DeepSeek 余额 `GET /user/balance` 只有 **2 位小数、且不是逐笔更新**（实测一次 1882 output token
  的调用余额纹丝不动）→ 做不了逐笔校验，只能隔几天看大额变化。

## 八、常用自证命令

```bash
node .tmp/smoke-run.mjs                    # 全量 104 个守卫（别用 bash 的 timeout，Windows 上杀不掉子进程）
node scripts/p70-pages-render.mjs          # 三端页面真渲染（改前端必跑）
node scripts/p66-student-grant-gate.mjs    # 进课门禁
node scripts/p69-teacher-data-scope.mjs    # 教师数据范围（安全相关必跑）
node scripts/p92-sale-price-scope.mjs      # 机构/学员「消耗」= 对外售价
node scripts/p93-material-preview.mjs      # 素材在线预览（票据/转换不回落）
node scripts/p94-artifact-submission.mjs   # 按产物提交（复合唯一性）
```

改前端还要 build 三端。发布照 `deploy/production/RUNBOOK.md` 的「发布流程（当前）」；
**重建表类迁移必须先在生成库副本上真跑**（见 §七）。

## 九、入口与账号

```text
入口：https://iicili.cyou/{admin,org,student}/   （官网在根路径 /）
账号：平台 root　机构 org-admin　教师 teacher-1　学生 student-1（凭据不写入仓库）
提示：演示的 VIBECODING 课堂 `csession_3d51df198de64515bf3c` 归 org-admin 所有 ——
      教师只能开自己建的课堂，拿 teacher-1 去开它会被拒（这条规则真机验证过）。
```
