# 新对话交接 · 第二十七轮（生成链路修复 + 客户端接入 + 页面收口）

> **新会话先读这一份。** 本文覆盖第二十六轮之后的**整个 2026-09-21 会话**（同一个会话里连着做了十一件事，
> 每件都已发布、都跑过守卫/截图）。**口径 61–80、坑 76–99 在这一份里**；承接第二十轮 **1–6 / 1–11**、
> 第二十一轮 **7–24 / 12–36**、第二十二轮 **25–36 / 37–47**、第二十三轮 **37–51 / 48–65**、
> 第二十四轮 **52–57 / 66–71**、第二十五轮 **58–60 / 72–75** —— 正文里「见口径⑰」「坑㉜」这类引用**六份通用**。
> （本文原名「框体与画布读面同源」，会话后半段又落了好几件，故改名。）

**这十五件事（按发生的顺序，细节在 §二 各小节）**：

```
A  框体读面同源    学生画布画的框体（实时）与点生成时判定的那份（快照）不是同一份 →
                   报「生成框体不存在或已不属于本课」。口径：**没发布=学生整体用老的一套且能正常用，
                   发布=一起换新**（用户原话在下面）。生产上 17 节课全中。
B  两条界面口径    「有未发布的改动」要在「版本发布」标签上就看得见（角标）；
                   生成框体在画布左侧要有一圈金环（与普通素材区分）。
C  发布带上别人的提交  发布按 HEAD 走，把另一条线"还没打算部署"的客户端更新实现一起上线了（惰性功能）。
D  客户端更新清单  按客户端那份《接入要求》逐条核 + 补两处：**原子写**（客户端随时在读，不能出半截 JSON）、
                   后台策略**另存 + 服务启动复写**（否则客户端每发一版都会静默重置后台策略）；清单属主改 ai-kids-prod。
E  运行时门禁      客户端运行时（dsh）原来**不筛上课类型** → 画布课也下发 classroom + 网关密钥。
                   现在只认**"这节课声明了 VibeCoding"**（课时类型可多选，不是课堂那个单值）；交作品也挡。
F  上传上限        单文件 25MB → **200MB**（应用 + 代码硬顶 + nginx 三层），并加「读 body 之前的内存闸」
                   与带内存上限的扫描器包装 —— 因为实测 clamscan 扫 150MB 峰值 RSS ≈990MB 而这机器 1.6GB。
G  备份留存        14 天 → **7 天**（用户口径）：7.3G → 4.7G，磁盘 59% → 52%。
H  视频参考语义    学生连一张图写「图片动起来」要的是**首帧**，原来被当成"参考"（启发素材）发 →
                   出来另一段画面。收成一处判定：**能当帧当帧**，够不着才走全能参考。顺带修尾帧判定。
I  数字进度条      四类框体（文本/图片/视频/音乐）显示「小灵施展魔法中 N%」+ 底部细条。
                   ⚠️ **是估算**（上游只回状态），按实测中位耗时算、封顶 95%。
J  2.5 生图模型    报 output_format → 内置模板改成**按模型名**给。（⚠️ 这一版改的是"字段位置"，
                   **判断错了**，M 才修对。）
K  生成被打断      发布重启打断了用户的视频生成（上游扣费、结果丢失、框体被锁）→ 现在
                   **启动收孤儿（按 worker 判）+ 关服收自己的**，被打断变成"可重试"。
M  素材送不到上游  ⭐⭐ 用户复验"还是没法参考" → 真根因是**上游在境外抓不到我们域名的图**
                   （连不上 / 读一半断，且**读不到不报错**，静默当文生跑）。修法：要交给上游的素材
                   先传到上游自己的暂存接口、换成它的 URL（口径 71）；2.5 那条也顺带修对（口径 72）。
                   已真跑验收：视频第 0 帧就是那张画、2.5 出图且参考真的生效。
N  首帧又被丢掉      ⭐⭐ 用户第二次复验"还是没法参考" → 这次是 **worker 第二次解析**把首帧丢了
                   （入队存绝对地址、执行时只认站内相对地址）。口径 73：素材解析要幂等；
                   教训 91：验收必须走用户真正走的那条路（走队列，不是直接调 provider）。
P  生成方式锁定      ⭐⭐ 「文生/图生/首尾帧/全能参考」由**课包**定（不能再靠连了几条线推）：
                   老师配、画布按它限制连线、服务端按它定素材角色、请求体里 role 让上游知道；
                   生图框体同样有文生图/图生图。⭐ 顺带修「固定比例把首帧拉扁」：关键帧+固定比例
                   时先把画面**裁**成那个比例再发（口径 77–78）。
O  画布交互收口      ⭐ 不能生成/已生成的框体**不再显示生成按钮**（点了必然被服务端拒，纯误导）；
                   ⭐ **禁掉快捷键复制框体**（复制出来共用同一个 boxId）；
                   ⭐ 「自动」画幅按上游语义翻成 adaptive → **输出终于跟着源图比例**
                   （用户那张 1:1 的图以前被硬拉成 16:9，就是"扁"的原因；口径 74–76）。
```

**用户口径（原文，A 与 G 就是按它改的）**：
> 「我要的就是只要没发布，不管我如何更新，他们用的都是老的一套，正常使用。只要我更新就全部同步。
> 因为我们通常发布都是在凌晨，凌晨是不会有学生上课的。」

⚠️ **两条操作纪律**（这一轮都真踩过）：
· **另一个会话可能仍在同一工作区里写代码**：提交前先 `git status`、**用显式路径 `git add`**（坑 74；
  §二.C 就是"发布把它的提交一起带上线"的现场）。
· **发布 = 重启服务 = 打断在途生成**（坑 86）：发布前先查一眼
  `SELECT id,modality,status FROM generation_jobs WHERE status IN ('QUEUED','RUNNING')` ——
  视频大概 2 分钟，跑完再发。

## 一、当前生产状态（照这个对）

```text
release 20260921T092548Z / commit c306551（服务 active、NRestarts=0、enabled、ExecMainStatus=0）
  五个入口全 200：/、/admin/、/org/、/student/、/api/health     PROD_ACCEPTANCE_OK
  · ⚠️「生产跑的是哪一版」只认 release 目录里的 BUILD-METADATA.txt，不要拿分支 HEAD 去对
  · 生产代码 = c306551；仓库 HEAD 比它多**几个只动 docs 的提交**（截图/文档提交不发布，这是常规）
    —— 下次发布时一起带上即可，**这两个不相等是正常的**，不是漂移。
    ⚠️ 别记这里的 HEAD 短 sha（一提交就过期）：要看差了什么就跑
    `git log --oneline 06b6630..HEAD` —— **只该列出 docs 提交**；混进代码提交就说明有东西没发布。
可回滚上一版：release 20260921T082045Z / commit f391721（worker 二次解析）
              release 20260921T075627Z / commit 4ac552d（加素材大小闸）
release 目录：10 份（手工口径；清理脚本已收进仓库 deploy/production/prune-releases.sh）
磁盘：/ 40G / 20G / **52%**（剩 18G）    backups 4.8G（7 天留存）    监控探针：status=ok
作品广场：205 件（= 公开的 works；vibecoding 提交 5 件、公开 0 件）
客户端安装包：仍是 0.1.6-alpha.2（含更新器的新版在客户端仓、未发布）
```

## 二、这一轮做了什么

### A. 报错是怎么触发的（生产数据实锤）

一句话：**同一节课的"框体列表"，画布读实时、判定读快照。**

```text
学生画布上画出来的框体 + 右侧「课堂素材」
  ← normalizeProject() → lessonCanvasConfig(lessonId)        = 实时素材表（course_lesson_materials）
点「生成」时服务端校验 boxId
  ← studentContext → normalizeLesson(asPublished: true)      = 已发布快照 published_content
两边指向两批不同的 id → 学生「看得见新框体，点一下说不属于本课」
```

生产上到底几节课会报（只读诊断 `.tmp/diag-normalize.mjs`，**直接调用生产代码** `normalizeLesson`）：

| 课时 | 快照里有 `generationBoxes` 键？ | 实时框体 | 学生读面拿到 | 结果 |
|---|---|---|---|---|
| **1-1 AI 初识：召唤哪吒** | 有（11 个） | 11 个（**全新 id**） | 快照那 11 个旧 id | 0/11 相交 → 点哪个都报 |
| 其余 16 节（1-2、1-4…2-10） | **没有这一键** | 各有 2~13 个 | **0 个** | 判定侧拿到空数组 → 点哪个都报 |

⚠️ 这里有个**我上一轮（第二十五轮文档）没说准的地方**，记下来免得下次又踩：
`normalizeLesson` 对缺键的默认是**空数组**，不是"回退实时"——
`merged = snapshot ? { ...snapshot, generationBoxes: Array.isArray(...) ? ... : [] } : null`
所以"快照存在但缺这一键"的课，判定侧一个框体都不认。**一眼看不出**，只有让真代码跑一遍才知道。

**时间线**（1-1）：`course_lessons.updated_at` 与它全部素材/素材组的 `created_at` 都停在
**2026-09-20T08:53:23.968Z** —— 那一刻这节的素材被**整体重建**成一批新 id（服务端
`replaceLessonCanvasConfig` 本来是**保留 id** 的，注释写着"每次保存换新 id 会让学生端节点认不出来"，
前提是调用方把 id 传回来），重建之后**没有人对课包点「更新发布」**，快照还停在重建前那批 id 上。
当天 9:06–15:43 还有一串"单节课整体重建"（1-2、2-1、2-2、2-5…2-9 各一次）。

### B. 修法：读面与判定**共用同一个函数**

```text
lib.js 新增 mergedLessonCanvas(lessonRow, liveCanvas)
  · 快照里**有哪一档用哪一档**（capabilities / materialGroups / generationBoxes），缺的档回退实时；
  · normalizeLesson（判定侧）与 normalizeProject（画布侧）都改用它 —— 再也不各写一遍；
  · normalizeProject 需要课时的快照 → student.js 三处 project 查询补 `lesson.published_content AS lesson_published_content`。
```

为什么缺键必须**回退实时**而不是给空数组：那 16 节课的快照是"发布早于生成框体这个功能"时定格的，
给空数组等于**把学生手里的框体全没收**（它们现在能正常用，正是因为两边都回退到同一份）。
这与第二十四轮「教学素材可见性」那条判据是同一个形状（逐键回退）。

### C. 修完之后，每节课是什么行为（要跟用户对齐这点）

| | 学生看到 | 生成 | 你要怎么才能换新 |
|---|---|---|---|
| **1-1**（快照里有框体这一键） | 快照里那批**旧的** 11 个（标题「任务一：」那套） | 正常（两边同源） | 点一次「更新发布」 |
| **其余 16 节**（快照里没有这一键） | 仍然是实时那批（= 你重建后的新框体） | 正常 | 也是「更新发布」——**发布之后快照里就有这一键了，从那一刻起才真正隔离** |

也就是说："没发布不生效"这条口径**对"发布过带框体版本"的课立刻成立**（1-1），
对那 16 节要等你发布一次（它们此前从没带着框体发布过）——**这是数据事实，不是代码没改到位**。

### D. 守卫：p54 补了一整段 ④（含反向自检）

```
④ 框体 / 课堂素材也要隔离，而且画布与「点生成」的判定必须是同一份
  ✓ 发布后：学生端课时读面拿到框体 A
  ✓ 发布后：学生画布（项目 payload）也是框体 A
  ✓ 重建但没发布：学生端课时读面仍是框体 A（未发布不可见）
  ✓ 重建但没发布：学生画布（项目 payload）仍是框体 A —— 与判定同一份
  ✓ 重建但没发布：拿**画布上那个框体**去生成，不会报「生成框体不存在或已不属于本课」
  ✓ 更新发布后：学生端课时读面换成框体 B / 学生画布也一起换成框体 B（全部同步）
```

⚠️ **为什么钉在 p54 而不是 p28**：p28（生成框体守卫）一直全绿，是因为它那节课**从来没发布过** ——
没有快照时两边都回退实时，天然同源。**这个 bug 只在"发布过"的课上出现**，只能钉在会发布的那条守卫里。
反向自检（把 `normalizeProject` 改回读实时）实测**原样复现**用户看到的那句话：
`GENERATION_BOX_NOT_FOUND / 生成框体不存在或已不属于本课`。

### E. 同一天用户提的两条界面口径（也都做了、也自己截图看过）

**① 有未发布的改动，要在「版本发布」标签上就看得见**（用户：「而不是点进去才看得到」）：
课包详情的 `版本发布` 标签后面加一个琥珀色角标 **「有未发布」**，判据用**同一处**
`detail.hasUnpublishedChanges`（与面板里那个 status 同源，不会漂）；选中态在紫底上换半透明白。
截图验过：S1 课包（有未发布）标签上是 `版本发布 [有未发布]`，选中态也清楚；
面板里同时写着「已发布课时：9/16（未发布的课时无法随课包上线）」。

**② 画布左侧的生成框体，要有一圈金色环绕**（用户：「一看就知道这个是生成框体…只需要区分生成框体即可」）：
`.cv-item.is-gen-box` = border + box-shadow **各一圈**（四边完整，悬停换色时不会被吃掉），
图标那套模态色（生图/生视频/生音乐）**不动**。截图验过：`图片模块` 那一组（全是框体）每一项都有金边；
`提示词模块` 那一组（普通素材）没有 —— 对比就在这两张图里。

⚠️ **为了截后面这一张，给 `scripts/page-shot.mjs` 加了 `--fixture <文件.mjs>`**：页面需要
"生产上不存在或不敢动"的状态时先自己造 —— 夹具里用 `api` / `db` / `hashPassword` 把学生排进一节
"已发布 + 有生成框体 + 课堂已开始"的课，再把画布页面截下来（本轮那份夹具 `fixture-canvas-box.mjs`
是一次性的、没入库）。**入口面板默认是收起的**，得先 `--click "图片模块"` 才会展开。

### F. ⚠️ 这次发布把另一个会话的提交一起带上线了

- 发布脚本按 `HEAD` 构建（自检只要求"期望提交在 HEAD 里"），而工作区/远端上已经有另一个会话
  推送的 `d1d44a7 feat(客户端更新): 后台配置更新策略`，他的文档里写着**"尚未部署"**（域名备案 + 旧客户端没有更新器）。
- **实际影响很小**：那一提交只是**平台端新增一个页面（系统管理 → 客户端更新）+ 两条 admin 接口
  （`GET/PUT /api/admin/client-update`）**，权限 `ADMIN_AUDIT`、写操作进审计；
  对机构端/学生端/客户端都是**惰性**的 —— 没人在后台点「保存更新配置」就不会动
  `/srv/ai-kids-platform/downloads/manifest.json`。我用 `scripts/page-shot.mjs` 截过那一页，渲染正常。
- **已按事实更新**：`开新会话先读-现状对齐` 里"后台更新配置代码已实现但**未部署**"这句已改成
  "已随 release 20260921T035517Z 上线（发布按 HEAD 走带上的）"，客户端侧仍**未发布**。
- **下次发布前先看** `git log --oneline origin/<branch> -3`，并确认对方那条是否 ready（坑 77）。

### G. 客户端更新清单：按客户端那份《接入要求》逐条核 + 补的两处

客户端项目给了一份契约（`GET /downloads/manifest.json?t=` 免鉴权返回**合并后**的 JSON；后台只改策略字段、
文件身份字段由发布脚本生成；并列了「必须验证」十条）。实现提交 `d1d44a7` 已在分支上，
**现在完全归平台维护**（对方原话：「后续完全交给平台项目处理」）。逐条核对结果：

```text
① 公开清单     未登录 /downloads/manifest.json?t=… → 200 · application/json · 可解析         ✓
② 清单↔真包     size 308138555 与磁盘一致；sha256 e83a1cb5…1008 与磁盘一致；HEAD 200 可下      ✓
③ 写入原子性    另起进程猛读：写 300 次、读 299578 次，**一次半截 JSON 都没有**                 ✓（本轮补）
④ 后台生效      临时实例 PUT /api/admin/client-update → 清单文件立刻反映策略，
                且身份字段（版本/文件名/size/sha256）原样不动                                  ✓
⑤ 启动复写      清单被发布脚本整体覆盖后，策略能被复写回来（mandatory/minVersion 不丢）         ✓（本轮补）
```

**本轮补的两处（契约明确要求、原实现缺）**：

1. **原子写**（契约：「清单更新过程中不能出现半截 JSON」）：原来直接 `writeFileSync`，而客户端随时在
   带 `?t=` 读 → 改成**同目录**临时文件 → `fsync` → `rename` 覆盖（同目录是必须的：跨设备 rename
   会退化成拷贝，就不原子了）。
2. **策略另存 + 启动复写**：客户端发布脚本是**整体覆盖** manifest.json 的（只写身份字段），
   覆盖之后后台配的 `enabled/mandatory/minVersion/note/channel` 就没了 —— 即「每发一次客户端版本，
   后台策略被静默重置」。现在后台保存时把策略另存一份（清单旁边的 `manifest.json.policy.json`），
   **服务启动时只补差异复写回清单**（失败只记一行，绝不影响启动；身份字段永远由发布脚本说了算）。
   ⚠️ **仍有一个窗口**：发布脚本覆盖之后、平台下次重启（或后台点一次保存）之前，清单里是发布脚本那套
   默认值。要彻底对称，客户端那条发布流水线应改成**合并写入**（或发布后调一次后台保存）—— 已同步给对方。

**运维**（契约 3.4：服务进程用户要有权限改清单）：生产那份清单原来是 `root:root`（服务跑在
`ai-kids-prod`）→ 已 `chown ai-kids-prod:ai-kids-prod` 目录与清单，并**以服务用户身份实测**
（建临时文件 + rename + touch 都通过）。客户端发布脚本里本来就有 `chown ai-kids-prod` 那一行 ✓。

**守卫**：`p120-client-update-config` 从 4 条扩到 8 条（原子写机制 + 契约字段落盘 + 策略复写 +
并发读不断 JSON）。⚠️ 并发那条在 **Windows 上跳过并说明**：rename 覆盖一个正被别的进程打开的文件
在 Windows 会 EPERM，而生产是 Linux（POSIX 下 rename 原子、读的人拿旧 inode）——**这条证据只能在
Linux 上跑**，别在 Windows 上看到"跳过"就以为它没做事。
逐条跑生产的那份验收脚本：`.tmp/verify-client-manifest.mjs`（只读生产清单 + 临时实例指向清单副本）。

### H. 客户端运行时只认"这节课声明了 VibeCoding"（客户端项目报的门禁缺口）

客户端报的**属实**：平台建课时就定了上课类型，但 `apps/server/src/routes/studentRuntime.js` 的三个解析器
**什么都没筛** → 画布课也被当成"你现在能进的课"下发：`client-context.classroom` 有值、
**网关密钥照发**，客户端看到 `classroom != null` 就打开 VibeCoding 环境
（它拿不到类型字段，自己判断不了）。**这道门禁必须在服务端**：客户端只执行"平台下发的可进入结论"。

⭐ **判据用课时声明的类型，不用课堂那个单值** —— 这一条差点做错，记下来：

```
平台建课时：勾选框，可多选 → course_lessons.delivery_modes（只画布 / 只 VibeCoding / 两种同时）
老师建课堂：**只读展示**，跟着课时走（9-16 口径「老师不再选课堂模式」）——
           存进 class_sessions.delivery_mode 的是 publishedModes(课时)[0]，也就是"第一个"
⇒ 两者只在"两种同时开"时分叉：课堂那个单值是 CANVAS，而课时其实两种都开
```

第一版按客户端的字面要求筛 `class_sessions.delivery_mode = 'VIBECODING'`：单类型课时行为正确，
但**双开课时会被误挡**（网页两个入口都亮、客户端却回"当前是画布课堂"）。
改成 `deliveryModesOf(课时两列).includes('VIBECODING')`（与网页侧 `lessonAvailability` **同源**，口径 61）：

| 课时声明的类型 | 网页 | 客户端 |
|---|---|---|
| 只画布 | 只亮画布入口 | **不发密钥**（回 `CLASSROOM_MODE_MISMATCH`） |
| 只 VibeCoding | 只亮"去客户端" | 正常下发 |
| **两种同时** | 两个入口都亮 | **也放行**（这一版修掉的误挡） |

其余口径不变：多课堂时**候选只放"声明了 VibeCoding"的课**；点名纯画布那节 → 明说
`CLASSROOM_MODE_MISMATCH`（**绝不静默换课**）；只有画布课时也回 MISMATCH（不是"老师还没开始上课"）；
`client-context` 遇到 MISMATCH 回 `classroom:null / classrooms:[] / upcoming:null / reason / message`
且**不带 gateway/presets/sends**；交作品（`/submit`、`/submit-upload`、`/deliverables`）在画布课下 403
`RUNTIME_NO_ACTIVE_CLASSROOM` + 「当前是画布课堂，不能提交 VibeCoding 作品」（错误码沿用既有的，客户端不用改）。

**守卫** p119 从 14 条扩到 **23 条**，其中「同时开两种」那条单独钉住上面的表格最后一格。
反向自检（把判据换回"按课堂单值"）→ **6 条红**：画布课又拿到 `classroom` + 网关密钥，
**并且真的把作品交上去了**（返回 `vibesub_*`）—— 两种判据的差别就固定在守卫里了。
⚠️ 顺带修了两条守卫**自己的夹具**（p118/p119）：只把课时/课堂改成一半，一上闸就被自己的门禁挡住
（见坑 79）；现在两列（`delivery_mode` + `delivery_modes`）都写全，避免 JSON 与单值打架。

**生产实况**（只读查过）：36 节课里 **18 节只画布 / 4 节只 VibeCoding / 0 节双开**，
每间课堂的类型都与它课时的类型一致；当时全库唯一在上的那节正是画布课（「ketangceshi」，1 名学生）
—— 修复前他会拿到密钥，现在会看到那句提示；历史上**没有**画布课提交过作品（0 件），不用清数据。

### I. 单文件上传上限 25MB → 200MB（用户要传 100MB+ 的课件 PPT）

用户问「创建课包上传素材最大多少，我有 100 多 MB 的 PPT 能传吗」→ 当时是 **25MB**（`FILE_UPLOAD_MAX_BYTES=26214400`），
传不了。按三层一起改：

| 层 | 改前 | 改后 | 说明 |
|---|---|---|---|
| **应用** `FILE_UPLOAD_MAX_BYTES` | 25 MiB | **200 MiB** | 代码里还有一层硬顶，同步 100MB → **200MB**（`MAX_UPLOAD_CEILING_BYTES`；env 只能往下调） |
| **nginx** `client_max_body_size` | 30m | **300m** | 必须 > 应用上限，否则轮不到我们说中文、只给一句裸 413（sites-enabled 与 sites-available **两份都改了**） |
| **Cloudflare** | — | 不参与 | 域名现在是 **DNS-only**（公网解析直给源站 IP、nginx 没配 real_ip、日志无 cf-ray）→ 它那层 100MB 上限今天不适用 |

**真正的约束不是读 body，而是病毒扫描**（这次量出来的，也是新加两道保护的原因）：

```text
clamscan 每次调用都要把病毒库读进内存：扫 150MB 文件 峰值 RSS ≈990MB / 9.4s
⇒ 一次 200MB 上传的峰值 ≈ app 400MB + 扫描 1GB ≈ 1.4GB，而机器只有 1.6GB（可用 ~1.1GB）
```

所以同期补了两条**守卫机器**的机制（不是可选项，是 200MB 的前提）：

1. **读 body 之前的内存闸**（`services/uploadBodyGate.js`）：默认「同时 2 份 / 合计 256MB」——
   一份 200MB 天然互斥；超了当场 429 `UPLOAD_BUSY`（中文），**不走到分配内存那一步**；
   名额在请求结束（finish/close）归还，release 幂等。
2. **扫描器包一层内存上限**：`/usr/local/bin/clamscan-limited`（`ulimit -v 1.3GB` + exec clamscan）；
   内存真不够时让**这次扫描失败**（应用回「文件安全扫描失败，上传已拒绝」），而不是让内核 OOM 去挑受害者
   —— 这台机器上 OOM 挑走过学生的创作环境。`FILE_UPLOAD_SCANNER` 指向它。
   另把扫描超时 120s → **240s**（150MB 实测 53s，机器忙时要留余量）。

**实测（新配置下，端到端）**：

```text
① 穿过 nginx 发 120MB 请求体      → 返回**应用自己的 400**（不是 nginx 413）✓ nginx 那道已抬上去
② 生产 env 值起临时实例、真传 150MB → HTTP 200 / 60s，落盘字节 157286404 与源文件一致 ✓
③ 并发两份 150MB                   → 第一份 200 ✓，第二份 429 UPLOAD_BUSY（不是 OOM）✓
   过程中可用内存最低 ~90–125MB（峰值几乎全是 clamscan 的 ~990MB）
```

**守卫**：`p10-file-upload-security` 补三组（硬顶 200MB 且 env 越不过去、超限文案用配置值、
内存闸「2 份/256MB」的四条边界 + release 幂等 + 归零）。

⚠️ **两条遗留（都记进 §五）**：
1. **机器内存薄**：一次 200MB 上传的峰值把可用内存压到 ~90MB。要么**扩容**（这台机器上面还挂着
   「学生环境被 OOM 杀掉」那段事故），要么把上限往回收 —— 现在能跑，但没有余量。
2. **ClamAV 病毒库 16 天没更新**（`/var/lib/clamav/main.cvd` 是 9-05 的，扫描时 ClamAV 自己会警告
   「database is older than 7 days」），而且**没有 freshclam 定时任务** —— 等于拿两周前的库在扫。

### J. 视频「参考」的语义修好了 + 四类框体的数字进度条（用户同一天提的两条）

> ⚠️ **2026-09-21 晚补正（见 §二.M）**：这一节把根因定成"语义错了（该当首帧却当参考发）"——
> **语义确实错了、也确实修对了，但这不是全部**：图片**压根没到上游**（上游在境外抓不到我们域名的图，
> 且它读不到不报错）。所以两种发法出来的都是"与参考无关的画面"，用户复验才会说"还是没法参考"。
> 真跑验收证明：把素材**先传到上游**之后，首帧才真的生效。

**① 「视频生成出来跟参考也不一样啊。完全是两种东西啊」** —— 学生把《清明上河图》连到视频框体、
写「图片动起来」，出来的是一段毫不相干的画面（另一个人物场景）。

排查结论（**先排掉了两个看起来很像的假设**，别重复走）：

```text
✗ "参考没发出去" —— 生产那个模型级模板里 `{{referenceItems}}` 是**在的**
  （content: [ {text:{{prompt}}}, {{frameItems}}, {{referenceItems}} ]），图确实发出去了
✓ 真正的根因是**语义错了**：MiniMax-H3 在生产上声明了四种输入方式（含 OMNI_REFERENCE），
  而客户端只要看到"支持全能参考"就一律把连过来的图当**参考**发 ——
  可"参考"在上游是"启发素材"（画一段像它的），不是"这张图动起来"（**首帧**）。
  学生那句「图片动起来」要的是后者。
```

改法（判定收成**一处** `videoInputPlan`，生成 payload 与面板那行提示都读它 —— 口径 61 同源）：
**能当帧就当帧** —— 连过来的全是图片、1~2 张、模型也支持帧 → 走**首帧**（+尾帧）；
够不着的（多张图 / 视频 / 音频混合参考）才走全能参考。
顺带修一个真 bug：尾帧判定原来写的是 `LAST_FRAME`，而模态值是 **`FIRST_LAST_FRAME`**
（服务端 `acceptsLastFrame` 看的是后者）→ 尾帧永远不亮，客户端与服务端口径又不一致。

同时把视频这条的两处静默补齐（与图片那条 9-17 的口径对齐）：
· 默认模板新增 **`VIDEO_OMNI`**（带 `{{referenceItems}}`）—— 原来**没有任何默认模板**能承载全能参考；
· `generationOptionsFor` 的视频分支加「模板带不了参考就**当场拒绝**」（原来只有图片分支有）。

**② 「能否增加个数字进度条：小灵施展魔法中 1%」** —— 四类框体（文本/图片/视频/音乐）都加上了：
底部一条细进度条 + 「小灵施展魔法中 N%」（`.learning-node__progress`）。
⚠️ **这不是上游的真实进度**：上游只回状态、我们也没存过原始响应（只有 request/payload id），
所以百分比是按**实测中位耗时**估的 —— 生产 `generation_jobs` 实测：图片中位 45s（最长 82s）、
文本 2s、视频 132s，音乐取 90s 量级；**封顶 95%**，最后 5% 留给真正的完成事件，
不出现"进度满了还在等"的假完成。要真进度得让上游回 progress 并加列存（见 §五 遗留）。

**守卫**：p109 补 5 条（默认模板带得动参考、参考真进请求体、没参考时形状不变、显式模板带不了就拒绝、
以及客户端四条：能当帧当帧 / 尾帧用 `FIRST_LAST_FRAME` / 面板与 payload 同源）。
截图验过：夹具把视频框体置成「40 秒前开始生成」，画布上显示「小灵施展魔法中40%」+ 进度条 ✓。

### K. 2.5 生图模型报 output_format + 小屏「生成画面」按钮变形（用户同一天提的两条）

> ⚠️ **2026-09-21 晚就地更正（见 §二.M）**：这一节 ① 的**根因判断是错的** —— 不是"字段位置"
> （改成顶层照样报同一个错），而是**这款模型根本没有 output_format 这个能力**（低价扩展版）。
> 下面这段保留原样当"错误示范"看，结论以 §二.M 为准。

**① 「为什么使用 2.5 这个模型会报错…2.0 的模型好像是可以正常的」**，上游原话：
「AI 供应商调用失败（上游：`output_format is not supported by this model`）」。

```text
根因：上游图像模型**分两代、请求体形状不同** ——
  · 2.5 系（`zhenzhen-image-g-v2.5-*`）把 resolution / output_format / quality / background 放**请求体顶层**
    （docs: api.seedance.nz/docs/#image-g-v25-ext）
  · 2.0 收的是我们默认模板那套（塞在 `metadata` 里）→ 所以它一直正常
```

改法：新增 `MODEL_TEMPLATE_DEFAULTS`（**按模型名**给内置模板），`requestTemplateFor` 的优先级变成
**渠道/模型上配过的 > 内置按模型 > 内置按模态** —— 管理员在后台配过的永远赢，内置只兜底。
2.5 那份仍然带 `{{referenceImageUrls}}`，所以图片参考那条门禁（模板带不了参考就当场拒绝）不受影响。
运维层面也可以不改代码：在「AI 能力与价格 → 渠道与模型」里给这个模型配一份请求模板即可（优先级最高那条）。

**② 「小屏幕这个生成画面按钮变形了」**（截图里「生成画面」被压成一列竖排）。
根因：那一行是 flex，按钮既没 `white-space: nowrap` 也没 `flex: 0 0 auto` ——
窄屏时同一行还有配置胶囊与报错文字，按钮被压到只剩一列字宽，**中文于是按字折行**。
修完在 900px 宽下截图验过：按钮保持完整药丸、报错文字自己换行 ✓。

**守卫** p109 补 5 条：2.5 拿到顶层模板 / 2.0 与其它模型形状不变 / 2.5 仍带得动参考 /
渠道配过的模板优先 / 渲染出来的 2.5 请求体字段位置正确（顶层、无 metadata）。

### L. 事故：发布重启打断了在途生成（用户报「上游已经扣钱了，但是这边不返回了」）

用户原话：「现在生视频你修改后，等了一会，上游已经扣钱了，但是这边不返回了。下面的报错之前是：
视频正在生成xxx，但是一直不显示出来，再点击再次生成，也不行了」。

**现场（生产库实查，时间线对得上）**：

```text
07:00:38Z  学生点了「生成短片」→ 任务入队、worker 认领、上游受理（会计费）
07:02:12Z  **发布重启了服务** ← 就是这一轮我自己的那次发布
之后       任务永远停在 RUNNING：没人再接它 → 结果拿不回来；
           而 assertBoxNotGenerated 把"在途任务"也算占用 → 框体被判「该生成框体已经生成过了」
           → 学生**再也点不动**（他说的"再点击再次生成也不行"就是这个）
```

**为什么兜底没接住**：`initializeAsyncGenerationQueue` 当时只回收**租约过期（>5.5 分钟）**的 RUNNING，
而重启那一刻这条任务才 **94 秒** → 被跳过 → 之后再也没有任何东西看它。

**改法（按 worker 判，不按时间猜）**：`worker_id` 里带进程号（`ai-worker-<pid>-<rand>`），
所以**本进程刚起来时，任何 RUNNING 都属于已经没了的那个 worker**：
· 启动时 `interruptOrphanedJobs()` 一律收成 FAILED（码 `GENERATION_INTERRUPTED`，文案给学生看：
  「生成过程中服务重启了，这次的结果没拿到（上游可能已受理）；请重新点一次生成」）
  → 框体随之解封（FAILED 不算占用 ✓ 学生可以重试）；
· **关服钩子**（SIGTERM/SIGINT）也收一次自己的在途任务 → 重启后立刻是"可重试"状态，不必等下次启动；
· 旧的租约判据保留，作为**崩溃**（没走关服钩子）的兜底。

**本次事故的收尾**：那条任务用同一个码/文案标成 FAILED，框体的在途数归零 ✓（学生已可重新生成）。
⚠️ **账上注意**：`usage_records` 里**没有**这笔成本（我们的账本没记到），而上游可能已经计费 ——
这类"上游扣了、我们没记"的差额只能靠官方账单对账找回来（见 `docs/operations/真实三账对照-20260914.md`）。

**守卫** p4-o12 加一条**带反向自检**的用例：造一条"别的 worker 认领、才开始 10 秒"的 RUNNING
（旧判据不会管它）→ 服务启动后必须 FAILED/`GENERATION_INTERRUPTED`、且该框体在途数归零。
摘掉回收调用时这条会红（实测：孤儿仍 RUNNING、在途=1 —— 正是用户看到的现象）。

### M. ⭐⭐ 用户复验之后：真正的根因是「上游读不到我们域名上的素材」（同一天晚上补的）

用户复验回来说两件都没好：
> 「1、图1还是没法参考，完全不一样的内容。我是需要重新开个画布吗？我已经强制刷新过了。
>   2、图2图片生成还是报错。」

**② 2.5 生图：§二.K 的判断是错的**（下面那条已就地更正）。生产库里那几条失败任务的
`created_at` 是 07:14–07:29 UTC，**都在"顶层 output_format"那版上线（07:01）之后** —— 也就是说
上一版改了位置、照样报同一个错。上游文档（`https://api.seedance.nz/docs/llms.txt`，768KB 全量 Markdown，
可直接投喂）里写着 2.5 有**两款不同能力**的模型：

```text
· Image G v2.5：Flare / Sunburst —— 收 output_format / quality / background（放**顶层**）
· Image G v2.5：低价扩展版（线上在用的 zhenzhen-image-g-v2.5-lowprice）——
  只认 model / prompt / images / n / size / resolution / nsfw_check，
  文档原话：「此扩展版**不提供质量档位、输出格式、透明背景或流式选项**」→ 传 output_format 就是 400
```

所以"`output_format is not supported by this model`"**不是在说位置放错了，是在说这个模型没有这个能力**。
修法：低价版单独一份内置模板，一个不支持的字都不带（flare/sunburst 与 2.0 形状一律不动）。

**① 视频首帧：请求体形状本来就是对的，坏的是"图没到上游"**。排查顺序（照这个走，别重复）：

```text
✗ "形状不对" —— 用生产渠道配置 + 生产代码把请求体渲染出来看：
    content: [{text}, {image_url, role:'first_frame'}] + duration/resolution/ratio，与上游文档逐字对得上。
✗ "上游不支持首帧" —— 上游文档明写支持 first_frame，而且做了受控实验 ↓
✓ 真根因：**上游在境外，抓不到 iicili.cyou 上的图**，而且**它读不到不会报错**：
    · 用户那两条视频任务（15:21）上游**压根没到我们的 nginx** —— access.log 里一条都没有；
    · 唯一到了的一条（15:35，python-httpx、腾讯云香港 IP 34.92.153.105）
      **只读了 105703 / 538855 字节**就断了；
    · 读不到图时它当**文生视频**跑 → 出来一段与参考毫无关系的画面（用户看到的"完全不一样"）；
      任务状态、任务列表里的 ratio/duration 全部正常，**看日志看不出任何异常** —— 只有把产物
      第 0 帧抽出来跟源图比才看得见（受控实验就是这么做的：同一张图先传到上游再当首帧 →
      视频第一帧就是那张画 ✓）。
```

改法（`services/upstreamMediaMirror.js`）：请求体里凡是指向**我们自己域名**的素材（首帧/尾帧/参考图，
图片/视频/音频一视同仁），先 `POST {上游}/v1/files/upload` 换成**上游自己的 URL** 再发 ——
这也是上游文档推荐的流程，上传免费。

```text
· 按源 URL 缓存 20h（上游那份只活 24h）；同一张图被多处引用时并行也只传一次
· 上传必须带**文件名后缀**（上游按后缀认类型：filename=asset → 400
  「unsupported file type; allowed: jpg/jpeg/png/webp, mp3/wav/flac, mp4/avi/mov/mkv」），
  而我们的下载路径是 /download 没有后缀 → 按 content-type 补一个
· 素材超过上游上限（图片 30MB / 音视频 50MB）在读 body 之前按 content-length 拦下，
  给一句中文（我们自己单文件上限已抬到 200MB，"传个大课件当参考"完全可能）
· 上传失败**当场报错**（`素材上传到上游失败：…`，带上上游原文），**不许退回原 URL 静默继续** ——
  静默的结果就是"生成出一段跟参考无关的作品"，比报错糟得多
```

**真跑验收（走生产代码路径，不是看文档猜）**：

```text
① 视频 MiniMax-H3 + 首帧 = 我们域名上的《清明上河图》→ 成功；
   把结果视频第 0 帧抽出来：**就是那张画**（虹桥、桥上人群都在，画面在动）。
② 图片 zhenzhen-image-g-v2.5-lowprice + 参考图 = 同一张画 → 成功；产物是**同一幅画的夜景灯光版**
   （虹桥、河道、市集构图一致）—— 既证明 2.5 不再报错，也证明参考图真的进了上游。
```

⚠️ 这两次验收**花的是上游的钱**（各约 ¥0.75，合计约 ¥1.5–2）：用户是知情的测试账号，
但这笔实跑是必要的 —— 不真跑一次，"机制对"和"上游画成了什么"是两件事。

### N. ⭐⭐ 用户复验"还是没法参考"：**worker 第二次解析把首帧悄悄丢了**（当晚第二轮）

用户原话：「视频模型还是没法参考，完全是两个东西」（并给了上游文档链接 `…/docs/#minimax-h3`）。
生产实查（那笔任务 **08:08:53，跑的就是 §二.M 修完的新代码**）：

```text
· 入队写进 job 记录的 source_asset_url 是**绝对地址**
  https://iicili.cyou/api/public/file-assets/file_56ec1343bfef…/download
· worker 真正执行时，拿**同一个绝对地址**再解析一次（`resolveFirstFrameUrl`）——
  而 `publicFileAssetUrl` 当时只认**站内相对地址**（正则要求以 `/api/...` 开头）→ 第二次解析返回空串
  → 首帧被丢掉 → 上游收到纯文生请求 → 出了一段石头猴子，而任务 SUCCEEDED、日志干净。
· 铁证：nginx 访问日志里**我们自己的服务器从没抓过那张图**（只有学生浏览器抓过）。
  也就是说 §二.M 修的镜像那段代码是好的 —— 它压根没被喂到东西。
· 反推上一轮为什么没发现：我验的是**直接调 provider** 那条捷径，恰好绕过了 worker 这一步。
  「我验过」和「用户在画布上看到的」差了整整一层。
```

改法两处（**缺一不可**：第一处是这次的真因，第二处让同类问题不再静默）：

```text
① `publicFileAssetUrl` 认**自己域名的绝对地址**：摘掉 origin（域名万一变了就退一步用 pathname）。
   文件 id 仍回查数据库、仍要求「公开可见 + 非教学素材」，所以不存在"把外部地址送给上游"的口子
   （守卫里有反例钉着：私有素材、外部地址都仍然解析不出来）。
② worker 里加 `assertMediaResolved`：**入队时交给我们的素材，执行时不许凭空消失** ——
   解析不出来就当场失败，并告诉学生「回到画布重新连一张再生成」。
   静默降级成"没有输入"是最糟的结果（与「模板带不了参考就当场拒绝」同一条口径）。
   ⚠️ 只在 worker 那条路加：同步接口那条路的素材来自请求体，行为已被 p11 正面钉着（外部地址要被拦）。
```

**真跑验收（这次走的是"用户真正走的那条路"）**：生产库副本 + 临时实例 + 真上游 →
**让它自己的 worker** 跑完这条任务 → 产物**第 0 帧就是那张源图**（用户那张石头小怪兽）✓；
对**已发布产物**（`LIVE_CHECK_REPO=production/current`）又跑了一遍同样的验收 ✓。
nginx 里也能看到镜像真去抓了我们自己的图（3,871,942 字节全取）。

脚本已收进仓库：`deploy/production/live-canvas-video-check.mjs`（⚠️ 会真花钱，用法见 §六）。

### O. 画布交互收口：按钮该藏就藏、复制禁掉、「自动」画幅要自适应（用户当晚第三批）

用户原话（四条）：
> 「1、已经生成过的框体/无法生成的框体，这个按钮应该都要隐藏。不然会误导别人。比如图1和图2，本来就不能生成就不要显示这个按钮了。
>   2、图3我现在可以用快捷键复制框体，这个应该要禁用掉。
>   3、图4是别的平台……我们是不是也要分别适配，并且在配置课包的时候要选择视频模型是属于哪种。
>   4、图6为什么生成出来是扁的画面，没有自适应比例呢？是我们的问题还是模型的问题呢？」

**① 生成按钮该藏就藏**（图1 是已生成框体上的「重新生成」；图2 是"非框体节点 + 生成按钮 + 红字"）。
面板按钮原来只看 `state !== 'running'`，于是两种**必然报错**的节点也长着按钮：

```text
· 不是课时框体的节点（拖进来的本地文件 / 普通素材节点）→ 点了收到 403「请从课时配置的生成框体发起生成」
· 已经生成过的框体 → 平台规则是"每个框体只能成功生成一次"，点了收到「该生成框体已经生成过了」
```

现在合成**一处**判定：`canGenerateHere = 本课没配该模态框体 || 这个节点有 boxId`（与服务端
`resolveLessonGenerationBox` 同一口径）；再叠一层"已出过东西就不给按钮"（`alreadyProduced`）。
**判据只有一处**：按钮显不显示、要不要忽略那条陈旧报错，都读它。
父层把"本课配了哪些模态的框体"（`boxModalities`）算好传给画布（画布自己拿不到 generationBoxes）。
顺带：不能生成的节点**不再挂着上一次那条误导报错**（状态胶囊回「未生成」）—— 那是"点了本来就不该点的按钮"造成的。

**② 禁掉快捷键复制框体**：那个 Ctrl+C/V 监听只判 `readOnly`，学生照样能复制。而复制出来的框体
**共用同一个 boxId** → 生成必被拒，还让"框体是否已在画布上"的判断错乱（图3 就是复制出来的三个）。
现在 `allowNodeCreation=false`（学生画布）整段不给复制/粘贴 —— **位置在撤销/重做两行之后**，
学生照旧能撤销；另外即便能自由建节点（机构端/老师），框体节点也不参与复制。

**③「自动」画幅要自适应**（用户问"是我们的问题还是模型的问题"——**是我们的**：
学生选「自动」= 不下发比例、用模型默认，而"模型默认"被折成了列表里的第一个 16:9）。
用户那张源图是 **2048×2048（1:1）**，固定 16:9 会让上游把图**硬拉**成 16:9（上游文档：关键帧请求
"会执行指定的固定比例"，不像官方那样忽略它并自适应）。现在：

```text
客户端：视频那条的「自动」原样送 'auto' 上去（不再在客户端折成第一个比例）
服务端：按上游（MiniMax V2）的语义翻 —— 关键帧（首帧/尾帧、无参考）→ adaptive（读首帧图比例）
        纯文生 / 全能参考 → 固定比例（上游：参考请求显式传 adaptive/auto 会报错）
        只在"模板把比例放在顶层 ratio"时才翻（别家上游/别代模型不替它们做决定）
        图片那条不翻（size: auto 的支持随型号而异，2.0 的文档里没有）
```

真跑验收（走队列）：同一张 1:1 的源图 → 产物 **768×768**（原来是 1038×576 的扁画面）✓

**④ 逐模型的输入方式：绝大部分早就有了**（回答用户第 3 条，见下面"现状/差距"）。

```text
现状（2026-09-21 实测生产配置）：
· 每个模型在「AI 能力与价格 → 渠道与模型」里**声明自己支持哪些输入方式**：
  MiniMax-H3 = TEXT / FIRST_FRAME / FIRST_LAST_FRAME / OMNI_REFERENCE（就是用户贴的那四条）
· 客户端按声明自适应：只有 TEXT → 只给提示词；FIRST_FRAME → 首帧一行；
  FIRST_LAST_FRAME → 首帧+尾帧两格；OMNI_REFERENCE → 「参考」行（能连图片/视频/音频）；
  模型声明了多种时**能当帧就当帧**（口径 68）
· 服务端逐条拦：混用帧与参考、模型不支持却带了画面、i2v 模型没给画面…都有明确错误码
· 参考数量上限就是用户说的那几个数：图片 ≤9 / 视频 ≤3 / 音频 ≤3（服务端 REFERENCE_LIMITS）
差距（本轮补掉的）：
· 超上限以前是**悄悄丢掉多的**（连了 10 张只发 9 张）→ 现在当场报错说清"最多 9 张、你连了 10 张"
· 参考区缩略图原来只显示前 6 个 → 提到 9（连满 9 张却只看到 6 个，学生会以为没连上）
可选（没做，等口径）：
· 课包配置里给框体加一个"强制输入方式"（比如这节课只允许首尾帧）—— 现在模式完全跟着**模型**走，
  老师选的是模型；要"按课锁模式"就得再加一个框体配置项 + 面板上显示当前模式
```

### P. 生成方式（文生/图生/首尾帧/全能参考）由课包锁定 + 固定比例先裁再发（用户当晚第四批）

用户原话：
> 「1、图1是我们的图片，我让他生成 16:9 的比例，是自适应的。图2是我们生成出来，也是同样的参考图让它生成 16:9，明显是扁的。是什么问题。
>   2、文生视频/图生视频、首尾帧、全能参考是完全不一样的概念……并不是靠我们连线的多少来决定的。
>   有可能连 2 张图片，但是并不需要首尾帧，而是全能参考的模式。这个是否可以支持，并且在课包选择，
>   而且还必须是能够让上游知道的状态。而且这也会映射到画布，比如课包的课程选择的是文生视频，那么就无法连线。
>   图生视频就只能连 1 张图片，类似这样的判定要有。如果视频可以做到，生图框体也同样有文生图和图生图的模式。」

**① 「明显是扁的」——是我们的问题，不是模型的**。用我们自己的产物比出来的：

```text
源图 2048×2048（1:1）→ 我们固定 16:9 生成的视频：1038×576，人物被**横向拉伸**（眼睛变椭圆、身子压扁）。
同一次对比里的生图（图1）看着正常，是因为**图片模型是重画一张 16:9**，不是把输入拉扁 —— 两件事不同。
上游文档自己写着：「本服务会执行关键帧请求指定的固定比例，而官方会忽略它并使用自适应，因此这部分行为有差异」
→ 也就是说，是**这个中继的扩展行为**在把画面拉扁，而我们把比例原样传了下去、没做任何处理。
```

改法：**关键帧（首帧/尾帧）+ 固定比例时，先把画面按目标比例居中裁好再发给上游**。
新增 `services/mediaFit.js`（ffmpeg/ffprobe；裁不动一律回退成原图、比例本来就一致时不重编码）。
实测：2048×2048 → 2048×1152（人物比例不变）；走队列真跑的产物第 0 帧也确认了。
`adaptive`/`auto` 不裁（那是"跟着图走"）。

**② 生成方式由课包锁定**（这是产品口径的变化，不是 bug 修复）。原来"怎么用连过来的素材"是
**客户端按连线条数推断**的（能当帧就当帧）；用户指出这几个是**完全不一样的概念**，必须显式定。

```text
新增框体字段 inputMode（单数；'' = 不锁，旧课包行为不变），与模型声明的 inputModes（复数）分工：
  · inputModes 回答"这个模型能不能"（在「渠道与模型」里配）
  · inputMode  回答"这节课就要哪种"（在课时配置里选）—— 只能从模型支持的方式里挑，挑不中当没锁
值：VIDEO = TEXT(文生视频) / FIRST_FRAME(图生视频) / FIRST_LAST_FRAME(首尾帧) / OMNI_REFERENCE(全能参考)
    IMAGE = TEXT(文生图) / IMAGE_REFERENCE(图生图)      ← 用户要求"生图框体也同样有"
落点（**一处都不能漏**，漏一处就变成"界面看得见、发出去的不是那样"）：
  ① 老师：平台端「课时 → 画布素材 → 生成框体」多一个「生成方式」下拉（留空＝不锁）
  ② 写入白名单 admin/helpers.js（不加这里，字段会被静默丢掉）
  ③ 下发 normalizeGenerationBox（lib.js）：带 inputMode + inputModeLabel（**中文标签由服务端算**，
     客户端不抄第二份标签表）+ requiresFirstFrame 跟着锁定的方式走
  ④ 服务端语义 generationOptionsFor：**锁定的方式赢过推断** —— 锁全能参考时一张图也走 reference
     （不是首帧）、锁文生视频时不许带画面、锁图生视频时第二张图被拒；
     options.inputModes 收成**一种**，分支/预检/模板选择全部跟着它走（判断只有一处）
  ⑤ 画布连线闸门 isValidConnection → canConnect：文生视频禁止连线、图生视频 1 张、首尾帧 2 张、
     全能参考 9/3/3、图生图 9 张（上限与服务端 REFERENCE_LIMITS 同源）
  ⑥ 画布面板：配置胶囊与素材面板副标题都显示当前方式；锁成文生视频时明说"不用连线，直接写提示词"
  ⑦ 上游"知道"：请求体里首帧/尾帧/参考的 role 与锁定方式一致（锁定决定走哪个分支、选哪份模板）
截图自查（page-shot + 夹具 .tmp/fixture-canvas-modes.mjs）：锁文生视频的框体面板显示
「生成方式 文生视频：不用连线，直接写提示词」+ 配置胶囊「生视频 · 文生视频」；
锁全能参考的框体 + 连一张图 → 走的是**「参考」那一行**（不是「画面/首帧」）。
```

**③ 两种语义的对照（用户问「这个是上游的问题还是我们的问题」，当晚两种都真跑了一遍）**：

```text
模式                    我们发的 role      上游拿到后做什么                        1:1 的图 + 目标 16:9 的结果
图生视频 / 首尾帧        first_frame       把这一帧当成"起点像素"，并按指定比例     人物被**横向拉伸**（变扁）
（关键帧）                                 **执行**（它文档自己承认这一点）          → 我们本地先裁成 16:9 再发，已修
全能参考 / 图生图        reference_image   当**内容和风格**参考，在目标比例画布上    自然的 16:9（模型重画了构图）
（启发素材）                               **重新画**（人物比例正常）
```

结论：**拉伸是上游关键帧模式的执行方式**（它选择"执行固定比例"而不是裁切/补全，
官方 API 反而会忽略比例并用自适应）；而"参考=重画"是**另一个模式**（全能参考/图生图），
我们同样支持。所以别的平台描述的那种效果，在本平台把课包锁成「全能参考」就有 ——
当晚实测：1:1 的图当参考 + 16:9 → 出来的是一段自然的 16:9 画面（构图被重新安排）。
**别把两种语义混起来讲**：一个要"从这张图开始动"，一个要"照它的意思再画一版"。


**④ 用户当晚又定了口径：下拉只留两种** —— 视频 =「文生视频 / 全能参考」，图片 =「文生图 / 图生图」。
图生视频（首帧）/ 首尾帧**不再列进选项**（模型能力里仍可能有；老课包若配过，下拉里显示成
「首尾帧（首帧 + 尾帧）（历史配置）」—— **历史值必须看得见**，否则老师打开是一片空白、
既不知道原来配的是什么也没法改）。

**⑤ 用户报「选了文生图/图生图，保存后再打开还是学生自选」——真 bug，已修**：

```text
复现（生产库副本 + 直调保存链路）：
  lessonCanvasConfig → 写 inputMode=IMAGE_REFERENCE → replaceLessonCanvasConfig → 再读回来
  库里根本没有 inputMode（PUT 还是 200，界面也不报错）→ 典型的"能选、存不下来"。
根因：`normalizeModelCapabilities` 对 IMAGE 把 `inputModes` **写死成 []**，
  而生产的图片渠道**给两个图片模型都配过能力** → 走"按模型能力"那条路时恒为空 →
  保存时 `inputMode` 过不了"只能从模型支持的方式里挑" → 被静默丢掉。
改法：没配就按图片的两种默认（文生图/图生图）；显式配了用配的；不认识的值不顶掉默认；
  顺带补上 IMAGE 的能力校验（原来只校验视频）。
守卫：p125 补 4 条（配过能力后 inputModes 不能变空 / 图片默认两种 / 不认识的值不顶默认 / 视频那条别被带偏）；
  p122 ⑧ 钉住"下拉只留两种 + 历史值有标签"。
```

**口径更新**：这条把上一轮的"七处落点"扩到"八处"——**第 8 处是"能力默认值"**：
加了新的能力字段（如 `inputModes`）时，必须同时想清楚「**给某个模型配过能力的渠道**会不会把它吃掉」。


**⑥ 上传闸：名额被占满时"先排队等"，而不是立刻 429**（用户 2026-09-21 报「同时上传的文件太多」）：

```text
这道闸为什么存在（别删）：请求体是**整块读进内存**的，紧接着每次上传都会 spawn 一个 clamscan，
  它要重新加载 108MB 病毒库、实测扫 150MB 峰值 RSS ≈990MB —— 而这台机器只有 1.6G，
  上限又刚抬到 200MB。所以必须"读 body 之前"先占名额，否则两份大请求叠一起就是 OOM
  （这台机器上 OOM 挑走过学生的创作环境）。当前口径：**同时 2 份 / 合计 250MB**（平台级，不分用户）。
用户为什么撞上：不是"并发太高"，而是**前一个上传还在扫毒、名额一直占着**（200MB 扫起来几十秒），
  他接着传第二个文件就必然撞到 → 而那句文案让人以为是自己操作太猛。
改法：调到名额时**先排队等**（默认最多 18 秒，FILE_UPLOAD_GATE_WAIT_MS），等不到才报那句；
  等待期间**不申请任何内存**，内存保护一点没松；state 加 waited 计数（诊断"到底排不排队"）。
没改的部分（有意）：份数与字节预算保持原样 —— 拆机看内存，重启前 swap 是**满的**；
  真要"同时传好几份大文件"，正解是加内存或把 clamscan 换成常驻的 clamd（遗留清单里那条）。
```

**运维记录（当晚 18:31 服务器重启过一次）**：`journalctl -b -1` 在重启前连续出现
"Under memory pressure, flushing caches"，没有 OOM-kill 记录；重启后平台自动恢复
（服务 active、五入口 200、当天唯一那条 `GENERATION_INTERRUPTED` 是**上午 07:00 那次旧事故**，
不是这次重启造成的 —— 我在每次发布前都查了在途任务）。
⚠️ 但要老实说：**我在机器上跑的截图脚手架（vite build + chromium + 第二个实例）很吃内存**，
在这台 1.6G 的机器上连续跑有把内存压垮的嫌疑 —— 以后要么挑内存宽松的时候跑，
要么把这份脚手架放到本地/另一台机器（本机 node 16 跑不了，这是它一直挤在服务器上的原因）。


**⑦ 用户报「连接了为什么还报错」（图1：全能参考 + 两张自己传的图）—— 三个坑叠在一起**：

```text
生产数据实锤：那两张图是 file_assets 里 PRIVATE + MEDIA_ASSET + owner=这个学生本人。
坑①平台的规矩是"只把**公开**素材交给上游"（publicFileAssetUrl 要 PUBLIC_PLATFORM/PUBLIC_RELEASE），
   而**学生自己上传的图是 PRIVATE** → 两张参考全被静默丢掉 → 预检发现"没有输入" →
   报「需要先连接画面（首帧）再生成」—— 屏幕上明明写着「参考：图片1 图片2」，这句话完全误导。
坑②（修完①才露出来）镜像用 HTTP 取素材，而私有素材在 /api/public/... 上是 **403**（那条路由只服务公开文件）、
   /api/student/... 又要会话 → 照样读不到。
坑③预检那句"缺什么"没看**框体锁定的方式**：锁全能参考时缺的是参考素材，不是"画面（首帧）"。
```

修法（三处一起才通）：

```text
① publicFileAssetUrl 接受**学生本人上传的素材**（owner_type=USER 且 owner_user_id=本人）——
   公开素材照旧；**别人的私有素材与教学素材仍然一律不发**（边界没松；p11 与新守卫都钉着）
② 镜像：我们自己的文件资产**先从磁盘读**（按 file_assets.storage_key 解析到上传根；只认 INTERNAL_PROXY），
   读不到才回落 HTTP —— 顺带省掉一次"自己请求自己"的回环
③ 参考**一个都没留住**时当场报错（GENERATION_MEDIA_UNUSABLE，说清"连过来的素材不能发给 AI"），
   不再静默变成"没有输入"；预检文案跟着锁定方式说
真跑验收：拿那张**私有**图当参考（走队列、走生产代码）→ 生成成功，产物第 0 帧就是那只石头小怪兽 ✓
```

**⑧ 音乐播放器太窄**（用户报「都没法正常播放了」）：音乐卡片原来跟"素材撑宽"那组一起被去掉
`min-width`，而图片/视频是素材撑多宽卡片就多宽、音乐里却是浏览器原生播放条（它自己会收缩）→
卡片缩成一个小药丸。现在给音乐卡片 260px 的最小宽度（守卫 p122 ⑨ 钉住）。


**⑨ 提示词里的中文引用芯片要翻成上游认的写法**（用户 2026-09-21 给的请求体里看出来的）：

```text
用户那条请求发出去的是：「@图片 2 在跳舞然后自然转场到@图片 1」—— 中文芯片。
而上游文档里多模态参考的指代写法是 **@Image 1 / @Video 1**（原文："多模态场景可用 @Image 1、
@Video 1 指代第几个参考素材"）。也就是说这句提示词的**核心语义（哪张跳舞、转场到哪张）全压在芯片上，
上游很可能只当成普通文字** —— 模型得自己猜"图片 1"是谁。
```

改法：发上游前把芯片翻掉（图片 N→Image N、视频 N→Video N、音频 N→Audio N），**只改发给上游的那份**，
库里与画布上学生看到的仍是中文芯片。两条生成路径（同步 + 画布走的 worker）都接上了。
⚠️ 两个细节：序号按**实际发出去**的那份参考算（有参考没留住时会前移）；但**被丢掉的那张保持原样** ——
宁可不动，也绝不能把学生的号指到别人身上（守卫 p125 ⑧ 两个方向都钉着）。

**⚠️ 老实说**：这条**没有**做成 A/B 对照（我准备的对照脚本反复卡住，而且第一版用了张**坏图**：
生产库里 `file_d193a8a6c6b0406b937c` 是个 **8 字节**的文件（只有 PNG 签名），上游对它一律 805「工作流运行失败」）。
所以这里是**按上游文档的写法对齐**，不是"实测两种写法哪个更好"。
另外上游的 H3 专节**没有**写 @ 引用（`@Image N` 那套写在它多模态模型的通用文档里）——
所以只能说"跟文档一致"，不敢保证模型一定照做。真要确认，得拿同一条提示词两种写法各跑一次对比。


## 三、不许破的口径（承接 1–60；**本文新增 61–80**）

61. ⭐⭐ **同一份东西的"读面"与"判定"必须同源**：学生画布画的框体、与点生成时服务端校验 boxId 用的
    列表，**必须是同一个函数的输出**（`mergedLessonCanvas`）。这一条与口径 34/54 是同一族
    （「界面看得到、点开说无权」/「预检与真正那一步不同口径」）——**写两处迟早会漂**，这次漂的代价是
    17 节课的框体点不动。
62. ⭐ **"缺键给空数组"与"缺键回退实时"是两个语义，选错就是静默事故**：快照缺某键时，
    **给空数组 = 把用户手里的东西全没收**（那 16 节课的框体），**回退实时 = 老数据行为不变**。
    老数据（发布早于该功能）要的是后者；「已定格的课包不允许回退」只适用于**可见性判据**
    （第二十三轮），**展示/可用性**一律逐键回退。
63. ⭐ **"要人知道的状态"必须放在**看得见的那一层**上**（用户 2026-09-21：「有修改未发布的应该在
    版本发布这里有明显的提示，而不是点进去才看得到」）。判据与它所指的那处**共用同一个值**
    （`hasUnpublishedChanges`），别各算一遍；角标要能在**选中态**下也看得清（紫底上换半透明白）。
64. ⭐ **画布左侧「生成框体」一眼可辨：四周一圈金色**（border + box-shadow 各一圈，四边完整；
    悬停换色不吃掉它）。**只区分"是不是生成框体"**，模态色（生图/生视频/生音乐）照旧
    —— 那是 2026-09-17 的口径，两条都要在。
66. ⭐⭐ **"能不能进"这类结论必须在服务端定，并按对象类型限死**：客户端运行时（dsh / VibeCoding）
    **只认"这节课声明了 VibeCoding"**（课时类型**可多选**）—— 画布课不下发 `classroom`、
    不下发网关密钥、也不许交作品；**两种同时开时客户端这一侧也放行**。
    ⚠️ **别拿 `class_sessions.delivery_mode` 判**：那只是"跟着课时带的第一个"（老师不再选课堂模式，
    只作历史兼容），用它判会把双开课时误挡（网页两个入口都亮、客户端却回"当前是画布课堂"）。
    客户端拿不到的字段（如 `deliveryMode`）绝不能成为它的判断依据；它只执行平台下发的结论。
    多课堂时**先按类型过滤候选**，点名了不对类型的那节要**明说**（`CLASSROOM_MODE_MISMATCH`），
    绝不静默换成另一节。
65. ⭐⭐ **静态清单是客户端的唯一真相源，两个写方各写一半、谁都不许覆盖对方那半**：
    后台只写**策略**（enabled/mandatory/minVersion/note/channel），发布脚本只写**身份**
    （version/files.name/size/sha256/publishedAt）。当前实现靠"策略另存 + 启动复写"兜住发布脚本的
    整体覆盖；**写这份清单必须原子**（同目录 temp → fsync → rename），因为客户端随时在读它。

67. ⭐⭐ **上传上限有三层，且真正的约束常常不是"读 body"**：应用（`FILE_UPLOAD_MAX_BYTES`，硬顶 200MB）
    **必须小于** nginx 的 `client_max_body_size`（300m）—— 这样"文件太大"这句中文才轮得到我们说；
    Cloudflare 代理时它还有一层 100MB（今天域名是 DNS-only，不参与）。
    ⚠️ 而这个规模下最吃内存的是**病毒扫描**（clamscan 每次加载病毒库 ≈1GB）：
    抬上限必须同时看机器 RAM，并保证有「**读 body 之前**的内存闸」（`uploadBodyGate.js`）——
    否则两份大请求叠起来就是 OOM，而被 OOM 挑走的不止这个进程。
68. ⭐⭐ **"连过来的素材怎么用"是语义问题，不是传输问题**：视频框体上连一张图，
    学生说「图片动起来」= **首帧**（i2v）；只有多素材/视频/音频混合才叫**参考**（启发素材）。
    模型"支持全能参考"不等于"该走参考"——**能当帧就当帧**（`videoInputPlan`）。
    同类提醒：模态值是 `FIRST_LAST_FRAME`（不是 `LAST_FRAME`），客户端与服务端必须用同一串。
69. ⭐ **同一个上游的不同代模型，请求体形状可能不一样 —— 内置模板要按模型名给**：
    2.5 系图片模型把 `output_format`/`resolution` 放**顶层**，2.0 收的是 `metadata` 里那套
    （2026-09-21 用户报「2.5 报错、2.0 正常」）。`requestTemplateFor` 的优先级固定为
    **渠道/模型上配过的 > 内置按模型 > 内置按模态** —— 管理员配过的永远赢，内置只兜底。
70. ⭐⭐ **在途任务必须在"进程边界"上收尾**：启动时收**孤儿**（`worker_id` 不是本进程的一律收掉）、
    关服时收**自己的**。只按"租约过期"回收会漏掉**年轻孤儿** —— 而年轻孤儿的表现最坏：
    上游的钱花了、结果拿不到、**入口还锁着**（框体被判"已经生成过了"，学生点不动）。
    收尾要给**学生看得懂**的码与文案（`GENERATION_INTERRUPTED`「服务重启了…请重新点一次生成」），
    别写"请核查后人工处理"那种给自己看的话。
71. ⭐⭐ **交给我们自己域名之外的素材，别当"传过去了"—— 上游可能压根读不到，而且它不报错**（§二.M）：
    上游在境外，抓 `iicili.cyou` 要么连不上（nginx 日志里一条都没有）、要么读一半断
    （实测 105703/538855 字节），**读不到就当文生跑** → 出一段与参考毫无关系的作品，
    任务状态/列表字段全部正常。修法：要交给上游的素材（首帧/尾帧/参考）先
    `POST {上游}/v1/files/upload` 换成**上游自己的 URL** 再发（上游文档推荐流程，免费，URL 活 24h）。
    ⚠️ 上传要带**文件名后缀**（上游按后缀认类型），且失败**必须报错**（静默等于生成垃圾）。
    诊断入口：`nginx access.log` 里 `GET /api/public/file-assets/<id>/download` 有没有**境外 IP**
    的请求、字节数够不够。
72. ⭐ **同一个上游的"同名系列"可能能力完全不同：内置模板要按**具体模型名**给，别用前缀一把盖**（§二.M）：
    `zhenzhen-image-g-v2.5-flare/sunburst` 收 output_format/quality/background，
    `zhenzhen-image-g-v2.5-lowprice`（线上在用）文档原话"不提供质量档位、输出格式、透明背景或流式选项"
    → 传了就是 400。上一版就是被 `/^zhenzhen-image-g-v2\.5-/` 这个前缀正则一起盖了才没修好。
    上游的全量文档（`/docs/llms.txt`）里**逐模型**列了支持的字段 —— **先读文档，再改代码**。
73. ⭐⭐ **同一份素材在"入队"和"真正执行"两个时刻各解析一次，两次必须得到同一个结果**（§二.N）：
    入队把站内地址写成**绝对地址**存进 job 记录，worker 执行时再拿它解析一遍 ——
    解析函数只认"站内相对地址"的那一版，第二次就得到了空串 → 首帧/参考**被悄悄丢掉** →
    上游收到"没有输入"的请求 → 出一段与参考无关的作品，任务还成功。
    规矩：**素材解析要幂等**（入队的结果喂回去必须还原成它自己），worker 里再加一道
    `assertMediaResolved`（入队有的、执行时不许没有；没有就当场失败并告诉学生怎么办）。
74. ⭐ **不可能成的操作不要给入口**（按钮该藏就藏，§二.O）：
    已经生成过的框体（平台规则：每个框体只能成功生成一次）、不是课时框体的节点 —— 点了必然被服务端拒
    （`GENERATION_BOX_USED` / `GENERATION_BOX_REQUIRED`），留着按钮就是误导（用户原话：「不然会误导别人」）。
    客户端判据必须与服务端**同源**：本课该模态配了框体 → 只有框体节点能生成；
    判据只写一处（按钮显不显示、要不要忽略那条陈旧报错，都读同一个值）。
75. ⭐ **框体不能被"复制"**（§二.O）：框体是课时配置的、每个只能生成一次，复制出来的节点**共用同一个 boxId**
    → 生成必被拒、还让"框体是否已在画布上"的判断错乱。所以学生画布（`allowNodeCreation=false`）
    整段不给复制/粘贴；**撤销/重做不受影响**（闸门位置在它们之后）。
76. ⭐ **「自动」是一个语义，不是"列表里的第一个值"**（§二.O）：视频这条的「自动」按上游语义翻译 ——
    关键帧请求发 `adaptive`（**读首帧图比例**），纯文生/参考请求发固定比例（上游对参考请求显式传
    adaptive/auto 会报错）。折成第一个比例的表现是"4:3 的图被硬拉成 16:9"，也就是用户说的「扁的画面」。
    只在**模板形状对得上**时才翻，别家上游不替它做决定。
77. ⭐⭐ **"怎么用连过来的素材"必须由课包显式定，不能靠连线条数推**（§二.P）：
    文生视频 / 图生视频 / 首尾帧 / 全能参考是**四个不一样的概念** —— 连 2 张图也可能要全能参考，
    不是首尾帧。字段是框体的 `inputMode`（单数，"这节课就要哪种"），与模型声明的 `inputModes`
    （复数，"这个模型能不能"）分工不同；**锁定的方式赢过一切推断**，而且要同时落到三处：
    画布（能不能连线、能连几张）、服务端（素材算什么角色）、请求体（role 让上游知道）。
    改这个字段时把 §二.P 里那七处落点一起看一遍。
79. ⭐⭐ **加"能力字段"时要想清楚"给某个模型配过能力的渠道会不会把它吃掉"**（§二.P ⑤）：
    图片模型的 `inputModes` 原来在 `normalizeModelCapabilities` 里写死成 `[]`，而生产的图片渠道
    **给两个模型都配过能力** → 恒为空 → 框体的「生成方式」存不下来（用户报的就是它）。
    凡是"没配就用默认"的字段，都要问一句：**配过能力的模型会拿到什么**（写死的空数组最坑）。
78. ⭐⭐ **关键帧 + 固定比例：先把画面裁成那个比例再发**（§二.P）：上游会**执行**关键帧请求里的固定比例，
    给它一张比例不符的首帧，它就把图**硬拉**扁（我们固定 16:9 + 1:1 的图 → 人物变椭圆）。
    "生图看着正常"不能当反例：图片模型是**重画**一张目标比例，视频模型是**变形**输入。
    裁不动（没有 ffmpeg 等）就回退成原图；比例本来就一致就不重编码。
80. ⭐⭐ **"平台自己的文件"不等于"能交给上游的文件"，而"能交给上游"也不等于"读得到"**（§二.P ⑦）：
    学生自己上传的素材是 PRIVATE —— 解析那层（只认公开素材）会把它丢掉，镜像那层（HTTP 取
    /api/public/...）又会 403。两道都得单独过：**解析按"是不是他本人的"放行，读取按 storage_key 走磁盘**。
    校验新路径时把这两层分开验，别只验一层就宣布通了。


## 四、这一轮踩过的坑（承接 1–75；**本文新增 76–99**）

76. ⭐⭐ **诊断要直接调用生产代码，别拿自己的 JSON 判断去推**：我第一版诊断脚本用
    `JSON.parse` 自己判"快照缺这一键时读面是不是回退实时"，推断成"回退实时 → 那 15 节没事"，
    于是只报了 1 节课；后来改成**在副本上真的 import `lib.js` 调 `normalizeLesson`**，
    才看清是**17 节全中**（缺键给的是空数组）。**差一点就按错的范围去修**。
77. ⭐ **发布按 HEAD 走，会把别人"还没打算部署"的提交一起带上线**：这次就带上了另一个会话的
    客户端更新后台配置（他文档里写着"尚未部署"）。发布前扫一眼
    `git log --oneline origin/<branch> -3`，看清 HEAD 里都有谁的东西；影响评估与处置见 §二.F。
78. ⭐ **平台差异会伪装成"测试通过"**：原子写的并发证据在 **Windows 上根本跑不了**
    （rename 覆盖一个正被别的进程打开的文件会 EPERM），只能跳过 —— 而生产是 Linux。
    同一条还有反例：同一份 `setInterval` 读取在同步写循环里**一次都不会跑到**（事件循环被占死），
    于是"没读到半截 JSON"其实是**空转通过**。**写这种并发证据时一定要断言"读的人真的读到了"**
    （本轮两处都是这么发现的）。

79. ⭐ **给一条路上了闸之后，守卫的夹具也要跟着改**：p118/p119 的夹具原来只把**课时**改成
    VIBECODING，**没改课堂**（课堂是夹具按当时还是画布课的课时建的）—— 门禁一上，它们就被自己的
    门禁挡住（红在"client-context 没给出课堂"）。**先认出这是夹具不真实，别去松门禁**：
    真实的开课流程里课堂类型是跟着课时走的（`orgAdmin` 建课堂时按课时的 `deliveryMode` 取）。

80. ⭐ **nginx 的 `include sites-enabled/*` 是通配的 —— 备份文件不能放在那个目录里**：我给
    `sites-enabled/iicili.cyou` 做备份时顺手 `cp` 成 `iicili.cyou.bak-…`，于是它**也成了一块启用的
    server 块**（`nginx -t` 立刻报 `conflicting server name`）。这次靠"真文件按字典序排在前面"没出事，
    但下次顺序一变就是"改了配置不生效"。**备份一律放 `/etc/nginx/backups/`**（历次都放那儿）。
81. ⭐ **用 curl 量"到底谁挡的"要看穿透结果，别看 `%{http_code}` 一个数**：`--data-binary @-`
    会先把 stdin 整个读进内存再发（120MB × 1 次 = curl RSS 139MB、耗时 2 分钟），
    而 `%{http_code}` 遇上 `Expect: 100-continue` 会报 **100**（我第一版就把它当成了"返回 100"）；
    真正可信的是 **nginx access.log 里的那一行**（400 = 应用回话 / 413 = nginx 挡住），
    顺便还能看出是谁挡的。
82. ⭐⭐ **别把"看起来最像的那个假设"当根因**：这次用户报「视频跟参考完全不一样」，
    我第一反应是"参考没发出去"，也真的找到了一个成立的机制（默认视频模板里没有 `{{referenceItems}}`）
    —— 正准备按它修时，把**生产策略 dump 全**（第一次的输出被 `head` 截断了 12 行）才发现：
    那个渠道配了**模型级模板**、里面 `{{referenceItems}}` 好好地在，图确实发出去了 ✗。
    真因是语义（当参考发而不是当首帧发）。**教训：先把配置/数据 dump 全、看到全貌再下结论**；
    截断的输出会替你造一个通顺但错误的因果。
83. ⭐ **上游的报错原文就是线索，别急着猜**：`output_format is not supported by this model` 一句话
    就把字段名点出来了（我们把它塞在 `metadata` 里，2.5 只认顶层）。同一条也提醒：
    **别把参数一股脑塞进 `metadata`** —— 那看着"稳妥"，其实每换一代模型都可能踩雷。
84. ⭐ **窄屏下 flex 行里的按钮会被压到只剩一列字宽，中文按字折行 = "按钮变形"**：
    按钮要 `white-space: nowrap` + `flex: 0 0 auto`（同行的说明文字让它自己去换行）。
    （口径 57 那条"窄列要 nowrap"是同一个毛病的另一处；这次是按钮，不是表格列。）
85. ⭐⭐ **`set -o pipefail` 会让 `sed … | head -1` 静默中止整个脚本**：head 读完就关管道，
    sed 收到 SIGPIPE 退出非零 → 管道非零 → `set -e` 把脚本带走。表现是
    **"把该做的事都列出来了、然后什么都没做"**（本轮 `prune-releases.sh` 就是这个：
    列出了要删的 release、一条都没删）。给这种管道加 `|| true`，或者别用 head 截。
    **凡是"打印了计划却没执行"的脚本，先怀疑这一条。**
86. ⭐⭐ **在这台机器上「发布 = 重启服务 = 打断所有在途生成」**：本轮就真打断了用户的一条视频
    （上游已受理、会计费，结果拿不回来）。**发布前先看一眼在途任务**：
    `SELECT id,modality,status FROM generation_jobs WHERE status IN ('QUEUED','RUNNING')` ——
    有就等它跑完（视频 ~2 分钟）再发。兜底（口径 70）会让"被打断"变成"可重试"，
    但**钱已经花了**，而且 `usage_records` 里不会留下这笔成本（我们的账明明少了钱却看不出来，
    只能靠官方账单对账找回来）。
87. ⭐⭐ **"上游报错"要先分清是「这个模型没有这个能力」还是「值/位置不对」**（§二.M）：
    `output_format is not supported by this model` 被理解成"位置放错了"，于是把字段从
    `metadata` 挪到顶层 —— 线上**照样报同一个错**。那句话的意思其实是"这模型压根没有这个字段"。
    **先读上游文档里逐模型的能力表**（`/docs/llms.txt` 是全量 Markdown），再动代码；
    同一个系列里不同款（flare/sunburst vs 低价扩展版）能力可以完全不同。
88. ⭐⭐ **"任务成功"不等于"上游用了你的输入"**（§二.M）：上游读不到首帧图时**静默降级为文生视频** ——
    任务状态 `succeeded`、列表里的 `ratio`/`duration`/费用全部正常，**日志里看不出任何异常**。
    判断"输入真的生效"只有一个办法：**看产物本身**（把结果视频第 0 帧抽出来跟源图比）。
    `ffmpeg -i out.mp4 -vf "select=eq(n\,0)" -vframes 1 f0.png` 是这次的定位手法，留着复用。
89. ⭐ **multipart 上传要带**文件名后缀**：上游按后缀认类型（`filename=asset` → 400
    「unsupported file type; allowed: jpg/jpeg/png/webp, mp3/wav/flac, mp4/avi/mov/mkv」），
    而我们的下载路径是 `/download`、没有后缀 —— 按 content-type 补一个。
    第一版镜像就是死在这儿，而且当时报错只剩一句 `HTTP 400`（**把上游原文带进报错**才一眼看出来）。
90. ⭐ **拿脚本复现生产调用，要装上生产那一套 env**：视频的轮询上限跟着
    `AI_PROVIDER_TIMEOUT_MS`（生产是 **300000**，默认是 120000）—— 我在验收脚本里没装它，
    一条 130 秒的正常视频被报成"AI 服务响应超时"，白跑一趟、还差点当成产品缺陷去修。
    **脚本读 `production.env` 时把 `AI_PROVIDER_*` 真的 set 进 `process.env`**，别只拿来取密钥。
91. ⭐⭐ **验收必须走用户真正走的那条路**（§二.N，本轮最贵的一条教训）：
    画布生成走的是 `POST /api/ai/generations/async` → 入队 → **worker 自己再解析一次素材** →
    才轮到 provider。我上一轮"直接调 provider"验过就宣布修好了，恰好跳过 worker 这一步 ——
    结果是"我这边绿、用户那边还是错的"。凡是要宣称"用户看到的现象修好了"，
    **就在那条真实链路上跑一遍**（本轮的 `live-canvas-video-check.mjs` 就是为此写的：
    生产库副本 + 临时实例，让 worker 自己跑）。
92. ⭐⭐ **手动往部署 checkout 里 scp 文件会挡住之后的 `git pull`，而"发布成功"不会报错**：
    untracked 文件挡合并 → `git pull` 直接 abort，我却只 `tail` 了输出、没看报错 →
    接着照常 build + 发布 → **上线的是老 commit**（新 release 目录、旧代码）。
    识别方法只有一个：**发布后读 `BUILD-METADATA.txt` 里的 `commit=`，与本地 HEAD 对一眼**（本轮对出来了）。
    往部署目录塞临时文件要记得删；`git checkout -- .` 并不会清 untracked。93. ⭐⭐ **守卫自己会哑：把新断言插在 `if (failures)` 之后，它就永远绿**（§二.O 现场）：
    我给 p122 补了 5 条断言，位置在该文件的失败判定**之后** —— 结果屏幕上明明打了 `✗`，
    脚本照样输出 "P122 PASSED"、退出码 0。**写完守卫要故意让它红一次**（改坏一处再看它红不红），
    否则"加了断言"和"守住了"是两回事 —— 这比没守卫更危险（它会给你假的安心）。
94. ⭐ **顺序断言别用 `indexOf` 找"第一次出现"**：同一个文件里别处也可能有同名代码
    （本轮 `if (!allowNodeCreation) return;` 在画布文件里有两处）→ 断言看似在查顺序、其实查错了地方。
    正确做法：先切出目标作用域（`source.slice(source.indexOf('const handleKeyDown = ...'))`）再在里面找。
95. ⭐ **异步 `execFile` 没有 `input` 选项**（§二.P 现场）：那是 `execFileSync` 才有的 ——
    传了会被忽略，子进程读空 stdin 直接失败（第一版 mediaFit 的 ffprobe 就是这么挂的，
    报错只有一句 "Command failed: ffprobe -v error …"）。要给子进程喂 bytes 就自己 `spawn` +
    `child.stdin.end(bytes)` 再收 stdout。同理：**转发报错前先看清它到底是哪一层失败**。
96. ⭐ **"手动 scp 进部署 checkout 的文件会挡住 git pull"本轮又犯了一次**（坑 92 的复发）：
    这次挡的是新文件 `services/mediaFit.js` → pull 又是 "Aborting"，而我只看到构建成功就往下走，
    于是**那一版发布的是上一个 commit**（从 BUILD-METADATA 里看出来的）。
    发布流程改成**硬校验**：`[ "$(git rev-parse HEAD)" = "$(grep ^commit= BUILD-METADATA.txt)" ] || exit 3`
    —— 不相等就停，不许接着切软链。
97. ⭐ **"界面上能选、保存后没了" = 服务端在某个白名单/校验里静默丢了字段**（§二.P ⑤）：
    本轮两个"能选但存不下来"的现场（生成方式、2.5 模板那次也一样）都不是前端问题，
    而是服务端在归一化/白名单里把它吃了 —— **复现方式**：在生产库副本上直接调保存链路
    （`lessonCanvasConfig` → 改一处 → `replaceLessonCanvasConfig` → 再读回来），
    不要靠点界面猜。
98. ⭐ **在 1.6G 的生产机上跑截图脚手架（vite build + chromium + 第二个实例）要挑时候**（§二.P ⑥）：
    当晚 18:31 机器重启过一次，journal 里重启前连续 "Under memory pressure"，没有 OOM-kill 记录。
    平台的发布/验收尽量用轻的（守卫 + 直调函数）；要跑真浏览器就先看 `free -m`，
    或者把它挪到别的机器上跑。
99. ⭐ **验收脚本自己打开的那个库/那套 env，会决定你看到的是"产品坏了"还是"脚本不对"**（§二.P ⑦）：
    本轮两次被自己的脚本误导：media-check 用**空临时库**跑 → 镜像查不到 file_assets → 当成外部 URL 去 HTTP 取
    → 403（看着像产品坏）；两个脚本都漏装 `FILE_UPLOAD_ROOT` → 镜像读盘会去找 <cwd>/var/uploads。
    规矩：验收脚本用**生产库副本** + 把生产那套 `FILE_UPLOAD_*` / `AI_PROVIDER_*` 真装进 process.env。


## 五、下一步 / 遗留

### A. 原来"等用户复验的两件" —— **已复验完并都修掉了**（§二.M）

1. ~~视频首帧~~：用户复验说"还是没法参考" → **真根因是上游读不到我们域名的图**（已修、已真跑验收：
   视频第 0 帧就是那张《清明上河图》）。**若用户画布上那个框体还显示老结果，需要他自己点一次
   「重新生成」**（老产物不会自动变）——旧框体不用重建、不用换课，页面刷新即可。
   ⚠️ **截至 2026-09-21 16:07 CST（08:07Z）线上还没有第二条视频任务**来复验这一条 ——
   镜像那版（release 20260921T080205Z）上线后只来过一次生图（见下）。
   也就是说："能当帧就当帧 + 素材先传上游"在线上**尚未被真实点击复验过**，别把它当已验收。
2. ~~2.5 生图~~：**已修**（低价扩展版不支持 output_format，之前那版改位置没对症），
   已真跑验收：出图成功，且参考图真的生效（同一幅画的夜景灯光版）。
   ✅ **线上用户自己那次也成功了**（最终确认，2026-09-21 实查生产库）：
   `generation_ac1a3771c859479f882c` 16:05:34 → 16:06:40 CST（**65 秒**）`SUCCEEDED`、
   `credits_charged=0`（测试账号），跑的就是 release **20260921T080205Z**；
   而这款模型的**全部 21 条失败都止于 15:29:16 那条**（都在修好那版上线之前）——
   修好之后 0 失败。
   ✅ **2.0 不受影响（用户问过"修了 2.5 会影响 2.0 生图吗"）**：内置模板的匹配只认
   `zhenzhen-image-g-v2.5-*`（正则里那个 `.5` 是真的），`zhenzhen-image-g-v2-lowprice` 一条规则都不命中、
   走的还是原来那份 `metadata` 形状（p109 ② 钉住）。生产上 2.0 今天没有任务
   （最近一次 09-18 20:55，历史 16 成功 / 7 失败），所以"今天没被改到"也有数据佐证。

3. 进度条是**时间估算**（上游只回状态、我们也没存原始响应）；要真进度得让上游回 progress + 加列存。
4. ⚠️ **镜像带来的新依赖**：每个用到自制素材的生成任务，会先往上游传一次素材（缓存 20h）。
   上游那接口限流是**每令牌每分钟 10 次**，一个班同时提交大量不同素材理论上会撞 429 ——
   撞上就是一句「素材上传到上游失败」，可重试。规模真上来了要给上游提额或改用自备对象存储。

### B. 运维欠账（这一轮新冒出来的）

5. **机器内存薄**：一次 200MB 上传的峰值把可用内存压到 ~90MB（`clamscan` 一个人吃 ~990MB）。
   这台机器上面还挂着「学生环境被 OOM 杀掉」那段事故 —— **扩容**（或把上限往回收）该定口径。
6. **ClamAV 病毒库 16 天没更新**（9-05 的库，ClamAV 自己警告 "database is older than 7 days"），
   而且**没有 freshclam 定时任务**：要么配 `clamav-freshclam`，要么明确"就用这个库"。
7. **发布前查一眼在途生成**（坑 86）：`SELECT id,modality,status FROM generation_jobs WHERE status IN ('QUEUED','RUNNING')`
   —— 视频约 2 分钟；被打断的钱我们账上看不见（`usage_records` 无行），只能靠官方账单对账。

### C. 代码/文档欠账（承接前几轮）

8. **把 deploy / verify 两个脚本从 `.tmp` 挪进仓库**（第二十五轮 §五.A，仍未做）——
   它们会自己漂（已修过一条 `capFen`），挪进仓库才能被 review。
   ✅ 本晚先把**新写的那个**收进去了：`deploy/production/live-ai-media-check.mjs`（真跑验收，
   `--dry` 不花钱）—— 剩下的两个照旧待办。
9. **承接口径 61 的同类排查**：还可能有别的"读面与判定不同源"的地方 —— 顺手核一遍
   `normalizeProject`（学生画布那一整份 payload）与生成接口各自读的字段还有没有不一致的
   （这次修的是 capabilities/materialGroups/generationBoxes 三档）。
10. 承接第二十四/二十五轮：素材草稿隔离要平台点一次「更新发布」、`测试` 课包仍挂在灵动课程页、
    生产 unit 是 `Restart=on-failure`、`p93` 需要 LibreOffice（生产机上 `soffice` 在）。
11. **那 16 节课要"严格隔离"就得发布一次**（用户凌晨发布、没有学生上课）：发布后快照里就有
    `generationBoxes` 这一键，此后"改了不发布学生看不到"对它们也成立。
12. **并行写者**：另一个会话可能还在同一工作区写"客户端更新"那条线，两边都会跑 `vite build`（写 `apps/*/dist`）。

## 六、怎么跑（这一轮动过的）

```bash
# 服务器上
cd /srv/ai-kids-platform/internal-test/source
export PATH=/srv/ai-kids-platform/runtime/node/bin:$PATH

# 本轮的核心守卫（框体隔离 + 画布与判定同源；含反向自检）
node scripts/p54-draft-isolation.mjs
# 框体那几条（p28 一直绿是对的：它那节课从没发布过）
node scripts/p28-generation-boxes.mjs && node scripts/p11-video-first-frame.mjs
node scripts/p31-music-generation-box.mjs && node scripts/p33-canvas-persistence.mjs && node scripts/p35-student-chosen-params.mjs
# 界面那两条（口径 63/64）+ 客户端更新清单（口径 65）
node scripts/p110-classroom-materials-ui.mjs   # 生成框体那一项带 .is-gen-box + 金环样式
node scripts/p70-pages-render.mjs              # 改前端必跑（90 页 0 失败）
node scripts/p120-client-update-config.mjs     # 客户端更新清单：原子写/契约字段/策略复写（8 条）
node scripts/p109-canvas-references.mjs       # 参考/连线真的生效（含 2.5 模板、能当帧当帧）
node scripts/p125-upstream-media-mirror.mjs   # 素材镜像（本晚新增，32 条）：自站素材先上传再发、
                                              #   后缀/大小闸/缓存/失败必须报错 + 2.5 低价版逐字段核对
node scripts/p4-o12-ai-generation-queue.mjs   # 生成队列：重启后的孤儿任务被收掉（含反向自检）
#   ⚠️ 其中「并发读不断半截 JSON」在 Windows 上会跳过（文件锁挡 rename），Linux 上才真跑
#   ⚠️ 改了生成链路/适配器（openaiCompatibleProvider / generationProvider / modelCapabilities）时，
#      下面这批都要一起跑（它们都 import 到这几个模块）：
for g in p6-a01-openai-compatible-adapter p11-video-first-frame p27-provider-capability-validation \
         p28-generation-boxes p30-provider-error-detail p31-music-generation-box p42-vibecoding-attachment-shape \
         p59-gateway-routing p75-compute-routing p79-soft-budget p87-compute-evidence \
         p89-sale-price-observation p90-contract-cost-computation p112-session-cost-cap \
         p108-vibecoding-options p109-canvas-references p122-canvas-box-rules p125-upstream-media-mirror; do
  node scripts/$g.mjs >/dev/null || echo "FAIL $g"; done; echo "（没列出 FAIL 就是全绿）"

# 只读诊断：哪些课的"学生读面"和"实时"不是同一批（.tmp/diag-normalize.mjs，跑在生产库副本上）
node .tmp/diag-normalize.mjs
#   ⚠️ 它 import 了 apps/server/src/lib.js（import 时会跑迁移）—— 必须指副本，脚本自己 VACUUM INTO 到 /tmp

# 真跑一次「素材到底有没有到上游」（**会花上游的钱，只手工跑**；本晚新增、已收进仓库）
#   ⚠️ 脚本不在 release 里（release 只有运行时代码），用仓库里那份；RELEASE_DIR 默认指 production/current
SCRIPT=/srv/ai-kids-platform/internal-test/source/deploy/production/live-ai-media-check.mjs
node $SCRIPT --dry                       # 不花钱：只把素材传到上游看能不能成（这一步过不了别往下花钱）
node $SCRIPT --only=image                # 约 ¥0.1–0.3
node $SCRIPT                            # 再加视频那条（约 ¥0.75）
#   脚本会把产物 URL 打出来，判据：视频**第 0 帧**应等于源图、图片应是基于源图改的
#   （任务状态看不出问题 —— 上游读不到素材时不报错，这是那一类故障的命门）
#   ⚠️ 但它**直接调 provider**，绕过了 worker —— 要验"用户在画布上看到的现象"得跑下面那条 ↓

# 真跑一次「画布视频框体的完整链路」（**走队列**，约 ¥0.75；本晚第二轮新增）
#   生产库副本 + 临时实例：插一条 QUEUED 任务，让**它自己的 worker** 去跑
node deploy/production/live-canvas-video-check.mjs
LIVE_CHECK_REPO=/srv/ai-kids-platform/production/current node deploy/production/live-canvas-video-check.mjs   # 验"已发布产物"
#   会打印产物视频与抽好的第 0 帧路径 —— 打开看一眼：应该就是源图，不是一段无关画面

# 页面自查（含"造一个生产上没有的状态"再看）：本轮它长出了 --fixture
CHROME_PATH=/usr/bin/chromium-browser node scripts/page-shot.mjs --prod-uploads --tag tab-flag \
  /admin/courses/<seriesId> --click "版本发布"
CHROME_PATH=/usr/bin/chromium-browser node scripts/page-shot.mjs --prod-uploads --as student-1 \
  --fixture .tmp/fixture-canvas-box.mjs --tag canvas-ring "/learn/canvas/{projectId}" --click "图片模块"
```

## 七、给新会话的"读这三份就够"
1. `docs/README.md` —— 唯一口径源 + 顶部指针（每轮的最值钱几条都在那儿）。
2. `docs/operations/开新会话先读-现状对齐-20260919.md` —— 两条线怎么分、生产事实、客户端三个交付面。
3. **本文** —— 这一段的施工记录 + **口径 61–70** + **坑 76–86** + 下一步。
   再往前是 `新对话交接-第二十六轮-客户端自动更新后台配置-20260920.md`（另一条线）、
   `新对话交接-第二十五轮-页面自查脚手架与素材页白屏-20260920.md`（口径 58–60、坑 72–75）。
