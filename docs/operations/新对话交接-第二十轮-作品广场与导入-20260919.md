# 新对话交接 · 第二十轮（作品广场：按参考站重做 + 导入 476 件 + 分类/图标/配色收口）

> **新会话先读这一份。** 本文覆盖第十九轮（`c45e24b`）之后的这一整段：**作品广场**从
> 「两颗分类按钮 + 渐变占位卡」重做成参考站那个样子（类型胶囊 + 封面卡片网格 + 看图层），
> 并把另一个站（`ltai.cc`，**也是用户自己的站**）的 476 件作品导进我们平台。
> 上一层的施工记录：`新对话交接-第十九轮-网站去dsh与客户端登录页-20260919.md`
> （官网去 dsh、客户端登录门、发送次数口径、客服端拆仓、离线出包）。
>
> ⚠️ **桌面客户端已经不在本仓库了**：独立仓（工作区 `E:\灵动ai客户端`，远端
> `chuangyilingdong/lingdong-client`），接口以那仓的 `docs/平台接口契约.md` 为准。
> 本仓库只做**平台 + 官网 + 客户端要用的服务端接口**。

## 一、当前生产状态（照这个对）

```text
release 20260919T120328Z / commit 34c9def（服务 active、NRestarts=0、五入口 200）
  · ⚠️「生产跑的是哪一版」只认 release 目录里的 BUILD-METADATA.txt；**不要拿分支 HEAD 去对**
可回滚（由新到旧，都在服务器上）：
  20260919T115451Z / f040d3f   20260919T114806Z / 5d1829a   20260919T112428Z / 7783380
  20260919T112100Z / 2b3a75c   20260919T111049Z / 892d842   20260919T104124Z / f7d9433
  20260919T104013Z / 55ad37f   20260919T102135Z / 496725e   20260919T084246Z / 2469bb2
  20260919T044004Z / ed9d6fa（更早在 RUNBOOK 里）
最新整库备份：发布脚本每次切软链前自动做（production/backups/<时间戳>）
分支 feature/vibecoding-ppt-quality-20260915（本地与远端 0/0）
作品广场：**205 件**（画布作品 186 / VibeCoding作品 19），媒体 1.7GB
  · 其中 **9 件是「能在我们站直接跑起来的网页作品」**（§二.J，2026-09-19 晚从 aimagc.cn 抓的）
客户端安装包：已发布（清单 win-x64 已置回；发布脚本在**客户端仓库**）
```

**发布**：`.tmp/deploy-002.sh <sha>`（转 LF → scp 到 `/tmp/deploy002.sh` → `ssh bash … <sha>`）
→ **验收**：`.tmp/verify-prod-002.sh`（同样转 LF 上传后跑）。⚠️ **发布前先 git push**：
这一轮有一次忘了推，服务器 pull 到旧 HEAD、脚本自检直接拒绝构建（自检是对的）。

## 二、这一轮做了什么

### A. 作品广场按参考站重做（展示）

用户口径：「我们现在灵动作品的展示有问题，参考这个网站的展示来设计」——参考站
`ltai.cc/works-square` **也是用户自己的站**。版式照它：**类型胶囊行**（全部 + 各类型，
带图标与件数，只列数据里真有的）+ **封面卡片网格**；卡片 = 封面（左上角类型角标、
视频中间播放键、右下角品牌小字）+ 标题（单行省略）+ 日期 + 作者 + **右下角机构名**。

我们比它多的：**点开能看/能播**（参考站卡片点开没动作）+ 搜索框。看图层按类型分：
图片看大图（多张可翻）、视频播、音频播、外链类**先试 iframe 内嵌**并永远给
「打不开？在新窗口打开 ↗」的出口。

### B. 导入 476 件（`scripts/import-plaza-works.mjs`，**在服务器上跑**）

为什么在服务器上：只有它能直连那个站（本机出网受限），媒体也直接落对外目录。
- 数据源：`GET https://ltai.cc/api/ke/creation/list?size=100&page=N&isMine=0`（**公开、不需要登录**）
- 一件作品 → `student_projects` + `works`（`is_public`/`PUBLISHED`/`share_token`/`copyright_confirmed_at`
  **四样齐了才会出现在广场** —— 这是那条查询的硬条件）；+ 1 个机构 + 54 个署名账号
- 元数据塞在 `works.canvas_snapshot.imported`（那是 NOT NULL 列，本来就是放作品内容的）
- 幂等（`INSERT OR IGNORE` + 文件在就跳过下载）
- 媒体 → `/srv/ai-kids-platform/public-media/`（**release 之外**，797 个文件 1.7GB、0 失败）

### C. 媒体托管：nginx 新增 `location ^~ /media/`

脚本 `deploy/production/add-media-location.sh`（备份 → `nginx -t` → reload → 公网核验）。
⚠️ **不能复用 `/downloads/`**：那条是 `default_type application/octet-stream` + nosniff，
图片/视频会被浏览器直接拒收。服务器上装了 **ffmpeg**（apt，只用于下面的转封装）。

### D. ⭐ `.mov` → mp4（本轮最贵的一课，见 §六.1）

`scripts/remux-imported-videos.mjs`：`ffmpeg -c copy -movflags +faststart` 就地转（不重编码，
28 个文件几秒一个），转完把库里 `contentUrls` 的地址一起改掉、删原 `.mov`。
**导完必须跑它**（导入脚本的文件头里也写了）。

### E. 机构名：编的少儿编程机构（8 家）

`scripts/randomize-import-orgs.mjs`：按**作者**分配（一个作者属于一个机构，作品跟着走），
固定随机种子（重跑结果一致）。现在分布：云雀AI创意学院 95 / 小海豚编程学院 89 /
星芽少儿编程 83 / 麦田少儿科创 79 / 蜂巢少儿AI课堂 50 / 木马工坊编程 38 /
银河少年创客中心 32 / 光点少年编程 10。「灵涛AI课」已归零。

### F. 分类：只有两个，且后台可配

用户口径：「分类目前就 2 个分类：画布作品和 VibeCoding作品，图片、视频、AI播客、音乐、品牌设计
放在画布作品里，其余是 VibeCoding 分类，并且分类在后台可以配置」。
- 实现：`apps/server/src/services/plazaCategories.js`（**唯一实现**），默认表 + 后台覆盖；
  配置存 `platform_settings.plaza_category_map`（schema 里加了 ALTER，服务启动即迁移）；
  后台接口 `GET/PUT /api/admin/plaza-category-map`（**改完立刻生效、不用发版**）。
- 公开接口每个作品都带 `plazaCategory` / `plazaCategoryLabel`，前端**不自己判类型**。
- ⚠️ 后台**表单**还没画（接口与存储都通了）。

### G. 分页：每页 12 件

前端切片 + 翻页条（页码窗口、换分类/换搜索词自动回第 1 页）。用户后来要求把
「· 每页 12 件」那半句删掉（已删，只剩「第 N / M 页」）。

### H. 收口三件（用户后续口径）

1. **外链作品全部下架**（「只留能在我们网站直接打开的」）：280 件本体只有别家平台链接的下架
   （`is_public=0` + `UNPUBLISHED`，**软删**、后台还看得到、能改回），广场剩 196 件。
2. **图标换线性 SVG**（「我们的 AI 味太重了」）：广场里的 emoji 全删，改用
   `packages/shared/src/icons.jsx` 那套（新增 brush/palette/code/mic/monitor/gamepad/globe/cpu/flow/book）。
3. **实心紫 → 紫粉渐变**（「所有紫色变成紫色粉色的渐变，高级一点」）：
   `--pl-grad: linear-gradient(135deg,#6A4BD8 0%,#9B4FE0 46%,#FF5C93 100%)`，
   只落在**实心填充面**（分类胶囊选中态、翻页选中态、封面角标）；正文紫色文字没动。
   ⚠️ **只改了作品广场这一页**，官网其它地方仍是纯紫/纯粉。

### I. 顺带修的两个真问题

- **公开接口的 limit 上限 60**（`/api/public/works` 与 `/vibecoding-works`）：广场是「一次取全、
  前端筛选/搜索」的结构，卡 60 就永远只显示前 60 件、胶囊件数也是错的 → 都提到 **500**。
- `vibecoding_submissions` **没有 `unpublished_at` 列**（我按 `works` 的列写 SQL 报错）：
  两张表各有一套「上广场」的字段，处理作品前先看是哪张表。

### J. ⭐ 抓 aimagc.cn 那 9 件「能在我们站直接跑的网页作品」（2026-09-19 晚补做，§五.A.1 已完成）

用户口径：「抓 `aimagc.cn/#works` 那批作品 —— 能抓吗？应该可以直接在我们网站打开吧？」

实地结论：**能抓**，而且是**单文件静态 HTML**（挂在用户自己的域名 `works.aimagc.cn`，阿里云 OSS），
`#works` 区就 9 件：点泡泡 / 山行·杜牧 / C++代码大冒险 / 单词小侦探 / 交互式3D机械臂 /
旋转地球 / 我的世界简化版 / 贪吃蛇 / 会动的彩色小猫。

做法（`scripts/import-aimagc-webworks.mjs`，**服务器上跑**，幂等可重跑）：

| 事项 | 做法 |
|---|---|
| 本体 + 封面 | 下到 `public-media/web-works/<uuid>/`；封面是源站 head 里现成的 `og:image`，正好当广场卡片封面 |
| 入库形状 | 与上一轮 ltai 那批一致（`works` + `canvas_snapshot.imported`），**只多一个 `entryUrl`**（入口页的站内地址） |
| 依赖 | **全部自托管**：three.js(+3 addon) / p5 / threejs.org 行星贴图 / 旧品牌域名下的背景图，改写引用 |
| 旧品牌 | 源站 head 写着「AI魔法学院」（五格殿下那套）→ 入库前换成「灵动ai学院」 |
| 署名 | 源站不公开作者名，编了 9 个（小宇/朵朵/…），机构「青藤少儿编程」（`org_webworks`） |
| 分类 | `workType: webpage` → 按现行映射落 **VibeCoding作品**（能跑能交互的那一类），没改映射表 |

**唯一没自托管的两条**（有意留的）：
- `upload.wikimedia.org` 那张月球图 —— **这台服务器连不上、也没有镜像**，就**保留原地址**
  （访客的浏览器取得到，作品照常显示）。宁留一条外链，也不悄悄换掉学生作品用的素材。
- `cdnjs` 的 p5 走 `MIRRORS` 换到 jsdelivr 的同一份文件（内容一样），照样自托管。

**为什么必须走沙箱**（这条是本轮最要紧的）：作品托管在 `/media/` 下，**与主站同源** ——
这跟上一轮 ltai 那批「外链件」完全不同。所以看图层那条 iframe 的 `sandbox`
**绝不能带 `allow-same-origin`**（带了学生 HTML 就能读我们的 cookie / localStorage），
而且**刻意不给**「在新窗口打开」的出口（同源入口页开到新窗口 = 拿我们的源跑学生代码）。

**验收**：新增守卫 `scripts/p120-webworks-sandbox-check.mjs`（真 Chromium + 金丝雀），
跑生产结果：源码口径 ✅ / 接口口径 ✅（广场 205 件含 9 件网页作品）/ 托管口径 ✅ /
同源隔离 ✅（cookie、localStorage、父窗口文档全是 `SecurityError`）/ **9 件逐件渲染 ✅**。



### K. 后台两件（2026-09-19 晚补做，§五.A.2 已完成）

用户口径：「后台两件还没画：作品**编辑/删除** + **分类映射的表单**」，删除按 **「软删 + 彻底删除两档」**。

| 能力 | 位置 | 口径 |
|---|---|---|
| 编辑 | `PUT /works/:id`、`PUT /vibecoding-works/:id` | **只改展示文案**（标题 200 / 描述 2000）。发布状态、展示授权、精选各有专门动作与审计 —— 不从这条走，否则"改个标题"能顺手改掉发布状态 |
| 软删 | 已有的 `PUT /works/:id/unpublish` | `is_public=0` + `UNPUBLISHED`，后台还看得到、能改回 —— **默认路径**，下架原因学生可见 |
| 彻底删 | `DELETE /works/:id`、`DELETE /vibecoding-works/:id` | 不可恢复。要求显式 `confirm:true` + 一句原因（进审计日志，学生看不到）。子表 5 张 `ON DELETE CASCADE` 由 SQLite 清 |
| 导入件媒体 | 彻底删时 | 目录**挪进 `public-media/_trash/`**（挪走不是 `rm -rf`）。⚠️ 挪之前先查"还有没有别的作品引用同一份媒体"，有就不挪；挪不动**不让整次删除失败**，只在返回里报出来 |
| 分类映射表单 | 后台作品页底部可折叠区 | 接口 `GET/PUT /admin/plaza-category-map` 早就通了，这轮画上界面。改完**即时生效、不用发版** |

后台页上「编辑」「彻底删除」在每行的操作列里；两个面板渲染在**表格上方**（沿用这一页既有动作面板的位置 ——
所以点完第 20 页某一行要往上滚才看得到，是这一页一直以来的行为，不是这轮引入的）。

**验收**：新增守卫 `scripts/p121-plaza-admin-check.mjs`（临时库 + 真服务 + 真接口，14 条断言全绿），
其中两条是"删错就没第二次机会"的：**共用媒体不许挪**、**挪不动不许让删除失败**。
界面另在**生产**上用真浏览器逐项看过（编辑面板 / 彻底删除面板在没填原因时确认按钮是禁用的 / 11 个类型的映射下拉）。



## 三、不许破的口径（本轮新增 12 条：原 6 条 + §二.J 的 4 条 + §二.K 的 2 条）

1. **广场只放"能在我们站直接打开"的作品**：本体只有别家平台链接的**不上广场**
   （对方大多用 `frame-ancestors` 禁止被嵌，跳出去等于把流量送人）。
2. **分类只有两个**（画布作品 / VibeCoding作品），**归哪一类由服务端算**、映射后台可配；
   前端不许自己按类型判分类。
3. **媒体放 release 之外**（`/srv/.../public-media/`）+ 走 `/media/` 那条 location；
   **不许**复用 `/downloads/`（octet-stream + nosniff 会让图片/视频打不开）。
4. **导入的视频必须是标准 mp4**（QuickTime 容器 `ftypqt` 浏览器播不了）——导完跑 remux。
5. **作品的"上广场"要四样齐全**：`is_public=1` + `status='PUBLISHED'` + `share_token` +
   `copyright_confirmed_at`（缺一个广场就不显示，而且**不报错**）。
6. **广场的配色与图标是"口径"**：图标用 `icons.jsx` 的线性 SVG（不用 emoji）、
   实心紫用 `--pl-grad` 渐变。改版要连 p115 一起改。
7. ⭐ **`/media/` 必须带 `Access-Control-Allow-Origin: *`**：网页作品跑在不带
   `allow-same-origin` 的沙箱 iframe 里，文档 origin 是 opaque，里面 `import` 的 ES module
   一律按 **CORS 模式**取 —— 少了这个头，three.js 那几件**整片白屏**（文件明明 200 拿得到，
   是浏览器拒收）。改 nginx 时别把这行删掉，p120 会拦。
8. ⭐ **跑网页作品的 iframe 不许带 `allow-same-origin`**，也不给它「在新窗口打开」的出口：
   作品在 `/media/` 下**与主站同源**，这两条都是"把学生代码提到我们源上跑"。
9. **网页作品的 `entryUrl` 必须是本站 `/media/` 下的地址**：放外域就等于破了口径①
   （而且沙箱壳的 `frame-src 'self'` 也会挡）。
10. **导入件要连"相对引用"一起抓**：`url('bg.jpg')` / `<source src="bgm.mp3">` 这种
    同目录素材，只抓 HTML 里写出来的绝对地址会漏（单词小侦探就是这么漏掉背景图和背景音乐的：
    广场上打开缺图 + 没声音）。
11. ⭐ **「删除」是两档，不是一个动作**：「下架」= 可恢复的软删（默认路径，学生看得到原因）；
    「彻底删除」= 不可恢复，必须 `confirm:true` + 原因，且**导入件的媒体只挪进 `_trash/`、不 rm**。
    改这块时那条"共用媒体不许挪"的检查一定要留着 —— 它会挡住"一件作品删掉、另一件作品的媒体跟着没了"。
12. **后台「编辑」只改展示文案**：发布状态 / 展示授权 / 精选各有专门动作与审计，
    不许从编辑接口走 —— 否则"改个标题"就能顺手把发布状态改掉，审计账对不上。

## 四、结构与关键文件

| 文件 | 作用 / 注意 |
|---|---|
| `scripts/import-plaza-works.mjs` | 导入那个站的作品（**服务器上跑**）；文件头写了"导完要 remux" |
| `scripts/import-aimagc-webworks.mjs` | ⭐ §二.J：抓 aimagc 那 9 件**静态网页作品**（服务器上跑；含依赖自托管 / 相对资源 / 去旧品牌 / 写库） |
| `scripts/p120-webworks-sandbox-check.mjs` | ⭐ 网页作品的守卫：源码沙箱口径 / entryUrl 同源 / `/media/` CORS / 金丝雀同源隔离 / 逐件渲染（**要 node≥20 + Chrome**） |
| `scripts/p121-plaza-admin-check.mjs` | ⭐ 后台「编辑 / 彻底删除 / 分类映射」守卫：临时库 + 真服务 + 真接口；钉住级联删除、共用媒体不挪、映射即时生效（**要 node≥20**） |
| `scripts/remux-imported-videos.mjs` | `.mov` → mp4（`-c copy` + faststart），并改库里地址 |
| `scripts/randomize-import-orgs.mjs` | 把导入件的机构换成编的机构名（按作者分配、固定种子） |
| `apps/server/src/services/plazaCategories.js` | **分类映射的唯一实现**（默认表 + 后台覆盖） |
| `apps/server/src/routes/communication/public.js` | 公开接口：`publicWorkRow` 吐 `imported` 那一坨 + `plazaCategory` + `entryUrl` |
| `apps/server/src/routes/admin/works.js` | 后台作品域（含新的 `GET/PUT /plaza-category-map`） |
| `apps/website/src/main.jsx` | 广场在 `Works()` / `Work()` / `WorkViewer()`；分类是 `plCategoryOf`；网页作品的看图层是 `kind==='webwork'` |
| `packages/shared/src/icons.jsx` | 线性 SVG 图标（广场与画布工作台共用） |
| `deploy/production/add-media-location.sh` | 加 `/media/`（含备份/自检/公网核验）；⭐ 幂等补 **CORS 头**（少了网页作品白屏） |
| `scripts/p115-website-ui-check.mjs` | ⚠️ 本轮改了两次：fixture 造到 15 件（翻页要 >12 件才测得出来）、按 `data-category` 判分类 |

## 五、下一步

### A. 用户已经点名的

1. ✅ **抓 `aimagc.cn/#works` 那批作品 —— 已完成**（2026-09-19 晚，见 §二.J）：
   9 件静态网页作品已上广场、依赖自托管、走沙箱跑，守卫 `scripts/p120-webworks-sandbox-check.mjs` 跑生产全绿。
   ⚠️ 有一处与当时的计划**不一样**：**没有**用 `/vibe-preview.html` 那个外壳 ——
   那外壳是为 `srcdoc` 准备的（srcdoc 会继承首页 CSP，所以要一张单独放宽 CSP 的静态页来转发）。
   这批作品是**按 URL** 加载的，框架文档有自己的响应头、不继承父页 CSP，所以直接
   `<iframe src=entryUrl sandbox="…">` 就够了，少一层转发少一个口子。
   要守的是**sandbox 参数**（不带 `allow-same-origin`），不是"必须经过某张壳页"。
2. ✅ **后台两件 —— 已完成**（2026-09-19 晚，见 §二.K）：作品编辑 / 软删+彻底删除两档 /
   分类映射表单；守卫 `scripts/p121-plaza-admin-check.mjs` 全绿，界面在生产上用真浏览器逐项看过。
3. ✅ **全站配色 —— 用户已定：保持现状**（只有作品广场用紫→粉渐变，官网其它地方维持纯紫/纯粉）。
   所以这份文档里那条"等用户定"已经结清，**不用再做**。
4. **aimagc 那批还能再抓**：这次只抓了首页 `#works` 区的 9 件（源站把自己全部作品都挂在
   那个区上了，没有"看更多"）。如果还有更多作品在别处（后台/接口里），得先问用户要到清单 ——
   那站的公开接口只有 `api.aimagc.cn/api/public/desktop-downloads`，没有公开的作品列表接口。

### B. 客户端那边（在**另一个仓库**）

还差：**预设提示词渲染成可点块**、**交作品入口**（服务端 `POST /api/student/runtime/submit-upload`
已就绪、守卫 p119 钉着）、**窗口标题**（改标题的补丁是上次出包之后才补的，下次出包生效）。

### C. 老欠账

Univer、扩容+镜像、共享 cookie、dsh stdout、P03 七个缺口、上游账户地址单独配、
线上 `/org/` 被机构后台占用（官网「机构方案」页打不开）—— 都还没动。

## 六、这一轮踩过的坑

1. ⭐ **`.mov` 的容器品牌 `ftypqt`，Chrome 加载不出来**：`<video>` 一直 `readyState=0`、
   `canPlayType('video/quicktime')` 返回空，而**同一份文件在源站能播**（源站 CDN 有 referer 保护，
   我们的 curl 拿不到它的响应头做对比）。**改 nginx 的 content-type、去掉 nosniff 都没用** ——
   问题在容器品牌。正解是转标准 mp4（`ffmpeg -c copy`，不重编码）。
2. ⭐ **不能复用 `/downloads/` 那条 location 放媒体**：它 `default_type application/octet-stream`
   + `nosniff`，`<img>`/`<video>` 直接被拒收（而 200 是好的、看着像"图片坏了"）。
3. **导入的图片有一批是"没有扩展名"的图床链接**（`space.coze.cn/api/coze_space/gen_image?...`）：
   只看扩展名判断"是不是图片"会漏 → 改成**先看类型、再看扩展名**。
4. **源站会返回 200 + 0 字节**（797 个文件里有 1 个）：下载器要**把空响应当失败**，
   否则会留下一个 0 字节文件 —— 广场上就是一张裂图，而脚本还以为"下过了"。
5. **公开接口的 limit 之前卡在 60**，两处（`works` / `vibecoding-works`）——改一处漏一处的典型。
6. **`vibecoding_submissions` 与 `works` 是两套字段**（前者没有 `unpublished_at`）：
   "把某件作品下架"先看它在哪张表。
7. **守卫的 fixture 要够量**：翻页这条**必须 >12 件**才测得出来；我一开始只造了 2 件，
   分页断言等于没测。
8. **判分类要用 `data-category` 而不是 `data-type`**：卡片的 `data-type` 是**类型角标**
   （图片/视频/网页…），`data-category` 才是**分类**（画布作品/VibeCoding作品）——两者不是一回事。
9. **点站内作品会跳详情页**：守卫里"点开看查看层"必须点**导入件**（站内作品是 `<Link>`），
   否则页面一跳，后面的断言全找不到元素。
10. **`margin-left:auto` 的 computed 值不是 `"auto"`**（flex 项上会被解算成 px）：
    守卫要判"机构名贴不贴右边"，得比**右边缘距离**，不是比字符串。
11. **本机出网受限是常态**：`iicili.cyou` 本机 curl 不通、`npm registry` 不通、GitHub release 资产不通。
    离线办法：能通的那个方向借服务器（`ssh … curl`）；出包用本地 Electron 镜像（客户端仓库有脚本）。

§二.J（抓 aimagc 那批）踩到的：

12. ⭐⭐ **沙箱 iframe 里 `import` 的 ES module 必须走 CORS**：文档 origin 是 opaque（`null`），
    从**同源** `/media/` 取 `three.module.js` 也会被当成跨域 —— nginx 不带
    `Access-Control-Allow-Origin` 就**整片白屏**，而文件本身 `curl` 拿到的是**好好的 200**。
    我是用**真浏览器**跑才发现的（`Access to script … from origin 'null' has been blocked by CORS`）。
    → 后来 4 件 three.js 作品全白屏的根因就是这条；修法是 `/media/` 加 `ACAO: *`。
    ⚠️ 别用"文件 200 能下载"当验证 —— 这是**浏览器拒收**，不是取不到。
13. ⭐ **只抓 HTML 里写出来的绝对地址会漏"同目录素材"**：`单词小侦探` 的
    `url('bg.jpg')` 与 `<source src="bgm.mp3">` 都是相对路径，不在绝对 URL 扫描里 ——
    广场上打开就是缺图 + 没声音。爬虫必须**另外扫一遍相对引用**（并且只在作品自己目录内取，
    不许顺着 `../` 爬到站上别处）。
14. **这台服务器连不上 `cdnjs` 与 `wikimedia`**（`unpkg`/`jsdelivr`/`threejs.org`/源站都通）。
    所以下载器要：**失败不抛异常**（第一版一遇到连不上就把整轮打断，剩下的作品全没抓）、
    **有镜像就换**（p5 从 cdnjs 换 jsdelivr 的同一份文件）、**没镜像就保留原地址**并报出来
    （月球图 —— 宁可留一条外链，也不悄悄换掉学生作品用的素材）。
15. ⭐ **改前端的 importmap 前缀，必须先把 addon 文件全下到**：前缀改了、文件没下到 → 白屏。
    （所以脚本里是"全成功才改前缀"，不是"下不到就算了"。）
16. **`works.aimagc.cn` 的根路径是 403 而不是 200**：那是阿里云 OSS 的 bucket ACL
    （禁止目录列表），**具体作品的路径是好的** —— 别看到 403 就以为"抓不了"。
17. **别把脚本 scp 成"未跟踪文件"留在服务器 checkout 里**：`git pull` 要创建同名文件时会
    `Aborting`（`untracked working tree files would be overwritten by merge`），
    发布就卡在第一步（这次卡了两次 —— 第二次是守卫脚本）。要么先提交再 pull，
    要么在服务器上把那份未跟踪副本删掉。
18. **`deploy-002.sh` 的期望提交要传真 sha，不能传 `HEAD`**：它拿 `HEAD` 去比
    BUILD-METADATA 里的 `commit=<sha>`，比不中就**拒绝切软链**（自检是对的，我传错了）。
19. **金丝雀测"沙箱有没有隔离同源"时，别拿 `location.origin` 当判据**：Chrome 在
    sandbox iframe 里**照样把 location.origin 报成真实源**，但它确实是 opaque origin ——
    我第一版因此误判"隔离不成立"。判据要用**碰不碰得到**：`document.cookie` /
    `localStorage` / `window.parent.document` 三项都必须是 `SecurityError`。
20. **金丝雀的 `postMessage` 监听要挂在导航之前**（`addInitScript`）：金丝雀脚本在 `goto`
    期间就发完了，等 `goto` 回来再挂 listener，消息早丢了 —— 这也会误判成"隔离不成立"。

§二.K（后台两件）踩到的：

21. ⭐ **改了服务端却没发布，守卫会把"路由根本不存在"报成通过**：p121 第一跑 9 条红，
    原因是我只改了本地 `works.js`、没提交也没发布，服务器上还是旧代码。
    而且我 ⑥ 那条断言当时只判 `status === 404` —— 路由没注册也是 404，**看不出来**。
    教训是两层：改完服务端**先发布再跑守卫**；断言要**认错误码**（`ROUTE_NOT_FOUND` vs
    `VIBECODING_SUBMISSION_NOT_FOUND`），不能只认状态码。
22. **直连应用时要打 `/health`，不是 `/api/health`**：后者是 nginx 那条
    `location = /api/health` 代理过去的；在起真服务的守卫里打错路径，会一直等到超时，
    而服务其实早就起来了（日志里那句 `listening on …` 就在超时消息后面）。
23. **`scripts/` 里新加的脚本 scp 到服务器算"未跟踪文件"**：它会让 `git pull` 报
    `untracked working tree files would be overwritten by merge` 而中止（§六.17 记过一次，
    这轮又踩了一次）。**先提交再传**，或者传完就在服务器上删掉。

## 七、怎么跑这三道守卫（p115 / p120 / p121）

三道都要 **node ≥ 20**（本机 node 16 跑不了，服务器上跑）；p115 / p120 还要 Chrome：

```bash
# 服务器上
cd /srv/ai-kids-platform/internal-test/source
export PATH=/srv/ai-kids-platform/runtime/node/bin:$PATH

# p120（网页作品的沙箱 / CORS / 同源隔离 / 逐件渲染）—— 打线上，不建库不发布
CHROME_PATH=/usr/bin/chromium-browser node scripts/p120-webworks-sandbox-check.mjs --site https://iicili.cyou

# p121（后台编辑 / 彻底删除 / 分类映射）—— 临时库 + 真服务，不碰生产数据，不用浏览器
node scripts/p121-plaza-admin-check.mjs

# p115（官网首页与登录页）—— 自己构建 apps/website + 全新种子库，不影响生产数据
CHROME_PATH=/usr/bin/chromium-browser node scripts/p115-website-ui-check.mjs
```

