# 交接说明（新对话上手基线）

> 状态：当前有效
> VibeCoding 已按 OpenSquilla 的设计全栈重做并**发布到生产**（release `20260911T072107Z`）；
>   迁移已执行（18 个旧文件 → 18 个产物），入口、产物包、路由挂载均已复核，详见第三节最后一条。
> 更新日期：2026-09-11
> 代码基线：`a037229`（已部署）；服务器源码 checkout：`a037229`；最新文档提交：`a037229`
> 生产 release：`20260911T120321Z`（2026-09-11 12:03 UTC）——本次内容：**PPT 会去「收集素材」了**
>   （按规格生成插画、复用平台生图渠道；每份最多 3 张，门禁与画布生图同一套）。
>   切换前备份 `20260911T120317Z`，回滚锚点 = 上一版 release `20260911T113915Z`。详见第三节。
> 上一版生产 release：`20260911T113915Z`（11:39 UTC）——**工作台行为修正**
>   （默认收起 / 产出时自动弹出 / 文档先预览再下载 / 修好那个看不见的关闭键）。
> 再上一版 `20260911T112517Z`（11:25 UTC）——**PPT 能配图**
>   （用学生自己传的图，规格里写 `{"attachment": N}`）。
> 再上一版 `20260911T110724Z`（11:07 UTC）——**学生能直接产出 PPT / Word / Excel**
>   （模型按平台规格给内容，服务端自己渲染成真文件，不引任何依赖）。
> 再上一版 `20260911T104909Z`（10:49 UTC）——输入框支持粘贴上传。
> 再上一版 `20260911T103014Z`（10:30 UTC）——**学生端复查三条**：
>   附件放开到全部类型 / 删掉露出来的原生「选择文件」控件 / 模型下拉自动选中后台默认模型。
> 再上一版生产 release：`20260911T095614Z`（09:56 UTC）——09:21 全栈重做的两次上线后修复 →
>   09:36 第二批 4 条 → 09:47 图片上传 → 09:56 图片改内联。
> 2026-09-11 06:28 UTC 只读复核（新对话接手前逐项实测）：app 直连 `127.0.0.1:8789/health` → 200 `status ok`
>   （`/api/health` 是 nginx 剥掉 `/api` 前缀后的**公网**路径，直接打后端 8789 会 404 —— 别误判成服务挂了）；
>   current 软链 → `releases/20260911T060152Z`、BUILD-METADATA `commit=bcdf8f8…`、服务器 checkout HEAD=`a3a5fd0`；
>   备份 `20260911T060152Z` 与 `provider-policy-before-20260911T060129Z.json` 均在；
>   Node v24.19.0、`vm.swappiness=10`、Mem 1607MB（available 1261MB）、Swap **已用 176MB**（发布时是 0，现在有常驻占用，属正常）
> 2026-09-11 03:41 UTC 复核：服务 active / NRestarts=0 / ExecMainStatus=0、health ok、
>   / 与 /admin/ 与 /org/ 与 /api/health 全 200（在服务器上实测，本机出口封锁 HTTPS，别急着重启 nginx）；
>   systemd 「unit changed on disk」告警处理：`systemctl daemon-reload` 仍未做（存量漂移，与发布无关）
> 复核：运维调整（nginx/swap/ClamAV/内测下线）已于 2026-09-08 用只读 SSH 逐项复核（命令见第二节）；
>      2026-09-10 新增 nginx 主站 CSP media-src（见第二节第 8 条），已用真实浏览器复核；
>      release / commit / health 随每次发布更新，最后一次发布 2026-09-11 06:02 UTC
> 验收：2026-09-10 已用**真实上游**在生产跑通：生图、MiniMax-H3 文生视频、图生视频/首尾帧、
>      全能参考（图片参考）、音乐（歌词模式与描述模式各一次）、老师上传素材作参考；
>      2026-09-11 又用生产配置 + 真实上游复验：全能参考的图确实进了请求体（SUCCEEDED）、
>      VIDEO 32s 出真 mp4、MUSIC 104s 出真音频 —— 见第三节「全能参考根本没生效…」与
>      「生视频 / 生音乐在生产上根本跑不通…」两条；
>      2026-09-08 的早期实测见第二节末
> 用途：新对话/新同学接手本项目时的第一份上下文。服务器是活的，动手前仍建议按第二节命令自测一遍。

---

## 一、环境与访问

```text
仓库：chuangyilingdong/peixunwangzhan
本地：E:\学习平台正常（Windows / Git Bash）
分支：main（工作树干净，与 origin/main 同步）
Node：用 "C:\Program Files\nodejs\node"（v24）
      默认 PATH 里的 node 是 v16，不支持 node:sqlite
```

**生产服务器**（Alibaba ECS，1.6GB 内存）

```text
IP：39.106.183.200
SSH：ssh -i ~/.ssh/ai_kids_platform_ecs_temp_ed25519 root@39.106.183.200
部署用源码 checkout：/srv/ai-kids-platform/internal-test/source   ← 目录名沿用，内测服务已删除
生产 current：/srv/ai-kids-platform/production/current
服务：learning-platform-production（127.0.0.1:8789）
运行时 Node：/srv/ai-kids-platform/runtime/node/bin
内测服务：已删除（数据库备份在 /root/akp-internal-test-backup/）
```

---

## 二、当前生产状态（2026-09-11 07:21 UTC 发布后复核）

```text
release：20260911T073000Z（current 软链 → production/releases/20260911T073000Z）
commit：a797ef9（服务器源码 checkout 的 HEAD；build 元信息 mode=public）
发布历史：072107Z（重做上线）→ 072737Z（修 header 白横带）→ 073000Z（修产物卡片不显示）
本次发布（30c911f / release 20260911T072107Z）：VibeCoding 全栈重做 —— 学生/教师/官网三处换成
          OpenSquilla 的控制台设计，服务端改成「产物（artifact）」模型。详见第三节最后一条。
          发布前备份 20260911T072103Z；回滚锚点 releases/20260911T060152Z（上一版 release）。
服务：active/running，NRestarts=0，ExecMainStatus=0，OOMPolicy=continue
health：status ok；公网 /、/admin/、/org/、/api/health 均 200（在服务器上实测）
内存：Mem 1607MB、Swap 2047MB（已用 0）、vm.swappiness=10
最近备份：/srv/ai-kids-platform/production/backups/20260911T060152Z
          （另有一份渠道策略备份：backups/provider-policy-before-20260911T060129Z.json —— 补 {{referenceItems}} 前）
          （每次发布都会先备份；20260909T120052Z 是「框体即素材」数据迁移前的库）
本次发布（bcdf8f8 / release 20260911T060152Z）：① 全能参考真正生效（模板缺 {{referenceItems}} +
          渲染时没传 referenceAssets，两处都修）；② 生成中框体显示「魔法酝酿中」。详见第三节第 1 条。
上一版发布（8efcbfa / release 20260911T054539Z）：@ 引用改成**插在句子里**的内联芯片
          （上一版芯片与文本里的 @图片 1 重复，用户要求参考那种「[图片 1] 是主角人物」的结构）。
         详见第三节第 1 条。
上一版发布（3d2bee2 / release 20260911T053812Z）：① 生视频/生音乐在生产跑不通的真因找到并修掉
          （异步 worker 重建 provider 时丢了渠道的请求模板与路径）；② @ 引用改成「有图有文字、
          可整体删除」并与实际连线绑定的芯片。详见第三节两条。
上一次发布（8411103 / release 20260911T051012Z）：画布第二十轮——面板里的三个浮层不再被裁
          （参考缩略图的 ×、hover 大图预览、@ 引用候选）。根因是光束层 .learning-canvas__beam 的
          overflow:hidden 包住了整个面板内容，那层 overflow 本就多余（三束光各自都有 clip-path）。
          详见第三节「画布第二十轮」。
上一次发布（e6ad523 / release 20260911T045800Z）：画布第十九轮——连线不再把框体「换脸」（连过来的素材只当参考，
          不再替换框体画面）、参考缩略图 hover 出「×」可删、去掉「先写…再生成」这类前置红字提示。
          详见第三节「画布第十九轮」。
上一次发布（dd2fcf4 / release 20260911T043151Z）：画布第十八轮——**把文件拖进画布**这条链路修通：
          ① 删掉面板页脚那个看着像上传入口的「＋」；② 拖进来的图/视频/音频终于显示得出来
          （根因是 `/api/student/file-assets/...` 要 Bearer，`<img>` 带不了 token）；
          ③ 拖进去立刻落一个「上传中」的框体，传完补成真素材，并自动进左侧「本地素材」。
          详见第三节「画布第十八轮」。另：守卫 p34 修了个假警（注释里写 setNodes 会被误判），
          该提交 `3a88875` 只动脚本、不影响产物。
上一次发布（e57aeee / release 20260911T041051Z）：画布第十七轮——**去掉面板的「夹取」**。
          用户第二次报同一现象（画布上没有框体、输入框却杵在画布中间）。查了他的真实快照确认框体都在屏幕内，
          所以根因是面板被夹到画布边上、和框体分家（不是跟随没做好）。现在面板位置只有一条规则：
          永远居中在框体正下方 14px；会越界时改成**动画布让位**（左右/下），且只在「框体自己动了」时发。
          详见第三节「画布第十七轮」。
上一次发布（32792d3 / release 20260911T035247Z）：修**已登录学生打开官网首页整页白屏**
          （`ReferenceError: showStudentMenu is not defined`）。这是 2c7d6ec 留下的老 bug、
          **不是** 1c235bf 引入的（同一份代码在上一版 release 里就在），用户报「主页白屏」才发现。
          详见第三节「官网首页白屏（2c7d6ec 遗留）」。p34 守卫同步加了「组件用了 setXxx 却没声明」判定。
上一次发布（1c235bf / release 20260911T034013Z）：① 画布第十六轮——「平移画布时输入框和框体分家」；
          ② 课包「发布 ≠ 授权给机构」+ 授权时效按年选。两件都是用户 2026-09-11 提的。
          发布后复核：部署产物里 7 个文件都换成 `orgSeriesAccessSql`、旧的 `visibility='ALL_ORGS' OR`
          字面量只剩物料（promo_materials）那两处（无关子系统）；
          课程广场列表里的课包点开由 404 变 **200**（详情/列表口径对齐）；
          按新口径只读算了一遍机构可见数：示例创新学校 2 个、张老师机构 **0 个**（它没有任何授权记录，
          上线后课程中心会变空，要平台去「机构授权」补授）。详见第三节「画布第十六轮 + 课包发布/授权门禁」。
上一次发布（56e7c98 / release 20260911T030131Z）：画布第十五轮——修上一轮引入的毛刺：
          「放不下就让画布上移」的请求**在用户正拖画布时也会发**（overflow 按冻结锚点算，拖画布期间是常数，
          去抖一到就触发），表现为「移动画布时输入框乱动」。加了 viewportBusy 护栏（手势期间不发）
          + 同一缺口只请求一次（防来回弹）。节点拖动不受影响（拖节点不触发 onMoveStart）。
          详见第三节「画布第十五轮」。
          另：本轮还查明并回答了用户关于课包生命周期的疑问（见第四节「课程/课包生命周期」）。
上一次发布（4455856 / release 20260911T024227Z）：画布第十四轮——用户报「移动框体/视角一刷新全复原」的**真正原因**查到了，
          而且不是画布的问题：
          ①线上访问日志显示该学生的每个自动保存 PUT 都是 **404**；
            只读查生产库：作品所属课包（series「3213123」）**状态是 ARCHIVED**；
            而保存接口要求「课时属于已发布课包且在班级课程表里」（service/studentContext.js 的
            `resolveStudentLessonContext`）→ 于是每次都 404 → 旧的自动保存是**静默 catch**
            → 界面还显示「已保存」→ 刷新全丢。
          ②改：自动保存失败时右上角状态胶囊变成红色「保存失败：<服务端原因>」+ 悬浮提示
            「改动还没写进服务器，先别刷新；把这条信息发给老师」。
          服务端判定不动（课包归档后不允许继续创作是有意的），这次是让失败**看得见**。
          ③顺带按用户给的参考图做了：面板顶部写明「正在编辑 素材2」+「输入 @ 可引用：图片 1」；
            参考素材从纯文字胶囊改成**缩略图**（序号角标 + 名称），hover 在面板内向上展开大图；
            输入框支持 **@ 引用**（弹出候选 → 插入「图片 1」这样的名字）。
          ④用户的待办：把课包「3213123」恢复为已发布（或把课时移到已发布课包），保存就恢复正常；
            期间他做的改动都没有落库（最后一次成功保存是 2026-09-10T11:29Z）。
          详见第三节「画布第十四轮」。
上上次发布（487521f / release 20260911T022031Z）：画布第十三轮——用户反馈「挪了框体位置、视角，一刷新全复原」。
          两个原因都在客户端，服务端冒烟（p33 只测 PUT/GET 快照）发现不了：
          ①判断「有没有未保存改动」的 `canvasContentSignature` 只签了 id/type/data、**漏了 position**，
            挪框体不算改动 → 不触发自动保存 → 刷新回原位；
          ②`<ReactFlow fitView>` 无条件写死 → 每次挂载都重新适配，快照里存的视角等于没用。
          改：签名带上 position（取整）；`fitView={!hasStoredViewport}`（只有没存过视角的新画布才适配）。
          另加静态守卫 scripts/p39-canvas-dirty-guard.mjs（盯这两处）。详见第三节「画布第十三轮」。
          发布后复核：index-BSrQxUG1.js —— 挪框体 + 缩放后显示「已保存」，刷新后位置 (547,350) 与
          transform matrix(1.057,0,0,1.057,-24.8,-110.4) 都完全一致。
再上一次发布（91c9a6f / release 20260910T164238Z）：画布第十二轮——用户第三次反馈「输入框应该一直在框体下方」，
          前两轮都只做到「贴在下方、放不下就夹到画布底边」，夹的结果必然压住框体。这轮两条一起上：
          ①**放不下就把画布上移让位**（面板算出溢出多少 → 去抖 260ms → setViewport 上移同样距离 → 重新锚定），
            这样面板永远完整落在框体下方；
          ②**面板瘦身**：删掉顶部那条重复的「连线引用中」缩略图条，常见高度 ~300px → ~217px，多数情况不再触发上移。
          详见第三节「画布第十二轮」。
          发布后复核：index-D-IcLfMd.js / index-DEu-gYqn.css（CSS 与本机构建字节一致）——
          选中后与「把框体往下拖 240px」后，面板与框体间距都恒为 14px、零重叠、完整可见。
更早——画布第十一轮（6ab83cc / release 20260910T162404Z）——按用户三条反馈改，
          ①画面**按素材自身比例自适应**（原来用「槽位画幅」定尺寸，素材比例与槽位不一致时两侧出现黑边）；
          ②取消「点图放大」（灯箱）——画面不再是 nodrag，直接按住画面就能拖动框体，要看大图用画布缩放；
          ③顺手回归了生图/生视频/生音频三条链路（本地 mock，界面真实点击，三个任务全部 SUCCEEDED）。
          详见第三节「画布第十一轮」。
          发布后复核：index-CyCwa4ry.js / index-DEu-gYqn.css（CSS 与本机构建字节一致）——
          成图 960×540 落进框里、框比例 1.766 = 素材比例 1.778（无黑边）、按住画面拖动框体跟着走、无灯箱。
更早——画布第十轮（9d26a43 / release 20260910T160417Z）——按用户两条反馈改，
          ①素材框体改成参考那种展示（图2）：标题缩成画面左上方一行小字、画面本体就是卡片
            （原来套了一层深色卡片 + 深色内框，图被缩成一张小图，用户说「很难看」）；
          ②修第九轮改过头的面板锚点：平移/缩放**进行中**不动、**松手后重新锚定**，
            否则平移完面板会丢在一边（用户反馈的「图1 现象」）。详见第三节「画布第十轮」。
更早——画布第九轮（31dc538 / release 20260910T144631Z）——按用户拿着别家平台截图提的两条改，
          ①平移画布时输入面板不再跟着跑（锚点只在框体动了/换选中框体时重取），
          ②去掉框体手动缩放，卡片比例改成跟着「框位画幅」走（9:16 就是竖卡、16:9 就是横卡），
            历史快照里存的尺寸在加载时丢掉。详见第三节「画布第九轮」。
更早——画布第八轮（5efc604 / release 20260910T142409Z）——按用户反馈「输入框还是错位、
          不在图片下方；9:16 的图太长了，应该适配屏幕」改的，两件事叠加：
          ①竖版素材把卡片撑得比画布还高（9:16 成图能让卡片长到 1355 flow px）→
            给参考图/成图加 max-height 300px + object-fit: contain；
          ②fitView 把内容垂直居中，框体下方的空间最多只有 (画布高-框体高)/2，数学上放不下面板 →
            初始视野改成靠上、下边固定留出 320px 给面板（分边 padding，必须写 px 字符串）。
          详见第三节「画布第八轮」。
          发布后复核方式同上一轮：拉「生产实际下发的」产物（index-cg_NBY8-.css / index-QXxjY6HP.js，
          CSS 与本机构建字节一致）配独立临时库复测用户场景（视频框体 + 连一张 9:16 参考图）——
          卡片 429px 高、面板下方 14px、零重叠、面板与卡片都在屏幕内、水平居中（centerDelta=0）。
更早——画布第七轮（49d75ec / release 20260910T134928Z）——按用户四条反馈改，
          ①输入面板不再翻到框体上方（原来下方放不下就翻上去，学生会找不到输入框）；
          ②面板水平方向居中放不下时改成与框体左/右边对齐（原来居中后硬夹住，框体靠边时看着就是错位）；
          ③输入框边缘流光按参考**重做**——补上第一版漏掉的「内壁辉光层」（遮罩把光压在离边 28px 内），
            三层透明度/时长/配色全部对齐参考导出的原始 CSS；
          ④卡片标题 13px→16px、改成双击才进编辑态、编辑框收成半条标题栏；
          ⑤默认视野 fitView maxZoom 1.8→1.2。详见第三节「画布第七轮」。
          发布后复核：index-4l4gKoTy.css / index-CAVzQOn8.js（CSS 与本机构建字节一致）——
          流光三层透明度与参考逐位一致（0.1742 / 0.2814 / 0.1608）、拖动全程面板不低于框体顶边
          且完整可见、居中/左右对齐切换正确、单击标题只选中双击才编辑、只读态连线照常渲染。
更早——画布第六轮（f14a32f / release 20260910T131532Z）——①输入面板边缘流光（移植 border-beam
          的 md 档 CSS，配色换主色）；②框体两侧的小圆 ＋ 拖拽建连（Loose 模式，只认拖拽）；
          ③输入面板跟随选中框体（transform 逐帧算、无过渡）；④撤销历史不再被拖动/打字灌满、
          老连线补 handle、拦截自连与重复连。详见第三节「画布第六轮」。
更早（41a8a88）：①中文字体换成思源黑体（Noto Sans SC）；②连线可删除（选中连线中点出现 ×，
          键盘 Delete 也可）；③底部面板滑入动效、节点悬停/按下/拖动反馈、面板页脚顺序对齐参考、
          面板上方新增「连线引用中」参考图预览条。
更早（46a60ae）：①展示与输入分离——卡片只留「可改名的标题 + 画面」，提示词/参数/生成
          全部移到画布底部独立面板；②中文换霞鹜文楷（Geist 管数字与拉丁）；③侧栏「版本」模块整体删除。
更早（eb00db4）：画布第三轮——换 Geist 字体（OFL）、节点内部照参考重排为
          「媒体区 + 面板 + 页脚(状态胶囊 + 生成 ↑)」、学生可把桌面文件拖进画布
          （新增 /api/student/file-assets/upload）、清掉 123 条无人引用的旧壳层样式。
更早（ff73202）：画布细节复刻第二轮——连线改为贝塞尔曲线 + 沿路径游走的主色发光段
          （不再渲染箭头）、节点参数改为「标签 + 分段胶囊」、占位区改点阵底 + 居中提示、
          侧栏与工具栏换线性图标。
更早（2988a9c）：课堂画布换肤——按 ASUI Canvas Pro 2.2.0（MIT）的设计语言重做
          画布与工作台外壳（近黑底+酸柠檬绿、可收起侧栏、右上「课程大厅/提交作品」、
          提交即回课程大厅；生成逻辑未动），详见第四节第 16 条。
更早（3a56f4d）：同一账号支持多设备同时登录——登录不再顶掉既有会话，
          只在活跃会话超过 10 个时把最老的顶下线（详见第四节第 15 条）。
更早（d5353ac）：
          1) 生成状态回流：任务已完成但画布节点停在「AI生成中」时自动补结果/翻失败（此前只补缺失节点）
          2) 学生会话过期不再整页白屏：website main.jsx 里 useState 写在提前 return 之后（React #300）
          3) p34 守卫补强：识别 `if (...) {` 跨行早返回
更早（0f9490b）：问题反馈接口全线 500 的修复——拆 communication.js 时把
          helpFeedbackRows / normalizeHelpFeedback / HELP_* 留在各自文件里没导出，跨域调用直接
          ReferenceError；已统一挪进 communication/helpers.js，新增 p36 回归网。
更早（3550a6b）：
          1) 画布框体可缩放（把手 16px、修掉 overflow 裁切、同意条不再挡底部把手、图片随节点缩放）
          2) 素材面板点了有反应（已在画布的框体点击定位；不可编辑时真正禁用并写明原因）
          3) 框体参数平台可以不选（留空＝学生在课堂里自选，学生选的值落库 request_options；数据库加了一列）
          发布后用「生产实际下发的 index-B5AU9vU7.js」在本地独立环境逐条复核通过。
内测服务：learning-platform-internal-test → inactive / not-found（已下线）
ClamAV：clamav-daemon、clamav-freshclam 均 inactive
```

只读复核命令（不写库、不重启，可随时重跑）：

```bash
ssh -i ~/.ssh/ai_kids_platform_ecs_temp_ed25519 root@39.106.183.200 '
systemctl is-active learning-platform-production
systemctl show -p NRestarts -p ExecMainStatus -p OOMPolicy learning-platform-production
systemctl is-active learning-platform-internal-test
curl -fsS http://127.0.0.1:8789/health
free -m; cat /proc/sys/vm/swappiness
grep -rn client_max_body_size /etc/nginx/sites-enabled/
readlink -f /srv/ai-kids-platform/production/current
git -C /srv/ai-kids-platform/internal-test/source rev-parse --short HEAD
for u in https://iicili.cyou/ https://iicili.cyou/admin/ https://iicili.cyou/org/ https://iicili.cyou/api/health; do
  printf "%s " "$u"; curl -s -o /dev/null -m 10 -w "%{http_code}\n" "$u"; done'
```

### 服务器侧已做的运维调整（不在 git 里，勿回退）

```text
1. nginx client_max_body_size 2m → 30m（原值比应用 25MB 上限还小，封面上传必失败）
   /etc/nginx/sites-enabled/iicili.cyou 现为 30m，改动前备份 .bak-20260908T194905 里还是 2m
2. 服务加 OOMPolicy=continue：子进程被 OOM 杀掉不再重启整个服务
3. 新增 2GB swap + vm.swappiness=10（原机器 0 swap，曾因内存耗尽 OOM）
4. ClamAV 全部停用并禁止开机自启（应用未配置扫描器，clamd 常驻约 1GB 是 OOM 隐患）
5. 内测服务 learning-platform-internal-test 已删除（含单元、环境变量、发布目录、数据、健康检查定时器）
6. production.env 新增 `AI_PROVIDER_TIMEOUT_MS=300000`：视频生成实测约 145–155 秒，
   代码默认 provider 超时只有 120 秒，会先超时（且此前被误报成「上游调用失败」）
7. nginx /api/ location 增加 `proxy_read_timeout 300s` / `proxy_send_timeout 300s` / `proxy_buffering off`
   （2026-09-09 07:19 UTC，改动前备份 iicili.cyou.bak-20260909T071954Z）：推理型 TEXT 模型
   思考阶段可能几十秒不吐字，默认 60s 读超时会把 SSE 对话掐断
8. nginx 主站 CSP 补 `media-src 'self' data: blob: https:`
   （2026-09-10 07:50 UTC，改动前备份 iicili.cyou.bak-20260910T075045Z）：
   原来没有 media-src，回落到 default-src 'self'，导致**学生生成的作品音视频播不了**
   （上游返回的是腾讯云 COS 的外部 https 地址，浏览器报
   `Media load rejected by URL safety check`）。补上后实测：同一个 mp3 能读出 208 秒、
   mp4 能读出 4 秒。图片不受影响（img-src 本来就允许 https）。
9. nginx 新增 `location = /vibe-preview.html`，单独给这张静态页放宽 CSP
   （`script-src 'unsafe-inline' https:`，2026-09-09 08:14 UTC，改动前备份同名 .bak-20260909T0814xx）：
   主站 CSP 是 `script-src 'self'`，srcdoc 文档会继承父页 CSP，学生代码里的内联脚本会被全部拦掉；
   该页只做转发，学生代码仍跑在内层 sandbox iframe（无 allow-same-origin），碰不到主站
10. nginx 主站 CSP 的 **img-src 补 `blob:`**（2026-09-11 04:44 UTC，改动前备份
    iicili.cyou.bak-20260911T044410Z），现在是
    `img-src 'self' data: blob: https:` + `media-src 'self' data: blob: https:`：
    学生拖进画布的本地素材是「带 token 取回二进制 → blob: 地址」再喂给 `<img>` 的，
    img-src 少一个 blob: 就**静默变破图**（fetch 200、页面无报错、只是图不显示）。
    `/admin/`、`/org/` 没有自己的 add_header，继承同一份 CSP，所以一并生效。
    ⚠️ 同一目录下的 .bak-* 备份文件也会被 `include sites-enabled/*` 读进去，
    每次 nginx -t 都会刷一堆 "conflicting server name" 警告（无害：按文件名排序真配置在前、
    生效的仍是 iicili.cyou）。清理时可以把备份移出 sites-enabled。
```

### 本轮生产验收实测（2026-09-08，真实浏览器 + 真实上游）

**生图：通过。** 学生端画布「生成框体」→ 生图，job `generation_4e3f536c28644b288282`
（`zhenzhen-image-g-v2-lowprice`）SUCCEEDED，耗时 51s，产出真实 PNG 资产。这是发布后
（12:07 UTC）第一次真实生成，说明**新的图片请求模板被上游接受**。

**生视频：原配置不可能成功，换成文生视频模型后通过。**
- 原配置 `hailuo-h3-i2v` 必失败：job `generation_ebbc543e611042898890` FAILED /
  `GENERATION_PROVIDER_UPSTREAM_ERROR`。
- 手工复现上游请求，三种写法（默认模板 / 去掉 metadata / 字段提到顶层）都返回
  `HTTP 400 {"code":"invalid_parameter","message":"firstFrameUrl is required"}`。
  即 `hailuo-h3-i2v` 是**图生视频（i2v）**，必须传首帧图；平台只发文本，代码里也没有
  `firstFrameUrl` 支持，所以该模型在本产品中不可能成功。**与 metadata 字段位置无关**。
- 临时把渠道默认模型换成 `seedance-2.0-global-mini-t2v` 后，学生端 job
  `generation_6b579b8abd174fb1a4df` SUCCEEDED，耗时 76s，产出真实 `video/mp4`。
  验证后**已还原**为 `hailuo-h3-i2v`。
- 结论：**视频链路（请求模板 + 轮询 + 资产落库）是通的，问题只在模型选型**。

**失败不扣费：已确认。** 两个失败任务 `credits_charged=0`，两个成功任务各扣 1。

**学生端生成入口的前提**：课时必须配「生成框体数量 > 0」，否则学生端「素材」面板不出现
「生成框体」（`packages/shared/src/canvasWorkspace.jsx` 只在 `generationSlots.*.count > 0` 时渲染）。
第1课原配置为 0，验收时临时设为 1/3，**已还原为 0**（保存后形态是规范化后的
`{"version":1,"generationSlots":{...count:0...}}`，与原始的 `{}` 功能等价）。
⚠️ 2026-09-09 起该结构已被取代：先是 `generationBoxes[]`，当天又改为「框体即素材」
（`course_lesson_materials.material_type='GENERATION_BOX'`，学生端素材面板按素材顺序渲染）。
上面这段是当时的验收记录，现行口径见第四节第 12 条。

**音频 / 动画：当前不可用（代码分析，未实测）。**
- `platform_modality_settings` 里 MUSIC 是开的、套餐「标准创作套餐」`allow_music=1`，
  但 `modalityChannels` 没有 MUSIC / DUBBING / PODCAST 映射。
- 没有渠道时 `providerSelectionForModality` 会回落到全局默认渠道（文本渠道
  `https://www.xkwuai.cn/v1` + `gpt-6-astra`），而 MUSIC 没有请求模板，会拼出
  `{model,prompt,seconds,metadata}` 发到 `/music/generations` —— 必然失败。
- PODCAST / DUBBING 的平台开关本身是关的。

**验收用的探测脚本**（在服务器上只读运行，不入库）：`E:/tmp/ui-test/probe-video-upstream.mjs`、
`probe-t2v-models.mjs`（从密钥库解密渠道 key 后直接打上游，用于定位模型/字段问题）。

---

## 三、提交与部署记录（按时间顺序；标题里的「本轮」是写入当时的叫法，下面全是历史记录）

| commit | 内容 |
|---|---|
| `a1cb1e8` | 8 项修复：课包有效期移到机构授权、保存反馈、封面 /api 地址、比例下拉、能力联动框体、比例格式、上传竞态、教学素材隔离；附带修 file_assets CHECK 缺 TEACHING_ASSET |
| `6ac9102` | 新增交接文档 |
| `1691b91` | **生成参数按模型能力配置**：模型能力目录（比例/清晰度/时长/音频）+ 请求体模板化 |
| `5deabb4` | 课包向导/删除/下架、课程广场自动收录、作品广场发布、删除黑客松+客户端版本+商机管理 |
| `3131d49` | 补齐通用弹窗样式（此前 .modal-overlay 等类根本没定义，弹窗退化成内联块） |
| `2b32372` | 新建课包弹窗覆盖完整配置流程（课时能力/素材/框体/教学素材一次配完） |
| `1e73bb0` | 客户端错误信息按 HTTP 状态可读化（413/502/401 等） |
| `9a42c69` | 作品发布由平台直接控制；学员端我的作品/我的课程/学习统计落地 |
| `cc11cea` | 音频/动画节点接入生成；修正生成失败的错误归因与重试策略 |
| `d694c1b` | 仅文档：更新本交接说明（代码基线仍是 `cc11cea`） |

### 本轮新增代码改动（**已部署到生产**，代码基线 `934e144`）

```text
1. 图生视频（i2v）首帧链路（端到端已在生产验证通过）
   - 模型能力新增 inputFrame（NONE / FIRST），模型名带 -i2v 时默认判为 FIRST，
     可在「计费与模型 → 渠道 → 模型能力 → 需要输入画面」覆盖。
   - 学生端：生视频框体在缺首帧时**置灰**（原因只放在按钮 title 里；2026-09-11 起不再在面板里刷红字，
     用户要求「这种提示要全部删除」），从图片节点连线后按钮恢复可用，并把该图片作为首帧提交。
     连过来的图片**不会**替换框体画面（见第四节画布约定：连线只当参考）。
   - 服务端：只认本项目图片素材的 asset_url（回查 media_assets 校验归属），
     缺首帧在入队前就拦成 GENERATION_FIRST_FRAME_REQUIRED，不调上游、不扣费；
     首帧来源落库 generation_jobs.source_asset_url，重试与异步 worker 复用。
   - 请求模板新增占位符 {{firstFrameUrl}}，默认模板 VIDEO_I2V 把值映射到顶层 image 字段。
     ⚠️ 上游 api.seedance.nz 的报错文案写的是 "firstFrameUrl is required"，
     但实测传 firstFrameUrl / first_frame_url / image_url 都是 400，只有顶层 image 返回 200。
   - ⚠️ hailuo-h3-i2v 只接受清晰度 768P / 2K（传 480p 会 400），
     已按实测值写入该模型的「模型能力 → 清晰度」。
2. 修复：课时里选的模型一直没生效
   - lessonModelFor 用大写模态名取小写的 generationSlots 键，导致永远回落到渠道默认模型。
     现在课时选的模型真正优先于渠道默认模型（与文档约定一致）。
3. 修复：渠道保存会把 inputFrame 写死成 NONE
   - 管理端原用模态默认值回填下拉，保存渠道时覆盖掉按模型名的自动判断。
     现在下拉按模型名推断默认值，未显式改过就不会写坏。
4. 修复：provider 超时被误报为「AI 供应商调用失败」
   - normalizeProviderError 只认英文 timeout/AbortError，provider 抛的
     GENERATION_PROVIDER_TIMEOUT / GENERATION_TIMEOUT 落到了兜底分支。
5. 修复：官网作品广场「打开体验」打不开
   - 服务端只给 /works/<token>，前端没有对应路由。新增 /works/:token 与
     /works/shared/:token 作品详情页（只读画布渲染），列表接口补 share_token，
     卡片改站内跳转。生产已验证：错误 token 显示详情页错误态而非跳回首页。
6. 修复：管理端「官网内容」整页崩溃
   - WEBSITE_CONTENT_LABELS 未定义（`5deabb4` 删了定义没删引用），补回常量。
7. 修复：机构端课程中心按钮点击报错
   - OrgCourses 用了未定义的 navigate，补 useNavigate()。
8. 修复：课堂画布「保存并退出」长期不可点
   - 脏标记读 node.props，节点数据实际在 node.data；改为优先读 data（兼容旧 props），
     同步修版本对比与节点描述。生产已验证：只改文字会正确标记「有未保存修改」。
9. 修复：平台模态开关只影响展示、不拦调用
   - 同步 ai.js 与异步生成（预检 + 入队前）接入 isModalityEnabled（机构覆盖优先），
     新增 MODALITY_DISABLED 业务拦截；p11 冒烟补平台开关与机构覆盖两个用例。
10. 删除灰度开关系统（产品确认不需要，整套移除）
   - 路由模块 featureFlags.js、权限域 ADMIN_FEATURE_FLAGS、管理端页面/导航/路由、
     feature_flags 建表语句、p9-r02 用例；p9-r03 权限矩阵改为 7 个域。
     旧库里的 feature_flags 表是空转遗留，可手工 DROP（代码已不再引用）。
11. 新增管理端「预约线索」页
   - `/api/admin/leads` 早已实现但没有任何入口，官网预约表单的线索一直没人看。
     新增列表/筛选/状态流转/跟进记录，状态机与后端一致；新增 p12-lead-loop 冒烟。
12. 管理端补计费配置界面
   - 「计费与模型」新增 模态开关与单价 / 平台限额 / 预警阈值 三块（此前只有 AI 渠道），
     单价、限额、阈值在后台完全改不了的问题解决；变更原因写入配置变更日志。
13. 通知中心文案诚实化
   - 明确只有站内信（应用内）投递，没有邮件/短信/微信外发通道；
     「失败」指接收人账号已停用或删除，不是外发失败。
14. 删除桌面客户端（产品决定：客户端不要了）
   - `5deabb4` 已删管理端页面，本次清掉全部残留：`/api/admin/client-releases`、
     `/api/public/downloads`、官网 `/download` 页与头部/底部入口、`client_download_releases`
     建表语句、helpCenterPayload 的 downloads 段，以及官网营销文案里对客户端的承诺
     （机构方案模块、预约页「Mac / Windows 安装包」、页脚 Mac / Windows 字样）。
     旧库里的 `client_download_releases` 表仍是空表遗留，可手工 DROP。
15. 撤销上一轮新增的「预约线索」管理端页面
   - 该页是 `5deabb4` 删掉的「商机管理」模块，误重建后已撤除；
     后端 `/api/admin/leads` 按原决定保留（官网预约表单仍在写入）。
```

### 部署批次一（2026-09-08 14:46 UTC，`c3c761d` / release `20260908T144617Z`）

```text
1. 用量记录关联真实作品
   - usage_records.work_id 此前从未写入，报表的「作品」列与按作品标题检索静默失效
     （works.project_id 唯一，一个项目对应一个作品）。
   - 学生提交作品时回填该项目的用量记录；schema.js 增加启动迁移回填历史数据
     （幂等，只处理 work_id IS NULL 且已有作品的行）。
   - 新增 scripts/p13-usage-work-linkage.mjs：覆盖启动迁移回填、提交回填、
     平台端报表按作品标题检索到该记录。
2. 去掉恒为 0 的 token 字段
   - 上游图片/视频接口不返回 token 用量，input_tokens/output_tokens 恒为 0 却由
     机构端两个用量接口 + 平台端用量报表返回；现已从响应中移除，列保留并在 schema 注释说明。
3. 机构端「充值订单」诚实化
   - recharge_orders 没有写入方（未接入在线支付）；去掉恒为 0 的 pendingOrderCount /
     paidOrderCount，页面改为空态说明「充值走平台人工调整，记入积分流水」。

验证：13/13 冒烟脚本通过（12 个既有 + p13）、三端构建通过（admin / org / website）；
      生产部署后服务 active、NRestarts=0、公网 /、/admin/、/org/、/api/health 均 200。
      生产库只读复核：works 2 条、usage_records 38 条，两两不同项目，故回填迁移在生产无事可做
      （逻辑正确性由本地 p13 覆盖）。
```

### 部署批次二（2026-09-08 14:59 UTC，`4fb22f7` / release `20260908T145907Z`）

```text
列表分页（交接文档第五节「多处列表查询无分页/LIMIT」）
1. 服务端统一分页：apps/server/src/lib.js 新增 pageParams / pageResult
   （page/limit 解析 + 上限校验 + total/totalPages），以下接口从全量或硬编码 LIMIT 改为真分页：
   机构任务（services/teachingTasks.js）、课包、举报、积分流水（orgAdmin.js）、
   学员作品 / 项目 / 快照（student.js）。批注列表按 500 条封顶（单作品批注量级小，未做翻页）。
2. 前端接分页控件（复用 shared 的 ListResultSummary / Pagination）：
   机构端「课堂任务」「课程中心」「作品点评·待处理举报」「积分账务·积分流水」、
   学员端「我的作品」。学员作品的汇总（总数/已上广场/收到点评）改为服务端跨页统计，
   不再从当前页 items 推导。
3. 兼容性：响应为增量字段，未传 page/limit 时按默认每页 50（流水 50、上限 1000）返回第一页；
   机构任务列表此前实际由 services/teachingTasks.js 处理，改在服务层分页。

验证：14/14 冒烟脚本通过（新增 scripts/p14-list-pagination.mjs：翻页不重不漏、
      total 为筛选总数、非法 page/limit 返回 VALIDATION_ERROR）；三端构建通过；
      真实浏览器复核机构端「课堂任务」51 条 → 「共 51 个任务 · 第 1 / 2 页」，
      点「下一页」后为第 2 / 2 页且只剩 1 条；机构端积分账务页订单空态与流水分页摘要正常。
```

### 部署批次三（2026-09-08 15:11 UTC，`a37f6ec` / release `20260908T151102Z`）

```text
1. 删除自由画布 / 自由对话（产品决定：这两个功能不做）
   - 官网导航（两处）、路由、页面标题映射、满屏页判定、占位页全部移除；
     失效的 p6-learn-portal-e2e 导航断言同步删掉；后端从未实现。
   - personal_credit_ledger 表保留（供未开始的 VibeCoding），只更新了 schema 注释。
2. 画布版本管理 UI（把 6 个死代码函数接上入口）
   - 学生画布左侧工具栏新增「版本」抽屉：历史版本列表（预览 / 恢复 / 重命名 / 导出）、
     版本对比（选两版看节点/连线差异）、导入 JSON 快照。
   - 补齐此前缺失的导入校验 readImportedCanvas / MAX_CANVAS_IMPORT_BYTES
     （原代码调用了两个不存在的符号，一旦接入口就会 ReferenceError）；
     删除同样没有引用的 edgeDescription；差异列表改为显示节点标题而不是节点 id。
   - 服务端：快照详情接口补 actorName；新增 scripts/p15-canvas-versions.mjs 覆盖
     保存生成版本 → 列表分页倒序 → 取版本 → 重命名 → 恢复到新版本。

验证：15/15 冒烟脚本通过；三端构建通过；真实浏览器（学生账号）复核：版本抽屉渲染 3 个版本、
      对比 v2→v3 显示「新增节点「节点 3」」、预览 v1 只渲染 1 个节点、恢复到 v4 且列表变 4 条、
      导出/重命名/导入均返回预期提示。生产产物 grep 复核：自由画布/自由对话/free-canvas 已不在
      官网 bundle，版本管理/恢复版本字符串在。
```

### 部署批次四（2026-09-08 15:20 UTC，`e3eff92` / release `20260908T152038Z`）

```text
1. 机构下拉不再静默丢机构
   - 平台端 9 处下拉/筛选此前直接调分页接口 /api/admin/organizations（默认 100 条），
     机构超过 100 家会静默缺选项；新增 /api/admin/organizations/options
     （只返回 id/name/status，上限 500 并带 total），9 处全部改用它，机构列表页仍走分页接口。
2. 平台未登记端点默认拒绝
   - platformPermissionForPathname 原来对未登记的 /api/admin/* 兜底成 ADMIN_CONTENT，
     新增端点会静默落到内容域。现在返回 UNREGISTERED_PLATFORM_PERMISSION，
     requirePlatformPermission 对非 root 直接 403（PLATFORM_ENDPOINT_UNREGISTERED）。
   - 先把此前靠兜底工作的 7 个前缀显式登记（inbox / materials / notification-templates /
     notification-events / notification-failures / notification-queue → ADMIN_CONTENT，
     leads → ADMIN_ORGANIZATIONS），所以现有账号行为不变。
   - 复核口径：脚本比对前端与路由里出现的全部 admin 前缀，均能映射到业务域；
     生产上 /api/admin/organizations/options 返回 401（路由存在），未登记路径对匿名同样 401。

验证：15/15 冒烟脚本通过（p9-r03 增加「未登记默认拒绝 + options 接口形状/跨域」断言）；
      三端构建通过；生产 HEAD e3eff92、公网四入口 200、日志无报错。
```

### 部署批次五（2026-09-09 04:43 / 04:44 UTC，`3fd06f2`+`2b7179b`+`82641a3` / release `20260909T044447Z`）

```text
VibeCoding 课堂运行时（参考 Open WebUI 的对话式创作界面）
1. 数据模型（4 张新表）：vibecoding_conversations / vibecoding_messages /
   vibecoding_runs / vibecoding_submissions（含 entry_file）。提交没有复用 works，
   因为 works.project_id NOT NULL + UNIQUE 且 FK 到 student_projects，VibeCoding 没有画布项目。
2. 服务端（apps/server/src/routes/vibecoding.js）：
   - 会话 CRUD（列表/新建/详情含消息分页/重命名/保存文件/删除）
   - POST .../messages：SSE 真流式回复（provider 增加 generateStream + 上游 SSE 解析，
     模板新增 {{messages}} 占位符支持多轮；上游不支持流式则退化为整段）
   - 每轮成功回复扣 1 积分，走 credit_entries / usage_records（type=AI_VIBECODING_CHAT）
   - POST .../runs + GET .../runs：受限运行（见第 4 点）；GET /sandbox 返回能力状态
   - 提交与点评：POST .../submit、机构端 GET/PUT /api/org/vibecoding/submissions[/:id]
3. 前端：/learn/vibecoding 课堂入口 + /learn/vibecoding/:conversationId 工作区
   （会话侧栏 / 聊天 / 代码面板 / 预览 / 控制台），机构端新增「VibeCoding 点评」页。
   新增依赖 marked + highlight.js；Markdown 组件走词法分析渲染，不注入原始 HTML。
4. 受限运行（apps/server/src/services/vibecodingRunner.js）：
   - 浏览器侧：学生 HTML/CSS/JS 在 sandbox="allow-scripts" 的 iframe 里跑，
     控制台经 postMessage 桥回父页面（预览文档里注入桥脚本，必须在学生脚本之前）。
   - 服务端：systemd 瞬时单元（DynamicUser + PrivateNetwork + ProtectSystem=strict +
     MemoryMax=128M + CPUQuota=50% + RuntimeMaxSec + NoNewPrivileges），串行执行、输出截断 64KB。
   - ⚠️ 生产 fail-closed：只有 DEPLOYMENT_MODE=development 才允许无隔离的子进程后端；
     生产（public）必须显式设置 VIBECODING_SYSTEMD_RUNNER=1 且配好 systemd-run 调用权限，
     否则 /sandbox 报 available=false（生产实测就是这个状态，见下方「沙箱现状」）。
5. 放行 VibeCoding：studentContext 区分 canUseVibeCodingNow（画布与 VibeCoding 互斥，
   以教师开的课堂类型为准）；发布校验从「一律拦」改成「必须开放 AI 文字能力」。

验证：18/18 冒烟脚本通过（新增 p16 对话流式、p17 沙箱限制与 fail-closed、p18 提交点评闭环）；
      三端构建通过；真实浏览器复核：学生进入 VibeCoding 课堂 → 流式回复渲染 Markdown/代码块 →
      运行预览捕获 console → 服务端运行返回 stdout → 提交作品 → 老师驳回（带原因）→
      学生看到意见并可继续创作。生产复核：4 张新表已建、新路由 401（存在）、
      官网 bundle 含 VibeCoding 工作区、/sandbox 报 available=false。
```

**沙箱现状（2026-09-09 实测，需用户决策）**
```text
生产 ECS 上 systemd-run 存在，但 ai-kids-prod 用户调用被 polkit 拒绝
（"requires interactive authentication"）；unprivileged userns 也被内核策略挡住
（unshare -U 写 uid_map 报 Operation not permitted），且 ulimit -v 会让 Node 直接崩溃。
结论：要真正隔离地跑 Node 代码，需要额外一步运维改动（二选一）：
  a) 给 ai-kids-prod 配一条最小权限的 sudoers/systemd 代理（只允许固定的 systemd-run 模板）；
  b) 装一个只监听本机 unix socket 的沙箱助手服务（以 root 调 systemd-run，参数固定）。
在此之前服务端运行保持 available=false（不假装成功），学生仍可用浏览器预览 + 控制台。

**2026-09-09 用户决定：不启用服务端沙箱。** 判断依据：收益（跑纯 JS 脚本看文字输出）对当前
VibeCoding 产品形态边际价值有限，而代价是在承载数据库与上游密钥的生产机上常驻一条
「Web 应用可触发 root 操作」的通道（sudoers 代理或本机助手服务），最坏情况代价不对等；
生产内存也偏紧。后续若确有「跑脚本看输出」需求，优先考虑独立机器/容器承担代码执行，
而不是在主生产机上开特权通道。开启与否只影响工作区「服务端运行」按钮，浏览器预览与控制台不受影响。
```

### 部署批次六（2026-09-09 05:03 UTC，`6787856` / release `20260909T050302Z`）

```text
1. 生产 TEXT / 对话上游链路实测（VibeCoding 依赖它，此前从未端到端验证过）
   - 用生产渠道（custom / gpt-6-astra @ https://www.xkwuai.cn/v1）直接调 provider.generateStream：
     真实流式返回 13 个分片、约 2.5 秒，回答内容正常。结论：TEXT 流式链路可用。
2. 平台管理员自助改密（此前只能由 root 重置别人密码，自己改不了）
   - PUT /api/admin/me/password：校验当前密码 → 改密 → 全部会话失效（含当前会话）。
   - 自助端点登记为 ADMIN_SELF：只要登录了平台管理员就能用，不挂业务域权限。
   - 管理端侧栏底部新增「修改密码」入口（弹窗，改完强制重新登录）。
3. 平台用户详情 + 角色调整
   - GET /api/admin/platform-users/:id：账号/角色/机构/状态/活跃会话/最近登录。
   - PUT /api/admin/platform-users/:id/role：只能调成学生/教师/机构管理员；
     平台管理员角色不可在此修改（PLATFORM_ADMIN_ROLE_IMMUTABLE）；
     无机构用户不能调成机构内角色（USER_ORG_REQUIRED）；改角色后该账号全部会话失效并写审计。
   - 管理端「平台用户」列表新增「详情」按钮，详情面板内可改角色。

验证：19/19 冒烟脚本通过（新增 p19：改密边界/会话失效/受限权限管理员自助改密/
      详情/提权拦截/角色调整审计）；三端构建通过；真实浏览器复核：侧栏改密弹窗 →
      当前密码错误提示 → 改密成功跳登录 → 新密码登录 → 用户详情 → 改角色成功。
```

### 部署批次七（2026-09-09 05:11 UTC，`fee91a4` / release `20260909T051113Z`）

```text
机构端可见性（都是「功能已有、入口没有」的缺口）
1. 导航补两个入口：积分账务（/recharge，机构管理员可见）与积分用量（/usage）。
   此前这两个页面只能手输 URL，机构管理员实际上找不到自己的账务工作台。
2. 班级归档：班级卡片新增「归档班级」按钮（仅机构管理员、仅 ACTIVE 班级），
   走已有 DELETE /api/org/classes/:id（会同时结束进行中的课堂），带二次确认。

验证：19/19 冒烟脚本通过；三端构建通过；真实浏览器复核：机构端导航出现两个新入口，
      班级卡片出现「归档班级」，点击确认后班级状态变为 ARCHIVED。
```

### 部署批次八（2026-09-09 05:28 UTC，`6032456` / release `20260909T052829Z`）

```text
画布「文字框体」（补齐 text 能力的学生端入口）
1. 课时配置：generationSlots 新增 text（只有数量与模型，TEXT 没有比例/清晰度/时长）。
   管理端「课时编排 → 配置 → 本课生成框体限制」新增「文字框体数量 / 文字模型」，
   取消勾选 AI 文字时数量自动清零（与生图/生视频同一套联动）。
2. 画布：素材面板出现「文字框体」（显示模型与已用/上限），点击生成 prompt 节点；
   节点上新增「生成文字」按钮，生成结果写入 generatedText（保留学生自己写的提示词），
   节点内可滚动查看，可「重新生成文字」。
3. 服务端：lessonModelFor / slotLimit 支持 TEXT（课时指定的文字模型优先于渠道默认）；
   assertGenerationPreflight 增加框体上限判断，异步入队路径改为走完整预检
   （此前只查模态开关与首帧，课时能力/框体上限要等结算才失败）。
4. 顺带修正：local-mock provider 如实回报被请求的模型，便于验证「课时指定模型」是否生效。

验证：20/20 冒烟脚本通过（新增 p20：文字框体数量/模型下发 → TEXT 生成成功并落 metadata.text →
      数量用尽 403 LESSON_GENERATION_SLOT_LIMIT → 未开放 AI 文字 403 LESSON_CAPABILITY_DISABLED）；
      三端构建通过；真实浏览器复核：管理端课时配置填数量与模型 → 保存 →
      学生素材面板出现「文字框体 · mock-text-model · 已用 0/1」→ 加框体 → 生成文字 →
      节点内出现 AI 文字、按钮变「重新生成文字」→ 面板变「已用 1/1」并置灰。
```

### 部署批次九（2026-09-09 05:47 UTC，`40c57ef`+`40c57b3` / release `20260909T054746Z`）

```text
1. 框体规则（按产品要求）
   - 框体一旦拖进画布就不能删除（React Flow onBeforeDelete 只放行非框体节点），
     避免删掉后素材面板补不回来；提示文案同步说明。
   - 一个框体只生成一次：生成成功后不再出现「重新生成」按钮（生图/生视频/生文字一致）；
     生成失败时按钮仍在，可以重试（失败不占额度）。
2. 平台列表导出 CSV：机构管理 / 平台用户 / 平台作品三个列表新增「导出 CSV」，
   导出与列表共用同一套筛选（改前是各写各的），导出写审计（PLATFORM_*_EXPORT），
   权限按业务域校验（无对应域权限的平台管理员 403）。
3. 官网导航补「课程广场」（/marketplace 页面与详情页早就有，只是没有入口；页脚也加了）。
4. 机构自有课包：用户确认「机构不存在自有课包，全部由平台创建」，该项从待办移除。

验证：21/21 冒烟脚本通过（新增 p21：三份 CSV 的 BOM/表头/条数与列表一致、筛选一致、审计、越权 403）；
      三端构建通过；真实浏览器复核：平台用户页「导出 CSV」触发下载并提示「已导出 6 名用户」，
      官网导航出现「课程广场」且点击后正常渲染课程卡片。
```

### 部署批次十（2026-09-09 06:56 UTC，`9004792` / release `20260909T065612Z`）

```text
1. 平台管理员 MFA：TOTP（自实现 HMAC-SHA1）+ 10 枚一次性恢复码；自助绑定页 /admin/security；
   登录第二步校验（缺码 401 MFA_REQUIRED、错码计入登录限速、动态码防重放）；管理员列表加绑定状态列。
   新表 user_mfa_credentials（重启自动建）。
2. /courses 改由官网内容（CMS）驱动：新增 COURSES 区块 + 内置默认内容（库里没有行时公开接口回落默认）；
   管理端结构化编辑器（数据条 / 课包列表含课时）+ 草稿预览；顺带修掉 useWebsiteContent 把接口
   包装体当内容用的老 bug（此前 /org 常见问题一直显示硬编码文案）。
3. VibeCoding 多文件工程导入导出：工作区「导出工程 / 导入工程」，JSON 工程包，导入先按服务端同样的
   12 文件 / 单文件 64KB / 总量 256KB 限制本地校验。
4. VibeCoding 作品进作品广场：学生提交必须带版权确认 → 老师点评通过 → 平台「平台作品库」发布
   （vbt_ 分享码）→ 官网 /works 与画布作品合并展示，详情页在 sandbox iframe 里直接运行（能玩）。
   vibecoding_submissions 新增 7 列（重启自动补）。
5. 修复画布作品提交漏传 copyrightConfirmed：服务端自 3eed779 起要求该字段，学生端一直没传，
   点「提交作品」必然 400；现在提交前确认版权与展示授权并带上字段。

验证：25/25 冒烟脚本通过（新增 p22 MFA、p23 课程体系 CMS、p24 工程导入导出、p25 作品发布全链路）；
      三端构建通过；浏览器复核：管理端发布 → 官网 /works 出现「可在线玩」卡片 → 详情页学生 JS 已执行。
生产复核（部署后）：/、/admin/、/org/、/api/health 均 200；服务 active、NRestarts=0、日志无告警；
      /api/public/website-content/COURSES 返回 11 门课包（内置默认）；/api/public/vibecoding-works 200 空列表；
      /api/admin/me/mfa 未登录 401；user_mfa_credentials 表已建、vibecoding_submissions 7 个新列已补；
      官网 /courses 真实渲染 11 张课包卡片。
```

**生产数据配置（2026-09-09 07:07 UTC，经用户确认）**

```text
把草稿课包「测试1」（series_b60bb9c43e61449ca7e9，DRAFT，未授权、未进任何班级课单）的两个课时
改成 VibeCoding 课堂并补齐「AI 文字」能力：第一节（原 CANVAS/text）→ VIBECODING/text；
第二节（原 CANVAS/image+video）→ VIBECODING/image+text+video；课包版本 1.0 → 1.1。
不发布、不授权、不改课时状态；在读班级（三年级AI创作一班 / AI古诗词创意班）用的已发布课包
「AI古诗词创意营」5 个课时仍全部是 CANVAS，未受影响。两条变更各写了一条
PLATFORM_COURSE_LESSON_UPDATE 审计（request_path=ops:configure-vibecoding-draft-lessons，actor 为空
表示运维操作；当时生产管理员密码不在手边，未走管理端 UI，属有意为之）。变更前备份
production/backups/20260909T070743Z。
想让学生真正用起来还差三步（都在平台/机构端 UI 里点）：发布课包 → 授权给机构 → 老师开一节
VibeCoding 课堂。
```

**生产教学闭环 UAT（2026-09-09 07:15–07:25 UTC，四个检测账号实走）**

```text
平台端：发布课包「测试1」+ 授权示例创新学校（有效期至 2027-09-09）；课时保存被拦一次 ——
        第二节视频清晰度存的是 480p，而模型 hailuo-h3-i2v 只支持 768P/2K，且 VibeCoding 课时的
        抽屉里不渲染清晰度字段，界面无法修正；改用同一套鉴权 API 把清晰度改成 768P 后正常保存。
教师端：把「第一节」加入三年级AI创作一班课单 → 结束遗留的画布课堂（9/6 起一直挂着）→
        开了一节 VibeCoding 课堂（ACTIVE）。
学生端：/learn/vibecoding 进入第一节 → 与 AI 对话（真实上游流式）→ 代码侧边栏写 index.html/script.js
        → 预览里小游戏正常运行 → 提交作品（版权确认）成功。
教师端：VibeCoding 点评看到提交与代码 → 点评通过。
平台端：平台作品库 → VibeCoding 作品 → 发布到作品广场（vbt_ 分享码）。
官网：/works 出现「🎮 小创作者 · 可在线玩」卡片 → 点开详情页 sandbox iframe 里游戏正常渲染可玩。

过程中发现并修掉两个生产问题：
1. 流式对话在「思考阶段」零字节，nginx 默认 proxy_read_timeout 60s 直接断流：学生侧报
   network error，但服务端其实已拿到完整回复并扣了 1 积分（实测用户消息 07:15:25、助手消息
   07:16:46 SUCCEEDED）。根因是推理型模型先输出约 21k 字符 reasoning（约 27s）才吐第一个可见
   token，而平台只转发 delta.content。修复：等待期间每 15s 写一次 SSE 注释（: ping）保活
   （commit 93b1b4f，release 20260909T071929Z）；nginx 侧同时放宽 /api/ 的读/写超时到 300s
   并关闭 proxy_buffering（见第二节「服务器侧已做的运维调整」）。
2. 上面那条 480p/768P 的课时保存死锁（数据合法性问题，界面无法自愈）。
```

**VibeCoding 对话体验对齐 Open WebUI（2026-09-09 07:46–07:50 UTC，release `20260909T074821Z`）**

```text
按用户要求对照 open-webui 补齐课堂对话能力，见第四节第 11 条。上线后在学生端实测（真实上游）：
- 模型选择器列出 5 个模型 + 渠道默认；
- 发消息后思考阶段显示「已推理 4454 字」的实时进度；
- 点「停止生成」后气泡显示「已停止生成（没有扣积分）」，DB 里 usage_records 该次 credits_charged=0；
- 请求「完整打地鼠代码」时模型按约定输出 ```html index.html，界面出现「写入 index.html」，
  点击后代码落进编辑器并提示「已写入」；
- 「导出对话」触发 Markdown 下载。
验证：p26 冒烟覆盖上下文注入/SSE/重新生成/编辑重发/删除/清空/置顶/搜索/选模型/锁定守卫，
      全量 26/26 通过；三端构建通过。

同日后继（release `20260909T075725Z`）：按用户反馈精简代码面板操作栏 —— 原来 6 个按钮
（运行/刷新预览、服务端运行、导出工程、导入工程、上传文件、导出对话）挤成两行，现在只留
「入口文件 + 重新运行 + ⋯ 更多」，上传/导入/导出工程/导出对话收进「更多」菜单；
沙箱不可用时（生产）「服务端运行」直接不渲染，而不是显示成灰按钮。

再后（release `20260909T080840Z` / `20260909T081416Z`）：
1. 修会话列表置顶星标继承 `width:100%` 盖住标题（样式优先级 + 尺寸重置）；
2. **修学生代码在生产里根本不执行**（用户反馈「点开始游戏没反应」）：主站 CSP `script-src 'self'`
   + srcdoc 继承父页 CSP → 学生内联脚本全被拦。改为「静态外壳页 + postMessage + 内层 sandbox iframe」
   （`apps/website/public/vibe-preview.html` + nginx 单独放宽 CSP，见第二节第 8 条）。
   实测：工作区预览里 console 打出「打地鼠已就绪」、点开始游戏后倒计时与得分正常跑；
   官网作品详情页的 3×3 洞格由学生脚本渲染出来。

再后（release `20260909T092837Z` / `20260909T094157Z`）：模型能力编辑器按模态显示字段（文本/音乐等不再显示比例/清晰度）、
区分「已声明 / 默认值」并新增「清空声明」；模型能力填写加校验（比例格式、清晰度字符、时长必须 1–600 整数），
非法值当场 400 而不再静默丢弃（p27 覆盖，27 个冒烟通过）。
```

**生产实测记录（2026-09-08）**

```text
生图        SUCCEEDED  1 次（zhenzhen-image-g-v2-lowprice，约 51s）
文生视频    SUCCEEDED  1 次（seedance-2.0-global-mini-t2v，约 76s，临时验证后已还原模型）
图生视频    SUCCEEDED  1 次（hailuo-h3-i2v + 首帧 image，约 154s，credits_charged=1）
失败不扣费  已确认（失败任务 credits_charged=0）
上游故障    期间 api.seedance.nz 整站 Cloudflare 502 约 10 分钟，恢复后即成功，非平台问题
```

### 画布第六轮（2026-09-10，已发布 release `20260910T131532Z` / `f14a32f`）

按用户三条反馈改的，只动 `packages/canvas/src/index.jsx` 与 `styles.css`（后端零改动）：

```text
1. 输入面板边缘的流光 —— 移植 border-beam 的 md 旋转档（MIT，抄 CSS 不引依赖，配色换主色）
2. 框体两侧的小圆 ＋ —— 拖拽建连（不是参考那种「拖到空白处自动建节点并连线」，只连已有框体）
3. 输入面板跟随选中框体 —— 逐帧跟随，无 CSS 过渡
4. 顺手：撤销历史不再被拖动/打字灌满；老连线补 handle（facing 两侧）；拦截自连与重复连
```

验证证据（发布前本地 + 发布后用生产实际下发产物各跑一遍）：

```text
冒烟 38/38（node scripts/p*.mjs 全量复跑，见第七节清单）；三端构建通过；p34 静态守卫通过
发布前（本地临时库 8789 + 本地构建产物，student-2）：
- 流光：截三帧确认高光沿面板边框走；面板 padding 移到 panel-inner 后，光束层实测只比面板外沿内缩 1px
- 连接点：从「主图」右侧圆点拖到「短片」左侧圆点 → 建出 1 条连线，落库为
  { sourceHandle: 'right', targetHandle: 'left' }（拖拽中还确认了主色虚线的拖线提示）
- 重复/自连：正向重复、反向重复、同一框体自连各拖一次 → 连线数保持 1；换一对圆点 → 2（查重没有误杀）
- 面板跟随：拖动框体时按 16ms 采样，框体每移动一步面板同步位移；
  下方放不下时翻到框体上方（实测 773px 视口 + 314px 高框体就会触发），水平始终居中于框体
- 撤销：一次拖动后点一次撤销 → 位置精确回到拖动前；面板里连打 4 个字符后点一次撤销 → 4 个字符一起回退
- 只读（提交作品后）：节点 2 个、连线 2 条照常渲染，4 个连接点 opacity 全为 0，输入面板不渲染
发布后（拉「生产实际下发的」website 产物到本地，配独立临时库 + 生产同构静态服务复跑）：
- 产物核对：CSS 与本机构建**字节一致**（index-7SgMnjn2.css）；JS 为 index-bTk5CoJz.js，
  含 learning-node__port / learning-canvas__beam-bloom / ResizeObserver 等本轮标记
- 流光在跑：computed animationName = cv-beam-spin, cv-beam-fade-in，--cv-beam-angle 读数在变化
- 面板跟随：拖框体采样 5 个位移步，面板与框体同步（dy 先是 -199 停在框体上方，
  框体上移到有空间后**中途翻到下方**并保持 dy=+327 = 框体高 314 + 间距 14）
- 连线：从「主图」左侧圆点拖到「短片」左侧圆点 → 1 条连线 + 1 段发光段渲染出来
- 只读：提交作品后节点 2 个、连线 1 条照常渲染，4 个连接点 opacity 全为 0，面板不渲染
```

### 画布第七轮（2026-09-10，已发布 release `20260910T134928Z` / `49d75ec`）

用户拿参考实机截图对照后提的四条，只动 `packages/canvas`（后端零改动）：

```text
1. 「下方框体会跑到上面和错位，应该永远是下方」→ 去掉上翻；水平居中放不下时改成与框体左/右边对齐
2. 「输入框的边缘动效跟源码不一样」→ 补齐内壁辉光层 + 对齐参考的透明度/时长/配色（重做）
3. 「标题字体太小、单击就能改」→ 13px→16px，改成双击才进编辑态
4. 「图4 的输入框太长了，免得拖拽误触」→ 编辑态输入框收成 min(200px, 58%)，不再铺满标题栏
5.（连带）默认视野 fitView maxZoom 1.8 → 1.2，让框体下方留得出面板的位置
```

关键发现（怎么定位到「流光不一样」的根因）：

```text
参考的 CSS 是 border-beam 在运行时生成的（[data-beam] + ::after/::before + @property 动画），
光读源码很难还原。做法：把包的 dist 拷进临时目录，用 react-dom/server 渲染真实组件
（<BorderBeam size="md" colorVariant="colorful" strength={0.67} borderRadius={30}>），
组件把生成的 CSS 内联在 <style> 里，直接抓出来就是参考的原始 CSS；
再做一张并排对比页（左边真实组件、右边我们的 CSS）在同一屏里比。
对比后确认差异是：第一版只抄了「描边层」，漏了参考的「内壁辉光层」——
它用 conic 亮弧 ∩ 两条「离边 28px 以外全透明」的线性渐变，把彩色柔光压在贴边一圈，
是「柔光」观感的来源；没有它，描边层单独看就是一道硬线。
顺带发现三层的透明度参考给的是 0.26 / 0.42 / 0.24 × strength，第一版按 1.0 画，亮了 6 倍。
```

验证证据（发布前本地 + 发布后用生产实际下发产物各跑一遍）：

```text
冒烟 38/38（node scripts/p*.mjs 全量复跑）；三端构建通过；p34 静态守卫通过
并排对比：我们与参考三层透明度逐位一致（::after 0.1742 / ::before 0.2814 / bloom 0.1608，
          旋转时长 1.96s 一致）；视觉上都是「贴边一圈柔和彩色光 + 一道白色高光在跑」
真实浏览器（本地临时库 + 生产同构产物）：
- 面板位置：拖动全程采样 —— 面板从不高于框体顶边（neverAboveNodeTop=true）、始终完整在画布内；
  框体居中时 centerDelta=0，靠右/靠左时分别与框体右边/左边对齐（rightGap/leftGap=0）
- 标题：单击只选中不进编辑态；双击进编辑态且输入框占标题栏 53%（原来铺满 100%）；字号 16px
- 连线：从圆点拖到另一个圆点仍能建连线（1 条 edge + 1 段发光段）
- 只读（提交作品后）：节点 2 个、连线 1 条照常渲染，4 个连接点 opacity 全为 0，面板不渲染
```

已知取舍（不是 bug，用户如再反馈可从这里入手）：

```text
- 框体拖到画布很靠下时，面板会贴住画布底边并压住框体下半部——这是「永远在下方」的代价。
  fitView maxZoom 收到 1.2 后，默认视野下重叠只有约 14px。
- 面板高 163px、宽 660px，画布拥挤时会盖住相邻的框体（连它的连接点一起挡住，拖不动）。
  参考同样是 683px 宽、同样有此问题；学生拖开框体即可，必要时可收窄面板或改成贴画布底部。
```

### 画布第八轮（2026-09-10，已发布 release `20260910T142409Z` / `5efc604`）

用户反馈「图1图2 的输入框还是错位、不在图片下方；而且 9:16 的图太长了，应该适配屏幕」。
查下来是两件事叠加，都不是面板本身的问题：

```text
1. 竖版素材把卡片撑得比画布还高
   卡片宽度是内容撑出来的，图片 `width: 100%` 铺满卡片宽度后，高度 = 宽度 / 0.5625：
   一张 9:16 成图让卡片长到 1355 flow px（实测），而画布才 773 高 ——
   面板在下面根本没有位置，只能压住卡片（用户看到的「不在图片下方」）。
   改：`.learning-node__reference img` / `.learning-node__media` 加 `max-height: 300px` +
   `object-fit: contain`；竖图按比例缩到限高、左右居中，横图行为完全不变。
   ⚠️ 不要改成 `width: auto` 去「按比例自适应」：图片的原始宽度会参与卡片的 shrink-to-fit，
   卡片会被撑到 559 宽（实测过）。
2. fitView 把内容**垂直居中**，面板数学上永远放不下
   居中时框体下方最多只有 (画布高 - 框体高)/2 的空间；框体 400+ 高、画布 773 时只有 ~180px，
   而面板 160~320px → 必然压住框体。
   改：初始视野/「适配视图」用分边 padding `{ top:'24px', right:'40px', bottom:'320px', left:'40px' }`，
   内容靠上，下边固定留出面板的位置。
   ⚠️ ReactFlow 把**数字**当「比例」解析（`(h - h/(1+p))/2`），要留固定像素必须写 `'320px'` 这样的字符串。
```

验证证据（发布前本地 + 发布后用生产实际下发产物各跑一遍）：

```text
冒烟 38/38；三端构建通过；p34 静态守卫通过
复现方式（脚本 E:/tmp/place-aspect.mjs，可改 svg 的 width/height 切换横竖图）：
  把项目快照写成「一个 image 框体（assetUrl=9:16 图）+ 一个 video 框体（OMNI_REFERENCE）」
  再连一条线 —— 就是用户截图里那个现场
- 9:16：卡片 1355 → 394 flow px（屏幕 429）；面板落在卡片下方 14px、零重叠、卡片与面板都在屏幕内
- 16:9：横图行为不变（铺满卡片宽度、比例 1.777 保持），只是同样受 300px 限高保护
- 另外确认：面板水平仍居中于框体（centerDelta=0），左右对齐逻辑不受影响
```

### 画布第九轮（2026-09-10，已发布 release `20260910T144631Z` / `31dc538`）

用户拿了别家平台的截图（图1/2/5 是那家的、图3/4 是我们的）对照着提了两条：

```text
1.「左右移动的时候，输入框应该不会随着移动，只有拖动框体时，才移动」
   先把问题量出来：平移画布时面板并不是「跟着框体走」，而是追到画布边缘被硬夹住
   —— 节点移动 280px、面板只动了 31px，既没跟住也没停在原地（用户看到的「乱动」就是这个）。
   改：面板锚点（画布 transform）只在**框体位置变化 / 换选中框体**时重新取一次，
   平移与缩放都不重取。平移时它原地不动；想叫回来点一下任意框体即可（选中变化会重新锚定）。
2.「框体应该不支持收缩，参考图2图5 那样的默认比例，要放大用画布缩放就行」
   去掉 NodeResizer；卡片尺寸完全由内容 + 槽位画幅决定：
   - 素材区（占位/成图/参考图）用 `aspect-ratio` 定比例，
     尺寸 = min(宽度上限 226px, 高度上限 300px × 画幅) —— 9:16 得到竖卡、16:9 得到横卡，比例准。
   - `safeSnapshot` 加载时丢掉历史快照里的 width/height 与样式尺寸（学生以前手拉的尺寸不再生效）。
```

验证证据（发布前本地 + 发布后用生产实际下发产物各跑一遍）：

```text
冒烟 38/38；三端构建通过；p34 静态守卫通过
- 平移：面板位移 0、节点位移 300~400（发布后在产物 index-DkzOwmg9.js 上复测同样结果）
- 拖框体：面板立刻重新落位，与框体下方间距恒为 14px
- 比例：9:16 两张卡片同为 195 flow px 宽（高 373/394，差的是「画面来源」那行说明）；
  16:9 两张同为 252 flow px 宽、素材比例 1.778
- 缩放手柄计数 = 0（把手随 NodeResizer 一起删了）
- 只读（提交作品后）：节点 2 个、连线照常渲染、连接点全隐身、无把手、无面板
```

已知取舍（用户如再反馈可从这儿改）：

```text
平移画布后面板停在原地、不再跟回框体下面（这正是本轮要的行为）。为避免它「飘在无关位置」，
点选任意框体就会重新锚定；如果更希望「松开画布后自动归位」，在 CanvasSurface 的
onMoveEnd 里刷新一次锚点即可（约一行）。
```

**（第九轮这条已被第十轮推翻并改掉，见下。）**

### 全能参考根本没生效 + 生成中框体要有交代（2026-09-11，已发布 release `20260911T060152Z` / `bcdf8f8`）

用户两条：① 生图/生视频/生音频点生成后，框体应显示「魔法酝酿中」；② 引用了图片 1 和图片 2，
生成出来的画面却和参考图毫无关联。

**② 查到两处都断了**（缺任何一处都是静默丢弃：生成照样成功，只是参考没进请求体）：

```text
① requestBody 渲染模板时**没把 referenceAssets 传进 context** —— 模板里写不写 {{referenceItems}}
   都只会渲染成空数组（代码 bug，本次修）；
② 生产 MiniMax-H3 的 content 里**没有 {{referenceItems}}**（只有 {{frameItems}} 管首/尾帧），
   全能参考的图没有落点（配置问题，已在生产补上，改库前备份
   backups/provider-policy-before-20260911T060129Z.json）。
```

实测（本地跑生产配置 + 真实上游，给服务进程挂 fetch 探针抓真实请求体，两个修都到位之后）：

```text
修前 content: [{"type":"text","text":"这只猫跑到了人物的身上"}]                    ← 参考图不在里面
修后 content: [{text...}, {"type":"image_url","image_url":{url},"role":"reference_image"}] ← 进去了，SUCCEEDED
（用生产当前配置再跑一遍复核：模板 content 已含 {{referenceItems}}，请求体带参考图，生成成功）
```

**① 生成中的文案**：新增 `GeneratingState`（`✨ 魔法酝酿中…`，光点呼吸），图片/视频/音频/动画四个
框体在 `generationStatus = PENDING` 时显示它，不再只留一块占位板。实测点了「生成短片」后框体文案变成
`▶ 素材2 ✨ 魔法酝酿中…`，生成完成后恢复。

### @ 引用改成「插在句子里」的内联芯片（2026-09-11，已发布 release `20260911T054539Z` / `8efcbfa`）

第一版把芯片做成输入框里的一行、文本里还留着一份 `@图片 1` → 用户截图指出「出现了 2 个图片 1」，
要求参考结构：**芯片就在句子中间**（「[图片 1] 是主角人物」）。

纯 `textarea` 做不到（文字中间放不下缩略图），所以提示词输入换成富文本 `PromptEditor`（contenteditable）：

```text
- **React 不渲染它的 children**，DOM 由编辑器自己维护（否则每次重渲染都会冲掉光标）；
  值仍是纯文本：芯片序列化成 `@图片 N` —— 落库与交给模型的提示词和以前完全一致。
- 芯片 = 缩略图 + 名字 + ×，`contenteditable=false` 所以是原子的（退格一次删整颗）；
  点 × 连同后面那个空格一起删掉（别在句子里留双空格）。
- 有效性仍绑定真实连线：连线没了芯片变「已失效」（只改样式、不重建 DOM，保住光标）。
- @ 候选：光标前的纯文本从 Selection 取（富文本没有 selectionStart）；选中后在光标处插芯片。
- 中文输入法：组合输入期间不同步、组合结束再同步（不能打断 IME）；粘贴只收纯文本。
- ⚠️ 踩了一个坑：编辑器复用了 .learning-node__inputbox 的外框样式，而那层是 `display:flex` 列 →
  芯片和文字各占一行。`.learning-node__prompt` 必须显式 `display:block`。
- 回归脚本：`.tmp/citation-inline-test.mjs`（断言：无重复 @ 文本、芯片与文字同一行、
  落库文本仍是 `@图片 1`、点 × 后芯片与文本引用一起消失）。
```

### 生视频 / 生音乐在生产上根本跑不通：异步 worker 丢了渠道模板（2026-09-11，已发布 release `20260911T053428Z` / `881221d`）

用户要求「实际跑下生产环境的生图、生视频、生音乐是否真的可以生成」。用**生产实际在用的渠道配置 +
密钥 + AUTH_PEPPER**（只读取自生产，跑完即删）在本地起同一份代码真实调用上游，实测：

```text
IMAGE  ✅ SUCCEEDED（本来就是好的）
VIDEO  ❌ 上游：MiniMax-H3 requires the native V2 request in metadata.h3_request
MUSIC  ❌ 上游：version is required
生产库旁证：**MiniMax-H3 历史上一次都没成功过**（只有 hailuo-h3-i2v / seedance 成功过），
           mureka-v9-song 只有一条记录、也是 FAILED。
```

排查过程（每一步都有证据，别跳步）：

```text
1. 直接打上游：把渠道模板渲染出来的请求体手动发过去 → 视频/音乐都是 200 + task_id，
   说明**模板本身是对的**、上游也没改接口；
2. 从库里读策略 → normalize 后 modelRequestTemplates / requestPaths 都在；
3. 按运行时路径（providerSelectionForModality → getGenerationProvider → generate）抓包 → 请求体正确；
4. 但真跑时给**服务进程**挂 fetch 探针，抓到的却是
   POST https://api.seedance.nz/v1/videos（默认路径）+ 默认请求体（秒数/分辨率那套），
   音乐同样打到了默认的 /v1/music/generations。
→ 定位到 processAsyncGeneration（学生走的异步路径）重建 provider 时只挑了
   provider / model / endpoint / channelId 四个字段，把渠道的
   **modelRequestTemplates / requestTemplates / requestPaths / pollPaths 全丢了** → 退回内置默认模板与默认路径。
   图片的默认模板恰好符合上游要求，所以「只有视频和音乐坏」。
```

修法：`{ ...routedSelection, provider, model }` —— 整份带上，只覆盖这两个字段。

```text
实测验证（本地跑生产配置，真实上游）：VIDEO SUCCEEDED 32s 出真实 mp4、
MUSIC SUCCEEDED 104s 出真实音频，请求分别打到 /v2/video_generation 与 /v1/audio/generations。
复现脚本：.tmp/prod-gen-verify3.mjs（+ .tmp/fetch-tap.cjs 抓服务进程真实发出的包），
生产配置快照见 .tmp/prodcfg/（含密钥，**用完必须删**）。
```

### 画布第二十一轮：@ 引用变成「有图有文字、可整体删除」的芯片（2026-09-11，已发布 release `20260911T053812Z` / `3d2bee2`）

用户：「图1 是别的平台的 @ 引用，是一个整体有图有文字，删除也是一个整体直接删除，而不是像我们
图2 是一行字，**因为要确保引用确实是实际生效的**」。

```text
- 输入框改成「外框 + 芯片行 + 文本」三层：边框/背景/聚焦环画在外层，芯片落在输入框内（不是框外一行字）。
- 提示词里的 `@图片 N` 抽成芯片：**缩略图 + 名字 + ×**，一颗一个单元；点 × 一次性把芯片和文本里
  那处引用一起删掉。
- 芯片的**有效性绑定真实连线**：只有「图片 1 确实连着这个框体」时才是正常芯片；连线没了渲染成
  「已失效」红芯片（提示先连线）—— 这就是用户说的「确保引用确实生效」，不会只剩一行字却没有引用。
- @ 候选、插入逻辑、提交给模型的提示词文本都没动（模型侧行为不变）。
```

验证（真实 Chromium）：@ → 2 个候选 → 选一个 → 输入框内 1 颗带缩略图的芯片（"图片 1 ×"，矩形完全落在输入框内）
→ 点 × 后芯片数 0、文本从「让 @图片 1 」回到「让」；输入不存在的 `@图片 9` 渲染为 is-stale；页面零报错。

### 画布第二十轮：面板里的浮层不再被裁（2026-09-11，已发布 release `20260911T051012Z` / `8411103`）

用户三条反馈，其实是**两个不同的裁剪源**：

```text
1.「图1 hover 右上角的 X 被遮挡了」
2.「图2 hover 上去的图片预览被遮挡」
3.「图3 在输入框 @ 的时候，也被遮挡，只能点到图片 2，图片 1 根本看不到」

① 「×」被它自己的缩略图裁掉：× 原来放在 .learning-node__ref-thumb-pic 里面，而那个盒子是
   overflow: hidden（圆角裁图用），× 定位在 -7px 的溢出部分正好被裁掉。
   改成放进新的、不裁剪的定位锚点 .learning-node__ref-thumb-box（图片本体仍留在 pic 里裁圆角）。
② 参考大图预览 + @ 候选被**光束层**困住：.learning-canvas__beam 上有 overflow: hidden，
   而它包着整个面板内容 —— 凡是往面板外延伸的浮层（向上展开的预览大图、@ 候选列表）都被它裁掉。
   这层 overflow 本来就是多余的：三束光（::after / ::before / bloom）**各自都带**
   clip-path: inset(0 round 半径)，圆角处的裁切不依赖父级 overflow，所以直接去掉。
   ⚠️ 以后往面板里加浮层（下拉、hover 大图、气泡）都要先想一下「有没有祖先在裁」。
```

验证（真实 Chromium，**逐个量 rect + elementFromPoint 命中测试**，不再靠肉眼看截图）：

```text
- ×：hover 后 opacity 1、没有任何裁剪祖先命中、命中测试拿到的就是它自己
- 参考大图：display block、无裁剪祖先、命中测试拿到预览里的 <img>
- @ 候选：无裁剪祖先，两个候选都 visible 且都 hitIsItem=true（原来第一条整条看不到）
- 页面零报错；框体画面仍是默认占位（上一轮的「连线不换脸」没被破坏）
冒烟 40/40 全绿；本地构建产物与生产实际下发产物 + 生产同款 CSP 各跑一遍，结果一致。
```

### 画布第十九轮：连线不换脸 / 参考可删 / 去掉前置提示（2026-09-11，已发布 release `20260911T045800Z` / `e6ad523`）

用户三条反馈：

```text
1.「左边的素材连接到了右边，右边的框体会变成左边素材，应该是连线不会影响框体变化，
   应该是一直是默认框体，只有生成出来的图/视频/音频这些才会出现在框体内」
2.「这个参考 hover 上去，应该是右上角有个 x 可以删除这个参考」（参考图3 的交互）
3.「这种提示要全部删除」（指「先写下这一段的提示词，再生成」这类红色前置提示）
```

```text
① VideoNode 的画面来源只看**框体自己**（自己的预置素材 / 自己的生成结果），不再把 incoming[0]
   （连线连过来的素材）当自己的画面。连线素材只出现在底部面板的「参考 / 画面」行、生成时一起提交。
   注意：框体**自己的**预置素材仍照旧显示（那是框体的默认外观，第十一轮定的），用户没要求去掉。
② 参考缩略图加「× 删除这个参考」：默认隐身，hover / 键盘聚焦时在右上角出现（参考的 × 圆钮样式）；
   连过来的删那条连线（新增 removeIncomingRef：按 target/source 找边删），
   框体自己的预置素材则清掉 referenceUrl 配置。为此把取用从「只有 url」扩成带 sourceNodeId 的
   getIncomingImageRefs / getIncomingAssetRefs。
③ 面板里渲染 generate.blocked 的那条红字整条删掉（「先写下画面描述，再生成」等全部变体一起消失）。
   按钮该置灰还是置灰、hover title 里仍有原因；**生成失败仍然显示**（状态胶囊，见第五节第 33 条）。
```

⚠️ 这轮又踩了一次 TDZ：`removeIncomingRef` 的 deps 里引用了后面才声明的 `removeEdge`，
deps 数组是**立即求值**的 → `ReferenceError: Cannot access 'Et' before initialization` → 整页白屏。
构建通过、p34/p39 两个静态守卫也全过，**只有真开浏览器才炸**（就是第五节第 31 条那个坑）。
修法：hook 里引用的变量必须已经在它前面声明 —— 挪到 `removeEdge` 之后。

验证（真实 Chromium，本地构建产物 + **生产实际下发的产物 + 生产同款 CSP** 各跑一遍，结果一致）：

```text
① 连线前后框体画面完全一致：连前「▶ 素材2 / ▶ 在底部面板写提示词，生成短片」，连后一字不差
② 参考缩略图的 ×：未 hover 时 opacity 0 → hover 后 1 → 点一下连线数 1→0、缩略图消失、
   该行回到「未连接（图片/视频/音频连过来即可）」
③ .learning-node__generation-state 计数 0（前置提示全没了），面板只剩
   「正在编辑 … ✦ 生视频 · 9:16 未生成 生成短片」；页面零报错
冒烟 40/40 全绿。
```

### 拖进来的图还是不显示：生产 CSP 的 img-src 漏了 blob:（2026-09-11 04:44 UTC，现场改 nginx + 发 release `20260911T043151Z`）

用户回「外部拖入一张图片到画布，还是显示不了」。附图里节点只剩标题 + 一行 `AI生成画面`
—— 那正是 `<img>` 加载失败时浏览器显示的 **alt 文字**（配上选中态的绿色描边，看着像个绿色胶囊）。

**先看线上证据**（nginx access.log，别猜）：

```text
12:19:35 POST /api/student/file-assets/upload 200                     ← 修复部署前
12:19:35 GET  /api/student/file-assets/file_e7de…/download 401        ← 裸地址被 <img> 请求，没带 token
12:41:11 GET  /api/student/file-assets/file_f22d…/download 200 631832 ← 新产物：带 token 取回成功
12:41:14 GET  /api/student/file-assets/file_e7de…/download 200 631832
12:41:44 GET  /api/student/file-assets/file_3038…/download 200 960105
```

→ 12:41 之后**取数完全正常**（200 + 完整字节数），401 只出现在部署前。所以「取不到」被排除，
问题在「取回来了但渲染不出来」。

**根因是 CSP**：生产主站 `img-src 'self' data: https:` **没有 blob:**（而 `media-src` 有 blob:，
所以视频音频一直正常，特别容易误判成「图片那块代码坏了」）。于是链路变成：
JS 的 fetch（`connect-src 'self'`）成功 → `URL.createObjectURL` 成功 → 浏览器渲染
`<img src="blob:…">` 时被 CSP 拒掉 → **静默破图**，Network 面板里看不出任何异常。

本地复现（`.tmp/serve-with-csp.mjs` 把生产 CSP 原样加到静态服务上，同一份产物跑两遍）：

```text
img-src 'self' data: https:        → brokenImages 1，Console: Loading the image 'blob:…' violates … "img-src"
img-src 'self' data: blob: https:  → brokenImages 0，页面零报错
```

**修复**：改生产 nginx 主站 CSP 第 27 行（改动前备份 `iicili.cyou.bak-20260911T044410Z`，
`nginx -t` → `systemctl reload nginx`）；`/admin/`、`/org/` 没有自己的 add_header，
继承同一份 CSP，所以一并生效。仓库模板 `deploy/production/nginx.conf.example` 已同步（并加了注释），
服务器侧改动记在第二节运维调整第 10 条。实测公网 `img-src 'self' data: blob: https:`、四个入口全 200。

### 画布第十八轮：把文件拖进画布这条链路修通（2026-09-11，已发布 release `20260911T043151Z` / `dd2fcf4`）

用户三条反馈（都附了截图）：

```text
1. 「图1 这里有个加号，应该上传的地方。这个删除。目前只支持从外部拖拽进画布。」
2. 「图2 拖拽进画布，无法显示。」
3. 「图3 拖拽进画布目前只有右下角有个提示，画布上没任何感知，应该拖拽后立马有个框体，
   显示上传中。并且拖拽到画布的框体应该会出现在左边素材里。自动生成一个本地素材，放在里面。」
```

**① 删加号**：面板页脚那个「＋」（原本只是打开左侧素材面板）删掉；素材面板仍可从配置胶囊
（`✦ 生图 · 9:16`）打开。

**② 拖进去不显示 —— 根因是鉴权**：上传后文件地址是 `/api/student/file-assets/<id>/download`，
这条路由要 Bearer 登录态，而 `<img>/<video>/<audio>` 发出的请求**带不了 Authorization 头** →
必然 401 → 图永远出不来。（管理端给「框体预置素材」存的也是同一个地址，所以那类图一直也没显示出来。）

```text
- 新增 api.fetchBlobUrl()：带 token 取回二进制 → blob: 地址，同地址只取一次（宿主侧缓存）。
- 画布新增 resolveAssetUrl prop + useDisplayUrl()：凡是 /api/** 的素材地址一律「先解析再渲染」，
  并且**解析完成前不渲染原始地址**（否则浏览器白拿一个 401）。四类媒体节点、参考缩略图、
  @ 引用缩略图都过这条。
- 坑：fetchBlobUrl 不能再拼 baseUrl —— 快照里存的就是根路径，拼了会变成 /api/api/...（实测 404）。
- 机构端只读画布（apps/org）没传 resolveAssetUrl，对 /api/** 仍回落到原始地址；
  机构老师看学生上传的 PRIVATE 文件本来也没权限，这条没动。
```

**③ 拖拽反馈**：改成由**工作台**（持有快照的那一层）落占位框体。
⚠️ 我第一版是在画布组件的 `onDrop` 里 `setNodes` 落占位框体 —— 那是画布的局部状态，快照里没有，
上传完一写回快照就把它冲掉了：**拖进去什么都没发生，只剩右下角一条提示**（正是用户报的现象）。

```text
现在的流程：① 立刻把 uploading:true 的框体写进快照 → 画布上马上出现「⏳ 上传中…」的卡片；
            ② 逐个上传，把**同一个节点**补成真素材（地址 + uploaded 标记），传完一个写回一次；
            ③ 失败时撤掉「上传中」并在卡片上写明失败原因（不静默吞）；
            ④ 左侧「本地素材」分组从快照派生（带 uploaded / uploading 的节点），点击可定位到画布框体，
               所以拖进去的文件立刻就在那里、刷新后也在。
```

验证（真实 Chromium + **生产实际下发的产物**，把上传接口人为延迟 1.5s 才看得清中间态）：

```text
删加号     .learning-canvas__plus 计数 0，页脚只剩「✦ 生图 · 9:16 / 已生成 / 重新生成 ↑」
拖入 PNG   120ms 内画布出现「✦ drop-test.png ⏳ 上传中…」；3s 后 <img src> 变成 blob:，
           无破图、无 401/404、页面零报错
左侧素材   「本地素材 / ▧ drop-test.png / 图片 · 本地素材 · 已在画布上 / ◎」
```

### 画布第十七轮：面板不再被「夹」到画布边上（2026-09-11，已发布 release `20260911T041051Z` / `e57aeee`）

用户第二次报同一个现象，附截图：**画布上一个框体都没有，输入框却杵在画布中间**，
原话「为什么还是跟着画布跑呢？」。

```text
先核事实（只读查生产库他那份快照 project_516fab34a88c4098ab61）：
  两个框体 position 分别是 (-1744.5,122.7) / (-1123.2,127.5)，保存的 viewport x=1969 y=147.6 zoom=0.861
  → 换算到屏幕：素材1 在 x≈467、素材2 在 x≈1002，y≈253 —— **两个框体都在画布可视范围内**。
  所以「框体不见了」是假象：框体在，只是被输入面板盖住了/面板跑到了别处。

根因：不是跟随没做好，是**夹取**。三轮下来一直保留着「越界就把面板夹进画布」的逻辑
    （居中 → 越界改成与框体左/右边对齐 → 再夹住）。只要框体靠近画布边缘，面板就被按在画布边上，
    和框体彻底分家 —— 用户看到的「框体没了、边上却杵着输入框」就是这么来的。
    第十六轮我只改了「手势进行中不夹、松手后再夹」，所以松手那一刻照样分家。
```

改成（`packages/canvas/src/index.jsx` 的 `CanvasDockPanel`）：

```text
1. 位置**只有一条规则**：x = 框体中心 − 面板宽/2，y = 框体底边 + 14。删掉全部夹取/左右对齐。
   参考实现是「居中后硬夹住」，我们**故意不跟**（用户要的是「永远在框体正下方」）。
2. 面板会越出画布时改成**动画布让位**（左右/下都支持，deficit 一次挪到位）——
   这样面板既永远在框体下方，又不会被容器 overflow:hidden 裁掉右下角的「生成」按钮。
3. 让位只在「框体自己动了 / 换了选中框体」时发（frameKey 变化），且**框体已经不在画布里时不发**：
   用户平移画布是想看别处，这时候动画布就是橡皮筋（往左拖、被拽回来）。平移时面板照旧跟着框体走，
   越界就让它越界 —— 框体都出画布了，面板当然一起出去。
4. 手势中（平移画布 / 拖框体）既不请求也不消费 frameMoved：拖框体期间不能动画布，
   否则框体会在用户手底下被挪走；松手后再带着新位置补一次。
```

验证（真实 Chromium + **生产实际下发的产物** index--LRwRDz0.js，逐帧采样）：

```text
初始        面板居中(centerDX=0)、间距 14px、无重叠、完整可见（让位把画布挪了 219px）
大幅平移    360 帧里相对偏移恒为 -245（= (170-660)/2，始终居中）、间距恒 14px、全程零重叠
            松手后**零跳变**（旧实现会跳到画布边上）；平移量 100% 生效，没有橡皮筋
反向平移    完整生效（tx -201 → 399）
框体推出画布 面板跟着一起出去（截图 .tmp/panel-frame-away.png 留证），不再被夹回画布内
拖动框体    间距恒 14px、居中（拖拽中偶有 1 帧 14~15px 的滞后，是面板比 ReactFlow 内部 transform 晚一帧，可忽略）
```

⚠️ 新增的取舍（已接受）：用户自己把框体平移到画布边缘/画布外时，面板会跟着被画布裁掉一部分。
   要看得全就把框体挪回画布中间，或重新点一下框体（让位会把它带回来）。

### 官网首页白屏（2c7d6ec 遗留，2026-09-11 已修并发布 release `20260911T035247Z` / `32792d3`）

用户报「主页怎么白屏了」，附控制台截图：`Uncaught ReferenceError: showStudentMenu is not defined`。

```text
现象：**已登录学生**打开 https://iicili.cyou/ 整页白屏（正文 0 字）；未登录访问正常。
根因：InnerCircleHeader（InnerCircle 首页的头部组件）的「学生分支」用了
      showStudentMenu / setShowStudentMenu，但这个组件里只声明了 menuOpen —— 漏了 useState。
      是 2c7d6ec「修复官网首页学生下拉菜单未显示」留下的：加了下拉菜单的 JSX，没加对应的 state。
      未登录走的是另一个分支（`<Link to='/login'>登录</Link>`），所以只有登录状态才炸。
归属：**不是 1c235bf（画布第十六轮 + 课包授权）引入的**。同一段代码在 56e7c98 / release
      20260911T030131Z 里就是这样；已用线上产物对照复现：
      旧产物 index-49Iahivn.js → 学生会话 ReferenceError + 正文 0 字、未登录正常渲染；
      新产物 index-DQcX8dOf.js → 学生会话正常渲染、零报错、点开下拉菜单出得来。
为什么一直没被发现：p34 守卫只管「hook 在提前 return 之后」，p39 只管画布签名；
      服务端冒烟根本没有渲染官网首页这条路径；而这个 bug **只有「已登录学生看首页」会触发**。
```

p34 守卫加了**判定二**：组件里用了 `setXxx` 却没在同一个组件里声明。
关键点是**按组件分段**比较声明/使用 —— 这个 bug 藏得住的真正原因是
「同名 setter 在同文件的另一个组件里声明过」（main.jsx 里另一个组件确实声明了 `showStudentMenu`），
所以任何「按文件查未定义标识符」的检查都发现不了。

```text
排除项（都会造成误报，已在实现里处理）：
  - `localStorage.setItem` / `video.setAttribute` / `input.setSelectionRange` 这类**方法调用**
    （判定：前面不能是 `.`）；注意**不能**把「后面不能跟 :」写进正则的负向断言 ——
    `[\w$]*` 会回溯成 `setCooki` 这种截断名字（踩过一次，改用匹配后单独判断）；
  - `{ setCookie: null }` 这类对象字面量的键；
  - 全局 setTimeout / setInterval / setImmediate；
  - 多行 import 与 `export { ... } from` 的再导出清单（否则会误报 setAuthCookie 这些）。
改这个守卫前，先按「撤掉修复 → 守卫必须报出来」自证一次（实测会报 InnerCircleHeader）。
```

### 画布第十六轮 + 课包发布/授权门禁（2026-09-11，已发布 release `20260911T034013Z` / `1c235bf`）

用户这一轮提了两件事，一件画布、一件产品流程。

**① 平移画布时输入面板必须跟着框体一起走（画布）**

```text
用户原话：「我移动画布，看看左下角的输入框，并没有停留在对应的框体下方，反而跟着我走。
          我说了只有移动框体，下方的输入框才会跟随对应框体。如果我移动画布，输入框就保持跟框体一起。」
根因：前几轮的锚点口径是「手势中不动、松手后重新落位」——面板钉在**屏幕**坐标系里，
      而框体在画布坐标系里，一拖画布两者必然分家（松手后才跳回去，途中是脱开的）。
改法：面板改成直接订阅 ReactFlow 的视口 transform（`useStore(s => s.transform)`），
      每帧用 node.position × zoom + translate 现算位置 —— 面板和框体在同一个坐标系里，画布怎么动一起怎么动。
      手势进行中（viewportBusy）**完全不夹取**（夹取会让面板在半路停住，看着还是分家）；
      松手后才跑「居中 → 与框体左右边对齐 → 夹进画布」，夹取必须留（容器 overflow:hidden，
      不夹会把右下角「生成」按钮裁掉）。
      `viewportEpoch` 这个只为「松手后重新锚定」存在的 state 一并删掉。
```

浏览器实测（真实 Chromium + 本地构建产物，rAF 逐帧采样）：

```text
小幅平移（-70,+45）：框体位移 (-70,+45)、面板位移 (-70,+45) —— 逐像素一致，全程 drift=0（365 帧）
拖动框体（+95,-75）：框体与面板同样逐像素一致
面板与框体底边的间距：四个阶段全部恒为 14px（DOCK_GAP），无越位
大幅平移把框体推出画布后松手：面板会被夹回画布内（有意保留：面板 660px 宽、画布 1222px，
  居中必然越界；不夹就把生成按钮裁掉）
```

**② 课包「发布 ≠ 授权给机构」（产品流程，动了服务端访问控制）**

```text
用户原话：「建立课包，应该是发布。发布的时候所有机构都是不可见的情况。发布的时候只是会出现在
          首页课程广场里供大家看。然后再是授权给机构，才会出现在机构后台。这个流程是这样的。
          授权的时候也会有个授权时效，比如1年2年。授权有效期期间机构才能正常使用。」
旧行为：机构可见条件里有一条 `series.visibility='ALL_ORGS' OR assignment.id IS NOT NULL` ——
        新建课包默认就是 ALL_ORGS，于是「发布」等于对所有机构放行（用户看到的正是这个）。
改法：这条捷径删掉，机构能不能看到/使用平台课包**只认 course_assignments 里 ACTIVE 且未过期的授权**。
      判定收敛成唯一一处 `lib.js: orgSeriesAccessSql()`，替换掉散在 8 处的同款 SQL
      （机构端列表/详情、平台端 accessibleSeries/accessibleLesson、学生端 studentContext ×2、
        教学素材 fileAssets、教学任务 teachingTasks）。
      `visibility` 保留但只表示「上不上课程广场」：ALL_ORGS＝上架、ASSIGNED_ORGS＝不上架、PRIVATE＝私有。
      课程广场列表与详情的条件同步对齐（详情原本多要一个无入口的 marketplace_status='APPROVED'，
      导致广场里点开必 404，已修）。
      后台文案全部按新口径改写（可见范围下拉、发布确认、发布检查、机构授权面板、机构端课程中心）。
      授权时效 UI：1 年 / 2 年 / 3 年 预设 + 自定义天数，并实时显示「授权后有效期至 X」。
      （服务端 validityDays 1~3650 早就在算 expires_at，这次只是把入口做成按年选。）
```

**顺带查出一个真 bug（不是本轮的改动引起的）**：对**已有授权**的机构再次授权（续期）会直接抛
`status 无效` —— `routes/admin/courses.js` 里续期分支的 `SELECT id FROM course_assignments`
没把 `status` 查出来，`assertTransition` 拿到 `undefined` 就炸。生产库里 5 条授权各属不同课包，
所以一直没被触发。已修（`SELECT id, status`），并写进 p40 用例。

```text
冒烟 40/40 全绿（原有 39 个 + 新增 p40-course-publish-authorization-gate.mjs）+ 三端构建通过。
p40 覆盖：发布后未授权机构看不到（列表+详情+学生端）→ 授权后可见并带出到期时间 →
过期后立刻不可见（不依赖定时任务）→ 续期覆盖原有效期 → 撤销后立刻不可见 →
撤销后重新授权（REVOKED→ACTIVE）→ 下架退出广场 →「不上架」课包授权后机构照样可用。
另修了 3 个依赖旧口径的夹具（p6-classroom-course-tree-e2e / p9-r04 / p11-video-first-frame）
——它们插入的是 PLATFORM+ALL_ORGS 课包却没有授权记录，新口径下机构/学生看不到，按新规则补上授权。
```

⚠️ **上线后要提醒用户的连带影响**（上线后已按只读 SQL 复核，确实如此）：新口径下
「没被授权过的机构」会立刻看不到以前靠 ALL_ORGS 白拿的平台课包。生产库 7 个平台课包全是 ALL_ORGS：

```text
示例创新学校：可见课包 2 个（5 条授权记录里 2 条对应的课包是 PUBLISHED，其余 3 条指向已归档/草稿课包）
张老师机构　：可见课包 0 个（一条授权记录都没有）→ 课程中心变空，需要平台到「机构授权」补授
```

补救：机构管理 →（该机构）确认，或在「平台课程 → 课包 → 机构授权」里逐包授权（可设 1 年 / 2 年）。
**没有**批量给全部机构授权的入口 —— 如果用户要「一键全部机构」，那是下一个需求。

### 画布第十五轮（2026-09-11，已发布 release `20260911T030131Z` / `56e7c98`）

用户反馈「移动画布时输入框乱动」（并贴了参考平台的对照图）。查到根因是上一轮我自己引入的：

```text
「面板放不下就把画布上移让位」这个请求，在**用户正拖着画布**的时候也会发：
overflow 是按冻结的锚点算的 → 拖画布期间它是个常数 → 去抖 260ms 一到就触发
→ 用户拖到一半视图被顶一下 = 看起来输入框在乱动。
```

两条护栏：

```text
1. viewportBusy：CanvasSurface 在 onMoveStart 置真、onMoveEnd 置假；这期间面板不发让位请求。
   手势结束后面板先重新锚定（贴回框体下方），届时若仍放不下才补一次让位。
   节点拖动不受影响：拖节点不会触发 onMoveStart，所以拖到很低时仍会自动让位。
2. 同一个缺口只请求一次（记住上次请求的 overflow，差 < 4px 不重复请求），否则放不下时
   会每 260ms 再请求一次、画布来回弹。
```

验证：冒烟 39/39 + 三端构建 + p34 守卫通过；浏览器实测 60 步平移后：框体 (705,414)、面板 (488,582)，
与框体底边仍是 14px、零重叠。

### 画布第十四轮（2026-09-11，已发布 release `20260911T024227Z` / `4455856`）

用户连着报「挪框体/视角一刷新全复原」，第十三轮改完签名与 fitView 后**还是复原**。
这轮不猜了，直接查线上证据：

```text
1. nginx 访问日志：该学生 IP 的 PUT /api/student/projects/<id> 全部 404（119 字节 JSON），
   连一次成功的都没有 —— 所以压根没存进去。
2. 只读查生产库（/tmp 里跑一次性脚本，查完即删）：
   - 作品 project_516fab34a88c4098ab61：status=DRAFT、org/student 都对、学生可见 7 个项目；
   - 但它所属的课包 series「3213123」status=**ARCHIVED**；
   - 保存接口的 assertProjectUsable → resolveProjectUsageContext → resolveStudentLessonContext
     的 SQL 里有 `JOIN course_series series ON ... AND series.status = 'PUBLISHED'`
     → 查不到 → 抛 404 LESSON_NOT_ASSIGNED「该课时不在你的班级课程表中」。
3. 结论：**不是画布的问题，是课包被归档 + 自动保存静默失败**。
   旧代码 catch {} 里写着「自动保存失败不打扰学生」——于是学生一直以为存上了。
```

本轮改动：

```text
1. 自动保存失败必须可见：状态胶囊变红显示「保存失败：<服务端原因>」，title 里给行动建议。
   （服务端判定不改：课包归档后不允许继续创作是有意的。）
2. 面板顶部写明「正在编辑 <框体名>」+「输入 @ 可引用：图片 1」——
   面板贴的是**被选中**的框体，学生可能在看着另一个（用户就把贴着自己框体的面板误读成「跑偏」）。
3. 参考素材展示（按用户给的参考图）：
   - 纯文字「已连接 N 个素材」→ **缩略图**（小图 + 序号角标 + 名称胶囊，图片 1 / 视频 2…）；
   - hover 在**面板内部**向上展开大图（绝对定位：不改面板高度、不会被面板 overflow 裁掉，
     也不触发「放不下就上移画布」那条逻辑）；
   - 输入框 **@ 引用**：打 @ 弹候选（连线连过来的素材 + 框体预置素材），选中插入「图片 1 」。
     注：连线过来的素材本来就会作为参考发给模型，@ 是「在提示词里点名说的是哪张」的书写辅助。
```

验证证据（发布前本地复现生产条件 + 发布后比对产物）：

```text
- 本地把测试课包改成 ARCHIVED 后：手动 PUT 复现 404 LESSON_NOT_ASSIGNED（与线上一致）；
  浏览器里拖动框体 → 状态胶囊变成红色的「保存失败：该课时不在你的班级课程表中」+ 悬浮提示 ✓
- 缩略图 1 个（角标 1、名称「图片 1」）、hover 展开 166px 大图、@ 菜单弹出「图片 1 · 连线引用」
  且点击后插入「@图片 1 」并关闭菜单 ✓；面板高度 261px，仍贴在框体下方 ✓
- 冒烟 39/39 + 三端构建 + p34/p39 守卫通过；生产 CSS 与本机构建字节一致
```

### 画布第十三轮（2026-09-11，已发布 release `20260911T022031Z` / `487521f`）

用户反馈：「在画布移动了框体位置、视角什么的，一刷新全部复原」。

```text
1. 挪框体不算「改动」→ 不自动保存
   canvasContentSignature 只签 id/type/data，漏了 position。学生挪框体 → 签名没变 →
   changed=false → 既不显示「有未保存修改」也不触发自动保存 → 刷新回原位。
   改：签名带上 position（Math.round 取整，避开浮点噪声）。
2. 视角：每次挂载都无条件 fitView，把存下来的 viewport 覆盖掉
   改：fitView={!hasStoredViewport} —— 只有这份快照没存过视角（新画布 / 空项目）才自动适配一次；
   存过就照它显示。起始底稿 createStarterSnapshot 自带视角，也算「存过」。
3. 加回归网 scripts/p39-canvas-dirty-guard.mjs（纯静态）：
   盯「签名必须包含 position 与 viewport」+「画布不得出现无条件 fitView」。
   反向验证：把签名里的 position 删掉，守卫立刻 exit 1。
```

验证证据（发布前本地 + 发布后用生产实际下发产物各跑一遍）：

```text
冒烟 39/39（新增 p39；p34 静态守卫 + 38 个既有）；三端构建通过
- 挪框体：拖完立刻显示「有未保存修改」→ 3 秒后「已保存」→ 刷新后位置完全一致（635,369）
- 视角：平移 + 缩放后 transform = matrix(1.248,0,0,1.248,-108,-175.7) → 刷新后完全一致
- 生产产物上复测：挪框体 + 缩放 → 「已保存」→ 刷新后位置 (547,350) 与 transform
  matrix(1.057,0,0,1.057,-24.8,-110.4) 都一致
```

```text
为什么之前没人发现：p33 只测服务端的 PUT/GET 快照（能存能取就是通过），
而这里坏的是**客户端判断「要不要存」的那一步**。所以补了 p39 这种静态守卫。
```

### 画布第十二轮（2026-09-10，已发布 release `20260910T164238Z` / `91c9a6f`）

用户第三次反馈同一件事：面板压住了画面（「下面的输入框还是会这样，错位。应该一直在框体下方的」）。

```text
第七轮：永远在下方（去掉上翻），但放不下时夹到画布底边 → 夹住就压住框体
第十轮：修锚点时机，仍然是「夹住」
第十二轮：夹住这条路走不通 —— 改成「放不下就让位」：
   1. 面板算出自己超出画布底边多少（overflow）；
   2. 去抖 260ms（拖框体/拖画布过程中会连续变化，停下才请求一次，避免边拖边弹视图）；
   3. 请求 CanvasSurface 把画布内容上移同样的距离（ReactFlow 的命令式 setViewport），
      然后面板用新的 transform 重新锚定 → 面板完整落在框体下方。
   4. 顺带把面板瘦身：删掉顶部那条「连线引用中」的缩略图条（框体自己就显示着那张画面、
      下面「参考」行也写了连接情况），常见高度 300px → 217px，多数情况根本不触发上移。
```

验证证据（发布前本地 + 发布后用生产实际下发产物各跑一遍）：

```text
冒烟 38/38；三端构建通过；p34 静态守卫通过
- 选中视频框体（面板最高的那种）：框体 174..567、面板 581..798 —— 间距 14px、零重叠、面板完整可见、框体标题仍在屏内
- 把框体往下拖 240px：画布自动上移，框体落在 250..642、面板 656..873 —— 仍是 14px、零重叠、完整可见
- 面板顶部那条参考图预览条已消失（refStripGone=true），面板高度 217px
```

```text
待清理（不影响运行，下次动画布时顺手做）：.learning-node__ref-preview 的样式已成死代码；
另外 .learning-node__panel（第七轮就在的旧面板样式）也没人用了。
```

### 画布第十一轮（2026-09-10，已发布 release `20260910T162404Z` / `6ab83cc`）

用户三条反馈（第三条是回归测试）：

```text
1.「别的平台是自适应站满整个框体，我们的没法自适应」（竖图两侧有黑边）
   根因：第十轮给 `.learning-node__media` 定的尺寸用的是**槽位画幅**
   （`aspect-ratio: var(--cv-ratio-w)/var(--cv-ratio-h)`）。素材实际比例与槽位不一致时
   （16:9 的成图放进 9:16 的框体、或框体没配画幅时退回 2:1），画面框被拉成扁的，
   图在里面被 object-fit: contain 放宽 → 两侧黑边。
   改成：画面只受宽高上限约束（`width/height:auto; aspect-ratio:auto; max-width 226px; max-height 300px`），
   比例完全由素材自己决定 —— 16:9 得到横卡、9:16 得到竖卡，都贴着画面。槽位画幅只留给「没生成时的占位板」。
2.「有图像放大按钮后框体很难点击移动」→ 去掉点图放大（灯箱）：
   画面不再挂 onClick/openPreview、也不再是 nodrag，按住画面即可拖动框体；要看大图用画布缩放。
   顺手清掉灯箱的状态/渲染/样式（openPreview、previewImage、.learning-canvas__lightbox）
   以及已无人引用的 `.learning-node__reference` figure 样式。
3. 生图/生视频/生音频回归：本地 mock 通道 + 界面真实点击，
   三个框体逐个点「生成画面 / 生成短片 / 生成音乐」→ 服务端三个任务全部 SUCCEEDED
   （mock://generation/…），画布节点与底部面板同步变成「已生成 / 重新生成」。
```

验证证据（发布前本地 + 发布后用生产实际下发产物各跑一遍）：

```text
冒烟 38/38；三端构建通过；p34 静态守卫通过
- 自适应：成图 960×540，其所在框体比例 1.766（含 1px 边框）与素材比例 1.778 一致 → 无黑边
- 可拖动：按住画面拖动，框体位移 (30,20) 与拖拽位移一致；点画面不再弹灯箱
- 三条生成链路：IMAGE / VIDEO / MUSIC 任务均 SUCCEEDED，节点与面板状态同步
  （本地 mock 的产物是 data:/mock: 地址，只能验链路，不能验真机播放）
```

### 画布第十轮（2026-09-10，已发布 release `20260910T160417Z` / `9d26a43`）

用户又提两条：

```text
1.「选中素材后按着空格拖画布，出现面板丢在一边的现象（图1）」
   这是第九轮改过头了：当时锚点只在框体动/换选中时更新，平移后就不再归位，面板和框体彻底分家。
   现在改成两段口径（两轮反馈合起来）：**视口变化进行中面板不动，变化结束（onMoveEnd）后重新锚定一次**。
   - 「进行中不动」解决第七轮的反馈：实时订阅 transform 会让面板一路追着框体跑、追到画布边缘被夹住。
   - 「结束后归位」解决本轮的反馈：否则平移完面板丢在一边。
   实现：CanvasSurface 维护 viewportEpoch（onMoveEnd 自增），CanvasDockPanel 的锚点 effect 依赖它。
2.「图3 很难看，像图2那样设计」（素材展示）
   参考的展示是：画面上方一行小字说明、画面本体就是卡片。我们原来是「深色卡片 + 深色内框里
   缩着一张小图」，于是：
   - 图片/视频/动画三类改成 media 版式（`NodeFrame variant="media"`）：标题缩成画面左上方
     一行小字（18px 图标 + 12.5px 标题），不画卡片外壳（背景透明、无边框、无阴影）；
     画面自己就是卡片（12px 圆角 + 1px 边框，选中时主色描边），并按**自身比例**铺满。
   - 「画面来源（首帧）」「框体预置素材」这类 figure 说明不再显示（底部面板里已经写了来源），
     点图仍可放大看原图。
   - 没生成时的占位板仍按槽位画幅（空的 9:16 框体是一块竖板）。
```

验证证据（发布前本地 + 发布后用生产实际下发产物各跑一遍）：

```text
冒烟 38/38；三端构建通过；p34 静态守卫通过
- 面板锚点：在页内 16ms 采样一次视口变化（滚轮缩放）——
  变化后 60ms：节点 516→482 而动，面板停在 287 不动；
  稳定后：面板归位到 482（与框体左边对齐，因为框体已经靠近画布左边缘）、间距恒为 14px
  （这段 trace 在生产产物 index-CEzTlDLN.js 上复测一致）
- 素材展示：卡片背景 rgba(0,0,0,0)、说明行「✦素材1」、画面 239×425 比例 0.563 精确、
  选中描边落在画面上（不是落在一个深色外壳上）
```

---


### VibeCoding 全栈重做：按 OpenSquilla 的设计重建（2026-09-11，**本地完成，尚未部署**）

用户给了开源项目 OpenSquilla（Apache-2.0）的源码包，要求：把原来的 VibeCoding 代码全部删掉，
照它的设计重做页面 / 交互 / 动效 / 展示 / 侧边栏浏览。用户随后确认了四个边界：

| 问题 | 用户选择 |
|---|---|
| 服务端要不要一起删 | **前后端一起重写** |
| 视觉主题 | **深色，忠于参考** |
| 教师点评页 + 官网作品页 | **三处一起改** |
| 学生能不能手写代码 | **不能，只读源码（严格照参考）** |

#### 最终形态

- **学生端**：课程入口 + 三栏控制台工作区（会话侧栏 / 对话流 / 右侧工作台）。
  侧栏可拖拽调宽（240–480，拖到 200px 松手即收起，回拖 216px 才解除）、悬停卡片（272px，零延迟）、
  置顶与最近两个分区、搜索、行内菜单（置顶 / 重命名 / 删除）。
- **对话流**：用户气泡右对齐（≤82%）、助手正文无气泡占满列宽，两者共用同一条 `--chat-col` 左边缘；
  活动区用 `grid-template-rows: 0fr → 1fr` 做真实高度折叠，**流式结束强制收起**；
  实时行是 7px 脉冲点 + 400ms 延迟出现 + 每秒等宽计时；流式光标是 `steps(2, start)` 的方块光标；
  代码块自带语言标签与复制按钮（成功 `--ok` / 失败 `--danger`，1600ms 复原）。
- **工作台**：预览 / 源码 / 控制台 三页签，可拖拽调宽（默认是可用宽的 50%，上限 70% 且给对话留 480px），
  双击手柄复位、方向键步进 8px（Shift 32px）。预览在**真沙箱 iframe 里跑学生作品**。
- **产物卡片**：「主体 = 主动作」——能预览才给「打开」，否则只给下载，绝不出现点了打开其实在下载。
- **教师点评页与官网作品页**：复用同一套回放组件（`console/Replay.jsx`）——
  作品预览 + 创作对话 + 只读产物源码。官网那份嵌在浅色站点里，是一块深色「舞台」，
  且**不公开学生的创作对话**（只给成品与源码）。
- **归属**：设计令牌与尺寸常量取自 OpenSquilla，见仓库根 `THIRD-PARTY-NOTICES.md` 与
  `docs/third-party/opensquilla-LICENSE.txt`。

#### 服务端与数据模型的改动

原来的模型是「会话上挂一个 files JSON」，学生可手写、可导入导出。改成**产物（artifact）优先**：
AI 回复里每个带文件名的围栏（语言标识后面跟文件名）就是一个产物，有身份、类型、字节数、修订号。

- 新表 `vibecoding_artifacts`（`UNIQUE(conversation_id, name)`）。**同名 = 新修订**（revision + 1），
  不是新建一条——否则模型每次重写都会在聊天里堆出一串重复卡片。
  内容没变则不动修订号（模型常把同一个文件原样再写一遍）。
- **启动迁移**：旧的 `conversations.files` JSON 展开成产物行，幂等、空会话跳过、旧列保留可回滚。
  实测：3 个文件的旧会话 → 3 个产物（类型与字节数都对），重复加载不重复插。
- SSE 事件集升级：`start / status / delta / artifact / done / aborted / error`。
  **围栏闭合的那一刻就落库并推 artifact**，所以产物卡片是一个个出现的，而不是等整轮结束。
  未闭合的围栏不会命中（不会产出半截文件）；没有文件名的围栏只当示例代码，不算产物。
- 删掉了「手写代码」相关端点与功能：`PUT /conversations/:id` 传 files 现在返回
  `VIBECODING_FILES_NOT_EDITABLE`（明确拒绝，不静默忽略）；客户端的工程导入导出与代码编辑器一并移除。
- 会话详情新增 `sandbox` 能力（原来要单独请求 `GET /sandbox`），列表新增 `artifactCount`（悬停卡片用）。

#### 一处必须记住的 CSS 坑

平台的浅色全局样式里有 `input:focus, select:focus, textarea:focus` 那一条
（`border-color:#7d68db; box-shadow:0 0 0 3px #eeeaff`）——**元素选择器 + 伪类的特异性高于裸类选择器**，
所以控制台的输入框一开始顶着淡紫色光圈。修法是给 `.c-input:focus` 与 `.c-composer__input:focus`
显式接管 border-color 与 box-shadow。**在深色面板里加任何表单控件都要先检查这一条。**

#### 验证记录（本地，真浏览器）

- 学生端：以 student-2 登录 → 课程入口 → 进入创作 → 发消息，看到活动折叠「已完成 · 2 步」、
  两个代码块、产物卡片逐个出现、**工作台预览真的变成 AI 新写的页面**；收起 / 展开工作台、
  切源码页签、文件页签切换都实测过。
- 视觉隔离：输入框焦点态实测 `boxShadow: none / borderWidth: 0px`，无紫色光圈泄漏。
- 工作台收起后对话列从 460px 撑到 1180px，顶栏出现「预览作品」入口。
- 教师端（org）：学生提交 → 教师列表可见 → 打开详情，深色回放（作品预览 / 创作对话 / 产物源码）
  与令牌（`--bg` 解析为 #18181a）均正常。
- 官网公开作品页：发布后 `/works/shared/vbt_…` 渲染深色舞台，预览 iframe 真的在跑学生页面，
  只读源码页签可用，站点浅色头尾保留。
- 冒烟：p16 / p17 / p18 / p25 / p26 全绿；p34、p39 守卫全绿；三端 `vite build` 全绿。

#### 生产发布记录（2026-09-11 07:21 UTC，release `20260911T072107Z` / `30c911f`）

**发布前**：把新 `schema.js` 传到服务器，对着生产库的 `.backup` 快照先跑了一遍迁移验证——
18 个旧文件 → 18 个产物，**名字双向零差异、内容零不一致、类型推断零不符**，二次加载仍 18（幂等）。
生产数据没有离开服务器，验证完的快照与上传文件已删除。

**发布**：`git pull` → `backup-production.sh`（备份 `20260911T072103Z`）→ `build-production.sh` →
切软链 → 重启。回滚锚点是上一版 release `20260911T060152Z`（`rollback-production.sh` 或直接改软链）。

**发布后复核（服务器上只读实测）**：

| 项 | 结果 |
|---|---|
| 服务 | active、NRestarts=0、ExecMainStatus=0 |
| `/health`（app 直连 8789） | 200 `status ok` |
| 公网 `/` `/admin/` `/org/` `/api/health` `/vibe-preview.html` | 全部 200 |
| 数据库迁移 | 日志 `[schema] vibecoding: 已把旧工程文件迁移成 18 个产物`，与本地预演完全一致 |
| 产物包 | 线上 CSS 含 `.c-shell`/`.c-workbench`，旧 `vb-shell`/`vb-code` 命中 0；首页已引用新 hash |
| 新路由 | 无凭据访问返回 **401**（已挂载），不是 404 |
| 旧契约 | `PUT …/conversations/:id` 同样 401（鉴权优先，不是 500） |
| 存量数据 | 6 个会话**每个都有 1 个 html 产物**（共 3 个），预览入口都成立 |
| 服务日志 | 近 10 分钟无 error / exception |

**做不了的一步（需要你或一个可用账号）**：生产上 `student-2` 是 DISABLED，`student-1`
的文档密码已不适用，所以**没能用真实账号在生产点一遍**。建议你打开
`https://iicili.cyou/learn/vibecoding` 选一节课进创作、发一句话，确认工作台预览会更新。
本地同一份代码的完整链路（发消息→产物卡片→预览更新→提交→点评→发布）已经真浏览器验证过。

#### 上线后在真实账号上发现并修掉的两个问题

**能发现它们，靠的是拿到 student-1 的账号在生产真点了一遍。** 两次都是小改动、但都是「不修就像功能没生效」。

**① 课程入口顶部一条白色横带（release `20260911T072737Z`）**

`/learn/vibecoding` 的标题几乎看不见，顶部压着一条白带。根因是站点全局样式里有元素级的
`header{height:76px;position:sticky;background:rgba(255,255,255,.92);backdrop-filter:blur(14px)}`
（官网顶部导航）——**类选择器的特异性虽高，但只在本组件声明过的属性上才赢**；
`.c-page__head` 没声明 background，白底就直接压了上来（sticky/height/z-index/模糊一起漏进来）。
同类可渗透的还有 `nav`、`label{margin:12px 0}`、`table{width:100%}`、
`th{font-size:10px;letter-spacing:.5px;text-transform:uppercase}`。

修法的关键是**特异性必须卡在中间**：`[data-console] :where(header)` = (0,1,0)，
压得住站点的元素规则 (0,0,1)，又输给控制台自己的类规则（同为 0-1-0、加载在后）。
⚠️ 写成 `[data-console] header`（0,1,1）会连 `.c-topbar` 的 `height:49px` 一起压掉——
**工作台的顶栏也是 `<header>`**。这条已写进 `kit.css` 的注释里。

**② 老会话在聊天里一张产物卡片都没有（release `20260911T073000Z`）**

打开 student-1 的已有会话：每个会话有 3 个产物，聊天里却是空的。根因是产物卡片按
`messageId` 挂到消息上，而**启动迁移展开旧 files JSON 时不可能知道每个文件是哪一轮写的**，
`message_id` 为空。修法是在读接口补一个投影：没有归属的产物挂到最后一条助手消息上。

同步给 p16 加了断言并做了**双向自证**：撤掉投影 → 精确报出「仍有 3 条」；恢复 → 通过。

#### 用户试用后报的 4 条 + 排查中自查出的 2 条（release `20260911T074251Z` → `20260911T074514Z`）

**① 模型列表跨渠道污染** —— 用户问「学生端为什么这么多」「后台明明是 deepseek 却有 gpt 可选」。

后台那两个框读的是**不同数组**：`可用模型` 勾选框渲染 `modelMappings`（「读取模型」返回的候选，
几百条、跨供应商），`默认模型` 下拉渲染 `channel.models`（实际启用）。而 `textModelOptions()`
**把两者合并下发**，于是候选里的 deepseek-flash / deepseek-v4-pro 也冒到了学生端（4 个）。
→ 改成只下发实际启用的 `models` + 渠道默认模型。p26 加了「候选但未启用的模型不得下发」断言并双向自证。

数据侧另外发现：生产 TEXT 渠道的 `models` 里**真的存着 `gpt-6-astra`**，而该渠道 Endpoint 是
`api.deepseek.com`（g6-astra 是别家供应商的模型名，图片渠道的候选列表里就有 `zhenzhen/g6-astra`）。
已单独移除并备份策略（`backups/provider-policy-before-20260911T074203Z.json`），默认模型未动。
后台同时加了一条**漂移提示**：「已启用、但不在候选清单里」的模型会被显式列出来并给一键移除——
之前勾选框只渲染候选，所以这种脏数据在后台完全看不见，只在默认模型下拉里冒出来。
（提示要**排除渠道默认模型**：它是在下拉里特意选的，不算漂移。）

**② 元素重置把自己的按钮内边距清零（系统性，我引入的）**

用户报「侧栏左边有个竖杠」。查到根因是 `[data-console] button { padding: 0 }` 的特异性
是 **(0,1,1)**，压过了所有组件类的 (0,1,0)：`.c-btn` 的 `8px 14px` 实测变成 **0px**，
侧栏选中行的 `10px 11px` 也变成 0，文字顶到左边缘、被橙色竖条盖住。
→ 与 `header` 那次同一招：用 `:where()` 把元素名的特异性归零，只留 `[data-console]` 的 (0,1,0)，
压得住站点元素规则、又输给组件类。顺带把橙色竖条的定位基准从 `.c-session-row` 改回
`.c-session-item`（原本叠了两次左边距）。**已全量审计：现在 console/ 下没有裸的元素级选择器。**

**③ 输入框看起来没固定在底部** —— 实测几何本来就是对的（dock 钉在底部、对话流预留 208px）。
真因是 **dock 透明**：滚动到中途，内容会从面板四周的空隙里透出来，看起来像「跑到输入框下面」。
→ 给 dock 加一层从透明渐到画布底色的遮罩。

**④ 学生不该看到源码/控制台** —— 工作台加 `tabs` 白名单，学生端只给「预览」；
只有一个页签时不摆页签条、直接显示标题。产物卡片的「打开」改为打开工作台并停在预览。
顺带删掉侧栏常驻的「代码沙箱未启用」（沙箱已决定不启用，常驻一个灰条目只会让人困惑）
以及随之变成死代码的 `runOnServer` / `consoleLines`。

**⑤ 列表项里的加粗显示成字面 `**文字**`（自查发现）**

`markdown.jsx` 的兜底分支对 marked 的 `text` 块直接输出了 `token.text`（原文），没走内联解析；
而列表项正是这种 token。所以 AI 回复里所有「`- **要点**：说明`」都在冒星号。
→ 有 `token.tokens` 时走 InlineNodes。这个 bug 同样影响机构端点评页（共用该组件）。

**⑥ 已经过期名字的默认模型（留给用户判断，我没动）**

TEXT 渠道的默认模型叫 `deepseek-v4.1-flash-expires-on-0910`——按名字看是 9 月 10 日到期的临时模型，
而今天是 9 月 11 日。**我不敢替用户改默认模型**，但如果它真的已失效，学生一发言就会失败。
建议到「计费与模型」里确认一下。

**验证**：p16/p17/p18/p25/p26 全绿（p26 新增断言 + 双向自证）；p34/p39 守卫与三端构建全绿；
生产真浏览器逐项复核——模型下拉只剩 2 项（gpt 已消失）、侧栏只剩「返回课程」、工作台只有「预览」、
遮罩生效、竖条与文字间隔 5px、加粗渲染正常（5 处 strong、无字面星号）、产物卡片 3 张都在。

---

#### 字体对齐画布课堂（release `20260911T074909Z` / `20260911T075020Z`）

用户要求控制台字体跟画布课堂一致。画布用的是 `--cv-font`（见 packages/shared/src/styles.css）：
**Geist**（自托管，@font-face 只声明拉丁 unicode-range）+ **思源黑体 Noto Sans SC**（自托管，101 个
unicode-range 分片，浏览器只下用到的）+ 系统回退。控制台原先用的是系统字体栈，现已改成同一套。

- 字体文件在 `apps/website/public/fonts`，由域名根提供（`/fonts/…` 实测 200），所以学生端与
  机构端（点评页，同源）都取得到；取不到就按栈自然回退，不报错。
- 两套都是平台已有的 SIL OFL 字体；**OpenSquilla 自己的** Space Grotesk / IBM Plex 仍然没有引入。
- 实测：Geist 三个字重全部 loaded、思源黑体按需 12 个分片。

**同批修掉一个隐藏问题**：代码块/行内代码的计算字体是站点全局的 `ui-monospace…Consolas`，
不是 `--font-mono`——又是「本组件没声明该属性、被站点规则盖过」那个模式（Windows 上两者都落到
Consolas 所以肉眼看不出，但令牌等于摆设）。已给 `.md-code code.hljs` 与 `.md-inline-code` 显式声明。

---

#### 后台勾选框的「陈旧表单」陷阱 + 删除人设与产物约定（release `20260911T081437Z`）

**① 「后台选了 deepseek-flash，学生端还是 3 个模型」——不是后台没生效，是陈旧表单覆盖。**

查库发现 `models` 又变回 `[gpt-6-astra, deepseek-v4.1-flash-expires-on-0910, deepseek-flash]`。
原因：用户打开后台时表单里已带着旧的两个值（我上午删 gpt-6-astra 时他那个标签页还开着、
表单状态没刷新），他勾上 deepseek-flash 一保存，旧值被一起写回去了。

根因是勾选框**只渲染候选清单**：不在候选里的已启用模型在界面上看不见，却仍在表单状态里、
并会被保存。上午加的「漂移提示」要手动点一下移除，挡不住这种情况。
→ 修法：勾选框把**候选 + 已启用**两个来源合并渲染，不在候选里的标「（不在候选清单）」并用警示色。
**看得见的，才控制得住。** 提示同步瘦身（不再重复列一遍）。

⚠️ 由此得一条操作纪律：**改后台的模型配置前先硬刷新页面**，否则旧表单会把已经删掉的值写回来。

**② 按用户要求删除「人设与产物约定」**（`lessonSystemMessage`）

删掉：阿飞人设、「index.html 必须是完整文档」、「带文件名的围栏才算产物」那几条约定、
以及给模型的产物清单注入。保留两块：面向未成年人的一句安全底线、以及课时内容（标题/简介/正文）。
（安全那句不属于「人设」，是面向未成年人的平台底线，已告知用户；要删也是一行。）

⚠️ **后果**：产物仍然只从「带文件名的围栏」解析（服务端规则没变），所以现在要靠模型**自己**
用 ```语言 文件名 的写法，右侧预览才会更新。要把约定找回来，可以写进渠道配置的「请求模板」，
不必改服务端代码。p26 的断言已**反转**（原来断言 system 应含约定，现在断言不应含人设与约定）
并双向自证：加回人设 → 精确报出「不应再含人设」。

**③ 数据侧**：生产 TEXT 渠道的 `gpt-6-astra` 又被写回来了，已再次移除（备份
`backups/provider-policy-before-20260911T081506Z.json`），两个 deepseek 的保留。

---

#### ⚠️ 删掉产物约定的后果兑现了：AI 不再写文件名，预览不更新（release `20260911T092021Z`）

上一轮按用户要求删掉注入给模型的「产物约定」时，我就在文档里写了后果：
**产物仍然只从「带文件名的围栏」解析，所以要靠模型自己写 ```语言 文件名。**
用户接着问「现在还需要做什么」，我盘生产数据时发现它已经发生了：

- 全库 `revision>1`（= AI 真正写过）的产物共 6 条，**最新的停在 08:09**；
  而删约定的版本部署于 **08:14:37** —— 之后 **0 个**产物被 AI 写过。
- 决定性证据是 08:33 那条真实回复：AI 说「这是为您制作的三星堆青铜神树解说网页的HTML代码，
  打开就能看到…」，但围栏是 ```html ——**没有文件名**。那句话说了等于没说：
  该会话的产物至今还是新建时种下的三个默认文件（382/112/39 字节、revision 1）。

**修法（用户明确要求不加回约定）**：让服务端自己认，不再依赖模型遵守一条它没被告知的格式。

- 围栏写了文件名 → 用它的（行为不变）
- 没写文件名 → 按语言落到默认名（html→index.html / css→style.css / js→script.js），
  但**必须过「完整度」检查**：整篇 HTML 文档（有 doctype/<html> 且有 </html>，或有 <body> 闭合）、
  有规则块且≥3 行的 CSS、≥6 行或带 DOM 操作的 JS。片段一律不认。

**为什么必须挡片段**：错认的代价是**学生的页面被一段示例覆盖**（`<h1>示例</h1>` 这种
解释用代码块在生产回复里很常见）。判断是：**宁可漏认，不可错认**——漏认的后果是预览不更新
（用户看得见、会来问），错认的后果是学生的作品被悄悄覆盖（用户看不见、可能丢东西）。

新增 `scripts/p41-artifact-fallback-naming.mjs` 固化四条断言，**用那条真实生产消息作输入**：
① 不带文件名 → 认成 index.html 且内容完整；② 片段一个都不认；③ 显式文件名仍优先；
④ 流式期间未闭合的围栏不产出（不会写出半截文件）。
发布后又在服务器上**直接导入已部署的模块**复跑一遍：真实消息 → index.html，片段 → 0。

---

#### 用户试用第二批：4 条修复 + 历史数据回填（release `20260911T093623Z`）

用户一次报了 7 条，这轮落地其中 4 条可执行的。

**① 秒数恒为 0** —— 真因是个**类型错误**：`startedAt` 是 ISO **字符串**，而
`useElapsedSeconds` 里写的是 `Date.now() - startedAt`，字符串参与减法得 NaN，
再被 `Math.max(0, ...)` 和显示层的兜底吃掉，就成了永远的 0s。修：先转时间戳，并挡掉非有限值。

**② 看不到思考过程** —— 之前服务端只推推理**字数**（`{chars}`），界面只能显示「已推理 1124 字」。
改成推**增量文本**（不是累积全文：推理可能几万字，每段重发整段是 O(n²) 的流量），
前端累积（只留尾部 4000 字符）并用限高可滚动的块渲染，避免长推理把折叠区撑开。

**③ 清掉代码沙箱**（用户明确要求）—— 删 import / normalizeRun / serializeRun / `/runs` 两端点 /
`/sandbox` / 详情里的 sandbox 字段；删 services/vibecodingRunner.js 与 scripts/p17。
**数据库表 `vibecoding_runs` 保留**（历史数据，删表不可逆；代码不再读写它）。

**④ 文案不再暗示「只能做网页」** —— 用户原话：「这个对话又不是只能做网页，就是个类似豆包/ZCode
一样的智能体」。课程入口的「AI 把页面写出来」改成通用口径；开场建议里加了一条「帮我看看这段代码
哪里错了」（不产出文件，正好体现**产物是可选的**）。

**⑤ 历史数据回填（第 7 条「生成出来无法预览」）** —— 删约定那段时间 AI 写出来的内容只留在聊天记录里，
产物还是新建时的默认文件，所以「生成了但预览不动」。兜底命名上线后新消息不再有这问题，
这个脚本把**历史**补上：只处理「从未被 AI 写过」的会话（产物最高修订 <= 1），
重扫助手消息按新规则落成产物（幂等，只动 artifacts 表）。
生产实测：8 个候选里 4 个有可提取内容 → 各回填出一个 index.html；
三星堆那个会话的 index.html 从 382 字节的默认页变成 **10989 字节**的真实页面，**线上已确认能预览**。

**还没做的第 4 条（聊天上传图片/文件）**：不是「加个按钮」那么简单，查了两件事——
① 生产 TEXT 渠道的 `modelCapabilities` 是空的，也就是**当前模型不声明支持读图**，
上传图片 AI 也看不见；② 平台的文件素材是**带鉴权**的（预览 iframe 取不到），
所以「上传的图直接用在生成的页面里」也需要额外处理（内联成 data URL 或做占位符替换）。
两条路都可行，但都要先定方案，见对话。

---

#### 聊天框支持上传图片：两次实测推翻了我们自己的假设（release `20260911T095614Z`）

用户要求：聊天框能传图（跟豆包/ZCode 一样）、**外联公开**、支持从系统拖拽自动上传。

**第一版按「外联」做**（学生上传 → `PUBLIC_PLATFORM` → 把公网地址喂给模型），链路前半段实测全通：
学生登录 OK、上传 200、公网地址匿名可读 200。但**模型调用失败**。

于是做了 A/B 实测（同一内容块形状，只换图片地址）：

| 图片地址 | 结果 |
|---|---|
| `data:image/png;base64,…`（内联） | ✅ 模型答出「红色 蓝色」 |
| `https://iicili.cyou/api/public/…`（外链） | ❌ 上游 HTTP 报错 |

**结论：上游不会去抓我们的公网地址。** 所以"外联给模型看"这条路走不通——外链只对**浏览器**有效
（匿名可读，生成的页面里放图照旧能用）。最终做成**两半分开**：

- **给模型看的** → 内联 base64（客户端上传时顺手读成 data URL；浏览器手里就有文件）
- **给页面用的** → 公开外链（用户要的"外联公开"）

⚠️ 最要紧的一处防守：**没有内联的附件不进模型请求**。否则学生一配图，上游报错、界面显示
「AI 回复失败」——这是第一版上线后短暂存在的真实坏状态（宁可不带图，也不能让整轮对话失败）。
超过 1MB 的图在上传时就如实提示「AI 看不到它，但页面里可以用」。

另外两个实现细节：
- **附件必须发成内容块**（`content: [{type:'text'}, {type:'image_url',image_url:{url},role:'reference_image'}]`）：
  纯文本的话模型完全看不到图（实测：同样问题纯文本回「未看到图片」）。
- **不能靠渠道请求模板**放占位符：多轮历史会**整份替换**模板里的 `messages`，模板里的占位符会被一起替换掉。

新增 `scripts/p42-vibecoding-attachment-shape.mjs` 锁住形状（内容块 + 图片用 inline + 无 inline 的保持纯文本）。

**数据安全口径（用户明确要求"外联公开"）**：学生上传的图任何拿到 URL 的人都能访问。
两道闸：只收图片、一次最多 4 张 / 单张 4MB。

**当时记的「没验到的一环」**：完整 UI 链路（输入框选图/拖进去 → 发送 → 模型针对图回答），
理由写的是「要走真实课堂会话，本地过不了『课堂未开启』门禁」。**2026-09-11 复核：这个判断是错的**，见下节。

---

#### 图片上传的完整 UI 链路实测（2026-09-11，本地；**顺手修掉 3 个问题**）

**门禁其实绕得开**：`studentContext.js` 里 `HOME_PRACTICE` 的学员只要求课时是 VIBECODING、
**不要求课堂已开启**（`homePractice ? lessonMode === 'VIBECODING' : Boolean(activeSession) && …`），
而种子账号 `student-2` 的 `student_usage_scope` 正是 `HOME_PRACTICE`。
所以本地就能走通真实的学生对话链路，不必等老师开课、也不必碰生产。

**搭的现场**（都在 `.tmp/`，不入库）：临时库 + VIBECODING 课时 + 后端 18789 + 前端 6176（`apps/website/dist`，
按当前 HEAD 现构建），TEXT 渠道指向一个**记录型「视觉上游」**——它把收到请求里 messages 的内联图
**真的解码出来**、按左右半边报颜色。于是「模型答出左红右蓝」＝ 图片字节完整走通了
**浏览器 → 服务端 → 上游**，而不只是"请求里凑巧有个 url 字段"。请求体同时原样落到
`.tmp/vision-requests.jsonl`，可直接回看。

**验到的**（18 项全过 + 截图，`node .tmp/vision-ui.mjs`）：
- 点「上传图片」按钮选图 → 上传 → chip 出现且缩略图真的加载出来（`naturalWidth>0`）
- 把图从系统**拖进聊天区** → 出现「松手就上传」蒙层 → 上传 → chip 缩略图可见
- 发送 → 浏览器请求体里 `attachments[].inline` 是 `data:image/png;base64,…` → 上游收到内容块
  （`contentShapes: ["string","text+image_url",…]`）→ 模型答出左右两半的颜色
- 1.29MB 的图：照常上传、页面能用，但不内联给模型（`inline` 为空、**请求里最新那条用户消息是纯文本**），
  界面上如实提示「超过 1MB，AI 看不到它，但页面里可以用」
- 原生 file input 不可见；整页无 JS 报错、控制台无 error

**这一轮修掉的 3 个问题**：

1. **中文文件名落库变乱码**（`截图.png` → `æˆªå›¾.png`）。真实用户必中。
   根因：multipart 的头是按 latin1 读出来的（`body.subarray(...).toString('latin1')`），
   而浏览器发的中文文件名是 **UTF-8 字节**。修在 `parseDisposition`：按 UTF-8 还原一次
   （还原后出现替换字符说明本来就是 latin1，保持原值），并支持 RFC 5987 的 `filename*=UTF-8''`。
   新增 `scripts/p43-multipart-filename-encoding.mjs`。
2. **原生 file input 露在界面上**：`.c-file-input` 在 JSX 里用了，但**全仓没有一条 CSS 规则**，
   于是聊天区顶部躺着一行浏览器默认的「选择文件 / 未选择任何文件」。修：`chat.css` 里
   `.c-file-input { display: none; }`。**这条是开浏览器截图才看到的**（断言全过、可访问性树看不出来）。
3. **模拟渠道把带图消息显示成 `[object Object],[object Object]`**：「本地模拟」是本地开发的默认渠道，
   带图发一句话回复就是这串。修：`generationProvider.js` 的 mock 改从内容块里取 `type:'text'` 拼文本。
   顺手在 `p42` 里加了断言。

两个新守卫都按规矩自证过：**把修复撤掉，守卫必须报错**（p42 报 `[object Object]`、p43 报乱码），再还原即恢复全绿。

**留一个成本观察**：内联图会**进历史并逐轮重发**（实测第 3 轮请求里仍带着前两轮的图）。
单张上限 1MB 的 base64 约 130 万字符，4 张叠起来每轮都要重发一遍 —— 上下文和费用会随轮次线性涨。
本轮没动这个口径（属既有设计），但值得在做"限流/成本"时一起看。

---

#### 学生端复查三条：附件放开 / 隐藏控件 / 默认模型（2026-09-11，**本地完成，尚未部署**）

用户在线上做了一轮学生端复查，提了三条。逐条核过后两条是真问题、一条是我上一轮已修但**还没发布**：

1. **「只能上传图片」→ 改成「上传附件」**。原来按钮是图片图标、`accept="image/*"`、
   上传函数还 filter 掉非图片并提示「目前只支持图片」，服务端 `resolveAttachments` 也有一道
   `VIBECODING_ATTACHMENT_NOT_IMAGE`。现在放开成**平台白名单里的全部类型**
   （图片/音视频/pdf/office/txt/csv/zip），口径按用户确认：
   - **可见性沿用「公开外联」**（与图片一致：任何拿到 URL 的人都能访问）；
   - **只有图片能被模型看见**（上游只认 `image_url`，PDF/视频没有内联这条路）；
   - 其余附件**如实告诉模型「你看不到这些文件」**——见下面第 3 点，这是这次最要紧的一处。
2. **图2 那条「选择文件 / 未选择任何文件」入口要删掉** —— 就是我上一轮修掉的
   `.c-file-input` 缺 CSS（已加 `display:none`）。**用户看到它是因为线上还没发这一版**，
   代码本身在 `70ba445` 里已经好了。
3. **图3 模型下拉进来要自动选中后台的默认模型**。原来会话的 `model` 存 null、下拉值也是空，
   显示成占位文案「渠道默认模型」，学生看不出实际在用哪个。现在：
   - 服务端会话详情/创建响应多给一个 `defaultModel`（= TEXT 渠道的 `model`，即后台配的默认）；
   - 前端 `value={data.model || data.defaultModel}`，并在那个选项后标「（默认）」，空占位项去掉。
   - **存储仍然留空**（会话 `model` 为 null = 跟随渠道默认），这样后台改了默认，
     没自己选过模型的老会话会跟着走；显示成默认只是把"跟随中"这件事说清楚。

**这一轮改出来的最要紧的一条：附件进不了请求时，要如实告诉模型。**
原来"没有 inline 就不放进请求"只保证了整轮不失败，但学生传一篇 PDF 问「帮我看看」，
模型会当作没这回事、直接编一段内容出来。现在 `conversationHistory` 对**每个进不了请求的附件**
补一个文字块：`［平台提示］这条消息还附了 N 个文件：xxx.pdf、yyy.docx。你读不到它们的内容……`。
顺带把上一轮的洞也补上了：**超过 1MB 被丢掉的那张图，现在模型也会被告知**（以前是静默丢弃）。

**验到的**（`node .tmp/vision-ui.mjs`，26 项全过 + 截图）：
上传口是「附件」、accept 覆盖文档与音视频且不含 `.exe`、下拉以 `deepseek-flash（默认）` 选中、
PDF 的 chip 用文件图标（不是破图）、气泡里是「文件卡」、**匿名下载拿到的字节与上传的完全一致**
（"公开外联"的实际含义）、上游收到的最后一条消息是 `text+text`（只有文字块、没有 image_url）、
平台提示里点名了这份 PDF。
⚠️ 拍照也发现一处**测试脚本自己的坑**：`setInputFiles` 用的是**磁盘上的文件名**，
想覆盖中文名就得把文件按中文名落盘（我第一次按 `vision-note.pdf` 落盘，断言自然对不上）。

**新增 `scripts/p44-attachment-accept-parity.mjs`**：前端 `ATTACHMENT_ACCEPT` 不能超出服务端
`allowedUploadExtensions()`（新导出的访问口），图片类型必须在、危险类型必须不在、
大小口径（图片 4MB / 其他跟随平台上限）也要对得上。**自证过**：往 accept 里塞一个 `.exe` 守卫即报错。
`scripts/p42` 同步更新：非内联附件的期望从「保持纯文本」改成「内容块 + 平台提示 + 无 image_url」。

**已发布**（2026-09-11 10:30 UTC，release `20260911T103014Z` / commit `08d0961`）：
切换前备份 `20260911T103009Z`；发布后服务 active / NRestarts=0 / ExecMainStatus=0、`/health` ok、
`/` `/admin/` `/org/` 全 200。发布产物逐项核过：CSS 里 `c-file-input{display:none}` 在、
前端带「上传附件」文案与 `.pptx` 等 accept 项、服务端 `decodeHeaderText` / `平台提示` / `defaultModel`
都在 release 里。**并用生产实际下发的产物**（scp 回来本地起服务，因为本机出口封锁 HTTPS）
把 26 项附件链路实测重跑了一遍，全过；官网首页学生会话 + 未登录各开一次，无 pageerror。

---

#### 输入框支持粘贴上传（2026-09-11，已发布 release `20260911T104909Z` / `8734138`）

学生复查的补充一条：「外部复制一张图片，在输入框里粘贴应该自动上传」。

实现：`Composer` 的 textarea 加 `onPaste` —— 剪贴板里有文件就 `preventDefault`（**不挡的话浏览器会把图
当富文本塞进 textarea，或者干脆什么都不发生**）并交给 `onPasteFiles`，工作区接的是同一个 `uploadFiles`，
所以粘贴和拖拽走的是完全同一条路（上传 → 公开外联 → 图片内联给模型）。按钮 title 也补了「或直接粘贴」。

实测做法：在页内真派发 `ClipboardEvent('paste')` 并挂上 `DataTransfer`（截图见 `.tmp/vision-ui.png`），
断言事件被接住、textarea 没被塞进文字、chip 与缩略图正常、发出去后「模型」确实看见了那张图。
生产产物 29 项全过。

⚠️ **这次发布差点发错版本，记一笔**：提交完**忘了 push** 就去服务器 `git fetch`，
服务器 checkout 停在上一个提交，于是第一次构建出来的 release **不含这次改动**。
是靠「构建后核对 `BUILD-METADATA.txt` 里的 commit 和 release 目录里的产物」才发现的。
→ **以后固定动作：push 完在服务器上 `git rev-parse HEAD` 核对一次，切软链前再核一次 BUILD-METADATA。**
（这也说明「切了软链、服务是 active」完全不代表发布内容是对的 —— 又一个静默失败的样子。）

---

#### 学生能直接产出 PPT / Word / Excel（2026-09-11，已发布 release `20260911T110724Z` / `0ea03b8`）

起因：用户问「像豆包那样，说一句就能出一张插画图、出一份 PPT，它应该是调用了很多能力吧，
你看下这个源码有这些能力吗、我们能做吗」——并把 OpenSquilla 的源码包发了过来。
调查结论：**那不是某个模型自带的本事，是 Agent 在调工具**；OpenSquilla 的生图是
`nano-banana-pro` 这个技能在调 OpenRouter 的图片模型，PPT 则是「模型写 python-pptx 代码 →
在沙箱里跑」；`file_authoring.py` 里还有一条**不需要沙箱**的等价路径（收结构化 slides、进程内渲染），
我们要的就是后者。用户随即拍板：「服务器后续我会扩的，现在不管是生成 docx、xlsx、ppt 等，全部跑通。」

**做出来的东西**：学生在对话里要一份文档，模型按平台规格给**内容**，服务端当场渲染成真正的
`.pptx` / `.docx` / `.xlsx`，作为产物卡片可下载。规格与语言标签：

```text
```pptx 去新疆旅游.pptx   → JSON 提纲 {"title","subtitle","author","slides":[{"title","bullets"}]}
```docx 我的暑假计划.docx → Markdown 子集（# 标题 / - 列表 / 1. 有序 / | 表格 | / **粗体**）
```xlsx 暑假花销.xlsx     → CSV（首行表头）
```

**两个关键取舍**（都是被这个仓库的现状逼出来的，值得记住）：

1. **产物里存「规格文本」，下载的那一刻才渲染成文件。** 产物表是一列 TEXT（256KB 上限、要参与版本比对
   与「源码」视图），而 Office 文件是 zip —— base64 塞进去又大又不可读。存规格反而是**天然的**：
   学生能在工作台看懂自己那份文档是怎么写的，版本/配额沿用现有口径，渲染器升级后老产物重新下载即可受益。
   代价是每次下载渲染一次（几毫秒）。
2. **自己写 ZIP + OOXML，一个第三方库都不引。** 服务端目前**零 npm 依赖**，release 也只拷
   `apps/server/src` + `packages/database/src`（`build-production.sh` 不在 release 里装 node_modules）。
   引 `pptxgenjs` / `docx` 这类库会打破整套发布方式、还要服务器联网装包。所以用 `node:zlib` 手写了
   ZIP 写入器与三个渲染器（`apps/server/src/services/ooxml/`），确定性输出、可复现。

**守住它的两条守卫，互补、缺一不可**：

- `scripts/p45-office-documents.mjs`（**纯 Node，不依赖 python**）：ZIP 中央目录/CRC/偏移自洽、
  UTF-8 名字标志位、自带一个极小的 XML 良构检查器、`[Content_Types].xml` 必须存在且是第一个条目、
  每个部件都被它覆盖、每条 rels 目标都能解析到真实存在的部件。
- `scripts/p46-office-independent-reader.mjs`（**python 独立读回**）：python-pptx / python-docx / openpyxl
  三个完全独立的库读回文字、表格、数字类型、冻结窗格。
  缺依赖时**显式失败而不是静默跳过**——跳过等于没有守卫（这个项目已经吃过三次
  「界面不报错、日志不报错」的亏）。装法：`python -m pip install python-pptx python-docx openpyxl`。

两条都按规矩自证过：**把 `a:pPr` 的闭合 `>` 去掉**，p45 报「标签不匹配」、p46 报 lxml 语法错。
而且写守卫的过程本身就抓出两个真 bug：`paragraph()` 拼串缺了闭合 `>`（**整份 PPT 打不开**）、
`w:pPr` 子元素顺序违反 schema（Word 能开、别的实现未必）。

**端到端实测 20 项全过**（`.tmp/vision-doc.mjs`）：真开浏览器要一份 PPT / Excel / Word →
产物卡片出现 → 点下载 → 拿到的字节交给**独立实现读回**校验内容。
发布后又用**生产实际下发的产物**重跑了一遍，全过。

**顺带改的**：
- 产物文件名放开中日韩文字 —— 原来 `FILE_NAME_PATTERN` 只允许 ASCII，模型写的「去新疆旅游.pptx」
  会被静默丢掉、退回默认名「演示文稿.pptx」（服务端与 `packages/shared/src/vibecodingProject.js`
  两份规则要一起改）。仍拒绝路径分隔符与空白，穿越用例已覆盖。
- 空会话的推荐问题换成「做一份去新疆旅游的 PPT」「做一张记录心情的 Excel 表格」。
- 只产出文档时预览区给出说明（「这是 PPT/Word/Excel，点下载用 Office 打开」），不再对着空白发呆。

**当时记的「还没做的」是文档配图** —— 见下一条，已经做了。

---

#### PPT 配图：用学生自己传的那张图（2026-09-11，已发布 release `20260911T112517Z` / `b4f4566`）

学生在对话里传了图、又让做 PPT 时，「模型」现在会把图用上：那一页写
`{"title":"赛里木湖","bullets":[…],"image":{"attachment":1}}`，`attachment` 是**这条消息里第几张图**
（非图片附件不占号）。服务端在下载时把序号翻成真实字节，内嵌进 pptx。
只有图没有要点的那一页会占满正文区（配图页）；标题页不配图。

设计上守住的几条：
- **序号不是猜的**：带图的用户消息会多一条平台提示「附了 N 张图片，编号为 1、2…」——
  模型看不到我们给它编的号，不说清楚它就引用不对。
- **引用越界 / 素材被删 / 格式不是图片** → 只是**那一页不放图**，不让整份 PPT 下载失败。
- **按「产出那一轮」找图**，不是「最近一轮」：模型写「第 1 张」时指的是它当时看到的图，
  学生之后再聊几轮也不会串味（靠产物上的 `message_id` 回溯）。

**这一轮踩到三个「一句错都不报」的坑，值得记住**：

1. **图片形状的关系 id 写成了 `rId1`，而 `rId1` 被 slideLayout 占着，图片其实是 `rId2`。**
   于是图片形状指向版式 —— python-pptx 报 `image unreadable`、PowerPoint 会说文件需要修复。
   **p46 抓到了**（当初做「独立实现读回」就是为了这一刻）。
2. **`getArtifact()` 返回的是驼峰 `messageId`，我按数据库列名 `message_id` 去读** → 永远 undefined
   → 「这一轮有几张图」永远是 0 → PPT 里就是没图。
3. **`parseAttachments` 只带出 `id/name/url/inline`，把 `mime` 丢了** → 按 mime 筛图片永远筛不到。
   这是第 2 条的另一面：**字段在库里、在读取路径上丢了**（本项目第四次踩同一类）。

**新增 `scripts/p47-document-image-embedding.mjs`** 专门盯这条链路的三处断点
（产物 messageId / 附件 mime / 磁盘素材），不需要 python、不起服务。两条真 bug 都自证过：
**撤掉任一修复，p47 立刻报 `size=0`**。

`p42` 同步更新：带图消息的内容块不再固定「第 1 块文字、第 2 块图片」（中间多了编号提示块），
改为按类型找图片块，并新增「会告知图片编号」的断言。

端到端 23 项全过（`.tmp/vision-doc.mjs`）：学生真传一张图 → 要带图的 PPT → 点下载 →
用 python-pptx 把内嵌图**按 SHA256 跟原图比对**，确认就是同一张。发布后又用生产产物重跑了一遍。

**再往后一步（还没做）**：让 AI **生成**每页插画。我们已经有生图渠道，但渲染发生在下载那一刻，
而生成图既慢又花钱、还不能幂等 —— 所以它得挪到「助手这一轮结束时」去做，并接上配额与异步。
那是一个独立的产品决策（要不要为每页图扣积分），不是纯技术问题。

---

#### 工作台行为修正：默认收起 / 文档先预览再下载 / 那个看不见的关闭键（2026-09-11，已发布 `20260911T113915Z` / `c338258`）

用户复查提的三条，都落在右侧工作台上：

1. **「关闭键根本看不到」** —— 根因是 `IconButton icon="close"`，而**控制台图标集里只有 `x`**，
   `ConsoleIcon` 对未知名字直接 `return null` —— 于是那个按钮是个**看不见的空圈**（不报错、不白屏）。
   浏览器里实测 `hasSvg=false` 才坐实。改 `icon="x"`，并新增
   `scripts/p48-icon-name-guard.mjs`：静态扫出所有 `<ConsoleIcon name=…>` / `<IconButton icon=…>` /
   `<Icon name=…>` 的**字符串字面量**，按「组件名 + 引入路径」判断该查画布那套还是控制台那套图标集
   （两套互不相通，混用是最容易犯的错）。**自证过**：改回 `close` 立刻报错并列出可用名。
2. **「工作台默认一直开着」** —— 原默认值就是 `innerWidth > 720`（桌面端必开），右边长期杵着一块
   与当前任务无关的预览（还显示着种子产物 index.html）。现在**默认收起**；这一轮**第一次产出产物**
   时自动弹出并直接看向那个产物；窄屏不自动弹（会盖住对话）；学生手动关掉后同一轮不再顶开。
3. **「PPT 出来了应该先预览、再下载」** —— 原来预览区只认 HTML 产物，文档一律落回
   「没有可预览的页面」，而产物卡片对文档是「点主体直接下载」。现在：
   - 新增 `console/DocumentPreview.jsx`：PPT 渲染成 16:9 幻灯片卡片（含学生传的图）、
     Word 渲染成一张纸、Excel 渲染成表格。**在客户端渲染**——产物里存的就是规格文本，
     前端手上已有全部素材，不必多一次请求，也就不会出现「预览与下载两份实现」的漂移；
     真正的文件仍由服务端在下载那一刻渲染，所以预览是**近似**（界面上也这么写）。
   - 预览区优先看「学生点名 / 最近产出的那个」；是文档就渲染文档预览，否则回到网页预览。
   - 预览工具栏给「下载」按钮（文件名 + 下载），就是用户要的顺序。
   - 产物卡片对文档改成「预览」+「下载」两个动作，主体不再直接下载。

**实测 12 项全过**（`.tmp/workbench-ui.mjs` + 截图）：进来时收起 → 要 PPT 时自动弹出并预览到
**这份 PPT**（不再是 index.html）→ 关闭键有图标、可命中、可点 → 传图后预览里能看见那张图 →
手动关闭能收起。发布后又用生产产物跑了同一套。

⚠️ 一处自省：这条提交信息里我把实测项数写成了 14，**实际是 12**（分类相加数错了）。文档以 12 为准。

---

#### PPT 会去「收集素材」了：按规格生成插画（2026-09-11，已发布 `20260911T120321Z` / `a037229`）

用户拿豆包的成品来对比：豆包出 PPT 时会先「正在收集 PPT 素材」——规划风格、**生图 3 张**、再合成；
我们的几秒就出，而且是一页页干巴巴的要点。「是不是哪里不对」。

**核对结论：不是 bug，是能力缺口。** 豆包的流程是「规划 → 收集素材（生图/搜图）→ 合成」，
我们过去只做最后一步的排版，中间**没有素材环节** —— 所以快而单薄。这条把「生图」补上。

规格上多一种配图写法（与引用学生自己的图并存）：

```text
{"title":"赛里木湖","bullets":["湖水蓝得像宝石"],
 "image":{"prompt":"新疆赛里木湖的夏天，写实插画风格，蓝天、雪山倒影、湖边草地，横构图"}}
```

系统提示里明确：**全篇最多 3 张**、用在封面与最有画面感的页、不要每页都配；拿不准风格时不配反而更好。

**流程**：助手这一轮消息落库之后（产物已认领到这条消息、能回溯「产出那一轮」）→ 门禁 →
逐张调 IMAGE 渠道 → 存成**公开素材**（复用学生上传那套校验与下载地址）→ 记在产物的
`generated_images` 列 → 再把产物推一次（前端按 id 覆盖，预览立刻换上新版本）。
生成期间推 `status`（`phase=image`），前端显示「正在配图 第 1/3 张」——
对应豆包那句「正在收集 PPT 素材」，学生知道在干嘛而不是干等。

**三条刻意守住的规矩**：

- **门禁与画布生图完全同一套**（课时能力 / 课堂开关 / 平台模态开关）。为此把
  `assertGenerationPreflight` **导出复用**，而不是复制一份 —— 复制迟早漏检查，那就等于给
  文档产物开了绕过能力开关的后门。门禁不过就一张都不生成，且如实告诉学生，不报错、不打断对话。
- **单张失败只影响那一页**，其余照常；整份 PPT 永远能下载。
- **产物换版时清空旧插画**（content 变 → `revision+1` 且 `generated_images=NULL`），
  否则新 deck 的第 2 页会配上上一版的图，**而且不报错**。

**这一轮踩到三个「静默失败」，全在这条链路上**：

1. **调用时漏传了一个实参**（定义 4 个参数、调用只给 3 个）→ `artifactIds` 是 undefined →
   函数第一行就 return，整个功能静默失效。**是一行日志都没有才定位到的。**
2. **`fresh` 在 `transaction` 回调里声明，我在回调外引用** → 求值实参即 `ReferenceError`，
   被这一层 catch 吞掉，前端只表现为「没有插画」。
3. **平台的生图路径是 `/v1/image/generations`（单数）**，我按直觉给假上游写成了 `images` →
   404 被当成生成失败（这个反而**被正确记录**在产物的 `generated_images` 里，说明失败路径是好的）。

**守卫与实测**：

- `scripts/p49-illustration-planning.mjs`（新）：插画**规划**的口径 —— 只认 pptx、只认写了 prompt 的页、
  每份最多 3 张、提示词截断到 300 字。**把 3 改成 5 会立刻报「想改先想清楚账单」**（自证过）。
- `p45`/`p46` 覆盖「按幻灯片下标给生成图」以及「生成失败时仍能出文件」。
- 端到端 10 项全过（`.tmp/vision-illustration.mjs`）：真的要一份带插画的 PPT → 上游确实收到生图请求
  （带模型写的提示词、不超过 3 次）→ 预览里图能加载 → 下载的 pptx 里**内嵌图与这次生成的图
  SHA256 完全一致**。发布后又用生产产物跑了同一套。

**还没做**：豆包还会**搜真实照片**（它那份计划里写了「搜图关键词：喀纳斯、赛里木湖…大盘鸡、烤包子」）。
我们**没有搜索能力**，而且面向儿童的平台去搜开放网络的图片有内容安全风险 —— 要么接一个带
安全过滤的图库/搜索 API，要么明确不做。这是产品决策，不是技术问题。

---

#### 一条验证教训（比这两个 bug 更值钱）

**第一轮我验证课程入口时只看了 DOM 结构（"入口正常"），没有截图、也没有查计算样式**，
所以那条白横带从我眼皮底下过去了。同一个页面在本地渲染出来是坏的，我却以为它是好的。
→ **整页型界面（课程入口、公开作品页、点评回放）必须截图看，不能只看可访问性树。**
工作台那种三栏界面结构复杂，反而会让人认真看；越"简单"的页面越容易只查结构就放过。

---

**仍然存在的既有问题（与本次无关）**：`systemctl` 的 `NeedDaemonReload=yes` 漂移未处理（第五节待办 1）。

---

#### 这一轮里删掉的东西

- `scripts/p24-vibecoding-project-io.mjs` —— 测的是「工程导入导出」，该功能随「学生不能手写代码」一起移除。
- `vibecodingProject.js` 的 5 处导出（PROJECT_FORMAT / PROJECT_LIMITS / projectFileBase /
  buildProjectBundle / parseProjectBundle）——除 p24 外零引用。保留 FILE_NAME_PATTERN 与 parseFenceInfo
  （markdown.jsx 在用）与 buildPreviewDocument（工作区预览 + 官网作品页共用）。
- `packages/shared/src/styles.css` 里 30 行 `vb-*` 死样式——旧界面整体替换后全仓零引用。
- `apps/website/src/styles.css` 的 `.work-detail__play`。

---

## 四、关键设计约定（必须遵守）

```text
1. 学生功能在 apps/website，没有独立 apps/student（已删除，不恢复）
   学生登录 → 回 / → 右上角头像 → 进入学习/我的作品/我的积分/我的课程/学习统计
2. 课堂画布是"受控"的：学生不能右键建节点
   节点来源 = 左侧素材面板的「生成框体」+「本节课画布素材」
3. **课包「发布 ≠ 授权给机构」**（2026-09-11 第十六轮定稿，用户原话：「发布的时候所有机构都是
   不可见的情况，发布的时候只是会出现在首页课程广场里供大家看，然后再是授权给机构，才会出现在
   机构后台」）
   - 发布（PUBLISHED）＝ 上架官网课程广场给人看，**不对任何机构生效**；
   - 机构后台能看到/使用平台课包，唯一途径是「机构授权」里有一条 ACTIVE 且未过期的记录；
   - 平台课包本身没有有效期；有效期挂在「课包 → 机构」的授权上（`course_assignments.expires_at`，
     NULL＝永久），到期或撤销后该机构**立即**看不到（不需要定时任务，判定就在 SQL 里）；
   - 「可见范围 visibility」现在只表示**上不上课程广场**：`ALL_ORGS`＝上架（公开可见）、
     `ASSIGNED_ORGS`＝不上架（只给已授权机构）、`PRIVATE`＝不上架、不对外；
   - 唯一定义在 `lib.js: orgSeriesAccessSql()`，8 处查询共用（机构端列表/详情、平台端
     accessibleSeries/accessibleLesson、学生端 studentContext、教学素材、教学任务），
     别再往任何一处抄一份带 `visibility='ALL_ORGS'` 的旧条件；
   - 学生端同样受这条约束（课件来自班级课表，但 SQL 里也要求该机构对此课包有生效授权）。
4. 生成参数（比例/清晰度/时长/是否生成音频）来自「模型能力」配置，代码里不写死
   - 配置位置：计费与模型 → 渠道 → 模型能力（channel.modelCapabilities）
   - 请求体由模板生成：channel.requestTemplates，占位符见下
   - 生成时以「课时里该框体的配置」为准，客户端提交的取值一律不采信
   - 调用上游前先做能力/套餐预检，业务拦截不进入重试
5. 课程广场 = 所有「平台自有 + 已发布 + 可见范围＝上架（visibility='ALL_ORGS'）」的课包自动收录，
   分画布课程 / VibeCoding 课程两类，没有人工上架/审核流程。
   列表与详情必须同一套条件（`routes/communication/public.js`）——早先详情多要一个
   `marketplace_status='APPROVED'`（全站没有任何入口能把它置成 APPROVED），广场里点开必然 404，已修。
   可见范围选「不上架」的课包不进广场，但授权给机构后机构照常能用（上架与授权是两件事）。
   公开课包接口 `/api/public/course-series`（无前端调用）同步收敛到同一口径，不再外泄 ASSIGNED_ORGS 课包
6. 作品链路：学生提交 → 平台在「平台作品库」点「发布到作品广场」→ 官网 /works 可见
   机构端只做点评与机构精选，不再有审核/发布权
7. 教学素材 = 教师备课资料：学生端与公开接口不下发 teachingGroups；
   文件走机构端受控下载路由，公开下载路由拒绝该分类
8. 课堂类型 delivery_mode：课包级默认（course_series.delivery_mode），课时继承、可单独改
   VibeCoding 已预留但运行时未实现（发布会被拦）
9. AI 渠道：一个渠道可配多个模型；课时可从对应模态渠道的模型下拉单选
   生成时课时选的模型优先于渠道默认模型（此前该优先级因键名大小写 bug 失效，已修）
10. 视频模型的「输入画面」是**多选能力**（2026-09-10 定稿）：INPUT_MODES = TEXT / FIRST_FRAME / FIRST_LAST_FRAME / OMNI_REFERENCE
    - 四类：文生视频 / 图生视频（首帧）/ 首尾帧参考（首帧+尾帧）/ 全能参考（多图·多视频·多音频）
      一个模型可以同时勾多种（MiniMax-H3 四类都支持）；不勾＝按模型名推断（带 -i2v → 只图生）
    - 旧声明 `inputFrame` 仍可读：NONE→TEXT、FIRST→FIRST_FRAME；LAST/LAST_FRAME→FIRST_LAST_FRAME
    - 生成时按「这次真带了哪些画面」决定发什么，并用该模型的模板决定摆放位置：
      不带图且支持文生 → 渠道/模型模板原样；带首帧 → `{{firstFrameUrl}}`；
      带首尾帧 → `{{lastFrameUrl}}`；全能参考 → `{{referenceItems}}`（图片按 reference_image 展开，最多 9 张）
    - 拦截：全能参考与首/尾帧**互斥**（上游不允许混用，报 GENERATION_MIXED_INPUT_MODES）；
      给模型没声明的输入方式报 FIRST_FRAME_UNSUPPORTED / LAST_FRAME_UNSUPPORTED / REFERENCES_UNSUPPORTED；
      只给尾帧报 LAST_FRAME_WITHOUT_FIRST；只有「不支持文生」的模型才强制首帧
    - 画面只认本项目图片素材或该框体的预置素材；generation_jobs 记 source_asset_url /
      last_frame_asset_url / reference_asset_urls，重试与队列恢复都会复用
11. 请求模板可以**按模型配**（`channel.modelRequestTemplates[modelId]`，优先于渠道级 requestTemplates）：
    同一个渠道里的模型请求体可能完全不同——hailuo 要顶层 image，MiniMax-H3 V2 要 content[] 数组。
    占位符见第四节末尾；整串 `{{durationSecondsNumber}}` 给数字（老 `{{durationSeconds}}` 保持字符串，
    不改动线上请求形态），`{{frameItems}}` / `{{referenceItems}}` 会展开成 content 项并摊平进数组。
12. 上游路径可按渠道配：`channel.requestPaths` / `channel.pollPaths`（按模态）。
    MiniMax-H3 实测：提交 `POST /v2/video_generation`、查询 `GET /v2/query/video_generation/{id}`，
    与默认的 `/v1/videos`（要 prompt 字段）不是同一套；查询路径支持 `{id}` 占位符。
    另外 MiniMax V2 的提交响应只有 `{task_id}`（无 status）、结果包在 `task` 里
    （`{task:{status,content:{url}}}`），适配器的状态判断与素材解析都会往下取一层。

11. VibeCoding 课堂：delivery_mode 以「教师开的课堂」为准，与画布课堂互斥
    - 学生入口：/learn/vibecoding（课时列表）→ /learn/vibecoding/:conversationId（工作区）
    - 每轮 AI 回复扣 1 积分（type=AI_VIBECODING_CHAT）；提交后会话锁定，驳回后自动放开
    - 代码运行两条路：浏览器 iframe 预览（始终可用）+ 服务端沙箱（生产未启用，见第三节）
    - 发布校验：VibeCoding 课时必须开放「AI 文字」能力，否则 VIBECODING_TEXT_CAPABILITY_REQUIRED
    - 工程导入导出：JSON 工程包（format=ai-magic-academy/vibecoding-project），纯函数在
      packages/shared/src/vibecodingProject.js；导入先按服务端同样的限制（12 个文件 / 单文件 64KB /
      总量 256KB）本地校验，只写入编辑器，需点「保存代码」才落库
    - 作品进作品广场：学生提交必须带 copyrightConfirmed（与画布作品一致）→ 老师点评 APPROVED →
      平台「平台作品库 → VibeCoding 作品」发布（生成 vbt_ 分享码）→ 官网 /works 与画布作品合并展示，
      详情页用 packages/shared 的 buildPreviewDocument 在 sandbox iframe 里直接运行（能玩）
    - 公开接口只返回 is_public=1 且 status='APPROVED' 且已确认授权的作品；下架/重新提交即自动隐藏
    - 对话上下文：每轮都在 history 前注入一条 system（课时标题/简介/正文 + 儿童安全约束 +
      「```语言 文件名」代码块约定）。多轮 history 会覆盖渠道模板的 system 提示词，所以必须自己拼。
    - 对话操控：停止生成（客户端 abort → 服务端同步中止上游，不扣费、不落消息）、重新生成、
      编辑并重发、删除单条消息、清空记录；推理型模型的 reasoning 增量转发成 thinking 事件，
      界面显示「已推理 N 字」；等待期间每 15s 一次 SSE 注释保活（防 nginx 读超时）
    - 工作区：AI 回复里带文件名的代码块可一键写入文件（覆盖前确认）；上传文本类文件到工程；
      侧栏搜索 + 置顶（置顶/最近分组）；每会话选模型（PUT 会话 model，取值必须在渠道可选模型内）；
      导出对话为 Markdown
12. 生成框体：**框体就是素材**（2026-09-09 定稿，`course_lesson_materials.material_type='GENERATION_BOX'`）
    - 框体不再是 classroom_config 里的独立数组，而是素材组里的一条素材：参数存 snapshot.box
      （modality/model/aspectRatio/resolution/durationSeconds/audio），预填提示词存 snapshot.content，
      预置素材用素材的 asset_url 列
    - 顺序 = 素材顺序：一个素材组里可以「图片素材 → 框体 → 文字素材 → 框体」逐条交叉，
      学生端素材面板按这个顺序逐条点击，画布节点也按点击顺序排布
    - 每个框体单独选模型与参数；平台端独占配置（机构/老师不参与）
    - 一个框体只生成一次：generation_jobs.box_id 存**素材 id**；重复生成拦成 GENERATION_BOX_USED，
      本课配了该模态框体就必须带 boxId（GENERATION_BOX_REQUIRED），框体不存在/类型不符分别报
      GENERATION_BOX_NOT_FOUND / GENERATION_BOX_MODALITY_MISMATCH；失败可重试，失败不占额度
    - 素材/素材组 id 保存后保持不变（replaceLessonCanvasConfig 保留已有 id）：学生画布节点、
      generation_jobs.box_id 都按 id 指回来，教师改课时不会打断学生已加的框体
    - 保存时逐框体严格校验（比例格式/是否在模型声明范围、清晰度、时长 1–600 整数），非法值当场 400
      INVALID_GENERATION_CONFIG；发布校验要求框体模态落在本课开放能力内（GENERATION_BOX_CAPABILITY_MISMATCH）
    - 画布上提示词为空时「生成」按钮是禁用的（此前会发一次空 prompt 请求被 400）
13. 平台管理员 MFA：TOTP（RFC 6238，自实现 HMAC-SHA1）+ 一次性恢复码，只对 SUPER_ADMIN 生效、自愿开启
    - 表 user_mfa_credentials：secret 存 base32 明文；恢复码只存 sha256(pepper) 哈希；last_totp_counter 防重放
    - 登录：密码通过后若已开启 MFA，缺码返回 401 MFA_REQUIRED（前端切到验证码步骤），错码计入登录限速
    - 动态码 ±1 个 30 秒窗口，且必须大于上次用过的时间步（同一枚码不能重放）
    - 关闭 / 重发恢复码要求「登录密码 + 有效动态码或恢复码」；恢复码只在生成时明文返回一次
    - 不引第三方库、不做二维码：绑定页给 base32 手动密钥 + otpauth:// 文本
14. 官网内容（website_contents）：key 白名单与「内置默认内容」在 apps/server/src/services/websiteContentDefaults.js
    - 公开接口 /api/public/website-content/:key 只读已发布版本；该 key 有内置默认但库里没有行时回落到默认内容
    - 管理端列表只显示「有数据库行 或 有内置默认」的区块，避免出现改了却对官网无效的条目
      （ORG / HANDBOOK / COMPARE 仍是静态页，因此不给默认内容、不出现在列表里）
    - 官网 useWebsiteContent 返回的是 content 本身（接口的 {key,content,version,status} 包装体在 hook 里解包）；
      官网 bundle 另有一份离线兜底（断网/接口异常时用），改默认内容时两处要同步
    - 目前真正由 CMS 驱动的页面：/org 的常见问题（FAQ）；首页 HOME 仍走硬编码布局（/courses 已整页删除）
15. 登录会话策略（2026-09-10 起）：**同一账号允许多设备同时在线**
    - 登录不再顶掉既有会话；只在活跃会话超过 `MAX_ACTIVE_SESSIONS`（auth.js，默认 10）时把最老的顶下线
    - 被顶/被撤销的会话在 lib.js 的 resolveAuth 里报 `SESSION_SUPERSEDED`（文案「当前账号已在其他设备登录」）
    - 以下动作仍然会批量踢会话，属于有意保留的安全语义：改密码（自己改＝全部设备都要重登）、
      平台/机构停用或删除账号、改角色/权限、重置密码、撤销选课
    - 单设备撤销入口：学生端 `PUT /api/student/account/sessions/:id/revoke`（要当前密码）；
      机构端/平台端「账号安全」里有登录设备列表 + 退出其他设备；学生官网目前只做接口、没有设备列表页
    - 回归网：`scripts/p37-multi-device-sessions.mjs`（两台设备都可用、上限、退出只注销自己、
      撤销单台、改密码踢全部）
```

16. 课堂画布皮肤（2026-09-10）：视觉与布局参考 **ASUI Canvas Pro 2.2.0**（MIT，本地参考包解压在 E:/tmp/asui）
    - 设计语言：近黑底 #101012 / 面板 #151515 / 卡片 #202027 / 边框 #2A292E~#393842 /
      主色酸柠檬绿 #a3fe44 / 圆角 10px 档 / 过渡 200ms ease-out / 玻璃拟态（backdrop-blur）
    - 落点：画布本体在 packages/canvas/src/styles.css（:root 里的 --cv-* 变量）；
      工作台外壳在 packages/shared/src/styles.css 末尾的 cv-* 段
    - **类名统一 cv- 前缀**；旧的 student-canvas-* / student-tool-* 规则已在 2026-09-10 清理掉
      （共 123 条 / 13KB，仍被 VibeCoding 用的 ghost-canvas-button / primary-canvas-button /
      student-canvas-toast 保持原样，清理脚本逻辑见提交信息）
    - 注意：官网营销页有全局 `nav{flex:1}`、`header{height:76px}` 这类裸元素规则，
      画布侧栏因此不用 nav 元素（用 .cv-nav 的 div），新加壳层时避开裸元素选择器
    - 学生端取消「保存并退出/退出课堂」：右上只有「课程大厅 + 提交作品」，提交成功即回课程大厅；
      手工「保存为版本」移到了侧栏「版本」面板里（原来的入口随按钮一起没了）
    - 字体：画布用 **Geist**（数字/拉丁）+ **思源黑体 Noto Sans SC**（中文），两款都是 SIL OFL 1.1 可商用，
      文件与许可声明都在 apps/website/public/fonts 下（思源黑体按 unicode-range 分 101 片，浏览器只下用到的分片，
      整包约 2.7MB）。只有官网产物带字体文件，admin/org 自动回落系统字体
    - **展示与输入分离**（2026-09-10 定稿，按参考）：卡片只负责展示——可改名标题 + 画面/点阵占位；
      输入都在**贴在框体下方的独立浮动面板**里（第六轮起跟随选中框体，见下条）：提示词 + 画面来源行 +
      画幅/清晰度/时长分段胶囊 + 页脚（状态胶囊 + 配置胶囊「✦ 生图 · 9:16 · 1k」+ ＋ 打开素材面板 + 生成 ↑）。
      面板编辑「当前选中的框体」，取消选中后沿用上一次的（不会突然消失）；画布工具栏在右上角避让
    - **面板跟着框体走**（2026-09-10 第六轮，按参考）：输入面板贴在当前选中框体的正下方，
      拖动框体时逐帧跟随。实现在 `CanvasDockPanel`：位置用 `transform: translate3d()` 直接算、
      **不加 CSS 过渡**（加了就慢半拍）。
      面板**去掉了 backdrop-filter**——大块毛玻璃每帧重采样会让跟随明显掉帧
    - **面板锚在画布坐标系里**（2026-09-11 第十六轮定稿，改过三次，别再退回去）：
      面板和框体是同一坐标系里的两个东西，**画布怎么动它们就一起怎么动**。
      - 拖框体：框体 position 变 → 面板跟着走；
      - 平移/缩放画布：**面板必须跟着框体一起在屏幕上移动**（用户原话：「我移动画布，输入框就
        保持跟框体一起」）。实现是 `useStore((s) => s.transform)` 直接订阅视口 transform，每帧重算；
      - 手势进行中（`viewportBusy`=onMoveStart..onMoveEnd）**不做任何夹取**，面板是框体的刚体；
        松手后才把「居中→与框体左/右边对齐→夹进画布」这套落位逻辑跑一遍。
        夹取是必须留的：`.learning-canvas` 是 `overflow:hidden`，框体贴右边缘时面板不夹就会
        把右下角的「生成」按钮裁掉。
      - ⚠️ 别退回「手势中不动、松手后重新落位」的老口径：那样面板是钉在**屏幕**上的，
        一拖画布就和框体分家（第七、十、十五轮三次都栽在这条上）。
    - **课程/课包生命周期（用户 2026-09-11 问过，务必讲清）**：状态只有三档
      **DRAFT → PUBLISHED → ARCHIVED**（`/api/admin/course-series/:id/status` 的 publish/archive 两个动作，
      见 routes/admin/courses.js）；
      - 新建课包默认 **DRAFT** + 可见范围默认 **ALL_ORGS**（`body.status || 'DRAFT'`）。
        注意 ALL_ORGS 现在的含义是「**上架课程广场**」，不是「所有机构可用」（见第四节第 3 条）；
      - **只有 PUBLISHED 才对机构可见/可用**：机构端把课时加进班级课表走 `accessibleLesson()`，
        它要求 `lesson.status='PUBLISHED' AND series.status='PUBLISHED'` **且该机构对此课包有生效授权**
        （`orgSeriesAccessSql()`；2026-09-11 第十六轮起，`visibility='ALL_ORGS'` 不再等于对机构放行）；
      - 学生侧同理：`resolveStudentLessonContext()` 的 SQL 里 JOIN 条件带 `series.status='PUBLISHED'`；
      - **archive 只是把状态改掉**，不回滚既有的 班级课表 / 机构授权 / 课堂 / 学生作品
        → 所以「归档后机构那边还挂着、班级还有这节课、学生的作品还能打开」，
        但**新动作全被拒**：机构加不了课时、学生存不了画布也生成不了。
      - ⚠️ 因此归档一个还有班级在用的课包，会把那些班级**卡在半坏状态**（能打开、不能保存/生成），
        而「发布检查」不会提示这一点（它只查课时数量/状态/可见范围）。
        要恢复：把课包重新发布（发布课包按钮），或把课时迁到已发布课包。
    - **画布「有没有未保存改动」的签名**（2026-09-11 第十三轮定稿）：`canvasContentSignature`
      必须包含 `id / type / data / position / viewport` —— 少了 position，学生挪框体就不算改动、
      不触发自动保存、刷新回原位（线上踩过）。运行时状态（selected / measured）不要签进去，否则噪声太多。
      另外**别写无条件 `fitView`**：那会在每次挂载时覆盖掉快照里存的视角，
      要用 `fitView={!hasStoredViewport}`。两条都有静态守卫 `scripts/p39-canvas-dirty-guard.mjs`。
    - **面板位置口径**（2026-09-11 第十七轮定稿；前面第七/十/十五/十六轮四条老口径全部作废）：
      **只有一条规则：面板永远居中在框体正下方 14px**（`x = 框体中心 − 面板宽/2`，`y = 框体底边 + 14`）。
      - 实现：`const transform = useStore((s) => s.transform)`，每帧用
        `nodeLeft = node.position.x * zoom + tx` 现算，位置写进 `translate3d()`、**不加 CSS 过渡**；
      - ❌ **绝不夹取**（居中后夹住 / 越界改成与框体左·右边对齐，这些全删了）：一夹面板就和框体分家，
        用户看到的是「画布上没框体、边上却杵着输入框」——他前后报过两次。
        参考实现是「居中后硬夹住」，我们**故意不跟**；
      - 面板会越出画布时**动画布让位**（左右/下都支持），所以面板既贴着框体又不会被 `overflow:hidden` 裁掉；
      - 让位只在**框体自己动了 / 换了选中框体**时发，且框体已经不在画布里时不发（别跟用户抢视图）；
        手势中（`viewportBusy` = onMoveStart..onMoveEnd、`node.dragging`）既不请求也不消费这个标记；
      - ❌ 别再退回「手势中不动、松手后归位」：那是把面板钉在**屏幕**上，一拖画布就和框体分家。
      - 回归脚本：`.tmp/panel-scenarios.mjs`（初始 / 大幅平移 / 反向平移 / 拖框体四个场景，
        断言居中、间距 14、零重叠、松手零跳变、平移不被拽回）。
    - **框体不支持手动缩放**（2026-09-10 第九轮，按用户反馈改）：删掉了 `NodeResizer`，
      卡片尺寸完全由内容决定，要看大就用画布缩放（右下角控件/滚轮）。`safeSnapshot` 加载时
      会把历史快照里的 `width/height` 与样式尺寸丢掉，所以学生以前手拉过的尺寸不再生效
    - **面板里的浮层：先确认没有祖先在裁它**（2026-09-11 第二十轮）：
      面板内容整体包在 `.learning-canvas__beam` 里，那层**曾经有 overflow: hidden**（现已去掉）——
      凡是往面板外延伸的浮层（悬停大图、@ 候选列表、以后可能加的下拉/气泡）都会被它裁掉一半甚至整条看不到，
      而且在浏览器里只表现为「被遮挡」，很难联想。三束光束层各自都带 clip-path，圆角裁切不依赖父级 overflow。
      同理，缩略图本体（.learning-node__ref-thumb-pic）是 overflow:hidden 的圆角裁图盒子，
      **别把按钮放进去**（× 会被裁掉），要用它外层不裁剪的定位锚点。
      回归脚本：`.tmp/overlay-test.mjs`（逐个量 rect + elementFromPoint 命中测试）。
    - **连线只当参考，不改变框体画面**（2026-09-11 第十九轮，按用户反馈）：
      框体画面里只出现「**框体自己**的预置素材」和「**框体自己**生成出来的图/视频/音频」；
      从别的节点连过来的素材**永远不替换框体画面**——它只出现在底部面板的「参考 / 画面（首帧·尾帧）」行，
      以及生成时一起提交给模型。用户原话：「连线不会影响框体变化，应该是一直是默认框体，
      只有生成出来的图/视频/音频这些才会出现在框体内」。连一条线就把框体换脸，学生看不懂。
      面板里的参考缩略图带「× 删除这个参考」（默认隐身、hover 出现）：连过来的删那条连线，
      框体自己的预置素材清 `referenceUrl`。
    - **卡片比例跟着槽位画幅走**（2026-09-10 第九轮）：`NodeFrame` 把框体的 `aspectRatio`
      （'9:16' / '16:9' …）写成 `--cv-ratio-w/h`，素材区（占位/成图/参考图）据此设 `aspect-ratio`，
      尺寸 = `min(226px, 300px × 画幅)` —— 9:16 得到一张竖卡、16:9 得到一张横卡。
      宽度上限写死成 px 很关键：写成百分比或 `width:auto` 会让图片的原始宽度参与卡片的
      shrink-to-fit，卡片被撑到 500+ 宽（实测）。另外素材的内边距要用固定 12px + `align-self: center`，
      **不能用 `margin:auto`**（auto 外边距不参与卡片宽度计算，同类卡片会差 24px）
    - **面板永远在框体下方**（2026-09-10 第七 / 十二轮，按用户三次反馈定稿）：放不下时**把画布上移让位**，
      而不是夹到画布底边 —— 夹住必然压住框体，做不到「一直在下方」。实现：面板算 overflow →
      去抖 260ms → `CanvasSurface.requestRoom` 用 ReactFlow 的 `setViewport` 上移同样距离 → 面板重新锚定。
      面板本身也尽量瘦（顶部那条重复的参考图缩略条已删）。详见第三节「画布第十二轮」。
    - 早期版本「下方放不下就翻到框体上方」，
      实际用起来学生会找不到输入框，已经去掉——现在永远是 `框体底边 + 14px`，只在会越出画布底边时
      贴住底边（此时会压住框体下半部一点，是唯一代价）。水平方向：**优先居中于框体**；
      居中会越出画布时改成**与框体左/右边对齐**（参考实现是「居中后硬夹住」，
      框体靠边时面板会被推到离框体很远的地方，看着就是错位——这是用户反馈的「错位」根因）
    - **默认视野别贴太近**（2026-09-10 第七轮）：`fitViewOptions.maxZoom: 1.2`（原来是 maxZoom 1.8）。
      fitView 会把内容垂直居中，zoom 1.8 时两三个框体就铺满画布，最下面的框体紧贴底边、
      面板没地方放只能压住框体（实测重叠 100px+，收到 1.2 后约 14px）。工具栏「适配视图」同一口径
    - **默认视野给面板留位**（2026-09-10 第八轮）：初始视野/「适配视图」用**分边 padding**
      `{ top:'24px', right:'40px', bottom:'320px', left:'40px' }` + `maxZoom: 1.2`。
      原因是 fitView 会把内容**垂直居中**：居中时框体下方最多只有 (画布高-框体高)/2 的空间，
      框体 400+ 高、画布 773 时只剩 ~180px，而面板 160~320px —— 面板必然压住框体。
      内容靠上 + 下边留 320px 之后，面板恒定落在框体下方。
      ⚠️ ReactFlow 把**数字**当比例解析（`(h - h/(1+p))/2`），要留固定像素必须写 px 字符串。
    - **竖版素材限高**（2026-09-10 第八轮）：`.learning-node__reference img` 与 `.learning-node__media`
      都是 `max-height: 300px` + `object-fit: contain`。卡片宽度是内容撑出来的，
      图片按卡片宽度铺开后高度 = 宽度/比例，9:16 的成图能让卡片长到 1355 flow px（比画布还高），
      面板就没地方放了。**不要**改成 `width: auto`：图片原始宽度会参与卡片的 shrink-to-fit，
      卡片会被撑到 559 宽。视频卡片里「画面来源」的预览点一下可放大看原图。
    - **素材类框体的展示**（2026-09-10 第十轮，按参考的图2 定稿）：图片/视频/动画三类
      走 `NodeFrame variant="media"` —— 不画卡片外壳（背景透明、无边框、无阴影），
      标题是画面左上方一行小字（18px 图标 + 12.5px 标题），**画面本体就是卡片**
      （12px 圆角 + 1px 边框，选中时主色描边落在画面上）。
      画面按**素材自身比例**自适应：`width/height:auto; aspect-ratio:auto; max-width:226px; max-height:300px`。
      ⚠️ 别拿「槽位画幅」（`--cv-ratio-*`）去定画面的尺寸：两者不一致时画面框会被拉成扁的、两侧出黑边
      （第十一轮的用户反馈）。槽位画幅只用于「还没生成时的占位板」。
      画面**不再是 nodrag**：按住画面就能拖动框体；「点图放大」（灯箱）已按用户要求移除，
      要看大图用画布缩放。
      别退回「深色卡片 + 深色内框里缩一张小图」那种做法（用户原话「很难看」）。
      文字/角色/场景/便签类仍是原来的卡片版式（标题栏 + 内容）。
    - **输入面板边缘的流光**（2026-09-10 第六轮起）：移植 `border-beam` 的 md 旋转档
      （MIT © Jakub Antalik，**没有引依赖，是把 CSS 抄进 styles.css**）。
      第七轮按用户反馈「跟源码不一样」重做了一版——**参考的 CSS 是运行时生成的**，
      所以是把**真实组件渲染一遍导出的 CSS** 原样搬过来（脚本见第七节末），三层缺一不可：
      ① `::after` 描边：白色高光锥形 + 9 个彩色椭圆斑 ∩ 亮弧 ∩ 1px 边框圈；
      ② `::before` 内壁辉光：同一组彩色斑（更淡）+ 内阴影，遮罩把光压在**离边 28px 以内**
         ——这一层是「柔光」观感的关键，第一版漏掉它、只留描边，看起来就是一道硬线；
      ③ 外发光晕（blur 8px）。三层的透明度严格照参考的 `0.26 / 0.42 / 0.24 × strength(.67)`，
      别随手调亮。面板 padding 放在 `.learning-canvas__panel-inner` 而不是面板本身——
      遮罩是按元素自己的 content-box 挖圆环的，面板留 padding 会把光圈推进去
    - 连线：贝塞尔曲线 + 沿路径游走的主色发光段（自定义 GlowEdge），不渲染箭头，方向由光带动效表达；
      **选中连线会在中点出现红色 × 可删除**（`removeEdge`，记撤销历史；键盘 Delete/Backspace 同效），
      避免学生连错后没法取消；连线悬停变亮加粗、选中变主色
    - **框体两侧的小圆 ＋ 连接点**（2026-09-10 第六轮，按参考；**只认拖拽，不做点击自动连接**）：
      从圆点按住拖一条线，落到另一个框体的圆点上即建立连接（ReactFlow `ConnectionMode.Loose`，
      两侧 handle 都声明成 source，所以左右都能起线也能接收；`connectOnClick={false}` 关掉单击起线）。
      两个实现要点：handle 必须渲染在 `.learning-node` **外面**（卡片 overflow:hidden 会裁掉探出的圆点）；
      **只读模式下也要留在 DOM 里**（CSS opacity:0 隐身）——删掉的话 ReactFlow 找不到 handle，
      已提交作品里原有的连线会一起消失（已实测：只读态 2 条连线照常渲染、连接点全隐身）
    - **连线两端到底点在哪个圆点**：`resolvePortSides()` 是唯一规则（渲染 displayEdges 与查重 onConnect 共用）。
      历史快照里的连线没有 handle 字段，按两节点左右位置补 facing 的两侧——只影响渲染、**不写回数据**，
      免得把老画布白白标记成「有未保存改动」。查重按「哪两个圆点被连上」比较（`edgePortPairKey`，
      方向不算差异），否则一条老连线能从反方向再连一次、两条线正好重叠
    - **卡片标题**（2026-09-10 第七轮，按用户反馈）：默认是纯文本（16px，比原来的 13px 大两档），
      **双击才进编辑态**——单击就能编辑的话，学生想拖卡片往往点进输入框里，拖不动还以为卡了；
      编辑态的输入框只占标题栏一段（`min(200px, 58%)`）而不是铺满整条，剩下的地方留给拖卡片
    - **学生可以把桌面文件拖进画布**：走 `POST /api/student/file-assets/upload`（仅 STUDENT，
      归本人 + PRIVATE，复用 persistSecureUpload/reserveUpload 的扩展名/魔术字节/25MB/频次校验），
      落成节点后连线即可当首帧/全能参考素材。**没有上传控件是刻意的**（参考里那个「首帧上传」不做）——
      面板页脚原来那个「＋」也按用户要求删了，别再往回加。
    - **这一轮（第十八轮）拖拽链路的四条口径**：
      ① 占位框体必须由**工作台**（持有快照的那层 `canvasWorkspace.uploadFiles`）落，不能在画布组件的
         `onDrop` 里 `setNodes` —— 那是局部状态，写回快照时会被冲掉（第一版就这么错的）；
      ② 流程：立刻落 `uploading:true` 的框体 → 逐个上传 → 用同一个 id 补成真素材 → 失败要写在卡片上；
      ③ 左侧「本地素材」分组**从快照派生**（带 `uploaded`/`uploading` 的节点），不引入新的服务端状态，
         所以刷新后它还在，点击可定位到画布上的框体；
      ④ 素材地址凡是 `/api/**` 一律走 `useDisplayUrl` + `api.fetchBlobUrl`（带 token 取回转 blob:），
         且**解析完成前不要渲染原始地址**（浏览器会白挨一个 401）。
    - 回归网：`scripts/p38-student-canvas-upload.mjs`（本人可见/未登录取不到/类型拦截）；
      UI 侧用 `.tmp/drop-test.mjs`（真浏览器拖文件进画布，见第七节）

### 本轮踩到的坑（务必先看，能省大量时间）

```text
1. 上游（api.seedance.nz 中继）与文档不一致的地方，一律以实测为准：
   - 图片 zhenzhen-image-g-v2-lowprice：prompt 长度 5–5000 字符，太短会 400
     （错误原文「prompt length must be between 5 and 5000 characters」，现在会透出给学生）
   - MiniMax-H3：提交 POST /v2/video_generation、查询 GET /v2/query/video_generation/{id}
     （不是默认的 /v1/videos，那是另一套：要 prompt 字段）；提交只回 {task_id}，结果包在 task 里
   - mureka-v9-song：metadata.lyrics 必填、prompt（曲风）**也必填**、metadata.version 必填（文档都没写全）
   - 分辨率按原样发：MiniMax 只认 480P/768P（写 480p 会被拒）
2. 音乐没有时长参数：发 duration 会被静默忽略，产出永远是整首歌（实测 6.35MB / 208 秒）。
   不要在界面上放一个填了不生效的时长字段。
3. 生产 DEPLOYMENT_MODE=public（不是 production），环境判断不要写 === 'production'。
4. 主站 CSP：script-src 'self' → 学生代码预览必须走 /vibe-preview.html 外壳页；
   media-src 本轮才补上（在此之前音视频都播不了）→ 以后新增外部资源类型先看 CSP。
5. 课堂能力（class_sessions.allow_*）与课时能力是两套：开课时默认按课时兜底，
   但**已经开着的课堂**不会自动跟着改，要让老师勾一下或重开课。
6. 本地 mock 产出的音视频不是真文件（mock://…），本地验不了播放。
7. 模型能力是「管理员声明的白名单」，不是自动识别；上游 /models 只返回 id。
8. **hook 不能出现在提前 return 之后**（2026-09-10 画布课堂整页白屏的成因）：
   `canvasWorkspace.jsx` 里自动保存的 useState/useRef/useEffect 被写在
   `if (project.loading) return <Loading/>` 后面 → 加载态渲染 19 个 hook、数据到了变 24 个，
   React 抛 #310 并卸载整棵树（白屏，控制台只有一句 "Minified React error #310"）。
   三端构建能过、33 个 API 冒烟全过，**只有真人打开页面才会炸**。
   教训：冒烟过 ≠ 页面能开；动过前端组件后，除了冒烟还必须真在浏览器里打开一次。
   已加静态守卫 `scripts/p34-hook-order-guard.mjs`（命中即 exit 1）。
   移动这类 hook 时，把它依赖的衍生值（如 `editable` / `runningCount`）一起上移，否则 deps 数组会踩 TDZ。
9. `useData()` 每次渲染都返回新对象（`{...state, refresh}`），**不要把整个对象放进 useEffect 依赖**，
   否则定时器会被反复重置、永远等不到那 5 秒；依赖用 `generations.data` 这类稳定引用或派生标量。
10. React Flow 缩放把手有两个坑（学生反馈「框体没法放大缩小」的成因）：
    - 它自带的 `.react-flow__resize-control.handle{width:5px}` 比单类选择器更具体，
      自定义把手尺寸必须写足三层（`.learning-canvas .react-flow__resize-control.learning-node__resize-handle`），
      否则你的 16px 会被压回 5px；
    - `.learning-node` 的 `overflow:hidden` 会把探出节点四角的把手裁掉（右下角几乎完全点不到）→
      `NodeResizer` 要放在被裁切容器的**外面**（现在是 `.learning-node` 的兄弟节点）。
11. 官网匿名统计同意条是 fixed 悬浮在底部的，会盖住画布最下面一条（底部把手点不到）：
   用 `body:has(.analytics-consent) .student-canvas-viewport{height:calc(100vh - 330px);flex:0 0 auto}`
   在它出现时缩短画布。注意视口本身有 `flex:1`，只加 margin/padding 会被 flex 长回去。
12. 框体参数（比例/清晰度/时长/含音频）现在是「**平台留空＝学生在课堂里自选**」：
   留空的服务端不能填默认值（`normalizeGenerationBox` 会保持空），并把该模型的 `paramOptions`
   随框体下发给学生端渲染下拉；学生选的值必须**落库 `generation_jobs.request_options`**——
   异步 worker 会按框体重新解析一次参数，不落库的话学生的选择会被重算成默认值。
   平台填了则该参数学生只读：服务端只认框体，并且只记录真正生效的学生选择（锁定项不写库）。
14. 拆模块（2026-09-09 那次 communication.js 拆分）留下的隐藏坑：**同一个域里跨文件用了没导出的函数/常量**，
   静态看没问题、构建也过，直到真的请求到那个接口才 500（`ReferenceError: helpFeedbackRows is not defined`）。
   修法是把共享件放进本域的 `helpers.js` 统一导出；改完用 `esbuild --bundle` 打一遍、或写个
   「引用了兄弟文件里的名字却没导入」的小扫描，比逐个接口试快得多。
15. **守卫只认单行早返回会漏**：`if (pathname.startsWith('/learn') && !session) { return <Navigate/> }`
   这种跨行写法在 website main.jsx 里藏了很久——学生会话一过期就白屏（React #300），而不是跳登录页。
   p34 现已同时识别「单行 if(...) return」与「if(...) { 换行 return }」，全仓复扫无其他实例。
   同类坑的通用形态：**hook 必须全部在提前 return 之前**，包括那些「看起来不像 return」的提前返回。
16. 生成状态靠**回到页面时**用服务端任务对账：`generation_jobs` 告诉前端哪些框体已完成/失败，
   画布节点据此补结果或翻失败（只补「缺失节点」是不行的，已在画布上的节点会永远停在生成中）。
   轮询依赖该列表里还有 QUEUED/RUNNING；任务早结束而页面还停在「生成中」时，刷新一次即恢复。
17. 排查前端白屏别只信 dev server：反复改文件、HMR 大量 update 之后，dev 服务器会冒出**假的**
   hook 顺序报错。先用 `vite build` 出来的 dist 复现一次再下结论（本轮被这个绕了一圈）。
18. ReactFlow 的 `.connectionindicator` 是**常驻类**，不是「正在拖线」的标志：
   `connectionindicator: isConnectable && (!connectionInProcess || isPossibleEndHandle) && (…)`，
   没有连线时它也算 true，所以不能拿它做「平时隐身、拖线时才亮」的开关。
   想区分拖动中得自己看 `connectingfrom`（起点）/ `valid`（可落点），或者干脆让连接点常亮低透明度
   （第六轮就是这么定的：静止 0.4，悬停/选中/可落点 1）
19. ReactFlow 的 Handle 在只读态（`nodesConnectable={false}`）**仍然渲染 DOM**，别用 `return null`
   或 `display:none` 藏它：`getEdgePosition` 找不到 handle 就让整条连线不渲染（hide 要用 opacity:0）
20. 连线拖拽的**落点判定是 `elementFromPoint`**：如果 handle 里塞了图标（svg），命中到的是子元素，
   ReactFlow 会退回「最近 handle（connectionRadius 内）」的兜底路径——能用，但别把 connectionRadius 调太小。
   另外拖动时 `mousedown` 必须**正好落在圆点上**，偏出去十几像素就变成拖框体（本地验证脚本踩过好几次）
21. 撤销历史别按事件条数记：拖动框体是每帧一个 `position` change、打字是每键一个 change，
   照搬「每次变更 push 一条」会让一次拖动攒几十条记录（撤销只能一点一点退）。
   做法见 `CanvasSurface`：拖动/缩放用手势底稿（`gestureRef`，手势开始留底、结束记一条），
   连续输入用 700ms 窗口合并（`COALESCED_EDIT_KEYS` + `lastEditRef`，undo/redo 时重置）
22. **别靠读源码还原参考的视觉效果**：参考里很多组件的 CSS 是运行时生成的
   （border-beam 就是 `[data-beam]` + `::after/::before` + `@property` 动画拼出来的）。
   正确做法是把包渲染一遍、把生成的 CSS 抓出来（脚本见第七节「参考组件原始 CSS 的取法」），
   再做并排对比页。本轮第一版就是只读了源码、漏掉一层 `::before`，观感差了十万八千里。
23. **fitView 会把内容垂直居中**：内容越高，最下面的框体越贴近画布底边。
   面板类浮层要贴在框体下方时，这直接决定「框体下面还剩多少地方」——
   所以 `fitViewOptions.maxZoom` 不只是缩放观感问题（1.8 → 1.2 把面板与框体的重叠从 100px+ 收到约 14px）。
   同理，任何贴着框体渲染的浮层（状态条、工具栏）都要先想清楚「下面还有没有地方」。
24. 面板/浮层贴着框体算位置时，**水平居中要在夹不住的时候换策略**：
   参考实现的 `getFloatingPanelPosition` 是「居中后硬夹在视口内」，框体靠边时浮层会被推到
   离框体很远的地方（用户反馈的「错位」）。我们的做法是居中放不下就与框体的左/右边对齐，
   浮层始终「连着」框体。垂直方向则是永远在下方（只贴底边，不翻到上方）。
25. **卡片尺寸是内容撑出来的**，所以图片会反过来决定卡片多大：
   `width: 100%` 的图片按卡片宽度铺开后高度 = 宽度/比例（9:16 成图 → 卡片长到 1355 flow px），
   而图片的**原始宽度**只要写成 `width: auto` 就会参与卡片的 shrink-to-fit、把卡片撑宽（实测 559px）。
   贴边浮层（输入面板）依赖卡片尺寸，所以媒体一律「宽度占满 + `max-height` + `object-fit: contain`」。
26. **fitView 是「居中」的**：内容越高，底下预留的空间越少（正好是 (画布高-内容高)/2）。
   任何「贴在框体下方」的浮层都要靠**分边 padding** 把内容顶到上面去，
   而且 padding 数字是**比例**不是像素（`parsePadding`），要留固定像素得写 `'320px'`。
27. **贴框体的浮层：锚点必须和框体在同一个坐标系里**（第十六轮重新定稿，本条前面写过三版全错）：
   面板要「锚在画布坐标系」——平移/缩放画布时它跟着框体一起动，也就是**实时订阅视口 transform**。
   前几版写的是「手势中不动、松手后重新落位」，那是把浮层钉在**屏幕**坐标系里，
   一拖画布就和框体分家（用户第七、十、十五、十六轮反复反馈的根因，第十六轮原话：
   「如果我移动画布，输入框就保持跟框体一起」）。
   两条配套：① 手势进行中**不夹取**（夹取会让它在半路停住，看着还是分家），松手后再落位；
   ② 夹取不能删——容器是 `overflow:hidden`，框体贴右边缘时不夹会把面板右下角的「生成」按钮裁掉。
46. **上游模型不会去抓我们的公网地址**（2026-09-11 图片上传时实测）。给模型传
   `https://iicili.cyou/api/public/file-assets/…/download` 会**直接报错**；换成 `data:` URL 立刻正常。
   凡是"让模型看一张图"的需求，都必须**内联**（base64）；外链只对浏览器有效。
   推论：任何"先上传再给模型一个 URL"的设计，都要先问一句"上游抓得到吗"。
47. **别把"平台侧的能力声明"当成"模型的真实能力"**（同一轮的教训）。我先前根据渠道配置里
   `modelCapabilities` 是空的，就判断"当前模型不支持读图"，这是错的——那只是平台自己的声明字段，
   与模型无关；实测该模型能读图。**要判断模型能力，发一次真请求，别读配置猜。**
48. **渠道请求模板里的 `messages` 会被多轮历史整份替换**。想让 TEXT 渠道模板带的东西（system 提示词、
   参考图占位符 `{{referenceItems}}`）在对话链路里生效是**不成立的**：只要调用方传了 `messages`，
   模板里的 `messages` 就被整份换掉。这类东西必须放进**消息数组本身**（内容块）。

45. **生产的 nginx 配置有两份，而且只有一份生效——你改的很可能是没人读的那份**（2026-09-11 06:28 UTC 复核时发现，
   是那次「CSP 漏 blob:」现场改 nginx 留下的后患）。现状：`/etc/nginx/sites-enabled/iicili.cyou` 是**普通文件**、
   **不是**指向 `sites-available/` 的软链（4552B，09-11 12:44），里面才有 `img-src … blob:`、`media-src … blob:`、
   SSE 的 `proxy_read_timeout 300s`、`/vibe-preview.html` 外壳；而 `sites-available/iicili.cyou` **还是旧版**
   （没有 blob:、没有 media-src、仍带着 /student 那两段 location）。
   👉 谁按老习惯 `ln -sf`、或把 available 拷回 enabled，这三样会**静默一起回退**（拖进画布的图不显示、
   SSE 对话断流、学生作品预览被 CSP 拦），nginx 也不会报错。**动 nginx 前先 `diff` 这两份，改完记得两边都写。**
   另：`sites-enabled/` 里还堆着 5 个 `.bak-*`，而 `nginx.conf` 是 `include /etc/nginx/sites-enabled/*;`，
   会**把它们全部加载**（`nginx -t` 每次固定报 6 条 `conflicting server name "iicili.cyou" … ignored`）。
   现在真配能生效，只是因为 glob 按字母序把 `iicili.cyou` 排在第一个（它是 `.bak-` 文件名的前缀）——
   能跑，但一次重命名就可能变成「在用旧配置」。维护窗口里把这些 `.bak-*` 移出 `sites-enabled/`
   （挪到 `sites-available/` 旁边的备份目录），并把生效版回灌 `sites-available/iicili.cyou`。
49. **只设 `AI_PROVIDER` 环境变量是没用的：渠道策略里 provider 缺省会默认成 `local-mock`**
   （2026-09-11 本地验图片上传时踩到）。本地把 `AI_PROVIDER=custom`、端点指向自己的假上游，
   对话却回「这是本地模拟回复」、假上游一个请求都没收到。原因：只要 `platform_settings` 有行
   （种子库就有）就走**策略**，而 `normalizeProviderPolicy` 写的是 `provider = parsed.provider || 'local-mock'`
   —— 策略里没写 provider 就**静默**变成内置模拟，环境变量从头到尾没人读。
   **本地/预发要接真上游，必须把渠道策略写进库**（照生产的形状：channels + modalityChannels.TEXT）。
   反过来说：后台把策略清空 = 悄悄退回模拟，界面不报错。
50. **结束后台时子进程不一定跟着死**：端口被旧后端占着 → 新后端起不来，
   而健康检查照样连得上（连的是旧实例/旧库）→ 整轮测试静默跑在错的东西上。
   那天我因此白跑一轮（「第一次发送就带了 8 条历史」，才发现测的是上一轮的会话）。
   现在环境脚本先查端口占用，测试里也断言「起始会话必须是空的」。
51. **原生 `<input type="file">` 不写 CSS 就是可见的**：`.c-file-input` 在 JSX 里用了、
   全仓却一条规则都没有 → 聊天区顶部露出一行浏览器默认的「选择文件 / 未选择任何文件」控件。
   断言全过、可访问性树里也看不出来，**只有开浏览器截图才能发现**（同第 8 条的教训）。
   凡是「用 `.click()` 唤起隐藏 input」的写法，必须显式 `display:none`。
43. **「管理员配好了模板」不等于「线上真的用了模板」：异步 worker 重建 provider 时会丢字段**
   （2026-09-11 生视频/生音乐全挂就是这个）。`processAsyncGeneration` 原来只把
   provider/model/endpoint/channelId 四个字段挑出来重建 selection，渠道的
   `modelRequestTemplates / requestTemplates / requestPaths / pollPaths` 全被丢掉 → 退回内置默认
   请求体与默认路径。图片的默认模板恰好能用，所以只坏视频/音乐，最难查。
   **改 provider/selection 相关代码时，一律用 `{ ...routedSelection, ...覆盖 }` 整份带上。**
   排查这类「配置对、线上不对」问题的固定套路（本次就是这么定位的）：
   ① 直接打上游，用手工构造的请求体验证模板本身对不对（校验失败不花钱）；
   ② 在库里读策略，确认模板/路径确实存着了；
   ③ 按运行时路径在进程内抓包（多半是对的，因为没经过 worker）；
   ④ **给服务进程挂 `--require` 的 fetch 探针**（见第七节 `.tmp/fetch-tap.cjs`），抓它真实发出的
      URL 与请求体 —— 这一步才把「worker 丢字段」暴露出来。
44. **「模板里有占位符」≠「占位符有数据」**（2026-09-11 全能参考不生效就是这个）。
   两处缺一不可，缺任何一处都表现为**静默丢弃**：生成照样 SUCCEEDED，只是参考没生效、画面和参考图毫无关系。
   ① `requestBody` 渲染模板时要**把数据传进 context** —— `referenceAssets` 以前压根没传，
      于是模板写不写 `{{referenceItems}}` 都渲染成空数组；
   ② 渠道模板里要有对应占位符 —— 生产 MiniMax-H3 的 content 只有 `{{frameItems}}`（首/尾帧），
      没有 `{{referenceItems}}`，全能参考的图没有落点（已在生产补上，备份 `provider-policy-before-20260911T060129Z.json`）。
   排查话术：**光看「生成成功没有」不够**，要看「发给上游的请求体里有没有那张图」——
   给服务进程挂 fetch 探针（第七节 `.tmp/fetch-tap.cjs`）抓 body 是唯一可靠办法。
42. **要真跑 AI 生成，别在生产上拿账号试**：把生产的渠道配置 + 密钥文件 + `AUTH_PEPPER`
   （三者缺一不可，密钥是用 AUTH_PEPPER 派生密钥加密的）只读取到本地，用同一份代码起服务跑，
   产物落临时库。脚本见第七节 `.tmp/prod-gen-verify3.mjs`。
   ⚠️ 取下来的配置里有**真实 API Key**：只放 `.tmp/`（已 gitignore），跑完立刻删。
41. **「fetch 到了」不等于「显示得出来」：CSP 会把 blob: 悄悄拦掉**
   （2026-09-11 线上事故）。学生拖进来的图，代码走的是「带 token fetch 二进制 → `URL.createObjectURL`
   → 塞进 `<img src>`」。排查时看到的现象是：**图片不显示、页面无报错、Network 里那条
   `/api/student/file-assets/<id>/download` 明明是 200 还带完整字节数**。
   真相是 CSP `img-src` 里没有 `blob:`（`media-src` 里有，所以视频音频正常，更容易误判成「图片代码坏了」）。
   这类问题的排查顺序：① 先看线上访问日志那条请求的**状态码与字节数**（200+完整字节就说明取数没问题）；
   ② 再在本地**把生产的 CSP 原样加上**跑一遍（`.tmp/serve-with-csp.mjs`），复现后改 CSP 再跑一遍对照；
   ③ 别只看 Network（CSP 拦截是渲染层，Console 里会有一条 `Loading the image 'blob:…' violates …`）。
   经验：**凡是往 `<img>` 里塞 blob:/data: 的，先确认 CSP 的 img-src 放行**（data: 一直有、blob: 未必）。
40. **静态扫描必须先挖掉注释，否则自己写的说明会变成假警**：p34 的「用了 setXxx 却没声明」
   被我写在注释里的一句「画布组件里的 setNodes 只是它自己的局部状态」直接判红，全量冒烟跟着红。
   凡是按标识符扫源码的守卫，扫之前都要 `stripComments`（只挖整行注释 / 块注释 / 不含引号的
   尾随注释，别把 `'https://…'` 的 `//` 也切了）。**改守卫后一定要跑一次「撤掉已知修复必须报出来」**，
   两个方向都自证过再提交。
39. **受鉴权的素材地址不能直接塞进 `<img>/<video>/<audio>` 的 src**：这些标签发出的请求
   带不了 `Authorization` 头，`/api/student/file-assets/<id>/download` 这种路由必然 401、
   **图片永远出不来**（而且界面上看不出发过请求，只看得到一个空框）。
   做法：宿主传 `resolveAssetUrl`，画布用 `useDisplayUrl` 先解析成 blob: 再渲染；
   ⚠️ 解析完成前**先别渲染原始地址**（否则白挨一个 401），另外 `fetchBlobUrl` 别再拼 baseUrl
   （快照里存的就是 `/api/...` 根路径，拼完变成 `/api/api/...`，实测 404）。
38. **「浮层永远贴在目标旁边」和「浮层永远完整可见」只能选一个，别用夹取去凑**：
   第十六~十七轮在同一个地方来回改了两次，最后才想明白：面板 660px、框体 170px、画布 1222px，
   **居中必然越界**。三条路只有一条对：
   - 夹住面板（参考实现的做法）→ 框体靠边时面板被按在画布边上、和框体分家（用户报了两次）；
   - 让面板越界 → 被容器 `overflow:hidden` 裁掉，框体贴右边缘时连「生成」按钮都点不到；
   - **动画布让位**（选的这条）→ 面板永远在框体正下方且完整可见。代价是画布会被动一下，
     所以让位必须只在「用户动的是框体」时发（frameKey 变化），用户平移画布时不许发，否则就是橡皮筋。
   判断「用户到底在动什么」比调位置公式重要得多。
37. **「用了没声明的 state」这种白屏，静态检查必须按组件分段看**：
   website main.jsx 的 InnerCircleHeader 用了 `showStudentMenu` / `setShowStudentMenu` 却没声明，
   **已登录学生**打开官网首页 `ReferenceError` 整页白屏（未登录正常，所以藏了很久）。
   坑点：同名的 setter 在**同文件的另一个组件**里声明过 —— 按文件粒度查「未定义标识符」查不出来，
   必须按「缩进 0 的组件」分段比较「声明的名字」与「用到的 setXxx」。p34 的判定二就是这么做的。
   写这类扫描时记住：`[\w$]*` 配负向断言会回溯出截断名字（`setCooki`），正则里别写「后面不能跟 :」，
   改成匹配后单独判断；方法调用（`.setItem`）、对象键（`setX: null`）、全局定时器、
   多行 import / `export {...} from` 都要排除，否则误报到没法用（第一版报了 48 条，全是假的）。
36. **同一段访问控制 SQL 抄了 8 份 = 迟早改漏**：机构可见条件
   `((owner_type='PLATFORM' AND (visibility='ALL_ORGS' OR assignment.id IS NOT NULL)) OR ...)`
   在机构端列表/详情、平台端 accessibleSeries/accessibleLesson、学生端 studentContext、
   教学素材、教学任务里各有一份。第十六轮改口径时全部收敛到 `lib.js: orgSeriesAccessSql()`。
   以后改「谁能看到课包」只动这一处；新增查询要 LEFT JOIN course_assignments
   （条件用 `assignmentActiveSql()`）并把这个片段拼进去（片段里的 `?` 就是当前机构 id，
   参数顺序别挪位）。
35. **授权续期分支只查 id 会炸**：`course_assignments` 的续期走 `assertTransition(status → ACTIVE)`，
   如果 `SELECT` 没带 `status`，拿到 `undefined` 直接抛「status 无效」。这条路径只有在
   「对已授权的机构再次授权」时才会走到，所以生产上一直没暴露。改这类状态流转时，
   顺手确认查询把状态字段带出来了。
34. **「自动让位」这类会动画布的逻辑，必须区分「用户手势中」和「手势结束」**：
   面板算 overflow 用的是冻结的锚点，拖画布期间它是常数 → 去抖一到就会在用户拖动过程中插一脚
   （表现为「移动画布时输入框乱动」）。要用手势状态（ReactFlow 的 onMoveStart/onMoveEnd）挡掉，
   并且同一缺口只请求一次，否则会来回弹。
33. **静默 catch 会把「保存失败」变成「学生以为存上了」**：自动保存原来 `catch {}` 吞掉错误，
   线上出现过「课包被归档 → 保存接口一直 404 → 界面显示已保存 → 刷新全丢」。
   凡是「后台自动写回」的路径，失败都必须冒到界面上（哪怕只是一行红色状态）。
   排查这类问题时，**先看线上访问日志里那个写请求的返回码**，比在本地反复猜快得多。
32. **「能存能取」不等于「会去存」**：p33 这类冒烟只验证服务端 PUT/GET 快照成功，
   而「客户端判断要不要存」的逻辑（内容签名）坏掉时它照样绿 —— 线上表现是「刷新全复原」。
   凡是「前端决定何时把状态写回」的逻辑，都要么补静态守卫（见 p39），要么在浏览器里真做一次
   「改 → 等自动保存 → 刷新 → 对比」的闭环验证。
31. **在组件里插 hook 先看依赖变量声明了没有**：这次把 `requestRoom`（依赖 `getViewport`）插到了
   `useReactFlow()` 解构**之前**，deps 数组是立即求值的 → TDZ `ReferenceError` → 整页白屏
   （构建和 p34 都发现不了，只有真人打开才炸）。另外同一个组件里已有 `setViewport` 这个 state setter，
   引 ReactFlow 的命令式 `setViewport` 要改名（现在是 `setFlowViewport`），否则会静默调用错的那个。
   教训同第七轮：插完 hook 先在浏览器里打开一次。
30. **「槽位画幅」≠「素材比例」**：占位板（还没生成）没有自身尺寸，用槽位画幅是对的；
   但真实画面的尺寸必须由素材自己决定，否则素材比例与槽位不一致时（16:9 成图放进 9:16 框体、
   或框体没配画幅时退回 2:1）框会被拉成扁的、图被 contain 放宽出黑边（第十一轮用户反馈）。
   另外：给 `<img>` 同时写 `width:auto` 与 `aspect-ratio` 时，`aspect-ratio` 会盖掉图片的固有比例，
   要放开就显式写 `aspect-ratio: auto`。
29. **贴框体的浮层锚点别只做一半**：锚点「只在框体动时重取」→ 平移完面板丢在一边（第十轮反馈）；
   实时订阅 transform → 平移时追着框体跑、到边缘被夹住（第七轮反馈）。
   正确口径是「**进行中不动 + 结束后归位**」：视口变化时不重取，`onMoveEnd` 触发一次重取。
   验证这种时序问题别用 cua.drag（一次调用把整条路径发完，采不到中间帧），
   在页内 `setInterval` 采样、或用一个「离散的视口变化」（滚轮/缩放按钮）再分时刻取样最快。
28. **`margin: auto` 不参与宽度计算**：素材区用 `margin: 12px auto` 居中时，auto 那部分对卡片的
   max-content 贡献是 0，于是「图片框体卡片」比「视频框体卡片」窄 24px（同类卡片不一致）。
   要居中就用固定 `margin` + `align-self: center`（外层是 flex 列，交叉轴就是水平方向）。
```

### 生成请求模板占位符

```text
{{model}} {{prompt}} {{title}} {{aspectRatio}} {{resolution}}
{{durationSeconds}} {{durationSecondsNumber}} {{audio}} {{voice}} {{n}} {{messages}}
{{firstFrameUrl}} {{lastFrameUrl}} {{frameItems}} {{referenceItems}} {{lyrics}} {{style}}

说明：
- {{durationSeconds}} 是字符串（老模板 seconds: '5' 的形态），整串写 {{durationSecondsNumber}} 得到数字
- {{frameItems}} / {{referenceItems}} 是**数组占位符**：放进 content 数组会自动摊平
  （frameItems 按 first_frame/last_frame 展开；referenceItems 按 reference_image/video/audio 展开，有哪张给哪张）
- 音乐：{{lyrics}} 是要唱的词，{{style}} 是曲风/描述；歌词模式下 lyrics=学生输入、style=默认曲风
- 模板可以**按模型配**（channel.modelRequestTemplates[modelId]），优先于渠道级 requestTemplates
```

---

## 五、待办 / 已知问题

### 全系统缺口梳理（2026-09-08 逐端代码盘点）

```text
平台管理端
  ✅ 已修：官网内容页崩溃、作品广场详情缺失、计费配置无界面、预约线索无入口
  ✅ 已修（已部署）：用户详情/改角色、本人改密（自助端点 ADMIN_SELF，p19 覆盖）
  ✅ 已修（已部署）：列表导出 —— 机构 / 平台用户 / 平台作品支持导出 CSV（筛选与列表一致、写审计）
  ✅ 已部署（release 20260909T065612Z）：平台管理员 MFA —— TOTP 二次验证 + 10 枚一次性恢复码，
     自助绑定页 /admin/security（🔑 入口），登录第二步校验动态码/恢复码，管理员列表显示绑定状态（p22 覆盖）
  ✅ 已修（已部署）：机构下拉改用 /api/admin/organizations/options（id/name/status，上限 500 带 total）

机构端 / 教师端
  ❌ 不做（2026-09-09 用户决定）：排课/课表、考勤、学生档案与成长报告、家长通知
  ❌ 不做（2026-09-09 用户确认）：机构自有课包管理 —— 机构不存在自有课包，全部由平台创建提供
  ✅ 已修（已部署）：班级归档入口（班级卡片「归档班级」，仅 ACTIVE + 机构管理员）
  ✅ 已修（已部署）：/recharge、/usage 已加入导航（积分账务 / 积分用量）
  ⚠️ 教师数据范围靠 8 处零散补丁（/work-data 与 /work-data/export 已在服务端拦 ORG_ADMIN，
     2026-09-08 实测教师 403 / 管理员 200，原「仅前端拦」的说法已过期）

学生端 / 官网
  ✅ 已上线（服务端运行待启用）：VibeCoding 课堂运行时 —— 对话式创作 + SSE 流式 +
     代码预览/控制台 + 提交点评（p16/p17/p18 覆盖）；服务端沙箱见第三节「沙箱现状」
  ✅ 已部署（release 20260909T065612Z）：VibeCoding 作品进作品广场 —— 学生提交带版权确认 →
     老师点评通过 → 平台「平台作品库」发布（vbt_ 分享码）→ 官网 /works 卡片、点开在 sandbox
     iframe 里直接玩（p25 覆盖）；另修掉画布作品提交漏传 copyrightConfirmed 的老 bug
     （服务端 3eed779 起就要求该字段，而学生端一直没传，提交必然 400）
  ✅ 已修（已部署）：画布版本管理 UI —— 学生画布新增「版本」抽屉（预览/恢复/重命名/导出/
     对比/导入），补齐原先缺失的导入校验；新增 p15 冒烟
  ✅ 已修（已部署）：画布「文字框体」——课时配置文字框体数量/模型，PromptNode 生成文字（p20 覆盖）
  ✅ 已修（已部署）：/marketplace 已加入官网导航与页脚
  ✅ 已删除（2026-09-09 用户决定）：/courses 静态页 —— 与 /marketplace 课程广场重复（页面上列 11 门
     系统里并不存在的课包），整页删除：路由重定向到 /marketplace、页脚与首屏 CTA 改指课程广场、
     CMS 的 COURSES 区块（key 白名单/内置默认/管理端编辑器）一并移除，p23 改成通用官网内容 CMS 用例
  ✅ 已修（已部署）：useWebsiteContent 把接口包装体当内容用的老 bug（此前 HOME/FAQ 的 CMS 内容从未生效）

工程与数据
  ❌ 不做（2026-09-09 用户确认）：通知外发通道 —— 站内信真实可用，不再接短信/邮件/微信等外部通道
  ✅ 已修（已部署）：usage_records.work_id 恒为 NULL —— 提交作品时回填，启动迁移回填历史；
     机构端/平台端用量报表的「作品」列与按作品标题检索恢复可用；新增 p13 冒烟
  ✅ 已修（已部署）：usage_records.input_tokens/output_tokens 恒为 0 却由报表返回 ——
     上游图片/视频接口不返回 token 用量，报表不再读取（列保留，schema 已注释说明）
  ✅ 已修（已部署）：机构端「充值订单」恒空且返回恒为 0 的订单计数 ——
     页面改为空态说明（未接入在线支付，充值走平台人工调整）；表仍无写入方
  ⏳ 空转表：org_ai_budgets、personal_credit_ledger 仍未被引用；recharge_orders 只读不写
     （无在线支付是产品现状，不是缺陷）
  ⏳ reminderScheduler.triggerClassSessionReminder 无调用且 SQL 字段不存在
  ✅ 已修（已部署）：列表分页 —— 机构任务、课包、举报、积分流水、学员作品/项目/快照
     改为 page/limit/total/totalPages；批注列表按 500 条封顶；前端已接分页控件（p14 冒烟）
  ✅ 已修（已部署）：平台权限不再兜底 —— 未登记的 /api/admin/* 对非 root 直接 403；
     原先靠兜底的 7 个前缀已显式登记（通知/物料/线索），现有账号行为不变
  ✅ 已拆（2026-09-09，待部署）：adminOrg.js 2594 → 120 行调度器 + routes/admin/*（8 个业务域 + 90 个工具）、
     communication.js 1684 → 6 行入口 + routes/communication/*（4 个域 + 57 个工具）、
     admin main.jsx 1455 → 54 行入口 + pages/*（13 个页面）+ shared.jsx；org main.jsx 尚未拆
```

### 待决策 / 待办（按优先级）

```text
2026-09-09 用户已配置/决定，逐项核对生产库后的现状：
1. 【视频模型选型】已定：图生视频（i2v）。第 1 课 video 槽位 = hailuo-h3-i2v / 768P / 5 秒 / 不含音频，
   学生需先从图片节点连线提供首帧（首帧链路已实现并在生产验证过）。
2. 【音乐】已配渠道：MUSIC → mureka-v9-song，平台开关开启。
   【播客 / 配音】产品决定不做：平台开关保持关闭（PODCAST / DUBBING enabled=0），不再投入。
3. 【课时生成框体】已配：第 1 课 image=2、video=2；第 2–5 课未配置（=0），学生进课堂没有生成入口。
4. 【模型能力 / 请求模板】用户已配置。
5. 生产上传 4MB 以上封面：nginx 上限已放宽到 30m，待实际上传验证。

仍待用户决定：
- ~~服务端代码沙箱是否启用~~ **已删除**（2026-09-11 用户要求清掉）：路由 / 服务 / 冒烟脚本一并移除，
  数据库表 `vibecoding_runs` 保留（历史数据，删表不可逆；代码不再读写）。
- feature_flags / client_download_releases 两张空表是否清理（线上删表不可逆）。
- 排课 / 考勤 / 学生档案 / 家长通知：用户明确不做，已从待办移除。

待做的运维小事（都不影响当前运行）：
- 服务器 systemd 有**存量漂移**：`systemctl show -p NeedDaemonReload learning-platform-production`
  返回 yes（unit 文件是 2026-09-04 的、drop-in 是 09-05 / 09-08 的，与发布无关——发布脚本不动 unit）。
  当前跑的是「已加载」的配置且服务正常；要消掉这个告警需要在维护窗口跑一次 `systemctl daemon-reload`
  （会按磁盘上的 unit + drop-in 重新加载，建议顺带确认 `OOMPolicy=continue`、上传大小等 drop-in 内容符合预期）。
- **nginx 配置收尾**（详见第五节第 45 条）：把 `sites-enabled/` 里那 5 个 `.bak-*` 移出去，
  并把当前生效版回灌 `sites-available/iicili.cyou` —— 现在两份不一致，available 还是「没 blob:、没 media-src、
  带 /student」的旧版，谁把 available 拷回去就会静默回退三处线上行为。
  和上面的 `daemon-reload` 放**同一个维护窗口**做（都要 reload nginx / 都要停机片刻），
  做完 `nginx -t` 应只剩 0 条 `conflicting server name` 告警。
```

```text
2026-09-11 图片上传这一轮核出来、还没处理的三件（都已在代码/生产库里核对过，不是猜测）：
1. 【无效配置项】后台的「协议」下拉（CHAT / RESPONSES / ANTHROPIC）**服务端只存不读**：
   `billingConfig.js` 只做归一化与落库，发请求时一律走 `chatCompletionsEndpoint`（`/chat/completions`），
   全仓没有一处按 protocol 分支。要么接上（RESPONSES 走 /responses 与不同请求体），要么从后台去掉 ——
   别留一个看起来能改行为、实际改不了的空开关。
2. 【模型名】TEXT 渠道的启用清单里还留着 `deepseek-v4.1-flash-expires-on-0910`
   （按名字是 9-10 到期的临时模型，现在仍可用），所以学生下拉里有两个模型。
   处理：后台取消勾选即可（⚠️ 改完**硬刷新后台页面**再编辑，否则旧表单会把值写回去 —— 踩过）。
3. 【死代码】`packages/shared/src/vibecodingWorkspace.jsx` 的 `VibePreviewFrame` 导出：
   注释写着「兼容旧引用」，但全仓（apps / packages / scripts）零引用。可删。
```

### 下一步开发队列（2026-09-10 更新）

```text
可以直接做（不需要新决策，按建议顺序）：
1. 「用当前渠道试一次」按钮：拿模型能力里填的一个比例/清晰度/时长真发一次最小请求（约 1 积分），
   上游接受即说明参数可用——这是唯一能验证「填的值上游认不认」的办法。
   本轮踩过三次同类坑（image 的 prompt 长度、MiniMax 的路径/协议、Mureka 的 version 与 prompt 必填），
   这个按钮能把这些发现从「真机试出来」变成「保存时当场知道」。
2. 拆 apps/org/src/main.jsx（约 1000 行，最后一处巨石文件；纯重构、33 个冒烟兜底）

本轮（2026-09-10）新增能力一览（详细记录见下方各「已完成」块）：
- 生成框体：从「按模态计数」→「按框体配置」→「框体即素材（素材表 + 顺序）」，含两跳数据迁移
- 视频输入画面四类可多选：文生 / 图生(首帧) / 首尾帧 / 全能参考（图·视频·音频，图上限 9、视频音频各 3）
- 请求模板可按模型配（同渠道不同模型请求体不同）；上游请求/查询路径可配（MiniMax V2）
- 音乐生成框体：歌词生音乐 / 描述生音乐（平台用文本模型代写词）
- 播客 / 配音下线（只保留音乐）；音乐渠道（Mureka）已配好并实测
- 三端会话分桶（学生/平台/机构可同时登录）；画布自动保存 + 刷新后恢复生成状态
- 上游错误原因透出（不再只说「AI 供应商调用失败」）
- nginx 主站 CSP 补 media-src（修「生成的作品音视频播不了」）
- 课堂能力默认跟随课时（修「课时开了生视频，学生一点就被拦」）

已明确不做（2026-09-09 用户确认）：
- 官网首页作品展示区：用户没有提过这个需求，不要做（/works 已是独立页面）。
- 在家练习（HOME_PRACTICE）账号入口：不存在这个入口，也不新增。
  此前把它当成「待确认口径的缺口」记在队列里，是误解，已移除。
- VibeCoding 会话分享：用户明确不需要，不做。
- 通知外发通道（短信/邮件/微信等）：用户明确不需要，站内信保持现状。
- VibeCoding 的 Python 运行：依赖服务端沙箱，而沙箱已决定不启用 → 不做。
- 服务端代码沙箱：用户决定不启用（理由见第三节「沙箱现状」）。学生侧浏览器预览 + 控制台照常可用，
  VibeCoding 的「服务端运行」按钮保持灰态，不假装成功。

已完成（2026-09-10，已部署 release `20260910T061349Z` / `b4d58e9`）：
- 输入画面扩到四类（加「全能参考」）+ 请求模板按模型配 + 上游请求/查询路径可配（口径见第 10~12 条）。
  生产已配好 MiniMax-H3：四类勾选、分辨率 480P/768P（原来是 480p/720p，上游只认 480P/768P）、
  时长 5/10/15、V2 模板（content[] + duration 数字 + ratio + audio_control）与 V2 提交/查询路径。
  实测：用线上真实渠道跑通一次 MiniMax-H3 生成（纯文生 4s 480P，产出真实 mp4）。
  ✅ 全能参考已支持图片/视频/音频三类参考（release 20260910T062912Z）：画布按连线节点类型归类
     （image→图、video/animation→视频、audio→音频），上限 图 ≤9 / 视频 ≤3 / 音频 ≤3，
     只认本项目对应模态的素材；模板按 reference_image/reference_video/reference_audio 展开。
     实测：用线上真实渠道带一张图片参考生成成功。
     ✅ 老师上传的**画布素材**也能当首帧/参考（release 20260910T065613Z）：站内相对地址
     （/api/{student,public}/file-assets/<id>/download）在文件 ACTIVE 且 PUBLIC_PLATFORM /
     PUBLIC_RELEASE 时，会换成 <PUBLIC_SITE_URL>/api/public/file-assets/<id>/download 发给上游；
     私有/过期文件与「教学素材」（非公开资源，学生也看不到）一律拒绝。
     实测：拿线上一条老师上传的图片素材当参考，真实生成成功。
     ⚠️ 视频参考需要学生先生成过视频；音频参考需要音乐能力已开通（已配好 Mureka，播客/配音已下线）。

已完成（2026-09-10，已部署 release `20260910T053158Z` / `699326d`）：
- 视频「输入画面」改成多选能力（p11 覆盖，口径见第四节第 10 条）：文生 / 图生-首帧 / 首尾帧
  一个模型可以同时支持多种，不再二选一强制；按实际提供的画面选模板；
  新增 {{lastFrameUrl}} 占位符与 VIDEO_I2V_FRAMES 模板；generation_jobs 增 last_frame_asset_url。
  管理端三个勾选框、画布视频节点显示首帧/尾帧缩略图。生产旧声明读取验证：
  hailuo-h3-i2v（旧 FIRST）仍要求首帧、其余 NONE 仍按文生，行为不变。

已完成（2026-09-10，已部署 release `20260910T050622Z` / `93c032d`）：
- 上游错误原因不再被吞（p30 覆盖）：适配器把上游原文带进错误（例如
  「prompt length must be between 5 and 5000 characters」），normalizeProviderError 保留具体原因、
  不重复套前缀；安全策略拦截仍只给统一文案。此前一律显示「AI 供应商调用失败」，用户无从下手。
- 上传的图片/视频素材进画布时带上文件（addLessonMaterialToCanvas 写 assetUrl/previewUrl）：
  此前节点是空壳、还带「生成画面」按钮，看起来跟没配参数的框体一样。
- 模型能力输入框能填多个值了（PlatformBilling）：受控输入曾把解析后的数组回填，逗号/空格当场被吃掉；
  改成编辑期间用草稿文本、失焦再规范化，「清空声明 / 用上游能力填充」同步清草稿。

已完成（2026-09-10，已部署 release `20260910T043752Z` / `187e496`）：
- 三端会话分桶（p29 覆盖）：生产上 /、/admin/、/org/ 同源却共用一个 localStorage key，
  谁最后登录就把别人的会话覆盖掉（学生登录会顶掉平台端/机构端，被踢回登录页）。
  现在按路径前缀分桶（.admin / .org / .student），读写清都只动自己那一份；
  官网那份自己重复实现的会话读写已删除，统一用 packages/shared/src/auth.js。
  本地按生产布局（同源三端）真机验证：三端同时登录 + 两轮刷新都不掉线。
  ⚠️ 升级后所有人需要重新登录一次（旧会话存在共用 key 里）。

已完成（2026-09-09，已部署 release `20260909T120052Z` / `2b3a8d5`）：
- 生成框体改为「框体即素材」（p28 覆盖，口径见第四节第 12 条）：框体搬进 course_lesson_materials
  （type=GENERATION_BOX），顺序跟素材走，可以做到「素材1 → 框体1 → 素材2 → 框体2」逐条交叉；
  管理端素材类型下拉新增「生成框体」（行内配模态/模型/比例/清晰度/时长/音频/预填提示词/预置素材），
  学生端素材面板合并成一条有序列表。生成链路（按框体取模型与参数、每框体一次、box_id 守卫）口径不变，
  lessonCanvasConfig 改为从素材派生 generationBoxes。
  一次性数据迁移 deploy/production/migrate-generation-boxes.mjs（两跳幂等：generationSlots → 框体 → 素材行）；
  发布时已执行：生产 2 个课时迁移（第1课 4 条、测试1 第二节 6 条，都放进「生成框体」素材组）。
  本地自证：28/28 冒烟 + 三端构建 + 真实浏览器（学生端交叉顺序点击、按框体生成成功；管理端框体素材行内编辑并保存）。
- 同日更早的 release `20260909T111948Z`（按框体配置：generationBoxes 数组）已被本次取代，
  生成框体不再存在 classroom_config.generationBoxes 里。

已完成（2026-09-09，已部署 release 20260909T065612Z）：
- 平台管理员 MFA：TOTP + 恢复码 + 自助绑定页 /admin/security + 管理员列表绑定状态（p22 覆盖）
- useWebsiteContent 解包修复 + 官网内容 CMS 流程（p23 覆盖；/courses 已按用户决定删除）
- VibeCoding 多文件工程导入导出（工作区「导出工程 / 导入工程」，p24 覆盖）
  ⚠️ **该功能已于 2026-09-11 的 VibeCoding 全栈重做中移除**（学生不再手写代码），脚本 p24 同时删除；
  见第三节「VibeCoding 全栈重做：按 OpenSquilla 的设计重建」。
- VibeCoding 作品进作品广场：提交带版权确认 → 老师点评 → 平台发布 → 官网 /works 卡片可直接玩
  （p25 覆盖）；顺带修掉画布提交漏传 copyrightConfirmed 导致学生提交必然失败的老 bug

需要用户配合/决策：
- feature_flags / client_download_releases 两张空表是否清理（不可逆）。
- 4MB 以上封面上传生产实测（需要生产管理员账号）。
- 生产检测账号轮换 + 一次完整教学闭环 UAT。
```

### 未做的功能

```text
VibeCoding 的 Python 运行（依赖服务端沙箱，已决定不启用）、会话分享（用户明确不需要）
（当前有 JS 运行 + 浏览器预览 + 多文件工程导入导出 + 作品发布到作品广场）
```

已完成的功能（勿当占位）：
```text
VibeCoding 课堂运行时：2026-09-09 上线（对话式创作 + SSE 流式 + 代码预览/控制台 +
提交点评闭环）。服务端代码运行沙箱需先完成第三节「沙箱现状」里的运维改动才可用。
```

已删除的功能（勿恢复）：
```text
自由画布 / 自由对话：2026-09-08 按产品决定彻底删除（官网导航、路由、页面标题、满屏页判定、
失效 e2e 断言一并清理）。后端从未实现；personal_credit_ledger 表保留给未开始的 VibeCoding。
```

### 技术债

```text
1. apps/server/src/routes/adminOrg.js（约 2350 行，全仓最大路由）与
   apps/server/src/routes/communication.js（约 1699 行）按业务域拆
2. apps/admin/src/main.jsx（约 1274 行）按页面拆；
   另有 apps/admin/src/components/CourseManagement.jsx（约 769 行）
3. 积分字段待产品确认：personal_credits / magic_stones / monthly_credit_allowance 去留
4. 同步/异步 AI 扣费差异（sessionCreditCap、流水类型前缀）未收敛，
   见 docs/architecture/积分系统设计.md
5. 服务器 1.6GB 内存偏紧（已加 swap）；若要启用病毒扫描需先升内存
```

---

## 六、运维手册（生产发布流程）

```bash
# 1. 推送
git push origin main

# 2. 服务器：拉取 → 备份 → 构建 → 切软链 → 重启 → 健康检查
ssh -i ~/.ssh/ai_kids_platform_ecs_temp_ed25519 root@39.106.183.200
cd /srv/ai-kids-platform/internal-test/source        # 目录名沿用，实际是生产部署源
export PATH=/srv/ai-kids-platform/runtime/node/bin:$PATH
git pull --ff-only origin main
bash deploy/production/backup-production.sh          # 必须，先备份
bash deploy/production/build-production.sh           # 产出 production/releases/<stamp>
R=$(ls -dt /srv/ai-kids-platform/production/releases/* | head -1)
ln -sfn "$R" /srv/ai-kids-platform/production/current
systemctl restart learning-platform-production
curl -fsS http://127.0.0.1:8789/health
```

- **回滚**：用 `bash deploy/production/rollback-production.sh --release <known-good>`，或直接把 `production/current` 软链切回上一 release 再重启；数据库备份在 `production/backups/`。
- **注意**：数据库迁移是幂等 `ALTER TABLE`/`CREATE TABLE IF NOT EXISTS`，重启自动执行。
- **不要**再依赖内测服务回滚（`learning-platform-internal-test` 已删除，`systemctl` 报 not-found）。
- 详见 `deploy/production/README.md`、`RUNBOOK.md`。

---

## 七、本地验证套路

```bash
# 临时库（绝对路径，别用 /tmp）
PLATFORM_DATA_DIR=E:/tmp/ui-test PLATFORM_DB_PATH=E:/tmp/ui-test/platform.db node packages/database/src/db.js --init
PLATFORM_DATA_DIR=E:/tmp/ui-test PLATFORM_DB_PATH=E:/tmp/ui-test/platform.db node packages/database/src/seed.js
# 起服务：server 8787 + vite dev（admin 5173 / org 5175 / website 5176）
# 演示账号：root/admin123（admin）、org-admin/org123（org）、student-1/study123（学生）
```

本轮用过的一套（可复用）：库放 `E:/tmp/mfa-ui/platform.db`，起法见下；**本地 mock 提供方产出的音频/视频
不是真文件**（assetUrl 是 `mock://…`），所以本地永远「播不出声」，要验播放必须用真实渠道或线上。

```bash
# 后端（8787，库与演示数据沿用）
PLATFORM_DATA_DIR=E:/tmp/mfa-ui PLATFORM_DB_PATH=E:/tmp/mfa-ui/platform.db PORT=8787   DEPLOYMENT_MODE=local-mock AI_PROVIDER=local-mock node apps/server/src/index.js
# 三端 dev：npx vite apps/{admin,org,website} --config apps/{admin,org,website}/vite.config.mjs
# 按生产布局自测「三端同源」（verce 生产是 iicili.cyou 的 /、/admin/、/org/）：见下列临时脚本
```

**临时同源验证环境**（不在仓库，可随时重建）：一个 8090 端口的静态服务，
把三端 dist 分别挂在 `/`、`/admin/`、`/org/`，`/api` 反代到本地后端，
用于复现「同源三端」的会话/CSP/播放这类只有在生产布局下才暴露的问题。
本轮用它验证了会话分桶、音乐框体 UI、音视频播放。

**画布浏览器验证脚手架**（都不在仓库，`E:/tmp` 下，可复用）：

```bash
node E:/tmp/canvas-round6-env.mjs          # 独立临时库 + 后端 8789 + 一个 CANVAS 课时配好「主图/短片」两个框体，
                                           # 打印 projectId 后驻留（不碰生产、不碰用户的 8787）
node E:/tmp/serve-prod-website.mjs <dist目录> 6176 http://127.0.0.1:8789   # 静态服务 + /api 反代，
                                           # 用本地产物或「生产实际下发的」产物在本地打开画布页
node E:/tmp/place-nodes.mjs <projectId>    # 把项目里两个框体摆到确定坐标（便于做拖拽/连线验证）
```

**参考组件「原始 CSS」的取法**（2026-09-10 第七轮，看参考实现到底生成了什么时用）：

```bash
# E:/tmp/beam-compare 下有现成的：把 border-beam 的 dist 拷进来，用 react-dom/server 渲染真实组件，
# 组件会把生成的 CSS 内联在 <style> 里，直接抓出来就是参考的原始 CSS（比读源码猜可靠得多）
node E:/tmp/beam-compare/dump.mjs '{"size":"md","colorVariant":"colorful","strength":0.67,"borderRadius":30}'
node E:/tmp/beam-compare/mkhtml.mjs   # 生成并排对比页（左边真实组件、右边我们的 CSS），本地起静态服务看
```

**仓库自带冒烟脚本（2026-09-11 第二十二轮复跑：58 个里 54 个退出码 0；p34、p39、p45、p47、p48、p49 是纯静态检查，不起服务）**

其余 4 个**都不是回归**，逐个核过：`p11` 跑完不退出（见上面的说明，已打印 passed）、
`p6-learn-portal-e2e` 是已知失效用例（同上）、`p6-a01-openai-compatible-adapter` 与
`p6-a01-provider-policy-e2e` 在 `fdd1dc8`（改动前的 HEAD）上就失败 —— 用 `git stash` 回 HEAD 复跑确认过，
与附件这两轮改动无关。

```bash
node scripts/p49-illustration-planning.mjs
node scripts/p48-icon-name-guard.mjs
node scripts/p47-document-image-embedding.mjs
node scripts/p46-office-independent-reader.mjs
node scripts/p45-office-documents.mjs
node scripts/p44-attachment-accept-parity.mjs
node scripts/p43-multipart-filename-encoding.mjs
node scripts/p42-vibecoding-attachment-shape.mjs
node scripts/p41-artifact-fallback-naming.mjs
node scripts/p40-course-publish-authorization-gate.mjs
node scripts/p39-canvas-dirty-guard.mjs
node scripts/p38-student-canvas-upload.mjs
node scripts/p37-multi-device-sessions.mjs
node scripts/p36-org-help-feedback.mjs
node scripts/p35-student-chosen-params.mjs
node scripts/p34-hook-order-guard.mjs
node scripts/p30-provider-error-detail.mjs
node scripts/p33-canvas-persistence.mjs
node scripts/p32-session-capability-defaults.mjs
node scripts/p31-music-generation-box.mjs
node scripts/p29-session-scope.mjs
node scripts/p28-generation-boxes.mjs
node scripts/p27-provider-capability-validation.mjs
node scripts/p26-vibecoding-chat-ops.mjs
node scripts/p25-vibecoding-works-plaza.mjs
node scripts/p23-website-cms.mjs
node scripts/p22-platform-mfa.mjs
node scripts/p21-platform-export.mjs
node scripts/p20-canvas-text-slot.mjs
node scripts/p19-platform-account.mjs
node scripts/p18-vibecoding-submit.mjs
node scripts/p16-vibecoding-chat.mjs
node scripts/p15-canvas-versions.mjs
node scripts/p14-list-pagination.mjs
node scripts/p13-usage-work-linkage.mjs
node scripts/p12-lead-loop.mjs
node scripts/p11-video-first-frame.mjs
node scripts/p9-r03-platform-permissions.mjs
node scripts/p9-r04-ai-lesson-capability-guards.mjs
node scripts/p10-file-access-matrix.mjs
node scripts/p10-file-upload-security.mjs
node scripts/p6-classroom-delivery-mode-e2e.mjs
node scripts/p6-classroom-course-tree-e2e.mjs
node scripts/p4-o11-teaching-task-loop.mjs
node scripts/p4-o12-ai-generation-queue.mjs
node scripts/p4-o13-ai-generation-failure.mjs
node scripts/p6-unified-login-e2e.mjs
```

- 这些脚本都是自包含的临时 SQLite，不碰默认库/生产库，可直接跑。
- `p40-course-publish-authorization-gate.mjs`（2026-09-11 新增）盯住「发布 ≠ 授权给机构」：
  直接调 handler（不起服务）验证 未授权机构看不到 → 授权后可见 → 过期立刻不可见 → 续期覆盖 →
  撤销 → 撤销后重新授权 → 下架退出广场 →「不上架」课包授权后机构照样能用。
- ⚠️ `p11-video-first-frame.mjs`（`p9-r04` 同类）**跑完后不会自己退出**：断言全过、
  打印 `P11 video first-frame guard passed`，然后进程挂着（`handleAiGeneration` 留下了活句柄），
  全量复跑时必须给它套 `timeout`（例如 `timeout 120 node scripts/p11-...mjs`），
  否则整个循环会卡在这里。这是历史遗留、与业务无关。
- `p9-r02-feature-flags.mjs` 已随灰度开关模块删除。
- `p6-learn-portal-e2e.mjs` 是**既有失效用例**（断言 `LoginPage should call navigate("/learn")`，与现在「学生登录回 `/`」的设计冲突），不是回归。
- `p10-file-access-matrix.mjs` 会在仓库根目录留 `.tmp-p10-access-*.db`（已被 `.gitignore` 的 `*.db` 忽略，可随手删）。
- `p38-student-canvas-upload.mjs` 覆盖学生把文件拖进画布走的上传通道：仅本人可见、能自己下载、
  未登录取不到、能在自己列表里看到、可执行文件被拒。
- `p37-multi-device-sessions.mjs` 覆盖同账号多设备在线：两次登录都可用（旧策略会顶掉第一个）、
  超过 10 台把最老的顶下线、退出登录只注销自己、撤销单台设备、改密码踢掉全部。
- `p36-org-help-feedback.mjs` 覆盖问题反馈闭环（学生提交 → 机构端列表/详情/处理 → 学生端看到状态），
  这是修掉「拆 communication.js 时把 helpFeedbackRows / HELP_* 留在各自文件里，跨文件调用直接
  ReferenceError、接口全 500」之后加的回归网。
- `p35-student-chosen-params.mjs` 覆盖「平台留空＝学生自选」：留空框体下发空参数 + paramOptions、学生选的值落库、
  越界值当场 400、平台锁定的参数学生传什么都不采信。
- `p34-hook-order-guard.mjs` 是**纯静态扫描**（不连库、不起服务、秒出结果），挡两类整页白屏：
  ① hook 写在提前 return 之后（React #310）；② 组件里用了 `setXxx` 却没在同一个组件里声明
  （`ReferenceError`，2026-09-11 官网首页白屏就是这一类）。命中即 exit 1 并打印文件:行号。
  它按「缩进 0 的组件」分段比较声明/使用，所以要改它、或写同类扫描时，先按
  「把已知修复撤掉 → 守卫必须报出来」自证一遍（见第五节第 37 条列的那些误报坑）。
- 历史上做接口验收的一次性脚本在 `E:/tmp/ui-test/`（`capability-api.mjs`、`probe-video-upstream.mjs`、`verify-i2v-prod.mjs` 等），**不属于仓库资产**，随时可删；要长期保留的用例请按上面的形式写进 `scripts/`。
- **浏览器验证脚手架**：`E:/tmp/canvas-browser-env.mjs`（一次性脚本，不在仓库）——建独立临时库 + 后端 8789 +
  给 CANVAS 课时配生图框体 + 用 student-2 建项目 + 先跑一次生成，然后 `vite preview` 6176 指过来，
  就能在真实浏览器里打开画布页并顺带验「刷新后恢复生成状态」。改前端组件后建议跑一次（见第四节第 8 条）。
- **第十六轮新增的两个浏览器脚本（在仓库的 `.tmp/` 下，已被 .gitignore，不上库）**：
  - `.tmp/pan-check.mjs`：画布面板跟随验证 —— 起 Chromium → 选中框体 → 在画布空白处拖动平移，
    rAF 逐帧采样「框体屏幕坐标 / 面板屏幕坐标 / 视口 transform」，输出位移差与面板-框体间距。
    前置：`node E:/tmp/canvas-round11-env.mjs`（后端 8789 + 打印 projectId）、
    `node E:/tmp/place-aspect.mjs <projectId>`（摆两个框体）、
    `node E:/tmp/serve-prod-website.mjs E:/学习平台正常/apps/website/dist 6176 http://127.0.0.1:8789`。
    注意：每次跑前要重跑 place-aspect，否则上一次平移的视角会被存进快照、框体落在侧栏底下点不到。
  - `.tmp/serve-admin.mjs` + `.tmp/admin-check.mjs`：把 `apps/admin/dist` 挂在 `/admin/` 下起静态服务
    （6177，/api 反代 8789）并用 Chromium 打开课包详情，验证可见范围文案、授权时效预设、
    发布检查那几行 —— 管理端的改动用这个开一次。
  - `.tmp/panel-scenarios.mjs`：面板位置的场景矩阵（初始 / 大幅平移 / 反向平移 / 拖框体），
    逐帧断言「居中(centerDX=0)、间距恒 14px、零重叠、松手零跳变、平移不被拽回」。
    用 `HOME_BASE=http://127.0.0.1:6181` 可以指向**生产实际下发的产物**（先 scp 下来 +
    `serve-prod-website.mjs`），这是我验收画布改动的标准动作。
    ⚠️ 选拖动起点时别选到工具栏（`.learning-canvas__toolbar` 在画布右上角，1326~1471 x 128~176）和
    小地图（右下角，pannable！拖它会整块跳走）—— 我自己就踩了两次，白测一轮。
  - `.tmp/offscreen-pan.mjs`：把框体平移到画布外，验证面板跟着出去、不夹回边上。
  - `.tmp/drop-test.mjs`：**把文件拖进画布**的端到端验证（真 DataTransfer + 真 drop 事件）——
    断言页脚加号已删、拖进去 120ms 内出现「上传中」框体、传完 `<img src>` 是 blob: 且无破图/无 401、
    左侧「本地素材」里出现该文件。用 `page.route` 把上传接口延迟 1.5s 才看得清中间态。
    起点：`node E:/tmp/canvas-round11-env.mjs` + `node E:/tmp/place-aspect.mjs <projectId>`。
  - `.tmp/overlay-test.mjs` + `.tmp/make-two-refs.mjs`：**面板里的浮层有没有被裁/被遮**。
    后者先把项目摆成「两张图片素材 + 一个视频框体、两条连线都连过去」的现场，
    前者逐个量浮层的 rect（并向上找第一个会裁剪子孙的祖先）、再用 `elementFromPoint` 做命中测试：
    参考缩略图的 ×、hover 大图预览、@ 引用候选列表里的**每一项**都要 visible 且命中得到自己。
    注意：浮层测试要选**视频框体**（`filter({hasText:'素材2'})`），别再按 nth(1) 点，会点到图片节点上。
  - `.tmp/serve-with-csp.mjs`：带**生产同款 CSP** 起静态服务，用来复现「fetch 成功但图不显示」
    这类只在线上出现的问题（见第五节第 41 条）。
  - `.tmp/home-check.mjs`：官网首页开一次（**学生会话 + 未登录各一遍**，并点开右上角学生下拉），
    断言无 pageerror + 正文非空。`HOME_BASE=http://127.0.0.1:6178` 可以指向「线上当前下发的产物」，
    用来对照复现线上问题。**改官网组件后务必两张会话都跑** —— 2026-09-11 的白屏就只有「已登录学生」
    这一条路径会触发，未登录一切正常。
  - **图片上传整条链路的三件套（2026-09-11 新增，`.tmp/`，不入库）**：
    `vision-upstream.mjs`（记录型「视觉上游」：把收到请求里内联的图**真解码**、按左右半边报颜色，
    请求体原样落 `.tmp/vision-requests.jsonl`）→ `vision-env.mjs`（临时库 + VIBECODING 课时 +
    后端 18789 + 把**渠道策略写进库**指向假上游；⚠️ 只设 `AI_PROVIDER` 环境变量无效，见第五节第 49 条）
    → `vision-ui.mjs`（Playwright：点按钮选图 / 拖图进来 → 发送 → 断言请求体里的内联 base64、
    气泡缩略图、以及"模型"从图片字节解出来的颜色）。
    跑法：先 `vision-upstream`、再 `vision-env`（都前台常驻），然后 `vision-ui`。
    ⚠️ 重跑前先确认 18789/19999 上没有残留进程（第 50 条那种白跑一轮就是它）。
  - **文档产物端到端（2026-09-11 新增，`.tmp/vision-doc.mjs`）**：真开浏览器要一份 PPT/Excel/Word
    → 产物卡片出现 → 点下载 → **用 python-pptx / openpyxl / python-docx 独立读回**校验内容。
    20 项断言；前置与上面三件套相同（上游 + 环境 + 静态服务 6176）。
    注意下载是**异步**的（先 fetch 鉴权接口再 createObjectURL），拦 blob 要等捕获到再还原。
  - **插画链路（`.tmp/vision-illustration.mjs`，10 项）**：要一份带插画的 PPT → 上游确实收到生图请求
    （带模型写的提示词、≤3 次）→ 预览里图能加载 → 下载的 pptx 里内嵌图与这次生成的图 **SHA256 一致**。
    ⚠️ 假上游要认 **（单数）**；另外测试里等图要等「换成了新那张」，
    只等「有图且加载完」会量到上一版（我踩过）。
  - **工作台行为（`.tmp/workbench-ui.mjs`，12 项）**：进来时工作台收起 → 要一份 PPT →
    自动弹出且预览到这份 PPT → 关闭键有图标可点 → 传图后预览里能看见图 → 手动关闭能收起。
    **改工作台/预览相关的东西就跑它**：这一轮三个问题（空按钮、默认常开、预览看错产物）
    都是断言看不出来、必须开浏览器才发现的。
  - **配图链路（`.tmp/vision-doc.mjs` 的第 ④ 节）**：真传一张图 → 要带图的 PPT → 点下载 →
    用 python-pptx 把内嵌图按 SHA256 与原图比对。⚠️ 图片得是**真图片**（我用一张真的 1×1 PNG），
    非法字节会被渲染器按「这一页不放图」降级处理，测不出内嵌。
  - 这几个脚本都要用 `"C:/Program Files/nodejs/node.exe"` 跑：默认 PATH 里的 node 是 v16，
    playwright-core 会直接报「requires Node.js 20 or higher」。

**生产 admin 密码不是演示密码**，浏览器验证管理端要用本地环境。

---

## 八、文档一致性提醒

```text
5. 仓库根 THIRD-PARTY-NOTICES.md + docs/third-party/opensquilla-LICENSE.txt：
   VibeCoding 控制台的设计令牌与尺寸常量取自 OpenSquilla（Apache-2.0）。
   动 packages/shared/src/console/ 里的令牌或关键尺寸前先看一眼那份声明。
```

```text
1. docs/operations/验收清单.md 已随本次更新修正「内测服务健康」一项（内测已下线）。
2. deploy/production/RUNBOOK.md 的「回滚到内测」章节已标注失效；
   回滚改走 rollback-production.sh / 切 release 软链。
3. deploy/internal-test/* 是内测时代的文档，只作历史参考，不要照做。
4. 凡与本文件冲突的旧文档，以本文件 + 第二节只读复核结果为准。
```

---

## 九、给新对话的建议

```text
1. 先读本文件 + docs/architecture/代码结构与路由.md + 积分系统设计.md
2. 不要默认"代码已修复=线上正常"，先做真实浏览器/接口回归
3. 改生成链路前先看：apps/server/src/services/modelCapabilities.js、
   apps/server/src/services/openaiCompatibleProvider.js、apps/server/src/routes/aiGeneration.js
4. 改画布/框体前先看：packages/canvas/src/index.jsx、packages/shared/src/canvasWorkspace.jsx
5. 每次改动：本地构建 → 本地临时库验证 → 提交 → 备份 → 生产部署 → 健康检查
6. 不要直接删积分字段、不要恢复 apps/student、不要把 internal-test 当生产（已删除）
7. 生产服务器内存紧张：新增常驻服务前先看 free -m
```

---

## 相关文档

- `docs/operations/验收清单.md` —— 每次发布前的人工验收清单
- `docs/architecture/代码结构与路由.md` —— 代码结构与路由现状
- `docs/architecture/积分系统设计.md` —— 积分/扣费设计
- `deploy/production/README.md`、`RUNBOOK.md` —— 生产部署与运维
