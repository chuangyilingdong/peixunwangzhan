# 新对话交接 · 第二十八轮（生成产物归档 + 读面不再靠外链 / 开新会话先读这一份）

> **新会话先读这一份。** 本文覆盖 2026-09-22（第二十七轮之后）这一段的施工。
> **口径 88、坑 109–115 在这一份里**；承接第二十轮 **1–6 / 1–11**、第二十一轮 **7–24 / 12–36**、
> 第二十二轮 **25–36 / 37–47**、第二十三轮 **37–51 / 48–65**、第二十四轮 **52–57 / 66–71**、
> 第二十五轮 **58–60 / 72–75**、第二十六轮（客户端更新那条线）、第二十七轮 **61–87 / 76–108**
> —— 正文里「见口径⑰」「坑㉜」这类引用**七份通用**。

**这一轮做的事（细节在 §二）**：

```
A  补完上一轮欠的两处核验   ①画布「老师结束课堂 → 学生端自己退出」真浏览器跑通（提示 7.5s 出现、
                            1.4s 后跳 /learn）；②机构端作品预览单独截了图（读面对、默认停在「作品内容」）。
                            顺带查清 `--click` 点不动的真因：**同名按钮有多个它只点第一个**。
B  page-shot 长出一个钩子   `--then <文件.mjs>`：页面加载后跑一段真动作（等状态翻转、看跳没跳、自己断言），
                            返回 `{ expectRedirect }` 时把「被重定向了」那条断言**换准**而不是关掉。
C  ⭐⭐ 生成产物归档        **上游给的是临时地址、会用完就 403**（实测 getapib.org 那 21 条里 7 条已经失效），
                            而作品读面只存了引用 → 学生/老师/访客看到一片坏图。现在生成成功时把产物
                            下载回来存成**学生本人的私有素材**，读面认 fileId 就能显示。
D  三条读面一起接上        广场（新公开代理 `/api/public/works/<token>/images/<fileId>`）、机构端
                            （准入补归档件 + 列表下发 imageUrls + 前端 resolveSrc）、画廊（失效时说人话）。
E  存量回填                 `deploy/production/backfill-generated-media.mjs`：32 条里**24 条**收回本机
                            （14 图 / 10 视频 / 1 音频，约 49MB），8 条上游已经 403、救不回来。
F  ⭐⭐ 画布自动保存的路由**不可达**（用户报的图6）  第二十七轮插进来的 session-state 把复用的 `match`
                            覆盖了 → `PUT /projects/<id>` 落到「接口不存在」。**学生的画布改动一条都没存**。
                            修 + p128（打真 HTTP 的守卫，含反向自检）。这是本轮最重的一条。
G  用户同一天的另外四条观感/交互（图1/图2/图4/图5）  四类框体统一引导插画｜框体跟着画幅变形｜
                            全能参考不再显示首帧/尾帧｜文本区用 xyflow 的 `nowheel` 滚文本（坑 114）。
H  作品读面卡片化 + 两行式播放器（用户报的另两条）  媒体从"一张大图铺满"改成**卡片网格 + 点开放大**；
                            音乐框体的播放器改成**进度条一行、按钮一行**（原生控件在窄框体里就是会被压没）。
I  官网首页四个图标换真 SVG（用户 2026-09-23）  用户给了 topics/svg-icon，取了排第一的 css.gg（MIT，内联不引依赖）；
                            关键是**生产 CMS 里存的是老字符**，得靠映射才换得动（改完线上已生效）。
J  页脚按参考稿重做 + 首页也有页脚（用户 2026-09-23）  四列 + 三团漂移光晕（纯 CSS 复刻，不引 framer-motion）；
                            **首页原来把页脚排除了**，现在有了。用户看过页面后又让删掉品牌行那一排
                            与那句描述、并把底色从纯黑改成**跟首页一个色调的渐变红**。
K  课时素材默认值（用户 2026-09-23）  建课时少手填：提示词 → 「提示词（N）」；生图 → 图片框体 + 2.5 低价版 + 1k；
                            生视频 → 视频框体 + MiniMax-H3 + 480P + 5 秒 + 带音频 + 声音参考。
                            切类型与换「生成什么」**两条路都给**；老师改过的名字不会被覆盖。
```

---

## 一、当前生产状态（照这个对）

```text
release 20260923T044321Z / commit bd18c3c（服务 active、NRestarts=0、enabled、ExecMainStatus=0）
  五个入口全 200：/、/admin/、/org/、/student/、/api/health
  · ⚠️「生产跑的是哪一版」只认 release 目录里的 BUILD-METADATA.txt，不要拿分支 HEAD 去对
  · 这一版里含本轮的**全部**改动：生成产物归档（含"按上游声明的 MIME 判断"那一版）、
    广场的作品专属图片代理 `/api/public/works/<token>/images/<fileId>`、机构端预览的 imageUrls/resolveSrc、
    画廊失效提示。产物里逐项核过：`generatedAssetArchive` / `publicCanvasWorkImageMatch` /
    `generated-asset-archive` 都在。
  · 数据侧**已跑过两轮回填**：32 条站外媒体里 **24 条收回本机**（14 png / 10 mp4 / 1 音频，约 49MB），
    8 条上游已经 403、救不回来；快照改写 11 行（student_projects 7 / works 4）。
  · 本会话三连发：`5d2e331`（画布自动保存路由不可达，P0，见 §二.F）→ `94293e8`（用户报的另外四条观感/交互，见 §二.G）
  · 上一版（可回滚）：release 20260923T042116Z / commit a58fac2；再往前 20260923T041043Z / commit 25e9460
release 目录：**12 份**（今天发了两次；口径是留 10 份，下次发布前记得 prune）
磁盘 / 内存 / 在途生成 / 广场件数：见第二十七轮 §一 那张表（本轮没改这些口径）
```

> 本轮两次发布的 commit：`75d4b07`（第一版归档）→ `83b2a04`（把"按容器品牌拦"改成"按声明的 MIME 判"）。
> 第二次发布是因为**真数据把第一版否掉了**（见 §二.C 与坑 111），不是功能追加。

---

## 二、这一轮做了什么

### A. 补完上一轮欠的两处核验（第二十七轮 §五 第 6 条）

**① 画布「老师一点结束课堂，学生端就自己退出」—— 真浏览器，通过。**
走真链路：真浏览器里的学生画布 + org-admin 的**真接口**结束课堂（不是直接改库）。实测：

```text
结束前页面上没有那条提示（对）
老师结束课堂（接口 200，9ms）
结束 → 7526ms 提示出现（画布每 10 秒问一次 session-state，落在窗口内）
页面上的原话：老师已结束课堂（MANUAL），正在回到课程中心…
提示 → 跳走 1435ms（口径是留 1.6 秒）
落点 /learn，页面上是「我的课程」
```

截图三张在服务器 `.tmp/page-shots/sessend/`（提示那一刻、落点、跳之前）。核验脚本
`.tmp/then-session-end.mjs`（用下面 B 的 `--then` 钩子写的）。

**② 机构端/老师端作品预览（用户图5）—— 真浏览器，读面通过。**
`/org/works` 点「查看作品」→ 预览块「作品内容 · …」打开、**默认停在「作品内容」**（不是画布）、
「作品内容 / 创作画布」两个标签都在。截图 `.tmp/page-shots/orgwork/`。

⚠️ **同一次运行撞出了 C 那个真问题**：那 3 张图在浏览器里是**坏的** —— 脚手架日志里
`ERR_BLOCKED_BY_ORB https://getapib.org/...`。追下去才知道上游那些地址已经 403（见 §二.C）。

### B. `page-shot` 长出 `--then` 钩子（顺带查清 `--click` 为什么"点不动"）

`--click` 只能按文案点一下；而"老师结束课堂 → 学生端自己退出""点开预览看弹窗里的东西"这类
要**等状态翻转、看跳没跳**，按一下按钮什么都验不出来。新增 `--then <文件.mjs>`：
它导出的 `run({ page, api, db, log, shot })` 可以任意 await / 断言 / 自己截图，返回值进 `report[i].then`；
返回 `{ expectRedirect: '/learn' }` 时，底下那条「被重定向了」的断言会**改成"必须落到这个地址"**
（是把断言换准，不是关掉）。

**`--click` 点不动的真因**：机构端「作品管理」每一行都有一个「查看作品」（实测 **8 个**），
而 `--click` 只取 `first()`，命中哪一类全看列表排序 —— **不是按钮点不了**。这条已经写进脚手架头部。

### C. ⭐⭐ 生成产物归档：上游给的是**临时地址**，我们只存了引用

**怎么发现的**：验机构端作品预览时，浏览器日志里 3 条 `ERR_BLOCKED_BY_ORB`。
追下去——那些媒体地址全是站外的上游图床，逐条 curl：

```text
media_assets 44 行里，32 行的 asset_url 是**站外**地址：
  getapib.org（图）21 条、腾讯 COS（视频/音乐）11 条
getapib.org 那 21 条实测：14 条还 200，**7 条已经 403**（CloudFront 回 application/xml）
  → 浏览器按 ORB 挡掉 → 读面里就是一片坏图占位
视频那条域是好的（200 / video/mp4 / 513KB）
```

「任务成功」不等于「这件东西以后还在」—— 与口径 71（交给外部的素材对方可能读不到）是**同一族的另一半**：
外部给的地址，对方随时可以收回。

**修法**（`apps/server/src/services/generatedAssetArchive.js`）：生成成功时把产物**下载回来**、
存成**学生本人的私有素材**（`/api/student/file-assets/<id>/download`，与「学生自己传的图」「PPT 插画」
逐字同一条路），再把 `media_assets.asset_url/preview_url` 换成它。

三条刻意的取舍（**两条方向的失败策略正好相反，别改混**）：

```text
① 这个方向失败**绝不报错**：学生已经花过钱、上游已经出片，为了一次归档失败把整条成功判死，
   比丢一个地址糟得多 → 一律 best-effort，拿不到就保留原地址 + 在 metadata.archive 里记原因。
   （反面 upstreamMediaMirror 是"我们的素材传给上游"，那边失败**必须报错** ——
     静默的后果是"生成出一段跟参考无关的作品"。）
② 存成**私有**素材（PRIVATE + /api/student/...），不是 PUBLIC_PLATFORM：
   未提交作品里的图不该有公网地址。发布时广场走"作品专属公开代理"（见下）。
③ 只收**我们自己落盘校验认得的类型**（png/jpg/webp/gif/mp4/webm/mp3/wav/ogg）。
   上游会给 QuickTime 容器（`.mov`，品牌 `ftypqt`）—— 字节嗅探只看得到 `ftyp`、会判成 mp4，
   而浏览器**播不了** qt 品牌（2026-09-21 那次 `.mov` 事故）。存一份"看着是 mp4、其实播不动"的东西
   比留个会过期的外链更糟（它看起来是好的）→ 直接跳过并记原因。
```

### D. 三条读面一起接上（少一处就是"图还是不出来"）

产物变成我们自己的私有地址之后，**四处读面各有各的取法**，一处不接就是"看得到作品页、图却 403"：

```text
· 学生端「我的作品」/ 报名页：早就有 imageUrls + resolveSrc（转 data:）—— 不用动。✓
· 课堂预览 ClassroomWork：早就有 snapshotImage —— 不用动。✓
· **广场**（新增）：访客未登录，拿不动 /api/student/... →
    · 新路由 `GET /api/public/works/<token>/images/<fileId>`，准入与作品详情**逐字同一条件**
      （`share_token=? AND is_public=1`）+ fileId 必须出现在这份作品的快照里；
    · `publicWorkRow` 把 fileId 形式的媒体地址换成这个代理 → 前端 `srcOf` 直接用 `item.url` 就能显示，
      **前端一行没改**。
· **机构端**（补齐）：作品详情那条路由的准入清单原来只认画布快照里的素材 →
    把**归档件也算上**（`media_assets` 里的 fileId）；列表下发 `imageUrls`（批量查一次，
    别每行一个查询）；前端预览补 `resolveSrc`（`<img>` 发不出 Authorization 头，要转 data:）。
· 画廊：加载失败时显示「图片已失效，读不出来了」，而不是只留一个浏览器破图图标。
```

### E. 存量回填（这以前产出的东西不会自己变好）

`deploy/production/backfill-generated-media.mjs`，**默认干跑、要 `--apply` 才写**。三处一起改：

```text
① media_assets.asset_url / preview_url   —— 读面「做出来的东西」那一栏的来源
② student_projects.canvas_snapshot       —— **画布本身就存着那个地址**
③ works.canvas_snapshot                  —— 提交时抄的一份，**广场的媒体清单正是从它提的**
只改 ① 等于没改（广场依旧显示那个会过期的外链）。
```

**在副本上实跑的结果**：

```text
32 条目标 → 归档成功 25 条（14 png / 10 mp4 / 1 mp3，49MB，落到上传根）
            跳过 7 条：上游已经 403（救不回来，保留原地址）
复查：已归档 25；仍挂外链 7；快照里残留的**已归档**地址 0 处
```

⚠️ **这个脚本中途被杀过一次（被 580 秒的 timeout 掐掉），因此改了设计**：替换表从
`metadata.archive.sourceUrl` **从库里推**，而不是用"本次跑出来的那份内存清单" ——
否则库已经改了、快照还没改，下次重跑只看"这次成功了几条"就**永远漏改**那批快照。
现在它天然可续跑，跑第二遍会报「无需改动」。

### F. ⭐⭐ 「保存失败：接口不存在」= 画布自动保存的路由**根本不可达**（用户 2026-09-22 报的图6）

**这是这段里最重的一条：学生的画布改动一条都写不回服务器**，而界面上只是顶栏角落里一行小字。

根因在服务端，不在前端：`apps/server/src/routes/student.js` 那个大函数里 `match` 是**复用的一个变量**。
第二十七轮 §二.S 在 GET 之后、PUT 之前插进了 `session-state` 那条，把它覆盖成
`/projects/([^/]+)/session-state` —— 于是 `PUT /api/student/projects/<id>` 走到
`if (match && method === 'PUT')` 时 `match` 已经是 **null**（那个地址里没有 `/session-state`）→
整个分支被跳过 → 落到最外层变成 `ROUTE_NOT_FOUND` → 前端显示「保存失败：接口不存在」。

修法：那个分支前面**重新 match 一次**（PATCH/POST/DELETE 用的是同一个正则、没受影响，一并核对过）。
新增 **p128**：起真服务、发真 HTTP，钉 ①建项目 ②PUT 带 autoSave 走通 ③内容真的落库
④同路由的改名也通 ⑤【反向】不存在的子路径仍是 `ROUTE_NOT_FOUND` ⑥紧挨着的 session-state 没被碰坏。
**反向自检**：把那行去掉 → ② 立刻红，报的就是 `{"code":"ROUTE_NOT_FOUND","message":"接口不存在"}`，
与用户截图逐字一致。

### G. 用户同一天报的另外四条画布观感/交互（图1/图2/图4/图5）

```text
图1  引导插画只有图片框体有，视频/动画/音乐/文字还是 ▶/✧/♫/✎ 那套老占位
     → 五处统一成 learning-node__art--illustration，老占位文案一并删掉。
图2  「选择不同的尺寸，框体应该也要跟着变」——画幅选取器写的是 studentParams.aspectRatio，
     而框体尺寸读的是 data.aspectRatio，两个字段 → 学生点了 9:16 框体纹丝不动。
     新增 boxDisplayRatio(data)（优先学生选的、其次课包定的），8 处 NodeFrame 都改读它。
     真浏览器实测：选 9:16 前 226x140 → 后 169x327。
图4  「右边的滚轮目前只能用鼠标手动拉，用滚轮就是画布缩放」→ 用 xyflow 自己的 `nowheel` 类（见坑 114）。
图5  「全能参考的模式，为什么画面那还显示首帧尾帧」——面板显示哪一行原来跟着 useOmni，
     而它要求"已经连了东西"才为真；锁成全能参考但还没连线时就退回「画面/首帧未连接」那一行。
     拆出 omniRow：**面板跟着模式走、payload 跟着连线走**，没锁时两者完全相同。
```

核验：真浏览器（`.tmp/fixture-canvas-omni.mjs` + `.tmp/then-canvas-ui.mjs`，见 §六）——
8 个框体 8 张引导插画、老占位 0 处；9:16 后框体从 226×140 变成 169×327；
全能参考下没有「首帧/尾帧未连接」；在文本区滚滚轮画布**不再缩放**、在画布上滚仍缩放。
源码级另加 **p129**（10 条，含一条反向自检：不许退回 `onWheel` 那种修法）。

⚠️ 这四条一开始**一条都照不出来**：夹具挑课时的规则太宽 —— 先挑到内容很薄的「测试2」
（左侧面板里只有一条素材），再挑到**没授权给本机构**的课包（建课堂报 `COURSE_NOT_ASSIGNED`）。
最后落到「**这个学生已被授权的课包**里、框体最多且带视频框体的那节课」（1-9）。
**教训**：夹具挑数据要按"能不能走完后面所有步骤"来挑，不是按"有没有那个字段"。

### H. 作品读面改成**卡片 + 点开放大**、音乐框体播放器改成**两行式**（用户 2026-09-22 报的另两条）

```text
第 1 条原话：「提交的作品，如果是画布的作品，一个画布课堂可能会产出很多图像和视频/音频，
             这里应该是一个卡片形式展示，点击后放大。而不是现在这样一个大图，还看不完比如下拉。」
  · 原来：图片撑满一列、视频/音频整行铺开（`grid-column: 1 / -1`）→ 一件作品出十来张图
    就是几屏长的瀑布，看不到全貌。
  · 现在：**固定 4:3 缩略图卡片平铺**（`repeat(auto-fill, minmax(150px, 1fr))`）+
    **点开浮层看完整那一份**（图片/视频原尺寸、音频给播放器、文字给整段），
    Esc / 关闭按钮 / 点背景都能关。
  · 四个读面共用 `WorkMediaGallery`（广场 / 我的作品 / 机构预览 / 课堂预览）→ 一处改全改到。
  · 真浏览器实测：5 项媒体 → 5 张 159px 卡片（改前会是一张 1184px 的大图）。

第 2 条原话：「画布课堂音乐生成出来，这个播放器进度条被压缩很小了，有没有可能是两行，
             第一行是进度条，第二行才是操作按钮这些。」
  · 原因：原生 `<audio controls>` 是**浏览器控件**，内部布局改不了 —— 框体一窄，进度条就被压成一小段。
  · 现在：新增 `packages/canvas/src/AudioPlayer.jsx`（**进度条一行、按钮 + 时间一行**），
    **音乐框体与作品读面共用同一个组件**（别各写一份）。
  · 真浏览器实测：进度条 234px / 卡片 260px（占满整行）、按钮在下一行、时长 `0:00 / 0:50` 读得出来。
```

守卫 **p130**（11 条，含一条反向自检：两处都不许再用原生 `<audio controls>`；
⚠️ 判之前要**去掉注释** —— 那两份文件里恰好都写了这句说明，直接 grep 会假红）。

⚠️ 这次重构也让**两条老守卫跟着换了判据**（都不是新问题，是"断言钉着旧形状"）：
`p127 ⑯`（失效提示从一处变两处）、`p64 ②`（原来钉 `<audio controls>`）。两条都改成钉**同一件意图**。

### I. 官网首页数据区那 4 个图标：字符 → 真 SVG（用户 2026-09-23）

用户原话：「用这些 logo，把官网首页图1这4个logo换一下」，并给了
`https://github.com/topics/svg-icon`。**取那一页排第一的 css.gg**（10k star、MIT），
与站内其它图标同一条做法：**内联 SVG、不引依赖**（官网依赖里没有图标库，也不为四个图标加一个）。

```text
gg-album      → 标准课包（一本带书签的书）
gg-notes      → 课时总量（一叠带行的纸）
gg-board      → 课堂形式（一块分成三栏的板）
gg-briefcase  → 机构工作台（公文包）
尺寸用 1em、颜色用 currentColor —— 都跟着 `.hp-stat i` 那层走（实测 28x28 / rgba(255,255,255,.72)）。
出处与 MIT 许可写在代码注释里（口径：换图标时把许可说明一起换）。
```

**三件容易做漏的，都处理了**：

```text
① **生产 CMS 里是有值的**：HOME.stats 存的正是 ◆ ◇ ✧ ⌘ 那几个字符（2026-09-18 改版写进去的）
   —— 只改代码兜底的话**线上一个图标都不会变**。所以加了「老字符 → 新图标」的映射：
   不动生产数据就能立刻换掉；认不出来的（运维写了个 emoji）仍按文字渲染，不被吃掉。
② 三处来源（代码兜底 / CMS 兜底 / 种子默认值）**逐字段对齐**成图标名 ——
   原来是一处字符、一处名字的话，同一页会因为"接口通/断"显示两套东西（这条口径踩过）。
③ 后台「图标」输入框原来是 `maxLength={4}`：**图标名会被截断**（package→pack）→ 认不出来就退回文字，
   运维在后台永远配不出图标。放到 24、给可选值提示、新增项默认一个真图标。
```

核验：真浏览器（`.tmp/then-home-stats.mjs`）4 个格子 4 个 SVG / 28x28 / 旧字符 0 / 颜色继承；
**打线上**同样四项全过（生产 CMS 数据没动）。源码级 **p131**（11 条）。

### J. 页脚按参考稿重做 + **首页也有页脚了**（用户 2026-09-23）

用户原话：「页脚根据这个来改造下，官网下方也要有页脚」，给了参考稿 **Footer03Luma**
（Tailwind + framer-motion + lucide 的深色页脚：四列链接 + 三团漂移光晕 + 品牌行 + 社交图标）。

```text
✅ 照做的：深色底 + 四列链接 + 三团漂移光晕 + 链接悬停露出 ↗ + 品牌行（logo / 图标按钮 / 版权）。
⚠️ 不能照抄的三处：
 ① 参考稿那三个依赖官网**一个都没引** —— /faq 那次已经定过这条口径：不为一张页面引依赖
    进来跟全局 styles.css 打架。所以：布局用 Grid、光晕用 @keyframes 动 transform/opacity、
    ↗ 用 ::after、图标内联 svg（css.gg，MIT，与首页数据区那四个同源）。reduced-motion 下不飘。
 ② 参考稿那一排是**社交账号**（Twitter/GitHub/LinkedIn/YouTube），我们一个都没有 ——
    **不放假链接**（既有口径：「点了没用的比不放更糟」）→ 换成四个**真能点的入口**：
    邮箱（mailto）/ 联系我们（/demo）/ 下载客户端（/download）/ 机构后台（ORG_APP_URL）。
 ③ **首页原来把页脚排除了**：渲染处写的是 `{!isFullPage && loc.pathname !== '/' && <Footer/>}`
    —— 这一条正是用户要的。现在所有非全屏页（含首页）都有页脚，首页从"单屏不滚动"变成可以往下滚。
```

内容仍是我们自己的（四列：产品 / 合作 / 了解更多 / 条款与隐私），版权行照旧。
样式**全部带 `.site-footer` 前缀** —— 这张表是全站共用的，不带前缀会漏到别人的页面上。

核验（真浏览器三档 + 打线上两页）：四列、三团光晕都在动、四个按钮**零假链接**、↗ 配好、
390 窄屏不横向溢出。源码级 **p132**（11 条）；另跑 p70 / p115 官网守卫全过。

**用户看过实际页面之后又改了两处（同一天，release `a58fac2`）**：

```text
① 「图1图2这部分删除」= 品牌行那一排四个圆图标按钮 + 「面向教培机构与学校的 / 青少年 AI 通识与
   VibeCoding 开课平台。」那句描述。删掉后品牌行只剩「logo 左 / 版权右」一行。
   ⚠️ 那一排本来就是**换过的**（参考稿是社交账号、我们没有）→ 用户要删就一并删掉，
      代码里留了一句「别再放这一排」的说明，免得下一轮又有人照着参考稿加回来。
② 「页脚现在是纯黑，可以跟首页那样渐变红就行」：首页那片红**是视频背景里的颜色**
   （`.hp-bg` 里的 hero-animal.mp4），没有可复用的 CSS 渐变 → 按那个色调自己调了一条：
   上缘深（接得住首页 hero 的暗底）→ 中段酒红 → 下缘再压暗，另外加了一层顶部红径向光。
   三团光晕也跟着从「粉+紫+橙」改成暖红/粉 —— **紫色落在红底上会脏**，这是配色上的一条小经验。
```

⚠️ 改完之后**真浏览器报的那两项要跟着换**（`p132` 原来钉着"那一排四个按钮都指向真地址"，
那句在删掉之后必然是红的 —— 断言跟着口径走，不是删掉它）：现在钉
**「那一排已删干净 + 不许再出现 `href="#"` 的假链接 + 底色是渐变红」**。
另外 `then-footer.mjs` 也加了"图标按钮数应为 0""computed background-image 里要有渐变"两条。


### K. 课时素材的默认值：建课时少手填（用户 2026-09-23）

用户原话（三条，都是"建课时要一个个手填太麻烦"）：

```text
① 选「提示词」        → 素材标题默认「提示词（1）」
② 生成框体 - AI 生图  → 标题「图片框体」+ 模型 zhenzhen-image-g-v2.5-lowprice + 清晰度 1k
③ 生成框体 - AI 生视频 → 标题「视频框体」+ 模型 MiniMax-H3 + 清晰度 480P + 时长 5 秒 +
                        生成音频「带音频」+ 音频怎么用「声音参考（只借音色）」
```

实现（`apps/admin` 的课时素材编辑器）：

```text
· BOX_DEFAULTS + boxDefaultsFor(modality)：四个模态各一套（文字/音乐只给标题，模型与档位留空）；
  **比例刻意不设默认** —— 图/视频各有各的合适比例，留给学生在课堂上挑。
· **两条路都给默认值**：切素材类型（changeMaterialType）与换「生成什么」（changeBoxModality），
  用户两种都会用到。
· titleOrNext：只有标题还是"没改过"的（空 / `素材N` / `提示词（N）` / 四个框体名）才换默认标题，
  **老师改过的名字不会被覆盖**。
· 新增素材的标题也从「素材N」改成「提示词（N）」。
```

⚠️ **这几个值必须落在渠道配置里那个模型真实的能力清单上** —— 已实查生产配置确认：
2.5 低价版 = `1k/2k/4k`、MiniMax-H3 = `480P/768P` + `5/10/15` 秒 + `audio:true`。
写渠道里没有的档位，下拉框会显示成空值，而且换模型时会被「新模型不支持就退回学生自选」清掉。

核验：**真浏览器**（新脚本 `.tmp/then-box-defaults.mjs`：真打开课时抽屉 → 真新增一条素材 →
真改类型与「生成什么」→ 读回界面上那几个框）。实测：`提示词（2）`（这一组里已有一条 `提示词（1）`，
序号接着排 ✓）/ `图片框体` + `zhenzhen-image-g-v2.5-lowprice` + `1k` /
`视频框体` + `MiniMax-H3` + `480P` + `5` + `YES`（带音频）+ `VOICE_REFERENCE`（声音参考），全部命中。
源码级 **p133**（10 条）。

⚠️ **那条核验路上有三个坑**（都写在脚本注释里了，免得下次重摸一遍）：
① 课时列表在「课时编排」**标签页**里（默认停在「基本信息」）；
② 抽屉是**分步**的（1 基础与预算 / 2 学生能力与素材 / 3 教师素材与确认），素材区在第 2 步 ——
   不点过去的话元素在 DOM 里但尺寸 0×0；
③ 「＋素材」按钮被 CSS 弄成 **0×0**，Playwright 的 click / force click 都不认，只能**用 DOM 事件点**。

---

## 二·附：这一轮**验到了哪一步**（新会话照着往下接）

| 验的东西 | 怎么验的 | 结果 |
|---|---|---|
| 归档函数真的会落盘 | 守卫 p127 注入 fetch **真跑**（不联网）：字节 → 私有素材 → 磁盘上真有 PNG | ✓ 18/18 |
| 归档失败不判死生成 | p127 ④⑤⑥⑦：403 / 白名单外 / data: / 混合一批 | ✓ |
| 两条生成链路都接上了 | p127 ⑨ 钉住"两处都在结算前归档、且把**归档后**的清单交给结算" | ✓ |
| **归档在真环境跑得动** | 生产回填：从生产机真下载上游产物 **24 条**（49MB）落盘 + 改写库 | ✓ |
| **广场读面**（未登录访客） | **打线上**作品页 + 真 Chromium：图 940×1672 真出图、正文在、无「已失效」 | ✓ 见截图 |
| **机构读面** | 接口级：调 org 的作品详情/取图路由（副本 + 生产上传根） | ✓ 5/5 |
| 反向：不属于作品的 fileId 一律拒 | 同上第 ⑤ 条 | ✓ |
| **worker 在真生成时也会归档** | 走队列的真跑（¥0.75） | ✓ **第 7 次跑通**：任务 `SUCCEEDED`，产物地址是 `/api/student/file-assets/file_3b5567c253be42168f06/download`（**我们自己的私有素材**）、`metadata.archive.mirrored=true`、落盘 2074229 字节 video/mp4。前 6 次卡在上游素材暂存接口 **520/524** |

⚠️ **那六次 520 是什么**（记下来省下一次误判）：上游的素材暂存接口 `POST /v1/files/upload`
**不是整站挂了** —— 裸打它回 422「缺 file」、换个小 jpg 也传得上去。它是**时好时坏**：
同一张 2.2MB 的 PNG 连着传就 520、**静置 90 秒再传就成功**。所以"我们这边报 520"先别改代码，
**把同一件素材隔一会儿单独重试一次**，能过就是对方在抖（也怀疑与它"每令牌每分钟 10 次"的限流有关 ——
我连着跑了六轮，等于自己把自己限流了）。
判据一句话：**520/524 发生在"镜像那一步"，上游根本没受理，所以不花钱**；重试是安全的。

⚠️ **一处顺手要记住的副作用**：回填把 `media_assets.asset_url` 换成我们的地址之后，
**历史任务记录**（`generation_jobs.reference_asset_urls`）里存的还是上游原串，而
`resolveReferenceAssets` / `resolveFirstFrameUrl` 是**按整串匹配**的 → 拿老任务的素材去重跑
（`--refs-from=<老任务>`）会报 `GENERATION_MEDIA_UNRESOLVED`。**正常用户路径不受影响**
（画布快照与 `media_assets` 是同步换的），只有"回放历史任务"这条路会踩。


## 三、不许破的口径（承接 1–87；**本文新增 88**）

### ⭐⭐ 口径 88：外部给的产物地址**不是你的** —— 拿到手就存一份

上游返回的图/视频地址是**临时的**，过期后返回 403，而且**不会有人通知你**：
任务成功、日志干净、数据库里那一行看着好好的，只有把读面打开才发现是一片坏图。
判据只能量**产物本身现在还在不在**（逐条请求看状态码），不能看任务状态。

与口径 71 是同一条道理的两半：
71 是"你交给外部的东西，对方可能读不到"（素材传上游）；
88 是"外部交给你的东西，对方随时会收回"（产物归档）。
**两边都不能假设"传过去了/拿到了"就等于"以后还在"。**

配套的三条落地口径（都写进 §二.C 的取舍里了）：

```text
· 归档失败**绝不能让这次生成失败**（学生已经花过钱、上游已经出片）—— 与"素材传上游失败必须报错"正好相反；
· 归档件存成**本人私有**素材，别图省事存成公网可取（未提交作品里的图不该有公网地址）；
· 只存**我们自己的落盘校验认得的类型**：上游会给 `.mov`（`ftypqt`，浏览器播不了），
  存成一份"看着是 mp4、其实播不动"的东西比留个会过期的外链更糟。
```

配套一条**读面口径**：我们自己的媒体在四个读面上各有各的取法 ——
**改一处读面时要把四个都过一遍**（广场 / 我的作品 / 机构预览 / 课堂预览），
其中广场那条是唯一"未登录访客"的，必须由服务端给**作品专属的公开代理**，不能让前端去取私有地址。

---

## 四、这一轮踩过的坑（承接 1–108；**本文新增 109–115**）

### 坑 109：`--click` 遇到**同名按钮**只点第一个，看起来像"按钮点不动"

机构端「作品管理」每一行都有一个「查看作品」（实测 8 个），`page.getByRole(...).first()` 命中哪一条
**完全看列表排序**。我上一轮记的"`--click` 在这几页点不动"其实是这个 —— **不是按钮真的点不了**。
要挑特定那一行就自己按行找（`--then`），别急着改选择器。
**教训**：说"点不动"之前先数一下同名元素有几个。

### 坑 110：脚本跑到一半被杀 → 库改了、快照没改（**半成品状态最难发现**）

回填脚本逐条下载、慢，被 580 秒的 timeout 掐掉过一次。第一版的"替换表"是**本次跑出来的内存清单**，
于是：库里的 25 条已经改了，而快照里的旧地址一个都没换 —— 重跑时因为"这次成功 0 条"，
那批快照就**永远漏改**，而且现象是"看起来已经做完了"。

**修法**：替换表从**库里的 `metadata.archive.sourceUrl`** 推（谁归档过就换谁），天然可续跑；
并且脚本自己复查"快照里还有没有**已归档**的地址"，非 0 就 `exitCode=1`。
**教训**：一次性迁移脚本要能**中途重入**，判据要从**落库的状态**推，不能从"本次跑了什么"推。

### 坑 111：口径改了，**守卫的断言没跟着换** → 那条每跑必红

第二十七轮 §二.T 把画布 toast 的红绿与"留不留"从**猜消息里的字**（`message.includes('失败')`）
换成了 `errorText()` 的标记（`isErrorText`），但 `p110` 那条断言还钉着旧写法 —— 全量守卫里
**它一直是红的**，而红的内容是"上一轮已经改掉的东西"。这正是 README 里记过的同一类毛病
（口径删了断言要跟着删，否则发布会误报 / 真出问题时被当成老毛病）。

**同类第二处**：`p75` 钉着"轮询次数恒等于 2"，而第二十七轮 §二.Q ③ 改成"瞬时错误按退避重试到
deadline"之后次数不再固定（实测 8 次）。**在未改动的 HEAD 上实跑确认过它也是红的**（不是本轮引入）。
本轮把两条都换到新口径上（p75 改成"至少提交 + 轮询一次"）。
**教训**：改判据的那一轮，**顺手 grep 一遍钉着旧判据的守卫**。

### 坑 112：以 root 跑的数据脚本，落盘文件**服务用户读不到** → 前台 502 + "图片已失效"

回填脚本我是以 `root` 跑的（迁移脚本的常态），而 `persistSecureUpload` 落盘的文件属主是 root、
权限 `0640`；生产服务跑在 `ai-kids-prod` 下 —— **它读不到**。现象很有迷惑性：

```text
公网：502（nginx 说 "upstream prematurely closed connection while reading response header"）
盘上：文件在、字节数也对（stat 看不出任何异常）
界面：作品页整页「图片已失效，读不出来了」
```

**而且它骗过了副本上的浏览器核验** —— page-shot 起的临时实例也是 root，读得到，截图一切正常。
**只有打线上才发现。**（与 README 里"客户端更新清单属主从 root 改成 ai-kids-prod"是同一条。）

修法：① 本次落盘的 24 个文件当场 `chown ai-kids-prod:ai-kids-prod`；
② 回填脚本里加上"以 root 跑时把刚落盘的文件交还给上传根的属主"（`process.getuid() === 0` 才做）。
**教训**：**以 root 跑的数据脚本，凡是写了文件，都要把属主交还给服务用户**；
以及**权限这类问题只有"以服务用户打线上"才看得出来**。

### 坑 113：路由处理函数里**复用 `match` 变量** —— 中间插一条新路由，后面的全变不可达

`student.js` 那个大函数是「`match = part.match(...)` → `if (match && method === 'X')`」一路排下来的。
第二十七轮插 `session-state` 时把它写在 GET 与 PUT 之间，`match` 被覆盖 → **PUT 整条静默失效**，
而且是"外层的兜底把它变成 404"这种最难看出来的形态：接口在、前端也在调、就是永远失败。

**判据**：同一个函数里每插一条新路由，**它后面所有用 `match` 的地方都要重新 match 一次**；
而这种"路由不可达"**只有打真 HTTP 才验得出来**（直接调处理函数、看前端源码都是绿的）——
p128 就是照这个写的。
**教训**：报「接口不存在 / 404」先看**路由匹配**，别先怀疑参数或权限。

### 坑 114：React 的合成 `onWheel` 拦不住 xyflow 的滚轮缩放（顺序是反的）

修图4 的第一版是在文本区上写 `onWheel={(e) => e.stopPropagation()}` —— **看着完全正确、实测没用**：
xyflow 是在**画布那层**挂的**原生**监听器，而 React 的合成事件挂在**根容器**上，
冒泡顺序是「元素 → 画布(原生) → 根容器(React)」，等 React 的处理器跑到时画布**早就缩放了**。
真正的开关是 xyflow 认的类名 **`nowheel`**（它内部 `event.target.closest('.nowheel')` 命中就跳过缩放）。

**教训**：拦第三方库的原生事件监听，要用**它认的约定**（类名 / 属性），别赌 React 的合成事件顺序。
这条有真凭实据：改前浏览器里明明白白报「图4 没修好」、改完就过（p129 里留了反向自检，防止被改回去）。

### 坑 115：**批量改文件的脚本，把 `write(内容)` 写成了 `write(路径)`** → 一个 114KB 的源文件被截断成 49 字节

这一轮我用脚本改官网首页那四个图标（三处文件：main.jsx / styles.css / 种子默认值）。
其中一个文件的收尾写成了 `io.open(c, "w", ...).write(c)` —— **`c` 是路径、`css` 才是内容**。
`open(..., "w")` 打开时就把文件截断了，紧接着把 `E:\学习平台正常\apps\website\src\styles.css`
这行字符串写了进去：**`styles.css` 从 113984 字节变成 49 字节**，而脚本照样打印了 "styles.css OK"。

**怎么发现的**：真浏览器核验报"图标尺寸 1440×1440、颜色是黑"（我加的 CSS 没生效）→
回头一查源码，文件只剩那行路径。**不是靠脚本自己报的**。

修法：`git checkout -- apps/website/src/styles.css` 恢复（它是已跟踪文件，所以救得回来），
然后重做 —— 这次带上三道：
```python
before = os.path.getsize(path)
s = io.open(path, encoding="utf-8").read()
assert 锚点 in s            # ① 读前断言（顺带挡住了"读到空文件"）
assert len(s) > 50000       # ② 内容量兜底（这次误写的就是"内容其实很短"）
io.open(path, "w", encoding="utf-8", newline="\n").write(s2)
assert os.path.getsize(path) > before and 关键词 in io.open(path).read()   # ③ 写后校验
```

**教训**：批量改源码的脚本，**必须"读前断言 + 写后校验"**，而且别让脚本自己打印的 OK 当判据 ——
它不知道自己写错了。**同一批另外两个文件没事，只是因为它们恰好写对了。**

---

## 五、下一步 / 遗留

0. **本会话最后又修了两批**（§二.F 的画布自动保存路由 P0 + §二.G 的四条观感/交互 + §二.H 的卡片化与两行式播放器），
   都已发布（`94293e8` / `6de34af`）。下面这几条是**仍然欠着**的：

0b. ~~等上游不抖了补跑一次「走队列的真跑」~~ —— **已跑通**（第 7 次，见 §二·附）：
   产物落成了我们自己的私有素材、`metadata.archive.mirrored=true`、字节数对得上。
   重跑命令仍在 §六最后一条。
1. **那 8 张图救不回来了**：上游 403、字节已经不在（其中 3 张挂在机构端那件「提交链路验证（可删）」上）。
   读面现在会说「图片已失效，读不出来了」。若要更干净，可以把"已失效"的项**折叠成一条提示**
   而不是占一个格子 —— 等口径；那件测试作品本身也可以删掉了。
2. **历史任务记录的素材地址没跟着换**（见 §二·附 的副作用）：老任务 `--refs-from` 重放会报
   `GENERATION_MEDIA_UNRESOLVED`。要么接受（它只是验证工具），要么给 `resolveReferenceAssets`
   加一条"也认归档后的地址"的回退 —— 等口径。
3. **归档件与作品生命周期的关系没定**：作品/项目删了，`file_assets` 里的归档件**不会跟着删**
   （现在没有任何清理）。磁盘会随生成量线性增长（24 条 ≈ 49MB，即平均 ~2MB/件）。
4. **上传侧的坏文件闸**（第二十七轮 §五 第 14 条）仍没做：生产库里有 8 字节的"只有 PNG 签名"的文件，
   学生连上它只会得到一句笼统的「生成失败」。ffprobe 在机器上，代价很小。
5. **图片指代写法**（第二十七轮 §五 第 13 条）："序号到底等不等于 content 数组位置"仍是未知数，
   建议同两张图交换数组顺序各跑一次（各 ¥0.75），判据别落在模型偏好上。
6. **参考图不保证照着画**（第二十七轮 §五 第 15 条）：要"我的图动起来"就该用图生视频/首帧，等口径。
7. **运维**：release 目录现在 **12 份**（口径是 10，下次发布前跑
   `deploy/production/prune-releases.sh --apply`）；机器内存薄（1.6G）；ClamAV 病毒库没更新且无
   freshclam 定时任务；`p93` 的 LibreOffice 转换一直红（与功能无关）。
8. **两个核验脚本仍在 `.tmp`**（`then-session-end.mjs` / `then-org-work-preview.mjs`）：
   要不要作为 `--then` 的示例收进仓库，等口径。回填脚本已入库。

---

## 六、怎么跑（这一轮动过的）

```bash
# 服务器上
cd /srv/ai-kids-platform/internal-test/source
export PATH=/srv/ai-kids-platform/runtime/node/bin:$PATH

# 本轮的守卫（真跑归档链路：注入 fetch，不联网）
node scripts/p127-generated-asset-archive.mjs
# 画布自动保存那条路由**真的可达**（起真服务、发真 HTTP —— 这类"路由不可达"只有打 HTTP 才验得出来）
node scripts/p128-canvas-autosave-route.mjs
# 画布四条观感/交互（源码级形状；真观感看下面那两条真浏览器核验）
node scripts/p129-canvas-box-visuals.mjs
# 作品读面卡片化 + 两行式播放器
node scripts/p130-work-gallery-audio-player.mjs
# 官网首页数据区那四个图标（含"生产 CMS 存的是老字符、靠映射换掉"这条）
node scripts/p131-home-stat-icons.mjs
# 页脚（含"首页也要有页脚"这条）
node scripts/p132-site-footer.mjs
# 课时素材的默认值（源码级；真观感看下面那条真浏览器核验）
node scripts/p133-lesson-material-defaults.mjs

# 上一轮那条被口径落下的断言（本轮改正）
node scripts/p110-classroom-materials-ui.mjs

# 真浏览器核验 · 画布那四条（图1/图2/图4/图5）—— 夹具会挑"这个学生已被授权、框体最多且带视频框体"的课时，
#   并把那个视频框体锁成全能参考（三处一起写），画布上再塞一个长文本节点给"滚轮滚文本"当现场。
CHROME_PATH=/snap/bin/chromium node scripts/page-shot.mjs \
  --fixture .tmp/fixture-canvas-omni.mjs --then .tmp/then-canvas-ui.mjs --no-build '/learn/canvas/{projectId}'
#   ↑ 判据（脚本自己断言）：8 个框体 8 张引导插画、老占位 0 处；选 9:16 后框体变竖；
#     全能参考下没有「首帧/未连接」；在文本区滚滚轮画布不缩放、在画布上滚仍缩放。

# 真浏览器核验 · 作品读面卡片化（夹具把一件公开作品的快照改成"一个课堂产出很多素材"）
CHROME_PATH=/snap/bin/chromium node scripts/page-shot.mjs \
  --fixture .tmp/fixture-plaza-gallery.mjs --then .tmp/then-work-gallery.mjs --no-build --prod-uploads '/works/{token}'
#   ↑ 判据：5 项媒体 → 5 张卡片（没有一张撑满容器）、点开有浮层、Esc 能关。

# 真浏览器核验 · 音乐框体的两行式播放器（夹具会塞一个"已生成音乐"的框体）
CHROME_PATH=/snap/bin/chromium node scripts/page-shot.mjs \
  --fixture .tmp/fixture-canvas-omni.mjs --then .tmp/then-audio-player.mjs --no-build --prod-uploads '/learn/canvas/{projectId}'
#   ↑ 判据：进度条宽度 ≥ 卡片宽度 70%、按钮在进度条**下面那一行**、没有原生 <audio controls>。

# 真浏览器核验（用新的 --then 钩子）
CHROME_PATH=/snap/bin/chromium node scripts/page-shot.mjs \
  --fixture .tmp/fixture-canvas-box.mjs --then .tmp/then-session-end.mjs --tag sessend '/learn/canvas/{projectId}'
CHROME_PATH=/snap/bin/chromium node scripts/page-shot.mjs \
  --prod-uploads --then .tmp/then-org-work-preview.mjs --tag orgwork /org/works

# 存量回填（**先干跑**；改的是生产库 + 上传根）
set -a; . /etc/ai-kids-platform/production.env; set +a
node deploy/production/backfill-generated-media.mjs --dry-run
node deploy/production/backfill-generated-media.mjs --apply

# 只读诊断：作品读面里的媒体有多少是站外地址（跑在生产库副本上）
node .tmp/diag-external-media.mjs

# ⚠️ **还没验收的那一条**：走队列的真跑，确认 worker 在真生成时也会归档。
#   判据：跑完看副本里的新任务 —— `media_assets.asset_url` 应该是 `/api/student/file-assets/...`
#   （不是上游地址），且日志里有一行 `[GENERATED ASSET ARCHIVE]`。
#   本轮三次都卡在上游的素材暂存接口（520/524，失败在镜像那一步、**没花钱**），等它状态好时补跑：
node deploy/production/live-canvas-video-check.mjs --project=project_d21c8d9aca014d6d997a \
  --refs='[{"type":"IMAGE","url":"https://iicili.cyou/api/public/file-assets/file_cb98c29c70224b3abb0c/download"},{"type":"IMAGE","url":"https://iicili.cyou/api/public/file-assets/file_78fbb5ccd38e4fc9b6bd/download"}]' \
  --prompt='让这两张图之间的画面轻轻动起来'
#   ⚠️ 别再用 --refs-from=<2026-09-21 那些老任务>：回填把 media_assets 的地址换了，
#      老任务记录里的原串匹配不上 → 会报 GENERATION_MEDIA_UNRESOLVED（见 §二·附 的副作用）。

# 真浏览器核验 · 课时素材默认值（真开课时抽屉、真新增素材、真改类型与「生成什么」）
#   ⚠️ 脚本注释里记着这条路上的三个坑（标签页 / 抽屉分步 / ＋素材 按钮 0×0）
CHROME_PATH=/snap/bin/chromium node scripts/page-shot.mjs \
  --then .tmp/then-box-defaults.mjs --no-build '/admin/courses/{seriesId}'

# 打线上首页看那四个图标（真 Chromium；判据：4 个 svg / 28x28 / 页面上没有 ◆ ◇ ✧ ⌘）
# 打线上看页脚（首页 + 内页各一次；判据：四列 / 三团光晕在动 / 图标排已删 / 无横向溢出）
# 想单独截页脚：node .tmp/shot-live-footer.mjs https://iicili.cyou/ /tmp/f.png
CHROME_PATH=/snap/bin/chromium node .tmp/check-live-footer.mjs
CHROME_PATH=/snap/bin/chromium node .tmp/check-live-home-icons.mjs

# 打线上作品页看媒体出不出来（真 Chromium；判据 naturalWidth>0）
CHROME_PATH=/snap/bin/chromium node .tmp/check-live-work.mjs "https://iicili.cyou/works/wst_42d7c6b66fb645efa8b5c8d3"
```

---

## 七、给新会话的"读这三份就够"

1. `docs/README.md` —— 唯一口径源 + 顶部指针（每轮最值钱的几条都在那儿）。
2. `docs/operations/开新会话先读-现状对齐-20260919.md` —— 两条线怎么分、生产事实、客户端三个交付面。
3. **本文** —— 这一段的施工记录 + **口径 88** + **坑 109–115** + 下一步。
   再往前是 `新对话交接-第二十七轮-生成链路修复与客户端接入-20260921.md`（口径 61–87、坑 76–108）、
   `新对话交接-第二十六轮-客户端自动更新后台配置-20260920.md`（另一条线）、
   `新对话交接-第二十五轮-页面自查脚手架与素材页白屏-20260920.md`（口径 58–60、坑 72–75）。
