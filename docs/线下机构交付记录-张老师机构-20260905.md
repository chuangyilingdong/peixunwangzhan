# 张老师机构｜首批正式交付记录

- 生产环境：`https://iicili.cyou`
- 交付性质：正式交付
- 创建时间：2026-09-05 14:03 CST
- 观察项：P9-D02 已按用户决定主动停止，未宣称 24 小时观察完成
- 安全约束：本记录不包含任何密码、Token 或 `.env` 内容

## 交付对象

| 项目 | 结果 |
|---|---|
| 机构名称 | 张老师机构 |
| 机构 ID | `org_c553e06c8c724083b4bc` |
| 机构状态 | `ACTIVE` |
| 机构管理员 | 张老师 / `zhangzhenfeng` |
| 教师 | 刘羽驰 / `liuyuchi` |
| 学生 | 方冰 / `fangbing` |
| 账号状态 | 管理员、教师、学生均 `ACTIVE` |

## 课程与班级

- 当前已配置平台已发布课程：**AI古诗词创意营**。
- 该课程当前包含 5 个已发布课时，机构可见性复核通过。
- 用户未指定单个课时编号，本批先按完整 5 课时配置；如需只开通指定课时，后续按机构要求调整班级课单。
- 班级名称：**AI古诗词创意班**。
- 班级 ID：`class_dd4a4ade699c490db151`。
- 班级状态：`ACTIVE`。
- 教师 ID：`user_6b6abf34d6074bdf84f8`。
- 学生 ID：`user_ec39875ff4fb460db8d7`。
- 当前班级课单：5/5 课时。
- 当前未开始课堂；正式上课前由教师按实际课时安排开课。

## 验证结果

- [x] 管理员账号登录 HTTP 200。
- [x] 教师账号登录 HTTP 200。
- [x] 学生账号登录 HTTP 200。
- [x] 管理员可读取目标机构课程。
- [x] 管理员可读取目标班级。
- [x] 教师已绑定班级。
- [x] 学生已加入班级。
- [x] 5 个课时已写入班级课单。
- [x] 账号密码已保存至本机受限文件 `D:\学习平台\张老师机构-生产交付账号-20260905.md`，未写入 Git、聊天、服务器源码或日志。
- [x] 页面级复核：机构管理员登录 `/org/`、教师登录并查看 `/org/classes`、学生登录 `/student/` 均通过。
- [x] 教师班级页面显示 1 个学员和 5 个已配置课时；学生端显示课程 `AI古诗词创意营`。
- [x] 交付后生产服务仍为 `active`，健康检查正常。

## 交付后动作

1. 通过安全方式分别向张老师、刘羽驰、方冰发送各自账号信息。
2. 首次登录后确认机构归属、班级和课程可见。
3. 按实际排课开始第一节课；不要在无人授课时提前创建课堂会话。
4. 第一节课结束后补充作品提交、教师审核点评和课堂收口记录。
5. 若只需部分课时，先暂停开课并调整班级课单，不直接删除已有历史关联数据。

## 服务器同步复核（2026-09-05 14:09 CST）

- [x] 服务器源码已从 `5869c2c` 快进同步到 `c19ef43`。
- [x] `learning-platform-production` 服务状态为 `active`。
- [x] 从 ECS 本机访问 `https://iicili.cyou/api/health` 返回 HTTP 200，`data.status=ok`。
- [x] 生产交付账号文件仍仅保存在本机受限路径，ACL 仅允许 `Administrator` 与 `SYSTEM`。

## 当前交付状态

**正式交付已完成，进入账号安全转交与排课阶段。**

- 账号初始密码不在本记录中，也不通过聊天发送。
- 张老师、刘羽驰、方冰分别收到各自账号后，应首次登录并自行修改初始密码。
- 未收到实际排课时间前，不创建课堂会话；第一节课按教师账号 `liuyuchi`、班级 `class_dd4a4ade699c490db151` 和课时 `lesson_dc4c0fcb7462494f811c` 开始。

## 第一节课堂启动记录（2026-09-05 14:13 CST）

- [x] 教师 `liuyuchi` 已通过生产登录启动第一节正式课堂。
- [x] 班级：`class_dd4a4ade699c490db151`。
- [x] 课时：`lesson_dc4c0fcb7462494f811c`，**第1课：认识古诗与创作主题**。
- [x] 课堂会话：`csession_605cc5d47f464d0dbd63`，状态 `ACTIVE`。
- [x] 学生 `fangbing` 登录复核通过，学生端 `canUseNow=true`，已识别到 1 个进行中的课堂。
- [ ] 课堂结束后由教师结束会话，并补充作品提交、审核点评和课堂收口记录。

## 第一节课堂结束记录（2026-09-05 14:14 CST）

- [x] 教师已结束课堂会话 `csession_605cc5d47f464d0dbd63`。
- [x] 会话状态：`ENDED`。
- [x] 结束原因：正式课堂结束。
- [x] 班级 `currentSessionId` 已清空，班级仍为 `ACTIVE`。
- [x] 教师侧历史会话记录显示该会话为 `ENDED`；学生端当前进行中课堂数量为 0。
- [ ] 后续补充本节课作品提交、教师审核点评和课堂收口内容（如本节课已产生作品）。

### 结束后权限说明

- 学生当前进行中课堂数量为 `0`，因此不存在遗留 ACTIVE 会话。
- 学生账号的 `studentUsageScope` 为 `HOME_PRACTICE`，所以学生端总览的 `canUseNow=true` 是家庭练习权限，不代表第一节课堂仍在进行。
