# P9 生产公网只读冒烟｜2026-09-04

- 时间：2026-09-04（本地执行，Asia/Shanghai）
- 范围：只读 HTTPS 请求；未登录、未写入生产数据库、未提交任何凭据。

| 路径 | 结果 | 说明 |
|---|---:|---|
| `/` | 200 | 官网入口可达 |
| `/api/health` | 200 | API 健康接口可达 |
| `/.env` | 404 | 未返回环境文件 |
| `/server.js` | 200 | 返回 SPA fallback 的 HTML；未泄露源码，但敏感路径未按预期返回 404，需要服务器侧 Nginx 规则修正 |

安全头抽查：HSTS、CSP、`X-Content-Type-Options: nosniff` 均存在。

## 待处理

- 在 ECS 上用授权运维账号检查 production Nginx 配置，将 `/server.js` 等源码 / 配置 / 运行时敏感路径在 SPA fallback 之前显式返回 404；修改前备份配置，修改后执行 `nginx -t`、reload 和再次只读验证。
- 当前 SSH 只读检查未获得授权公钥，未直接修改服务器；不得把该项记录为已修复。
